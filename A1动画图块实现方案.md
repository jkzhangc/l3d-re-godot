# VX Ace A1 动画图块 → Godot 实现方案

## 格式分析

### A1 文件规格
- **尺寸**：512×384 像素
- **Tile 网格**：16 列 × 12 行，每格 32×32
- **动画机制**：VX Ace 的 autotile 可以有多帧动画变体，引擎自动循环播放

### 5 个 Block 布局

| Block | 列范围 | 宽 | 内容 | 动画 | 帧排列 |
|-------|--------|-----|------|------|--------|
| **A** | 0–5 | 6 列 | 海洋 | ✅ 3 帧 | 水平（每帧 2 列 autotile 变体） |
| **B** | 6–7 | 2 列 | 深海 | ✅ 3 帧 | 水平（透明部分自动显示 Block A） |
| **C** | 8–9 | 2 列 | 海洋装饰 | ❌ | —（透明部分自动显示 Block A） |
| **D** | 10–11 | 2 列 | 河流/湖泊 | ✅ 3 帧 | 水平 |
| **E** | 12–15 | 4 列 | 瀑布 | ✅ 3 帧 | **垂直**（2 列宽 × 3 帧叠放） |

### 行结构

A1 的 12 行通常分为若干组，每组是一个 autotile 的 subtile 变体行。具体分组因 tileset 而异，需要在编辑器中看图确认。以 `TileA1.png` 为例：

- 行 0–2：Block A 动画帧的 subtile 行组 1
- 行 3–5：Block A/B/C/D 的 subtile 行组 2
- …（每个 tileset 可能不同）

---

## Godot 4.6 内置 Tile 动画 API

### 核心方法（TileSetAtlasSource）

```gdscript
# 启用动画：设置帧数
source.set_tile_animation_frames_count(atlas_coords, 3)

# 帧排列（0=单行水平排列，>0=多列网格排列）
source.set_tile_animation_columns(atlas_coords, 0)

# 每帧持续时间（秒）
source.set_tile_animation_frame_duration(atlas_coords, 0, 0.3)
source.set_tile_animation_frame_duration(atlas_coords, 1, 0.3)
source.set_tile_animation_frame_duration(atlas_coords, 2, 0.3)

# 整体播放速度倍率
source.set_tile_animation_speed(atlas_coords, 1.0)

# 动画模式（0=所有 tile 同步开始）
source.set_tile_animation_mode(atlas_coords, 0)
```

### 帧布局约束

动画帧必须是 atlas 中**连续的水平格子**：

```
帧0:(x,y) → 帧1:(x+1,y) → 帧2:(x+2,y)
```

设置 `columns` 参数后，帧按从左到右、从上到下排列：
```
columns=0: [0][1][2]           (单行)
columns=2: [0][1]              (2列网格)
           [2][3]
```

### 检查/读取方法

```gdscript
source.get_tile_animation_frames_count(atlas_coords)  # 帧数
source.get_tile_animation_total_duration(atlas_coords) # 总时长
source.get_tile_animation_columns(atlas_coords)        # 列数
source.get_tile_animation_speed(atlas_coords)          # 速度
```

---

## 兼容性矩阵

| Block | VX Ace 排列 | Godot 内置支持？ | 方案 |
|-------|-------------|------------------|------|
| A (海洋) | 3 帧水平，每帧 2 列 autotile | ✅ 完全支持 | 方案 A |
| B (深海) | 3 帧水平 | ✅ 支持 | 方案 A |
| C (海洋装饰) | 无动画 | — | 静态 tile |
| D (河流) | 3 帧水平 | ✅ 支持 | 方案 A |
| E (瀑布) | 3 帧**垂直**排列 | ❌ 只支持水平连续 | 方案 B/C |

---

## 方案 A：Block A/B/D — Godot 内置动画

### 原理

利用 `TileSetAtlasSource` 的 tile animation API，对动画 block 中的每个 tile 设置 3 帧动画。帧 0→1→2 循环播放。

### 优点
- 零代码，纯 Inspector 配置
- TileMapLayer 自动播放，无需额外节点
- 与 terrain/autotile 系统完美兼容
- 动画 tile 放在地图上自动循环

### 缺点/限制
- 只支持水平连续帧（A1 的 Block A/B/D 满足）
- Block E（瀑布）垂直排列，需要另案处理
- 动画帧必须占据 atlas 中连续的空格子

### 手动设置步骤

见方案 A 操作步骤章节。

---

## 方案 B：Block E（瀑布）— 纹理预处理

### 原理

写一个预处理脚本，把瀑布的 3 个垂直帧切出来，拼接成水平排列，输出新的纹理。然后用 Godot 内置动画（同方案 A）。

### 优点
- 最终统一使用内置动画
- 不需要额外运行时节点

### 缺点
- 需要预处理步骤
- 每次更换 A1 纹理都要重新处理
- 瀑布 tile 坐标需要映射到预处理后的纹理

---

## 方案 C：Block E（瀑布）— AnimatedSprite2D 覆盖层

### 原理

瀑布 tile 不设动画，在地图上方放置 `AnimatedSprite2D` 节点，位置跟随瀑布 tile，手动播放帧动画。

### 优点
- 不需要预处理
- 与原纹理保持 1:1 对应

### 缺点
- 需要额外节点管理
- 与 TileMapLayer 系统分离
- 多个瀑布需要多个节点（或用 MultiMesh）

---

## 方案 A 操作步骤（手动在 Godot 编辑器中设置）

### 前提条件
- 已创建包含 A1 纹理的 `TileSet` 资源
- 已添加 `TileSetAtlasSource`，纹理设为 A1 图片，`texture_region_size = 32×32`
- 已在 TileMapLayer 中铺设了 A1 tile

### 步骤 1：确认 Block 布局

在 Godot 编辑器中打开 TileSet 底部面板：
1. 切换到 **🛠️ Setup** 选项卡
2. 查看 atlas 纹理的 16×12 网格
3. 确认动画 block 的列范围：
   - Block A：列 0–5（6 列）
   - Block B：列 6–7（2 列）
   - Block D：列 10–11（2 列）

### 步骤 2：识别动画帧

以 Block A（海洋）为例，3 个动画帧各占 2 列：

| 帧 | 列范围 | 说明 |
|----|--------|------|
| 帧 0 | 0–1 | 海洋变体 1 |
| 帧 1 | 2–3 | 海洋变体 2 |
| 帧 2 | 4–5 | 海洋变体 3 |

每列内的不同行是不同的 autotile subtile 变体（角落、边缘、中心等）。

### 步骤 3：为单个 tile 设置动画

以 Block A 的某个具体 tile 为例，比如 atlas 坐标 `(0, 0)`（第 0 列、第 0 行）：

1. 在 **↖️ Select** 选项卡中，点击选中 tile `(0, 0)`
2. 在右侧 Inspector 面板找到 **Animation** 分组
3. 设置参数：

| 属性 | 值 | 说明 |
|------|------|------|
| **Frames** | `3` | 动画帧数（>1 即启用） |
| **Columns** | `0` | 0=单行水平排列（帧 1 在右边 1 格，帧 2 在右边 2 格） |
| **Speed** | `1.0` | 播放速度倍率 |
| **Mode** | `Default (0)` | 所有 tile 同步开始 |

4. 展开 **Frames** 列表，为每帧设置时长：

| 帧索引 | Duration | 说明 |
|--------|----------|------|
| `0` | `0.3` | 帧 0 持续 0.3 秒 |
| `1` | `0.3` | 帧 1 持续 0.3 秒 |
| `2` | `0.3` | 帧 2 持续 0.3 秒 |

### 步骤 4：帧在 atlas 中的对应关系

设置 `Frames = 3, Columns = 0` 后，帧自动按水平连续排列：

```
帧 0 → atlas_coords (0, 0)
帧 1 → atlas_coords (1, 0)  （右边 1 格）
帧 2 → atlas_coords (2, 0)  （右边 2 格）
```

**这就是为什么帧必须水平连续**：Godot 根据 `Columns` 参数自动计算每帧的 atlas 坐标。

### 步骤 5：批量设置

Block A 有 6 列 × N 行的 tile，需要对每个需要动画的 tile 重复步骤 3。

**注意**：动画只在**设置了 `Frames > 1` 的 tile** 上播放。同一列不同行的 tile 需要分别设置（因为它们有不同的 autotile 变体）。

以 Block A 为例：
- 列 0 的所有行 tile → 帧 0（自身）→ 帧 1（列 2 同行）→ 帧 2（列 4 同行）
- 列 1 的所有行 tile → 帧 0（自身）→ 帧 1（列 3 同行）→ 帧 2（列 5 同行）

### 步骤 6：验证动画

1. 在 TileMapLayer 中放置一个设置了动画的 tile
2. 运行游戏
3. 观察 tile 是否循环切换纹理

---

## 待解决问题

1. **Terrain/Autotile 集成**：动画 tile 可以同时设置 terrain bitmask 吗？需要验证。
2. **Block B 透明叠加**：Block B（深海）的透明区域如何在 Godot 中自动显示 Block A（海洋）？VX Ace 引擎自动处理，Godot 可能需要 terrain 系统或额外图层。
3. **Block E 最终方案**：瀑布的垂直帧需要确定用预处理还是覆盖层方案。
4. **动画速度**：VX Ace 默认帧速是多少？需要实测确认 0.3 秒/帧是否合适。
