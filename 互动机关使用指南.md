# 互动机关使用指南（剧情流程节点）

> 2026-09-12 落地。支撑「炸药 → 爆破墙 → 钥匙 → 锁门 → 同图传送」类流程。
> 对应原作 Map0144（突袭第三章 坑道）的推进链路。
> **2026-09-12 修订**：炸药改为**关键物品方案**（flag 携带、放到墙上定时引爆），
> 投掷物方案保留为可选项（手雷仍能炸墙）。

四个节点全部 **场景化/节点化**，在编辑器里摆进地图场景、Inspector 填参数即可，不需要写代码。

---

## 0. 全局 flag（流程进度的存储核心）

- 存在 `Global.quest_flags`（字典），API：`Global.has_quest_flag(name)` / `apply_quest_flag` / `request_quest_flag`。
- **不参与 checkpoint 回滚**：死亡回安全屋不会重置已开门/已炸墙/已拿道具。
- 随存档文件持久化（save v2 起带 `quest_flags` 字段）；新游戏自动清空。
- 联机：Host 权威，NetworkWorld 自动广播；客户端中途加入会在首个世界快照后自动拉取全量。
- **命名约定**：加章节前缀，全游戏唯一，例如 `ch3_got_key`、`ch3_dynamite_taken_1`、`ch3_wall_mine_broken`、`ch3_door_opened`。换章不自动清，靠前缀区分。

---

## 1. 关键道具拾取点 `ItemPickupPoint`

**脚本**：`script/director/item_pickup_point.gd`（@tool，编辑器内实时预览行走图/图标）

| 参数 | 说明 |
|------|------|
| `pickup_flag` | 拾取后置的 flag（**必填**，也是「只能拿一次」的依据）；关键物品方案下它**就是**道具 flag（如 `ch3_has_dynamite`） |
| `grant_throwable` | 发放的投掷物资源（**关键物品方案留空**；投掷物方案才填 `throwable_dynamite.tres`） |
| `grant_count` | 发放数量，同类投掷物自动叠加（投掷物方案用） |
| `pickup_hint` | 获得提示文字（拾取后残留显示约 2.5 秒，如「获得了炸药。」） |
| `activate_gate_flag` | 反向门条件：该 flag 为 true 时本点**永久隐藏**（填关联的 `wall_flag`） |
| `activate_when_out_of_explosives` | 保底补给点开关（**投掷物方案**）：平时隐藏，仅当全队炸药手雷归零且 gate flag 未置时激活 |
| `activate_when_missing_flag` | 保底补给点开关（**关键物品方案**）：该 flag 为 true 时隐藏、false 时显示——玩家放置炸药（flag 被消耗）后保底点现身 |
| `interact_label` / `interact_range` | 靠近提示 / 触发距离 |
| `walk_texture` / `walk_char_index` / `walk_direction` | 行走图外观；不填则回退显示道具图标 |

交互方式：靠近按**确定键**。联机：Client 提交请求，Host 校验距离后结算。

---

## 2. 可爆破墙体 `BlastWall`

**脚本**：`script/director/blast_wall.gd` —— 直接挂在 **TileMapLayer** 上，用图块画墙。

摆图步骤：
1. 地图场景里新建 TileMapLayer 节点，挂本脚本；
2. **节点名必须包含 `wall`**（如 `BlastWall_矿洞入口`）——敌人 A* 按层名把整格判为硬阻挡；
3. 用 TileSet 把墙画出来（图块需带物理层碰撞，玩家才走不过去）；
4. **墙下面的格子在 Ground/Floor 层要铺好地面图块**——A* 的可行走判定要求「有地面」；
5. 填 `wall_flag`。

| 参数 | 说明 |
|------|------|
| `wall_flag` | 破坏标记（**必填**）；置位后重进图/联机同步/死亡回安全屋都保持已炸开 |
| `hits_required` | 投掷物路线需要几次爆炸（默认 1）；**放置引爆无视此项**，一次炸穿 |
| `hint_text` / `hint_text_missing` | 靠近提示（有爆炸物 / 没爆炸物两态） |
| `resupply_hint` | 全队没爆炸物/没炸药时的指路文案（如「炸药的补给在入口附近」），空则用 missing |

**炸药放置（关键物品方案，推荐）**：

| 参数 | 说明 |
|------|------|
| `place_required_flag` | 放置所需的关键道具 flag（如 `ch3_has_dynamite`，由拾取点发放）；**空 = 不启用放置交互** |
| `place_consume_flag` | true = 放置后清除该 flag（一面墙消耗一瓶；保底拾取点靠它现身） |
| `place_flag` | 已放置标记（**建议填**，如 `ch3_dynamite_placed`）：联机同步、存档恢复、客户端显示引线都靠它 |
| `fuse_time` | 放置到引爆的秒数（引线时间，默认 3） |
| `place_range` | 可放置距离（距最近墙格，默认 96px） |
| `place_interact_label` | 靠近提示（自动拼 [确定]） |
| `place_sound` | 放置/点燃音效 |
| `place_walk_texture` / `place_char_index` / `place_direction` / `place_step_frames` / `place_step_duration` | 引线燃烧行走图（**同掉落物行走图**：48×64 帧矩阵）；不填则只倒计时不显示 |

玩法：持 flag 的玩家靠近按**确定键**放置 → 墙上出现引线行走图 → `fuse_time` 秒后自动引爆（擦图块、A* 重建、置 `wall_flag` 广播存档）。引线燃烧中靠近不再显示提示。
⚠ 存档简化：引线中途存档/切图，重进后引线从 `fuse_time` 重新计满（Host/单机）。

原理（投掷物路线）：投掷物爆炸（`throwable_projectile._explode`，仅单机/Host）→ 命中任意 painted 格 → 扣 `hits_required` → 归零时**擦除全部图块**（碰撞随之消失）→ `EnemyChaseState.notify_map_changed()` 让 A* 整体重建 → 置 flag 广播存档。

---

## 3. 锁门条件传送 `TeleportPoint`（扩展字段）

**脚本**：`script/director/teleport_point.gd`（原有传送功能不变，新增锁门组）

| 参数 | 说明 |
|------|------|
| `required_flag` | 需要持有该 flag 才能交互（如钥匙 `ch3_got_key`）；空=无锁 |
| `locked_hint` | 无钥匙时靠近显示的提示（默认「锁住了。需要钥匙。」） |
| `show_locked_hint` | false=没钥匙时节点完全隐藏；true=显示锁定提示（推荐） |
| `open_flag_to_set` | 开门成功后置的 flag（联机自动广播） |

门本体是图块的图（第三关矿洞）：锁的是传送点不是门图块，开门后传送走人，门图块无需处理。
同图传送：`target_scene` 填**本图场景路径**，配合 `use_target_arrival_position=true` + `target_arrival_position` 指定落点。

---

## 4. 炸药的两种形态

**关键物品方案（2026-09-12 定稿，推荐）**：
- 拾取点 `grant_throwable` **留空**，`pickup_flag` 直接填道具 flag（如 `ch3_has_dynamite`）——flag 即持有证明，不进背包 UI。
- 玩家对爆破墙按确定键**放置** → 引线行走图 → 定时引爆。参数见上节「炸药放置」。
- 优点：无法被路上挥霍（flag 只在放置时消耗），天然防软锁。

**投掷物方案（保留可选）**：
- `object/throwable_dynamite.tres`：爆炸半径 3 格、伤害 80，图标 `art/Ui/装備アイコンダイナマイト.png`。
- 走**投掷物槽 + 叠数**：`PlayerState.throwable_count`（HUD 图标下方显示 `×N`，扔一次减一，归零清槽）。
- 手雷同样能炸爆破墙（爆炸统一结算）；炸药/手雷之外的燃烧瓶**不能**（无爆炸半径，只有火焰）。
- 待补素材：`held_walk_texture`（举起行走图）与 `pickup_texture`（地面行走图），在 .tres 里自行配置。

---

## 5. 突袭第三章（矿洞）建议接线（关键物品方案）

```
拾取点A（起点）  pickup_flag=ch3_has_dynamite  grant_throwable 留空
                 pickup_hint="获得了炸药。"  activate_gate_flag=ch3_wall_mine_broken
爆破墙W（矿道）  wall_flag=ch3_wall_mine_broken
                 place_required_flag=ch3_has_dynamite  place_consume_flag=true
                 place_flag=ch3_dynamite_placed  fuse_time=3  place_walk_texture=炸药引线行走图
拾取点B（墙 3~4 屏内，保底）  pickup_flag=ch3_has_dynamite
                 activate_gate_flag=ch3_wall_mine_broken
                 activate_when_missing_flag=ch3_has_dynamite
                 ← 没拿到炸药 / 放置消耗后、且墙未破时才现身
拾取点K（炸开区域内）  flag=ch3_got_key  钥匙（无投掷物）
锁门传送D        required_flag=ch3_got_key  open_flag_to_set=ch3_door_opened
                 target_scene=本图  target_arrival_position=门后区域坐标
安全门（已有）   章节终点
```

防软锁三层：① 墙侧常驻提示（没炸药时指路）；② 手雷仍能炸（投掷物路线并存）；③ 保底拾取点在「没拿到/已消耗炸药 且 墙未破」时自动现身。

---

## 6. 联机 / 存档注意

- flag 全部走 Host 广播，客户端节点监听 `Global.quest_flag_changed` 做本地表现，不各自结算。
- 联机途中加入：客户端首个世界快照后自动拉全量 flag，拾取点/墙/门状态一致。
- 存档：`quest_flags` 随 save v2 持久化；旧档（v1/无字段）视为无 flag。
- 已知取舍：联机中替换叠数投掷物时旧投掷物掉落**不带余数**（掉 1 个模板）；武器拾取照旧。
