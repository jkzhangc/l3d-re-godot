# 多人倒地/救援与团灭恢复 实施方案

> 对应《L4D2缺失功能清单》下一批优先实现第 1 项、《联机系统架构设计》§9.4 任务 19–22（阶段 3）。
> **状态：已实施并回归通过（2026-08-28）。** 单人模式行为完全不变；本方案所有新状态只在联机会话（NetworkWorld）内生效。
> 回归：`--net-test-features`（街道图，双向倒地救援 + 快照 downed 标记 + 倒地碰撞断言）与 `--net-test=downed-wipe`（test 图，倒地→流血→团灭→重载→恢复验证）双端全绿；无头扫描退出码 0；单人安全屋冒烟 600 帧退出码 0。改动清单与设计决策如下文，未完成项见文末"边界与后续"。

---

## 1. 现状与可复用基础

- 救援基础设施已存在：`network_world.gd` 的 `_revive_attempts`、`revive_start_request` / `revive_cancel_request` / `revive_presentation` RPC、`_try_host_start_revive` / `_update_host_revives`（按住确定键 3 秒 → 恢复 30% HP）；`player.gd` 的 `apply_network_revive_state()`。
- 目前的问题：HP=0 直接进入"死亡"表现（`_apply_network_death_state`），没有倒地/流血阶段；死亡玩家永久不可复活且会软锁章节（安全门要求全员到场）；团灭没有任何恢复流程。
- 表现层复用：倒地与死亡共用"躺地精灵 + 关闭受击区"的表现（`_is_dying`），差别只在于权威状态与是否可救援、是否可爬行。
- 进度环复用：`hold_indicator.gd` 的绘制模式（背景圆环 + 进度弧），用于救援进度显示。

## 2. 状态模型

```
NORMAL（站立）
   │  HP 降到 0（Host 权威伤害）
   ▼
DOWNED（倒地）—— 躺地表现、可缓慢爬行（CRAWL_SPEED）、流血池递减、可被救援
   │  a) 队友按住确定键 3s（REVIVE_DURATION_MSEC，复用既有流程）
   │     → 回到 NORMAL，HP = max_hp × REVIVE_HP_RATIO(0.30)
   │  b) 流血池耗尽（DOWNED_BLEED_HP=100，DOWNED_BLEED_RATE=5/s ≈ 20s）
   ▼
DEAD（真正死亡）—— 同躺地表现、不可再救、不可移动；章节推进时满血复活
```

- **倒地是场景运行时状态**，只存于 NetworkWorld 的 `_players[peer]` entry（`downed` / `dead` / `downed_hp`），不写入 PlayerState、不进存档 —— 换图即重置。
- **团灭判定（Host，每帧）**：所有玩家节点均非站立（全部 downed/dead）→ 广播黑屏 → 重置会话 PlayerState（满血）→ `net.request_scene_change(当前场景)` 重载本章。
- **章节推进复活**：spawn 时会话状态 HP ≤ 0 的玩家按满血复活（解决"一人死亡、全员卡安全门"的软锁）。
- **安全门放行**：`_are_all_players_at_safe_door` 跳过 DEAD 玩家（DOWNED 玩家可爬行到门，仍计入）。
- **单人保护**：`network_controlled == false` 的实体不进入倒地；单机保持"切换存活队员 → 全灭黑屏读档"。

## 3. 改动清单

### `script/player.gd`
- 新增 `network_downed: bool` 与 `is_network_downed()`；`is_network_dead()` 语义保持"HP=0 的躺地表现"（含倒地），现有战斗/表现闸门不变。
- 新增 `set_network_downed(enabled)`：倒地期间**重新启用移动碰撞**（`$CollisionShape2D.disabled = false`，否则爬行会穿墙）；真正死亡/复活收尾由 NetworkWorld 决定。受击区保持关闭（倒地期间免伤，流血是唯一扣血来源）。

### `script/network_world.gd`（核心）
- 常量：`DOWNED_BLEED_HP := 100.0`、`DOWNED_BLEED_RATE := 5.0`、`CRAWL_SPEED := 60.0`、`WIPE_FADE_SECONDS := 2.0`、`WIPE_HOLD_SECONDS := 1.0`。
- Host 权威流程：
  - `_on_host_player_damage_applied`：伤害后检测 `node.current_hp <= 0` → `_handle_host_player_downed(peer_id)`（entry 记 downed=true、downed_hp=100；注意信号先于 `_die()` 触发，表现由 `_die()` 自己完成，碰撞再启用放到来帧 reconcile）。
  - `_update_host_downed(delta)`（每帧）：downed 节点 reconcile `set_network_downed(true)`；流血递减；耗尽 → entry `downed=false, dead=true`，节点回落死亡表现；打印 `HOST_BLEEDOUT` / `HOST_DOWNED`。
  - `_update_host_revives`：救援目标有效性从 `is_network_dead()` 改为 entry `downed == true`；完成时清 downed、`HOST_REVIVE_COMPLETE`（既有广播 `revive_presentation` 不变）。
  - `_find_revive_target_for`：目标改为 entry `downed == true`（Host 与 Client 共用此函数，Client 用快照数据预估，Host 仍复核）。
  - `_simulate_host_players`：downed 玩家以 `CRAWL_SPEED` 移动（保持 move_and_slide / update_facing），DEAD 才冻结输入。
  - `_check_host_team_wipe()`：全员非站立 → `_trigger_host_team_wipe()`：`_wipe_active=true`、清空救援尝试、`team_wipe_presentation.rpc()`（call_local，双端黑屏）、延迟 FADE+HOLD 后重置会话满血并 `net.request_scene_change(_scene_path)` 重载本章。
- 快照扩展（`_public_state` / `_build_compact_player_snapshot` / `_normalize_player_snapshot`，尾部追加，保持旧字段顺序）：
  - `downed`（bool）、`bleed_ratio`（0–1，未倒地为 -1）、`revive_progress`（0–1，被救援中才有值）。
- Client 应用（`_ensure_client_player`）：写回 entry `downed` / `bleed_ratio` / `revive_progress`；表现不变（HP=0 已触发躺地）。
- 救援进度环：NetworkWorld 在被救援的 downed 节点上挂/摘 `script/network_revive_indicator.gd` 子节点，按快照 `revive_progress` 更新（Host 本地直接由 `_revive_attempts` 驱动）。
- 救援者有效性：reviver 自身 downed/dead 时 `_find_revive_target_for` 返回 0 → 既有逻辑自动取消。
- 输入闸门：`_physics_process` 引入三态 `_get_local_player_life_state()`（NORMAL/DOWNED/DEAD）：DOWNED 时跳过战斗类输入（救援/投掷/武器/装填/推击/开火），仅提交移动；DEAD 才 block 全部输入。`_capture_host_input` / `_capture_client_input` 的 blocked 参数同步调整。

### `script/network_revive_indicator.gd`（新增）
- 自绘 Node2D：`progress` 属性 + 灰底/黄弧，样式对齐 weapon_pickup 的 HoldIndicator（偏移头顶上方）。

### `script/enemy.gd`
- `_is_player_body()` 追加：`body.get("current_hp") <= 0` 时不算玩家 → 普通感染者不再围尸（联机倒地/死亡、单机死亡瞬间都受益）。

### 测试（无头回归，复用 `--net-test` 框架）
- 新增 `--net-test=downed-revive`：Client 侧直接调用生产伤害路径把 Host 玩家打到 0 → 验证快照 `downed=true` → Client 按住确定键（`revive_start_request` 路径）→ 3s 后 `HOST_REVIVE_COMPLETE`、Client 收到 `revive_presentation` 且目标恢复站立 → `AUTO_CLIENT_DOWNED_REVIVE_COMPLETE` 打点。
- 团灭：Host 与 Client 玩家都置于 downed 且加速流血耗尽 → 黑屏 → 双端回到同场景、会话满血 → `AUTO_TEAM_WIPE_RECOVERY_COMPLETE` 打点。
- 单人回归：现有单机死亡切换（`character_switch_manager`）与全灭读档路径不受影响（无 `network_controlled`）。

## 4. 边界与后续（不在本轮）

- 观战镜头（死亡玩家当前停留原地等待章节推进/团灭，属已知限制）。
- 二次倒地递增流血速度（L4D2 第三振）——预留 `incap_count`，先不实现。
- 电击器复活、队友状态栏 HUD（清单后续项）。
- 断线重连场景下的 downed 状态恢复。

## 5. 验证环境备注

- 无头回归：`& "D:\Godot_v4.6.3-stable_win64.exe\Godot_v4.6.3-stable_win64.exe" --headless --path <项目> --quit`（退出码 0 = 解析通过）。
- godot-ai MCP 已接入 ZCode（`mcp__godot-ai__*`，stdio `godot-ai attach` → 编辑器 WS 9500），新会话可用其运行游戏/截图/eval 做可视化验证。
