# L3D 全面重做规划清单（Full Rewrite Plan）

> 决策依据：用户于 2026-09-03 确认"以 Q3 为准（全面重做）"。
> 配套文档：`技术债务与架构演进分析.md`（债务清单与对比论证）。
> 范围界定：**代码层架构全量重写**；**资产层与已验证设计保留复用**（见 §0）。
> 目标：消除 god-object、单例膨胀、测试脚手架污染、输入脆弱、存档静默清零、同步碎片化、敌种零抽象等七类债务。

---

## 0. 不可丢弃的既有资产（重做时必须保留）

| 类别 | 保留项 | 理由 |
|------|--------|------|
| 美术资产 | `art/` 全部 `.png`、精灵表、TileSet | 已完成，仅改引用方式 |
| 数据资源 | 现有 `.tres`（武器/角色/技能/战役） | 数据驱动已落地，重做直接复用 |
| 联机安全模型 | Host 权威 + ENet + 17 处 `any_peer` RPC 身份校验 + 资源白名单常量字典 | 已通过安全审计，推倒会 reintroduce 作弊漏洞 |
| 范式 | FSM 状态机、Director 子节点分解、流场寻路 | 方向正确，仅重写其承载结构 |
| 设计总纲 | `游戏设计方向-L3D混合.md` | 需求源，照此实现 |
| **原版 L3D 战斗设计支柱** | `L3D战斗系统设计拆解与重做清单.md` | 见切→反击→背刺即死、Rush 三型、角色=打法、Guts、属性/削损后置——重做必须兑现，详见该文件 §2 |

---

## 1. 目标架构（干净分层 + 硬约束）

```
Bootstrap / Autoload Managers（无状态服务，禁止 god-singleton）
   └─ Core：PlayerInput / DataResources / Interfaces(Damageable,HitResolver,StatBlock)
        └─ Gameplay：Player / Enemy / Director
             └─ Net：NetReplicator / DomainSystems / SessionHealth
                  └─ Presentation：UI / HUD / Save
                       └─ Content：第一章垂直切片
```

**重写硬约束（写入 AGENTS.md 作为准入门槛）**
1. 单文件 ≤ 500 行，超出必须拆分（god-object 为零容忍）。
2. 禁止 `find_child("字符串")` 隐式耦合 → 改用编译期可查的服务注册（`NetContext.get_replicator()`）。
3. 禁止 `get("_private_field")` / 鸭子类型读私有字段 → 统一 `Damageable` 接口。
4. 单例仅承载无状态服务；游戏状态禁止存单例。
5. 同步策略单一来源（`NetReplicator` 统一节拍+插值，`NETWORK_RENDER_DELAY` 仅一处定义）。
6. 测试代码禁止进入生产文件；回归用例走独立 harness。

---

## 2. 功能模块总表（制作顺序 / 优先级 / 依赖 / 难度）

> 优先级：P0 必做基石，P1 重要，P2 可后置。难度：1–5（5 最高）。阶段：R0–R8。

| 序号 | 模块 | 职责 | 优先级 | 依赖 | 难度 | 阶段 |
|------|------|------|--------|------|------|------|
| M01 | 工程骨架与 autoload 结构 | 项目初始化、目录、构建/导出管道 | P0 | — | 2 | R0 |
| M02 | 单元测试框架（GUT/gdUnit）+ headless CI 冒烟 | 防回归底线（旧项目完全缺失） | P0 | M01 | 2 | R0 |
| M03 | 全局服务拆分 | SettingsManager / AudioManager / CheckpointManager / TextConfig / NavDebug | P1 | M01 | 3 | R0→R6 |
| M04 | 数据资源层重构 | CharacterData（修 base_speed/critical_rate 零引用、暴击量纲统一为 0–100）、WeaponData、ItemData、ThrowableData、SkillData（含技能效果载体/冷却字段） | P0 | M01 | 2 | R1 |
| M05 | EnemyArchetype 资源 | 敌种数据+可选状态覆盖（Hunter/女巫/Tyrant 成"加配置"；含 boss_flag / instakill_resist 三档即死抗性） | P1 | M04 | 3 | R1 |
| M06 | 导演/战役配置资源 | DirectorConfig / CampaignData / PacingConfig / **BalanceConfig**（调参、难度倍率、紧张度权重全部入资源，去硬编码） | P1 | M04 | 2 | R1 |
| M07 | 核心接口 | `Damageable`（含 `take_damage(dmg, knockback, dir, is_deadly=false)` / `get_facing()`）/ `HitResolver` / `StatBlock`（统一受伤/命中/属性/即死语义） | P0 | M04 | 3 | R1 |
| M08 | PlayerInput 意图层 | 每帧产出 intent 结构 + 动作常量表 + WASD 绑定（解输入脆弱） | P0 | M01 | 2 | R2 |
| M09 | 玩家状态机+子状态 | Idle/Walk/Run/WeaponReady/WeaponAttack/Reload/Shove/Throwable（武器状态数据驱动合并、删 Sniper/Shotgun/Rifle/Smg 死节点；**见切优先级高于攻击输入**） | P0 | M07,M08 | 4 | R2 |
| M10 | **PlayerMikiriState** | 见切(0.3s窗口)→反击(超Push推开敌群)→背刺即死(敌我同向规则, Boss 1.5×) + Guts 保底不死（核心战斗循环，第一天类实现） | P0 | M09,M07 | 4 | R2 |
| M11 | 玩家表现/健康/网络呈现拆分 | PlayerVisual(图集算式一处) / PlayerHealth / PlayerNetworkPresenter | P1 | M09 | 3 | R2 |
| M12 | 子弹/投掷物系统 | 预加载 + 对象池，消除每发 `load()` | P1 | M07 | 3 | R2 |
| M13 | NavigationService | A*/流场/PassageGates/卡墙恢复/转向限速（从 EnemyChaseState 抽离） | P0 | M01 | 4 | R3 |
| M14 | WalkabilityProvider | 统一可行走性接口，解除对图层名子串判定耦合 | P1 | M13 | 3 | R3 |
| M15 | 敌人 FSM（Archetype 驱动） | Idle/Discover/Chase/Attack/Knockback/Hitstun/Death/HeadshotDeath | P0 | M05,M07,M13,M14 | 4 | R3 |
| M16 | Crimson Head 变体 | 参数化（速度快、10s 力竭），验证**单发型 Rush**+导演闭环 | P1 | M15 | 2 | R3 |
| M17 | Director 核心 | intensity/pacing/spawn/event/item 子节点（保留子节点分解）；含 **Rush 三型（单发/耐久/永续）调度** 与紧张度因子去硬编码（combat_factor 连续化） | P1 | M06,M13 | 4 | R4 |
| M18 | 章节流转 | SafeDoor / PassageGates / TeleportPoint | P1 | M17 | 3 | R4 |
| M19 | HoldoutMachine 精简重写 | 交互/阶段机/HUD/完成事件（去除 trigger_node 脆弱契约） | P1 | M17,M18 | 3 | R4 |
| M20 | Rush 事件三型 | 单发/耐久/永续（导演剧本） | P1 | M17 | 3 | R4 |
| M21 | **NetReplicator** | 实体注册 + 快照编解码 + 节拍 + 统一插值（消除 render_delay 分叉） | P0 | M09,M15 | 5 | R5 |
| M22 | 域系统 | CombatSystem/PickupSystem/ReviveDownedSystem/SafeDoorSystem/HoldoutSystem（host_tick + NetContext） | P0 | M21,M09,M15,M19 | 5 | R5 |
| M23 | **SessionHealth** | 心跳 / RTT / 丢包 / 带宽 / 重连退避（旧项目完全缺失） | P0 | M21 | 4 | R5 |
| M24 | 外部回归 harness | 对标 `tools/net_regression.py`，**不污染生产文件** | P1 | M21,M22 | 3 | R5 |
| M25 | HUD 信号订阅化 | CombatHUD 监听 `stat_updated` 信号，去除每帧轮询 | P1 | M09 | 3 | R6 |
| M26 | ChapterSummary | 联机确认结算（重写 ready_count 逻辑） | P1 | M22 | 3 | R6 |
| M27 | 菜单体系 | 标题/设置/角色选择/战役选择（数据驱动列表，去硬编码） | P1 | M03,M04 | 3 | R6 |
| M28 | GradientLabel 重构 | 用 Theme/BBCode 替代 SubViewport 逐像素 | P2 | M03 | 2 | R6 |
| M29 | **SaveManager 健壮性** | CRC32 + schema 校验 + `.bak` 回退（杜绝静默清零） | P1 | M04 | 2 | R6 |
| M30 | **第一章垂直切片** | 市街地+学校 5 图接入战役 + **还原图碰撞/通行性管线（地形ID→TileSet physics）** + 双 Rush（单发+耐久）+ Crimson Head + 双人回归 | P0 | M17,M21,M25,M29 | 3 | R7 |
| M31 | 静香实装（可选 M6） | 辅助角色 SA+反击差异化 | P2 | M04,M09,M10 | 3 | R7 |
| M32 | 打磨与诊断 | 重连/反作弊/性能/平衡 | P2 | 全部 | 3 | R8 |

---

## 3. 阶段性交付目标（R0–R8，每阶段保持可运行）

### R0 基础与护栏（约 1–2 周）
- **目标**：可编译空工程 + 测试框架 + 单例拆分骨架。
- **完成定义**：`godot --headless` 跑通空场景；GUT 冒烟用例通过；`Global` 拆分为 5 个无状态 Manager。
- **退出标准**：任何后续模块可挂载到 autoload 服务上，无需引用 god-singleton。

### R1 数据与接口层（约 1 周）
- **目标**：所有 `.tres` 资源可用；`Damageable/HitResolver` 就绪。
- **完成定义**：CharacterData 真正驱动移动速度/暴击（修复死字段）；单元测试覆盖 HitResolver。
- **退出标准**：玩法层可零依赖编译。

### R2 玩家与核心战斗循环（约 2–3 周）
- **目标**：单人可移动/射击/近战/推击/换弹/投掷；**见切→反击→背刺即死 + Guts 落地**。
- **完成定义**：test 图验证背刺即死、见切 0.3s 窗口、Guts 保底；输入加键不破 10 文件。
- **退出标准**：单机核心手感可玩（这是设计支柱，必须最先成型）。

### R3 敌人与导航（约 2–3 周）
- **目标**：NavigationService 独立；EnemyArchetype 驱动敌种；Crimson Head 可刷。
- **完成定义**：寻路无卡死（沿用已验证的流场+PassageGates 思路重写）；新增敌种=加配置。
- **退出标准**：单机可被打尸潮。

### R4 导演与章节流转（约 2 周）
- **目标**：Director 编排 + 安全门/传送 + 防守战 + Rush 三型。
- **完成定义**：单机可跑"赶路—尸潮—安全屋"骨架 + 一次 Rush。
- **退出标准**：战役流程可脚本化。

### R5 联机重写（最高风险，约 3–4 周）
- **目标**：NetReplicator + 域系统 + SessionHealth，复用旧安全模型。
- **完成定义**：双人可同步战斗/拾取/救援/安全门/防守战；**重连+RTT 指标可用**；外部 harness 对标旧回归。
- **退出标准**：联机不再有 render_delay 分叉、无 `find_child` 耦合。

### R6 UI / 存档 / 菜单（约 2 周）
- **目标**：HUD 信号化、菜单数据驱动、存档防清零、文字渲染收敛。
- **完成定义**：存档损坏回退 `.bak`；战役列表读目录；HUD 不每帧轮询。

### R7 第一章垂直切片（约 2 周）
- **目标**：市街地+学校 5 图贯通 + 双 Rush + Crimson Head + 双人回归。
- **完成定义**：单人从开头安全屋连续打到学校出口无空图/卡死；双人跑通全章。

### R8 打磨（持续）
- **目标**：重连实战、反作弊、性能与平衡。
- **退出标准**：达到原"第一章验收"全部 checkbox。

---

## 4. 风险与缓解（既然选定重做，给出兜底）

| 风险 | 触发条件 | 缓解 |
|------|---------|------|
| 单人开发长周期脱轨 | 某阶段 > 预估 1.5× | 阶段门禁：未达退出标准不进下一阶段；R5 之前全程可单机运行 |
| netcode 重建引入不同步/作弊 | 安全模型实现偏差 | 逐字复用旧 RPC 身份校验 + 资源白名单；M24 harness 对标 `net_regression.py` |
| 进度失控/范围蔓延 | 在 R2 前插入章节内容 | 严格按 R0→R8 顺序；内容（M30）排到最后 |
| 重写后又回到 god-object | 约束未强制执行 | §1 六条硬约束写入 AGENTS.md，PR 前静态检查单文件行数 |

---

## 5. 与迭代路线的成本对照（一句话结论）

全面重做估约 **2–4 倍于债务清理成本**、且 R5 前无联机交付物，但换来从零的统一边界与可扩展性上限；若中途发现风险不可控，**最晚可在 R4 结束时回退到"仅联机层局部重写 + 其余保留"的折中路线**（即此前选项里的局部重写方案），不损失 R0–R4 的单机成果。

---

## 6. 下一步建议

1. 从 **R0（M01/M02/M03）** 启动：搭骨架、接 GUT、拆单例——所有后续模块的底座。
2. 立即将 §1 六条硬约束写入 `AGENTS.md`，作为重写准入门槛。
3. 复用现有 `.tres` 与美术资源，R1 起即挂载，避免资产重做。

---

## 7. 战斗系统重做与模块映射（与 `L3D战斗系统设计拆解与重做清单.md` 统一）

> 本文是**执行编排**（模块/阶段/依赖），战斗维度的**详细设计依据**在 `L3D战斗系统设计拆解与重做清单.md`（现状分析 / 问题定位 / 具体重做规划方案）。
> 两文件必须同步维护：战斗重做项的优先级/依赖/阶段调整需同时更新两处。

| 战斗重做项（拆解文档 §2） | 对应模块 | 阶段 | 关键交付 |
|--------------------------|---------|------|---------|
| 核心循环：见切/反击/背刺/Guts | M07 + M10 | R2 | `Damageable.is_deadly` + `PlayerMikiriState` |
| 武器状态机归并 + 删死节点 | M09 | R2 | 合并 WeaponReady/WeaponAttack，删 4 个不可达节点 |
| 角色数据落地 + 暴击量纲统一 | M04 | R1 | 修 `base_speed`/`critical_rate` 死字段 |
| 平衡表 + 紧张度去硬编码 | M06 + M17 | R1/R4 | `BalanceConfig`/`DirectorConfig`，combat_factor 连续化 |
| 技能空壳→落地（效果/冷却/SA） | M04 + M09 + M10 + M31 | R1–R7 | `SkillData.effect_type`，角色 SA 差异化 |
| 技能 UI | M25 + M27 | R6 | HUD 技能图标栏 + 冷却遮罩 |
| 导航服务抽取 | M13 | R3 | 解 `EnemyChaseState` god-state |
| 敌种 Archetype + 阵容 | M05 + M15 + M16 | R1/R3 | 数据驱动敌种，Crimson Head 先行 |
| 可行走性统一 | M14 | R3 | `WalkabilityProvider` 接口 |
| Rush 三型 + 防守战 + 动态音乐 | M17 + M19 + M20 | R4 | 单发/耐久/永续 + BGM crossfade |
| 还原图碰撞管线 + 章节贯通 | M30 | R7 | 第一章垂直切片，双人回归 |
