# 原作突袭战役（Sudden Attack）防守战时间与机制

> 逆向自 `E:\15.L3D` 的 LMU 地图事件 + RPG_RT.ldb 公共事件（工具：`tools/rm2k3_map_restore/dump_orig_events.py`，2026-09-14）。
> 关键命令码经 liblcf 源码核对：`10230` = ControlTimer（タイマーの操作，参数 [操作(0=设定/1=开始/2=停止), 来源(0=常数/1=变量), 秒数, 是否显示, 战斗中继续, 计时器号]）；`12330` = 调用公共事件（参数 [来源, 事件ID, …]）；事件页条件 flags **bit5=计时器1**、timer_sec 在条件块 chunk 0x08。
> 官方说明书（`readme/system.html`）把ラッシュ分三类：単発型（杀满即止）、**耐久型（左上显示计时器，归零才结束）**、永続型（完成事件/到达地点才结束）。

---

## 一、突袭战役地图对照

| 地图 | 树名 | 章节 |
|---|---|---|
| Map0136 | CP1-1 | 第一章 市街地（ススキヶ原） |
| Map0141 / 0142 | CP2-1 / CP2-2 | 第二章 小学校 |
| Map0144 | CP3-1 | 第三章 坑道（防空壕跡） |
| Map0146 | CP-フィナーレ | 第四章 防災備蓄倉庫 |
| Map0147 | CPフィナーレ | フィナーレ（防守战） |

## 二、战役正篇的防守战/ラッシュ（3 场，仅 1 场有计时器）

### 1. 第二关·学校 —— 计时器 120 秒（唯一耐久型）★

- 入口：**Map0142 EV0066**（控制台，決定键交互，开关[1847] 解锁后才可用）。
  确认选择后：PlaySE「ラッシュ開始」→ [1947]（ラッシュ进行中）ON → var39=0、var1214=Random(0..10)（刷怪节奏量）、var1361=0 → 槽位迁移（131~136 → 752~757）→ [1940]（ゾンビ大群强化尸群）ON → **计时器1 设定 120 秒并启动（左上显示）** → [1948]（计时器已武装）ON。
- 结束：四台シャッター #67/#68/#69/#71 的 p2 页（并行处理，条件 = [1948] ON + **计时器剩余 ≤ 1 秒**）：
  - #68：var39/var1214 清零、[1940] OFF、[1947] OFF、BGM「(OFF)」停止 → 开门动画 → [1949]（门已开）ON；
  - 其余三台：开门动画 → [1949] ON。
- [1948] 是全局开关，要等你**进入下一张图的终点事件**才被关掉（Map0147 EV0057 p4；其它战役同款：Map0019 #87、Map0020 #57、Map0033/35 #69）。
- **防守时间 = 120 秒，杀不杀敌不影响开门**（阶段由计时器驱动，与 147 图的击杀数驱动不同）。

### 2. 第三关·坑道 —— 永続型，无计时器

- 入口：**Map0144 EV0060~66**（同一事件铺 6 格绊线，坐标 86-87, 75-77）。
  触发：大地1 震动 → PlaySE「ラッシュ開始」→ [1947] ON → var39=0、var1214=Random(0..10)、var1361=0 → 槽位迁移。**无计时器**。
- 结束：**階段 EV0059** p1 把 [1947] OFF——杀出一条路到达楼梯即结束。
- **防守时间 = 到达目标点为止，无固定时长**（跑得越快越短）。

### 3. フィナーレ·防災備蓄倉庫 —— 永続型，击杀数驱动

- 详见《原作147图防守战刷怪逻辑分析.md》：EV0057 p2 ラッシュ開始（[1954]+[1940] ON，无计时器）；
  杀满 **4 只组1敌人**（var1391≥4）→ 暴君窗口关闭、[1958] ON、开门。
- **防守时间 = 击杀数驱动，无固定时长**。

## 三、附加系统：Ｇ２マーセ「ゾンビ狩り」（限时杀敌支线）

与战役正篇防守战是两套系统，但共用计时器，一并列出：

- 入口：**CE821 PC3**（电脑终端）→「【サイドケース：ゾンビ狩り】時間内に倒せるだけゾンビを倒せ!!」→ 调用 **CE691 Ｇ２マーセ開始**：[2000] ON（活动标志）、积分变量清零（987/989/996~1000）、var988=Random(0..1000)、**计时器1 设定 60 秒并启动（左上显示）**。
- 击杀加时：敌人死亡页（全地图通用）调用 **CE693~698 Ｇ２マーセ加算用敵1~6**（按敌种给 var990 加点数、var1000 加 25/50/75 分）→ **CE700 マーセタイム加算本体**：停表 → 读剩余秒数(var985) → 按击杀加成叠加 → **总上限 180 秒**（超过截断为 180）→ 重新设表启动。
- 结束：**CE699 リザルト**（并行，[2000] 门控）：计时器剩余 ≤1 秒 → [1999]（时间到）ON → **CE692 リザルト起動**：结算画面（`\V[989]体撃滅 / 獲得経験値 \V[1000] / 賞与 \V[987]円`），关闭 1999/2000。
- **起始 60 秒、杀敌可续、上限 180 秒**。

## 四、其它说明

- 单发型普通ラッシュ（导演定期尸潮）全部是**杀满一定数量/衰弱结束**（公共事件 #150 一族），无计时器，不在本清单。
- 全库带计时器页条件的只有 5 个页（Map0017 #65 封鎖扉 + Map0142 四台シャッター），全是「1948 + 计时器≤1s」的门模板；全库计时器设定只有：Map0017（60s，第一战役同款防守）、**Map0142（120s）**、CE691（60s）、CE700（变量续表）、Map0140 #22（选人图停表）。
- 页条件计时器语义注：liblcf 把页计时器条件实现为「剩余 ≥ timer_sec」，但按官方说明书（归零才结束）与本作门开在计时结束的设计事实，实际语义是「剩余 ≤ timer_sec」——重制时按「归零开门」理解即可。

## 五、对重制版的对照建议（仅记录，未实现）

| 原作 | 数值/机制 | 重制侧落点 |
|---|---|---|
| 学校防守战 | **120 秒**计时器 + ゾンビ大群 + 结束开门 | `HoldoutMachine.holdout_duration = 120`（现默认 60），prepare/settle 各 3s 可对齐原作"确认对话→rush→开门"节奏 |
| 坑道ラッシュ | 无计时，到达目标点结束 | 用 ScriptedEventTrigger（到达即结束）或 holdout 完成事件，不建议做倒计时 |
| 147 フィナーレ | 无计时，击杀数驱动（≥4 开门+暴君二段） | 保持现有击杀数驱动设计，与《原作147图…分析.md》一致 |
| ゾンビ狩り支线 | 60s 起始、击杀续表、180s 上限、结算画面 | 可作为 PC 终端支线玩法复用 HoldoutMachine + 击杀加时钩子 |
