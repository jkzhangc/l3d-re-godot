# 寻路方案对比

> 2026-07-19 | AStarGrid2D 存在未解决的路由异常问题，调研替代方案

---

## 已尝试：AStarGrid2D（Godot 内置）

**问题**：网格初始化正确（所有 walkable/solid 标记已验证），但 `get_point_path()` 在有密集地面图块 + 边缘空洞的场景中返回异常长路径（绕整个地图边缘走），原因不明。可能是 Godot 4.6.3 的 bug 或特定网格布局触发的边界条件。

---

## 替代方案

| 方案 | 复杂度 | 性能 | 适用场景 | 说明 |
|------|--------|------|----------|------|
| **NavigationAgent2D** | 中 | 高(C++) | 任意形状障碍 | Godot 内置，需烘焙导航多边形 |
| **自定义 A\*** | 高 | 低(GDScript) | 栅格地图 | 完全可控，但 GDScript 实现慢 |
| **墙跟随** | 低 | 高 | 简单障碍 | 右/左手法则，走迷宫但非最短路径 |
| **射线绕障** | 低 | 高 | 稀疏障碍 | 当前项目的原始方案，卡墙角 |
| **寻路点图** | 中 | 高 | 固定关卡 | 手动放置导航点 + 图搜索 |
| **NavigationServer2D** | 中 | 高(C++) | 动态障碍 | 比 NavigationAgent 更底层灵活 |

---

## 各方案详解

### 1. NavigationAgent2D（推荐尝试）

```
设置步骤：
  1. 在场景中添加 NavigationRegion2D 节点
  2. 从 TileMapLayer 烘焙导航多边形（bake_navigation_polygon）
  3. 敌人挂 NavigationAgent2D，调用 target_position = 玩家位置
  4. 用 get_next_path_position() 获取下一个路径点
```

- ✅ Godot 内置 C++ 实现，性能好
- ✅ 烘焙一次，所有敌人共享
- ✅ 自动处理任意形状障碍
- ❌ 需要额外的 NavigationRegion 烘焙步骤
- ❌ 和 TileMapLayer 的集成需要测试

### 2. 自定义 A\*（GDScript）

```
在 GDScript 中实现标准 A* 算法：
  - 用 Dictionary 存储 grid（坐标→walkable）
  - 手动实现 open/closed 列表
  - 每 0.3s 为每个敌人计算一次路径
```

- ✅ 完全可控，可以加日志调试
- ✅ 不依赖 Godot 特定版本 API
- ❌ GDScript 性能差（相比 C++），敌人多时会卡
- ❌ 代码量大，维护成本高

### 3. 墙跟随（最简单）

```
if 前方被墙挡住:
    沿着墙走（右手法则）
else:
    直接朝玩家走
```

- ✅ 实现极其简单（~20 行代码）
- ✅ 能绕开凸形障碍
- ❌ 凹形障碍可能走不出来
- ❌ 路径可能很长（迷宫式）

### 4. 射线绕障（改进版）

```
当前项目的原始方案，可以改进：
  - 增宽检测角度范围
  - 多射线探测（3-5 条扇形射线）
  - 结合上下文追踪（记住上次绕的方向）
```

- ✅ 已有基础代码
- ✅ 简单障碍没问题
- ❌ 复杂布局还是会卡

### 5. 寻路点图（Navigation Points）

```
在编辑器中手动放置 NavigationPoint 节点
用 Area2D 检测邻近点
A* 在点图上搜索
```

- ✅ 可预测、可控
- ✅ 适合固定关卡
- ❌ 需要手动设置
- ❌ 不能动态适应

### 6. NavigationServer2D

```
直接用 NavigationServer2D API：
  - map_create() / region_create()
  - agent_create() / agent_set_target()
  - 比 NavigationAgent2D 更底层
```

- ✅ 最灵活
- ✅ C++ 性能
- ❌ API 复杂
- ❌ 文档少

---

## 建议优先级

1. **先试 NavigationAgent2D** — 和 AStarGrid2D 类似的内置方案，但烘焙机制不同，也许能避开 AStarGrid2D 的 bug
2. **再试墙跟随** — 如果 NavigationAgent2D 也不行为，用最简单的墙跟随保证基本可用
3. **最后手写 A\*** — 前两个都失败的话，手写是最可靠的后备

---

## 相关文件

- `script/global.gd` — 当前寻路网格代码（AStarGrid2D，待替换）
- `script/game_init.gd` — 寻路初始化
- `script/enemy.gd` — 敌人寻路调用（已清理）
- `script/enemy/EnemyChaseState.gd` — 追击状态（当前用 move_and_collide 直接走）
