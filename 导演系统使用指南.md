# 导演系统使用指南

> のび太的求生之路 — L4D2 AI Director 节点放置与参数配置手册

---

## 目录

1. [快速开始](#1-快速开始)
2. [DirectorConfig — 地图参数覆盖](#2-directorconfig)
3. [SpawnPoint — 敌人生成点](#3-spawnpoint)
4. [SpawnZone — 敌人生成区域](#4-spawnzone)
5. [SafeDoor — 安全门](#5-safedoor)
6. [TeleportPoint — 传送点](#6-teleportpoint)
7. [ScriptedEventTrigger — 剧本事件触发器](#7-scriptedeventtrigger)
8. [完整场景搭建示例](#8-完整场景搭建示例)
9. [调试快捷键](#9-调试快捷键)

---

## 1. 快速开始

导演系统通过 **Director autoload**（`script/director/director.gd`）自动运行。在地图中放置以下节点即可控制行为：

| 节点 | 用途 | 必放？ |
|------|------|--------|
| `DirectorConfig` | 覆盖地图的生成参数（节奏/数量/间隔） | 推荐（否则用默认值） |
| `SpawnPoint` / `SpawnZone` | 标记敌人从哪里出现 | **必须**（否则在玩家周围随机生成） |
| `SafeDoor` | 安全屋存档点 | 按需 |
| `TeleportPoint` | 场景间传送 | 按需 |
| `ScriptedEventTrigger` | 剧本防守事件 | 按需 |

**环境要求**：
- 场景必须有 `GroundLayer`、`DecorLayer`、`UpperLayer` 三层 TileMapLayer
- 玩家和敌人必须是 **DecorLayer 的子节点**
- 导演系统自动识别安全屋场景（名称含"安全屋"→不刷怪）

---

## 2. DirectorConfig

**文件**：`script/director/director_config.gd`  
**类型**：`Node`（普通 Node，可用 Inspector 调参）

### 放置方法

在场景根节点下添加一个 Node，挂 `DirectorConfig` 脚本：

```
街道.tscn
├── GroundLayer
├── DecorLayer
├── UpperLayer
├── DirectorConfig   ← 放在这里
├── Player
└── Camera2D
```

### 全部参数

#### 启用
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `spawn_enabled` | bool | true | 该地图是否启用敌人生成。关掉后完全不刷怪，适合安全屋/对话场景 |

#### 节奏 — Build-up（压力爬升）
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `build_min` | float | 36s | 爬升阶段最短时长 |
| `build_max` | float | 108s | 爬升阶段最长时长（超时强制进入尸潮） |

#### 节奏 — Peak（尸潮爆发）
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `peak_timeout` | float | 60s | 尸潮最长持续（超时强制结束） |
| `peak_intensity_threshold` | float | 0.7 | 紧张度触发线。设1.0=纯靠时间，设0.5=残血立刻尸潮 |

#### 节奏 — Cooldown（喘息）
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `cooldown_min` | float | 0s | 喘息最短时长 |
| `cooldown_max` | float | 10s | 喘息最长时长 |
| `cooldown_intensity_threshold` | float | 0.3 | 提前结束喘息的紧张度触发线 |

#### 生成 — 散兵（爬升阶段）
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `scatter_min` | int | 1 | 每次最少生成几个 |
| `scatter_max` | int | 3 | 每次最多生成几个 |
| `scatter_interval_min` | float | 15s | 两次生成最少间隔 |
| `scatter_interval_max` | float | 30s | 两次生成最多间隔 |

#### 生成 — 尸潮（爆发阶段）
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `horde_total_min` | int | 10 | 一波尸潮最少总敌人 |
| `horde_total_max` | int | 30 | 一波尸潮最多总敌人 |
| `horde_batch_size` | int | 4 | 每批同时生成几个 |
| `horde_batch_interval` | float | 3s | 批次间隔 |

#### 生成 — 限制
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `max_active_common` | int | 15 | 地图最多同时存活敌人 |
| `spawn_min_dist` | float | 400px | 生成离玩家至少多远 |

### 常用预设

**激进（更快更多敌人）**：
```
build_min=10, build_max=25
peak_timeout=20, cooldown_max=3
scatter_interval_min=3, scatter_interval_max=8
horde_total_min=12, horde_total_max=20
max_active_common=15
```

**休闲（更慢更少敌人）**：
```
build_min=30, build_max=60
peak_timeout=45, cooldown_max=15
scatter_interval_min=10, scatter_interval_max=25
horde_total_min=5, horde_total_max=10
max_active_common=6
```

**安全屋（不刷怪）**：
```
spawn_enabled=false
```

---

## 3. SpawnPoint

**文件**：`script/director/spawn_point.gd`  
**类型**：`class_name SpawnPoint extends Node2D`

### 放置方法

在 DecorLayer 下添加 Node2D 节点，挂 `SpawnPoint` 脚本。可用 Godot 编辑器的 Sprite2D 图标标记位置（可选）。

```
DecorLayer
├── SpawnPoint (x=200, y=300)   ← 敌人从这里出现
├── SpawnPoint2 (x=500, y=150)
├── SpawnPoint3 (x=800, y=400)
└── Player
```

### 参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `point_type` | int | COMMON(0) | 生成类型：0=普通感染者, 1=特殊感染者, 2=两者 |
| `facing` | int | 0 | 生成朝向：0=下, 1=左, 2=右, 3=上 |
| `priority` | float | 1.0 | 权重 >1 优先使用，<1 降低使用 |
| `random_facing` | bool | false | true=随机朝向 |
| `enabled` | bool | true | 是否启用 |

### 生成规则

- 距玩家 < `spawn_min_dist`（默认400px）的点会被过滤
- 在有碰撞 tile 上的点会被过滤
- 48px / 2秒内重复使用的点会被过滤
- 优先选择高 `priority` 的点

---

## 4. SpawnZone

**文件**：`script/director/spawn_zone.gd`  
**类型**：`class_name SpawnZone extends Node2D`

### 放置方法

在 DecorLayer 下添加 Node2D 节点，挂 `SpawnZone` 脚本：

```
DecorLayer
├── SpawnZone (x=400, y=500) zone_size=(256,128)  ← 敌人在此矩形内随机位置出现
└── Player
```

### 参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `zone_size` | Vector2 | (128,128) | 区域尺寸（px） |
| `zone_type` | int | COMMON(0) | 同 SpawnPoint |
| `max_spawns` | int | 5 | 同时最多从此区生成几个 |
| `facing` | int | 0 | 生成朝向 |
| `random_facing` | bool | false | 随机朝向 |
| `enabled` | bool | true | 是否启用 |

### 与 SpawnPoint 的区别

| | SpawnPoint | SpawnZone |
|------|-----------|-----------|
| 生成位置 | 精确坐标 + 小散步(±12px) | 区域内随机 |
| 适用场景 | 门口、角落等精确位置 | 开阔区域、从"某个方向"来 |
| 随机性 | 低 | 高 |

---

## 5. SafeDoor

**文件**：`script/director/safe_door.gd`  
**类型**：`class_name SafeDoor extends Area2D`

### 放置方法

在 DecorLayer 下添加 Area2D 节点，挂 `SafeDoor` 脚本，添加 `CollisionShape2D`（矩形）：

```
DecorLayer
├── SafeDoor
│   └── CollisionShape2D  (矩形，盖住门口区域)
└── Player
```

### 参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `heal_on_enter` | bool | true | 进入时回满血 |
| `save_on_enter` | bool | true | 进入时捕获内存 checkpoint（先回血再快照） |
| `reset_pacing` | bool | true | 进入时重置导演节奏（切到 Cooldown） |
| `one_shot` | bool | true | 只触发一次 |
| `enter_message` | String | "安全屋" | 进入提示文字 |

### 行为

玩家进入 Area2D → 先回满血 → **捕获内存 checkpoint**（纯内存，不写磁盘）→ 导演强制进入喘息阶段。文字标签显示 2 秒后自动隐藏。

---

## 6. TeleportPoint

**文件**：`script/director/teleport_point.gd`  
**类型**：`class_name TeleportPoint extends Node2D`

### 放置方法

在 DecorLayer 下添加 Node2D 节点，挂 `TeleportPoint` 脚本：

```
DecorLayer
├── TeleportPoint (x=277, y=121)   ← 安全屋出口
│   target_scene="res://scene/maps/突袭-第一关-街道.tscn"
└── Player
```

### 参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `target_scene` | String | "" | 目标场景路径，如 `"res://scene/maps/街道.tscn"` |
| `capture_checkpoint` | bool | true | 传送前捕获内存 checkpoint（安全屋出口应为 true） |
| `walk_texture` | Texture2D | — | 精灵表（VX Ace 行走图，4列×8角色格式） |
| `walk_char_index` | int | 0 | 精灵表中的角色索引（0-7） |
| `walk_direction` | int | 0 | 朝向：0=下, 1=左, 2=右, 3=上 |
| `step_frames` | Array[int] | [1,0,1,2] | 踏步帧序列 |
| `step_duration` | float | 0.25 | 每帧秒数 |
| `animated` | bool | true | 是否踏步动画（false=静态站立） |
| `interact_label` | String | "前往" | 交互提示文字 |
| `interact_range` | float | 48 | 交互触发距离（px） |

### 使用方法

1. 放置节点 → 设 `target_scene` → 拖入 `walk_texture` 精灵表
2. 调 `walk_char_index` 和 `walk_direction` 选角色和朝向
3. 玩家靠近（`interact_range` 内）时显示提示文字
4. 按**确定键**（Space/Z）→ 捕获内存 checkpoint → 切换场景

---

## 7. ScriptedEventTrigger

**文件**：`script/director/event_trigger.gd`  
**类型**：`class_name ScriptedEventTrigger extends Area2D`

### 放置方法

在 DecorLayer 下添加 Area2D 节点，挂 `ScriptedEventTrigger` 脚本，添加 `CollisionShape2D`（矩形）。编辑器里会绘制半透明橙色区域。

```
DecorLayer
├── ScriptedEventTrigger
│   ├── CollisionShape2D (矩形，盖住触发区域)
│   └── HintLabel (自动生成)
└── Player
```

### 参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `event_type` | int | CRESCENDO(0) | 事件类型（见下表） |
| `event_duration` | float | 60s | 防守倒计时（秒） |
| `spawn_interval` | float | 3s | 每波生成间隔 |
| `spawn_per_wave` | int | 3 | 每波敌人数量 |
| `max_active` | int | 12 | 事件期间最大存活敌人数 |
| `one_shot` | bool | true | 只触发一次 |
| `auto_trigger` | bool | true | 玩家进入自动触发 |
| `event_name` | String | "" | 事件名（为空则用节点名） |
| `hint_message` | String | "防守直到警报停止！" | HUD 提示 |
| `show_debug_rect` | bool | true | 是否绘制橙色触发区域 |
| `debug_rect_color` | Color | 橙色半透明 | 区域颜色 |

### 事件类型

| 值 | 类型 | 行为 |
|----|------|------|
| 0 | **CRESCENDO** | 计时防守 — 持续刷怪 `event_duration` 秒 → 时间到结束 |
| 1 | FINALE | 最终章节（预留） |
| 2 | ALARM | 警报事件（预留） |
| 3 | BOSS | Boss 出场（预留） |
| 4 | HORDE | 直接触发尸潮（预留） |

### Crescendo 防守事件流程

```
玩家进入触发区域
  ↓
EventTrigger → Director → EventManager
  ↓
① 暂停正常节奏，切到 Peak 模式
② 禁用 SpawnManager 自动生成
③ 按 spawn_interval 定时刷怪（每波 spawn_per_wave 个）
④ 场上敌人数 ≥ max_active 时暂停生成
⑤ 倒计时 event_duration 秒
  ↓
时间到 → 事件结束
  ↓
⑥ 恢复正常节奏，强制 Cooldown
⑦ 恢复 SpawnManager
⑧ 禁用触发区域（one_shot=true）
```

### 完整配置示例

**"开门警报"防守事件**：
```
event_type = CRESCENDO
event_duration = 45          ← 防守 45 秒
spawn_interval = 4           ← 每 4 秒出一波
spawn_per_wave = 3           ← 每波 3 个
max_active = 10              ← 场上最多 10 个
hint_message = "警报响起！守住门口！"
one_shot = true
```

---

## 8. 完整场景搭建示例

### 标准战斗地图（街道）

```
突袭-第一关-街道.tscn
├── GameInit (挂 game_init.gd)
├── GroundLayer (TileMapLayer)           ← 地面图块
├── DecorLayer (TileMapLayer, y_sort)    ← 玩家/敌人父节点
│   ├── Player (CharacterBody2D)
│   ├── SpawnZone (区域1)                ← 敌人生成区
│   ├── SpawnZone2 (区域2)
│   ├── SpawnZone3 (区域3)
│   ├── WeaponPickup (手枪)              ← 地图武器
│   ├── WeaponPickup2 (小刀)
│   ├── ScriptedEventTrigger             ← 剧本防守事件
│   │   └── CollisionShape2D
│   └── SafeDoor                         ← 关卡终点安全门
│       └── CollisionShape2D
├── UpperLayer (TileMapLayer, y_sort)    ← 上层装饰
├── DirectorConfig (挂 director_config.gd)  ← 生成参数
├── Menu (menu_controller)
└── Camera2D (camera_follow)
```

### 安全屋（不刷怪）

```
突袭-第一关-开头安全屋-户外.tscn
├── GameInit
├── GroundLayer
├── DecorLayer
│   ├── Player
│   ├── TeleportPoint                    ← 出门传送点
│   └── WeaponPickup (霰弹枪)            ← 可选武器
├── UpperLayer
├── Menu
└── Camera2D

注意：安全屋不放置 DirectorConfig，且场景名含"安全屋"→ 导演不刷怪
```

---

## 9. 调试快捷键

| 键 | 功能 |
|----|------|
| **F2** | 手动刷 5 个敌人在有效 SpawnPoint/SpawnZone |
| **TAB** | 切换调试可视化（碰撞体 + A* 路径 + 血条） |

### 日志关键词

Godot 控制台过滤以下关键词来追踪导演行为：

| 日志前缀 | 含义 |
|---------|------|
| `[Director]` | 总控：生成、场景切换、配置加载 |
| `[Pacing]` | 节奏阶段切换（build/peak/cooldown） |
| `[SpawnManager]` | 散兵/尸潮生成详情 |
| `[EventManager]` | 自然尸潮触发 + 剧本事件 |
| `[ItemManager]` | 物品投放决策 |
| `[EventTrigger]` | 剧本事件触发/完成 |
| `[SafeDoor]` | 安全门进入 |
| `[TeleportPoint]` | 传送执行 |

---

## 附录：紧张度公式

```
intensity = clamp(
    hp_factor       × 0.30 +   ← 血量越低越高，(1-hp%)²
    ammo_factor     × 0.20 +   ← 弹药越少越高
    proximity_factor × 0.15 +  ← 敌人越近越高
    progress_factor  × 0.15 +  ← 关卡进度越远越高
    combat_factor    × 0.20,   ← 战斗中=0.8, 脱战=0.2
    0.0, 1.0
)
```

紧张度直接影响：
- 散兵每次生成数量（0.2以下最少，0.4以上最多）
- 是否提前从 Cooldown → Build-up（>0.3）
- 是否提前从 Build-up → Peak（>0.7）
- 是否触发自然尸潮（>0.65 持续 8 秒）
