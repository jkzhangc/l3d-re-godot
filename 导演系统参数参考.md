# 导演系统参数参考

> 所有参数均可在地图场景中放置 `DirectorConfig` 节点独立设置。Inspector 中鼠标悬停可看详细中文提示。

---

## 一、节奏三阶段

```
紧张度
  ↑
  │           ████  ← Peak（尸潮爆发）
  │          █    █
  │    ██████      ██████
  │   █                    ██  ← Build-up（压力爬升，散兵渐多）
  │  █                        ██
  │ █                            ████  ← Cooldown（喘息，不刷怪）
  └────────────────────────────────────→ 时间
```

| 阶段 | 含义 | 敌人 | 时长 | 进入下一阶段条件 |
|------|------|------|------|----------------|
| **Cooldown**（喘息） | 尸潮刚结束，玩家搜刮回状态 | 不生成 | 0~10s | 时间到 **或** 紧张度>0.3（玩家主动撞怪） |
| **Build-up**（爬升） | 零星敌人慢慢增多，压迫感累积 | 每8~18s 出 1~3 个散兵，**首次立即刷** | 15~40s | 时间到 **或** 紧张度>0.7（残血/没子弹） |
| **Peak**（爆发） | 尸潮！大量敌人分批涌来 | 总共8~15个，每批4个，间隔2~5s | 最长30s | 敌人杀光 **或** 超时 |

---

## 二、紧张度（Intensity）是什么

一个 0.0~1.0 的浮点数，衡量玩家当前压力：

| 因子 | 权重 | 说明 |
|------|------|------|
| HP | 30% | 血量越低越高。满血=0，半血=0.25，濒死=0.81 |
| 弹药 | 20% | 弹药越少越高。弹尽粮绝=1.0 |
| 敌人距离 | 15% | 敌人越近越高。300px内有多个敌人=高压 |
| 关卡进度 | 15% | 越接近终点越高。刚出门=0.3，到终点=1.0 |
| 战斗状态 | 20% | 身边有活着的敌人=0.8，没有=0.2 |

紧张度直接影响：
- 是否提前从 Cooldown 进入 Build-up（>0.3）
- 是否提前从 Build-up 进入 Peak（>0.7）
- 散兵每次生成数量（越高越多，最低至少 `scatter_min` 个）

---

## 三、全部参数速查

### 启用

| 参数 | 默认 | 当前街道 | 说明 |
|------|------|---------|------|
| `spawn_enabled` | ✅ | ✅ | 关掉后该地图完全不刷怪 |

### 节奏 — Build-up（压力爬升）

| 参数 | 默认 | 当前街道 | 说明 |
|------|------|---------|------|
| `build_min` | 36s | **15s** | 爬升最短时长 |
| `build_max` | 108s | **40s** | 爬升最长时长（超过强制进入尸潮） |

### 节奏 — Peak（尸潮爆发）

| 参数 | 默认 | 当前街道 | 说明 |
|------|------|---------|------|
| `peak_timeout` | 60s | **30s** | 尸潮最长时长（超时强制结束，防止卡关） |
| `peak_intensity_threshold` | 0.7 | 0.7 | 紧张度触发线。设 1.0=纯靠时间，设 0.5=残血立刻尸潮 |

### 节奏 — Cooldown（喘息）

| 参数 | 默认 | 当前街道 | 说明 |
|------|------|---------|------|
| `cooldown_min` | 0s | **0s** | 喘息最短时长 |
| `cooldown_max` | 10s | **5s** | 喘息最长时长。0=结束立刻下一轮 |
| `cooldown_intensity_threshold` | 0.3 | 0.3 | 提前结束喘息的紧张度触发线 |

### 生成 — 散兵（爬升阶段）

| 参数 | 默认 | 当前街道 | 说明 |
|------|------|---------|------|
| `scatter_min` | 1 | 1 | 每次最少生几个（紧张度<0.2时也至少刷这么多） |
| `scatter_max` | 3 | 3 | 每次最多生几个（紧张度高时取此值） |
| `scatter_interval_min` | 15s | **4s** | 两次生成最少间隔 |
| `scatter_interval_max` | 30s | **10s** | 两次生成最多间隔（实际随机浮动） |

> **首次立即刷**：进入 Build-up 阶段时第一次散兵不等待，立即可生成。后续批次才按间隔走。

### 生成 — 尸潮（爆发阶段）

| 参数 | 默认 | 当前街道 | 说明 |
|------|------|---------|------|
| `horde_total_min` | 10 | **8** | 一波尸潮最少总敌人 |
| `horde_total_max` | 30 | **15** | 一波尸潮最多总敌人 |
| `horde_batch_size` | 4 | 4 | 每批生成几个（不是一次性全出） |
| `horde_batch_interval` | 3s | 3s | 批次间隔（实际 2~5s 随机） |

### 生成 — 限制

| 参数 | 默认 | 当前街道 | 说明 |
|------|------|---------|------|
| `max_active_common` | 15 | **10** | 地图上最多同时存活多少个敌人 |
| `spawn_min_dist` | 400px | **300px** | 生成位置离玩家至少多远 |

---

## 四、当前地图参数汇总

### 街道（突袭-第一关-街道.tscn）

已在场景中放置 `DirectorConfig` 节点，实际生效值：

```
build_min = 15s       build_max = 40s       ← 爬升 15~40s
peak_timeout = 30s                           ← 尸潮最多 30s
cooldown_min = 0s     cooldown_max = 5s      ← 喘息 0~5s
scatter_interval_min = 4s  scatter_interval_max = 10s  ← 每 4~10s 出散兵
horde_total_min = 8   horde_total_max = 15   ← 尸潮 8~15 个
max_active_common = 10                        ← 同屏上限 10
spawn_min_dist = 300px                        ← 生成距离 300px
```

### 安全屋（突袭-第一关-开头安全屋-户外.tscn）

未放置 `DirectorConfig`，且 `_is_spawn_map()` 黑名单中匹配"安全屋"关键词，**不刷怪**。

---

## 五、常见调参场景

### 更激进（想更快更多敌人）

```
build_min = 10, build_max = 25
peak_timeout = 20
cooldown_max = 3
scatter_interval_min = 3, scatter_interval_max = 8
horde_total_min = 12, horde_total_max = 20
max_active_common = 15
```

### 更休闲（想更慢更少敌人）

```
build_min = 30, build_max = 60
peak_timeout = 45
cooldown_max = 15
scatter_interval_min = 10, scatter_interval_max = 25
horde_total_min = 5, horde_total_max = 10
max_active_common = 6
```

### 安全屋 / 对话场景

```
spawn_enabled = false
```
