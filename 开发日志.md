# 开发日志

## 2026-08-27：主项目联机主干完善、客户端投掷物拾取修复与代码注释补充

### 联机主干现状

主项目的局域网合作模式已采用 **Host 权威 + 显式 RPC/快照**：`NetworkManager` 管理 ENet、握手、角色选择和跨场景会话；`NetworkWorld` 仅允许 Client 提交输入/操作请求，由 Host 结算移动、战斗、敌人、子弹、掉落物、安全门和章节流程。高频位置使用 `unreliable_ordered`，Spawn/Despawn、拾取和流程事件使用 reliable RPC；未引入 `MultiplayerSpawner` / `MultiplayerSynchronizer`。

### Bug 修复：Client 无法拾取投掷物

- **现象**：Client 能看见投掷物掉落物，却无法提交拾取请求。
- **根因**：Client 应用掉落物快照时，把投掷物节点错误标记成只读表现节点，交互逻辑因此被拦截。
- **修复**：投掷物在 Client 侧保留“请求拾取”的交互入口；实际距离、库存、物品归属和销毁仍只由 Host 的拾取事务校验并广播。
- **回归结果**：双端无头测试确认 Host 记录 `HOST_PICKUP`，Client 收到库存变化及掉落物移除，`AUTO_CLIENT_THROWABLE_PICKUP_COMPLETE` 通过。

### 可维护性

为联机与单人共用的核心脚本补充中文注释，重点标出单人/Host/Client 的职责边界、状态机切换、`PlayerState`/`Players` 数据来源，以及武器、治疗、投掷物、子弹、敌人和 Director 的权威路径。本轮共涉及 41 个核心脚本，新增约 140 行说明性注释。

### 验证

- Godot 4.6.3 无头编辑器扫描通过（退出码 `0`）。
- 联机 Host/Client 自动回归覆盖战斗、掉落物、投掷物拾取、安全门等已接入场景。
- 已知的文字渐变与 Windows 根证书警告为既有环境噪声，不属于本轮修改。

---

## 2026-08-19：Global 单例重构 S6 — 队伍上限扩为 4 人

完整方案与进度见 **`Global重构-PlayerState方案.md`**。

### 完成内容

- 角色选择菜单的队伍上限从 3 提高到 4，右侧队伍面板自动生成第 4 个槽位。
- 新增 InputMap 动作 `选择队员4键`，绑定 **Ctrl+4**。
- 角色切换管理器由 1–3 人的硬编码分支改为按实际座位数匹配 `选择队员N键`，1–4 名队员共用同一套逻辑。
- 删除无调用方的 `CharacterData.to_dict()`；存档继续由 `PlayerState.to_dict()` 与 `ItemCodec.to_dict()` 负责。

### 验证

- Godot 4.6.3 运行角色选择场景成功；第 4 槽位清晰可见，未超出菜单窗口或与底部操作提示重叠。
- 未引入 RPC、peer、`MultiplayerSpawner`、`MultiplayerSynchronizer`、房间或大厅逻辑；本阶段仍是纯单机架构清理。

### 结果

Global → PlayerState 与玩家注册表的 S1–S6 全部完成。后续若继续联机准备，应回到 `联机系统架构设计.md` 的启动门槛，优先完成特殊感染者、Director Phase 4、倒地/救援和完整关卡流程，而不是直接添加网络层。

---

## 2026-08-19：Global 单例重构 S4 — 调用侧脱离 per-player shim

完整方案与进度见 **`Global重构-PlayerState方案.md`**。

### 完成内容

- 玩家实体和全部玩家状态机改为从实体绑定的 `PlayerState` 读写 HP、TP、装备、弹药、消耗品与背包。
- 新增 `Players.get_state_for_entity(node)`：Director / 拾取物按实际交互或评估的实体反查 seat，避免多实体时错误使用本地激活座位。
- 武器与消耗品拾取、战斗 HUD、安全门回满血、启动日志、Director 紧张度/补给评估均完成迁移。
- 修复 Director 先前“HP 读传入玩家、弹药/装备读 Global”的状态源不一致。

### 验证

- 静态扫描确认 `script/global.gd` 之外不再有任何 `Global.<per-player>` 调用。
- 安全屋场景以 Godot 4.6.3 启动进入 live；玩家实体和 seat 0 的 HP/TP 一致，Director 的弹药和补给查询按传入实体状态正常返回。
- 未引入 RPC、peer、MultiplayerSpawner/Synchronizer 或房间/大厅逻辑；本阶段仍是纯单机架构清理。

### 下一步

S6 已完成；Global → PlayerState 与玩家注册表的 S1–S6 全部收尾。

---

本文件记录项目开发过程中的错误修复、架构变更和重要决策。

---

## 2026-08-19：Global 单例重构 S3 — 玩家实体注册表接线

完整方案与进度见 **`Global重构-PlayerState方案.md`**。

### 改动

- `player.gd` 在 `_ready()` / `_exit_tree()` 注册和注销玩家实体。
- HUD、相机、角色切换、菜单武器状态、Director、物品管理、传送点与敌人 fallback 统一改走 `Players`，删除各文件重复的 group 扫描和递归场景树查找。
- 消耗品扣除仍由 `PlayerState` 负责；HP/TP 效果迁到玩家实体的 `apply_item_effects()`。删除 `Global._apply_item_effects()`，S4 前的 `Global.use_healing_item()` / `use_support_item()` 只负责经注册表转发。

### 实测验证（Godot 4.6.3，MCP `project_run` + `game_eval`）

- 安全屋启动正常，注册表为 1 座位 / 1 实体；本地、全部、最近查询指向同一个 `Player`。
- HUD 与相机绑定注册实体；菜单武器状态和传送点查找结果正确。
- 主动注销后 group 时序兜底可恢复注册，实体数保持 1。
- 治疗品测试：数量 1 → 0，HP 100 → 125；辅助品测试：槽位清空，TP 10 → 40；测试结束后原状态完整还原。
- Director 正常启动；Director/ItemManager 查找包装与 `enemy.gd` fallback 已静态确认只读 `Players`；本测试地图未放置敌人节点。
- 运行期仅有既有文字色表加载警告，与本轮无关。

### 下一步

S1–S6 已完成。

---
## 2026-08-19：Global 单例重构（S1–S2）— per-player 状态迁至 PlayerState

完整方案与进度见 **`Global重构-PlayerState方案.md`**。

### 起因

评估「联机现在能不能加」，结论是**不能，但拦路的不是网络代码而是 `Global` 单例**。`Global` 的 per-player 字段结构上只装得下 1 个玩家，上一轮联机的 10 参数 RPC、傀儡节点、外观字段逐个广播全都是在绕这个限制。同时这套「顶层单值 ↔ team[idx] 手工双向拷贝」正是 08-12 那三个角色切换 bug 的温床。

**本轮是纯单人重构，不写任何网络代码**（遵守 `联机系统架构设计.md` §9.5 启动门槛，已核实特殊感染者/Director Phase4/倒地救援/完整关卡均未完成）。

### 架构变更

| 新增 | 作用 |
|------|------|
| `script/player_state.gd`（`PlayerState extends RefCounted`） | 单个玩家/队员的完整状态。单人下每个队伍座位一份 |
| `script/player_registry.gd`（autoload `Players`） | 座位层（PlayerState）+ 实体层（玩家节点）注册表 |
| `script/item_codec.gd`（`ItemCodec`） | 物品 Dictionary 编解码，**打断 PlayerState ↔ SaveManager 循环依赖** |

- 迁移期曾以 `Global` per-player 转发 shim 保护调用侧；调用迁移完成后已在 S5 全部删除，`PlayerState` 现为唯一入口
- `Global.team` / `current_team_index` 及 `_apply_team_member_to_global` / `_save_global_to_team_member` / `get_current_team_member` / `set_active_team_index` / `_find_player_node` **全部删除**
- **角色切换从「11 个字段来回拷贝」变成「换绑座位引用」**
- checkpoint 改存 `seats`（`PlayerState.clone()`），消除旧的「顶层单值 + team 数组」重复
- 存档升到 **v2**（`save_version` 字段），保留 v1 旧存档读取与迁移

### 顺带修掉的 4 个既有 bug

1. **存档漏字段** — v1 `_serialize_team()` 漏存 `current_tp` / `healing_item` / `support_item` / `throwable` / `inventory`，`_deserialize_team()` 还把它们硬编码成 `null`/`[]`。**读档后队员的 TP、治疗品、投掷物、背包会丢**
2. **共享 CharacterData 实例** — v1 `_deserialize_team()` 的 `load()` 后没 `duplicate()`，读档后同角色的多个队员共用一个实例，改一个影响全部，还污染资源缓存里的 `.tres` 母本
3. **双份真相源** — `CharacterData.current_hp/current_tp` 与 `Global.player_hp` 两边都在写。现在明确 **PlayerState 为唯一真相源**
4. **JSON int→float 漂移** — 弹夹值读档后变成 `5.0`，`from_dict()` 现在强制 `int()` 回收

### 实测验证（MCP `project_run` + `game_eval`）

单角色回退模式 ✅ / `to_dict`↔`from_dict` 内存往返 ✅ / 磁盘存档 v2 往返 ✅ / 双座位切换整套换绑 ✅（旧座位数据无损）/ 死亡切换 ✅ / 全队死亡走真死亡流程 ✅

### 关键坑

- **新增 autoload 后编辑器会报假错**：编辑器进程报 `Compile Error: Identifier not found: Players`，但游戏进程全新启动完全正常。编辑器需重启才认新 autoload 名 —— **判据看游戏进程而非编辑器进程**
- **GDScript 循环依赖报错极具误导性**：`PlayerState` ↔ `SaveManager` 互引时报的是 `Identifier not found: Players` 并级联到无关文件，而非「循环依赖」。解法是抽出无依赖的第三方类
- **`Resource.duplicate()` 清空 `resource_path`** → `PlayerState.character_path` 必须单独存
- **`script_patch` 在文件破损态的诊断输出极长**，批量改动改用 `Edit` 工具，最后统一 `project_run` 验证

### 待做

S1–S6 已完成。

---

## 2026-08-18：切换角色镜头跳变修复

### 问题

玩家移动中切换角色时，镜头会突然跳变位置。静止切换无此问题。

### 根源

`character_switch_manager.gd` 的 `_do_switch()` / `switch_after_death()` 末尾调用 `camera_follow.gd::teleport_to_player()` → pcam `teleport_position()`。

Phantom Camera 机制：pcam 节点 `global_position` 每帧直接=玩家位置，但实际渲染位置 `_transform_output.origin` 走 `_smooth_damp` 阻尼，**滞后**于玩家。

- 静止切换：阻尼已收敛，`_transform_output.origin` ≈ 玩家位置，teleport 设同样的值，视觉无变化
- 移动切换：阻尼未收敛，相机滞后于玩家，teleport 把它**瞬间硬拉**到玩家位置 → 可见跳变

而切换角色**不改玩家位置**（`_do_switch` 步骤8「不传送」、`refresh_after_switch` 也不动 position），teleport 是多余的早期遗留。

### 修复

- 移除 `character_switch_manager.gd` 两处 `teleport_to_player()` 调用（普通切换 + 死亡切换），改为让相机继续阻尼跟随
- `camera_follow.gd::teleport_to_player()` 方法保留，供未来场景切换/真正传送用
- 加注释说明为何切换时不瞬切
- 验证：`project_run` 无报错，`game_eval` 确认 pcam 健康（`follow_target=Player`/`is_following=true`/`follow_mode=SIMPLE`）；实测移动中切换无跳变

---

## 2026-08-13：寻路卡顿修复 + 碰撞检测修复 + 战斗 HUD 重构

### A* 寻路卡顿修复

首个敌人追击时才惰性构建 AStarGrid2D（`update()` 为一次性同步开销），街道大图（31897 格）会卡一帧。

- `EnemyChaseState.gd`：新增 `class_name` + 静态方法 `prebuild()` / `tick_build()`，网格构建/可行走判定相关方法改为 `static`
- `game_init.gd`：`_ready()` 延迟调用 `prebuild()`，新增 `_physics_process()` 每帧驱动 `tick_build()`，网格在加载阶段分帧建完（约 22 帧）

### UpperLayer / 半格碰撞体检测修复

- 原 `_tile_center_in_collision()` 单点 (16,16) 检测漏判「半格边缘碰撞体」（宽度/高度 <32 的薄墙条）
- 改为「有碰撞体即阻挡」，并给 `_is_sandwiched_passage()` 加方向校验（只豁免垂直于通道的细条，薄墙本身保持阻挡）

### 战斗 HUD 重构

- 删除旧 `script/ui/hud.gd` / `hud_slot.gd`，新建 `scene/ui/combat_hud.tscn` + `script/ui/combat_hud.gd`
- 左上：HP 框（ＨＰバーＴ）+ HP 填充（ＨＰメーターＴ，`scale.x` 按 HP% 横缩放）+ 急救喷雾数量（回復薬×N）
- 右上：主/副武器图标（`WeaponData.icon`，主上副下）
- 武器按键：`1`/`2` 直接举起对应武器，同键再按放下（改 Idle/Walk/Run + Pistol/Knife 状态）
- `Global` 新增 `healing_item_count`（急救喷雾数量，拾取 +1 使用 -1，接入 checkpoint/队伍存档）
- 5 把武器 `.tres` 填 `icon` 字段；街道/安全屋/main/test 四个场景的旧 HUD 均替换为 CombatHUD

---

## 2026-08-12：角色切换 Bug 修复 + 原地替换模式

### 问题

新增第二角色后切换出现问题：
1. 切换后行走图不更新（始终显示第一个角色）
2. 首次切换到未操控过的角色时传送到地图原点 (0,0)
3. 未操控的角色在地图上显示站立精灵（用户不希望）

### 根因分析

**Bug 1 — `_apply_character_data()` 不更新角色数据**

`player.gd:394` 中 `if not current_character` 守卫导致只在首次 `_ready()` 时初始化 `current_character`，之后切换永远不会从 `Global.player_character` 同步新角色的 `walk_char_index`、`walk_texture` 等字段，行走图维持旧角色不变。

**Bug 2 — 未操控队员初始位置为 Vector2.ZERO**

`character_select_menu.gd:_confirm_team()` 将所有队员的 `position` 初始化为 `Vector2.ZERO`。首次切换到该角色时 `_do_switch` 会传送到 (0,0)。

### 修复内容

| 文件 | 改动 |
|------|------|
| `player.gd::_apply_character_data()` | 移除 `if not current_character` 守卫，始终从 `Global.player_character` 同步 |
| `character_switch_manager.gd::_do_switch()` | 切换不再传送（保持当前玩家位置），改为更新目标队员位置 |
| `character_switch_manager.gd::switch_after_death()` | 同上，死亡切换也不传送 |
| `character_switch_manager.gd` | 新增 `@export var show_teammate_standins: bool = false`，默认不显示队友站立精灵 |
| `character_switch_manager.gd` | 所有 standin 操作函数增加 `show_teammate_standins` 守卫 |
| `character_switch_manager.gd` | 新增 `_reset_player_state_machine()`，切换时重置状态机为 Idle，防止卡在攻击状态 |

### 行为变更

```
旧：切换 → 保存旧角色位置 → 传送玩家到目标角色旧位置 → 创建/显示 standin
新：切换 → 保存旧角色状态 → 原地替换外观/装备/HP → 不创建 standin（默认）
```

切换现在是纯粹的"角色替换"：同一位置、同一玩家节点，只更换外观/装备/HP。

### 相关代码位置

| 文件 | 行号 | 内容 |
|------|------|------|
| `script/player.gd` | 394–399 | `_apply_character_data()` — 角色数据同步 |
| `script/character_switch_manager.gd` | 10–11 | `show_teammate_standins` 导出变量 |
| `script/character_switch_manager.gd` | 223–258 | `_do_switch()` — 切换主逻辑 |
| `script/character_switch_manager.gd` | 276–339 | `switch_after_death()` — 死亡切换 |
| `script/character_switch_manager.gd` | 261–274 | `_reset_player_state_machine()` — 状态机重置 |

---

## 2026-08-11：修复敌人寻路无法通过 32px 狭窄通道

### 问题

敌人 A\* 寻路时不会走进宽度或高度只有 32px（1 格）的狭窄通道，路径点绕开这些区域。

### 根因分析

两个问题叠加导致：

**1. `_is_walkable()` 碰撞判定对 autotile 边缘变体误判**

旧版 `_tile_center_in_collision()` 检查图块中心 (16,16) 落入碰撞多边形即视为墙阻挡。A4 墙壁 autotile 的边缘/角落变体（碰撞覆盖中心）会被放置在通道格中，导致通道被误判为墙体。

**2. `_push_from_walls()` 推力过大**

在通道入口处（3+ 面有墙），对角线推力累加可超过 40px — 大于 32px 的格子宽度，路径点被推出可行走格进入相邻墙体。

### 修复内容

| 文件 | 改动 |
|------|------|
| `EnemyChaseState.gd::_is_walkable()` | 重写为"收集信息→统一判定"模式。新增狭窄通道豁免：如果 Decor/Upper 碰撞格被两面相对的墙夹在中间（上下墙+左右地面 或 左右墙+上下地面），判定为 autotile 边缘变体进入通道 → 不阻挡 |
| `EnemyChaseState.gd` | 新增 `_is_sandwiched_passage()`（通道夹层检测）、`_cell_has_blocking_tile()`（直接阻挡检测）、`_cell_has_ground_tile()`（地面检测），均不依赖 `_tile_walk_cache` 避免递归 |
| `EnemyChaseState.gd::_push_from_walls()` | 新增狭窄通道保护：当 ≥2 面正方向有墙时，推力 clamp 到 ±14px 内（cell_half - 2），防止路径点被推出格子 |

### 判定逻辑变更

```
旧：center_in_collision? → blocked（autotile 边缘误判为墙）
新：center_in_collision? → blocked（初步）
      └─ 狭窄通道豁免：上下墙+左右地面？或 左右墙+上下地面？
           ├─ 是 → unblocked（autotile 边缘在通道中）
           └─ 否 → 保持 blocked（墙/柱子等实质障碍物）
```

---

## 2026-08-11：A\* 寻路重构 — 手写 A\* 替换为 Godot 内置 AStarGrid2D

### 背景

旧 A\* 寻路是手写实现（GDScript 字典 + while 循环），约 55 行，包含 `_heuristic()`、`_get_neighbors()`、`_reconstruct_path()` 等辅助函数。虽能工作，但：

- GDScript 循环性能不如 C++ 原生
- 代码量大，维护成本高
- 需要自己管理迭代上限、路径平滑等

### 替换内容

| 删除 | 新增 |
|------|------|
| `_heuristic()` — 曼哈顿距离 | `_astar_grid: AStarGrid2D` — 静态全图共享 |
| `_get_neighbors()` — 四方向邻居 | `_ensure_astar_grid()` — 分帧构建网格 |
| `_reconstruct_path()` — 路径回溯 | `_build_step()` — 每帧 1500 格 |
| `MAX_ASTAR_ITERATIONS` | `_mark_enemy_obstacles()` / `_clear_enemy_obstacles()` |
| A\* while 循环（~40 行） | 单行 `get_id_path()` 调用 |

### 分帧构建

**问题**：首次构建需扫描全图（~32000 格），同步阻塞导致首帧明显卡顿。

**方案**：`_ensure_astar_grid()` 创建网格后立即返回 false，`_build_step()` 每物理帧处理 1500 格（同帧去重），构建期间敌人走直接追击（已有降级模式）。200×200 地图约 22 帧（~0.37s）完成，无感知卡顿。

### 敌人互为障碍物

**变更**：寻路时通过 `_mark_enemy_obstacles()` 将其他存活敌人的格子临时标记为 `set_point_solid(true)`，寻路完成后 `_clear_enemy_obstacles()` 恢复。替代旧注释 "敌人互不绕路" 的策略。

### Debug 网格绘制性能修复

**问题**：预构建后 `_tile_walk_cache` 包含全图所有格子（~32000+），每帧 `_draw_debug_walk_grid()` 遍历整个字典导致 Debug 模式严重卡顿。

**修复**：改为以敌人为中心计算 13×13 格范围（~169 格），仅绘制范围内的格子。从遍历 32000+ 字典条目改为 169 次 HashMap 查询。

### 保留功能

- ✅ 路径平滑（共线简化）
- ✅ 碰撞体感知推墙 (`_push_from_walls`)
- ✅ 降级模式（连续失败 → 直接追击）
- ✅ 敌人间软推挤 (`_push_apart_from_other_enemies`)
- ✅ 卡墙恢复 (`_move_with_stuck_recovery`)
- ✅ 调试可视化（路径线、路径点、可行走网格）
- ✅ TileMapLayer 碰撞多边形中心检测

### 提交记录

| Commit | 内容 |
|--------|------|
| `5500a9e` | checkpoint：重构前存档点 |
| `c51b530` | refactor：AStarGrid2D 替换手写 A\* |
| `04cfd88` | perf+feat：Debug 网格性能 + 敌人互绕 |
| `052d44d` | perf：分帧构建消除首帧卡顿 |
| `ce6d4fd` | fix：障碍物计数改为跨帧累计 |

### 相关代码位置

| 文件 | 变更 |
|------|------|
| `script/enemy/EnemyChaseState.gd` | 主要重构：移除手写 A\*，接入 AStarGrid2D + 分帧构建 + 敌人互绕 |
| `script/enemy.gd` | `_draw_debug_walk_grid()` 性能优化 |

---

## 2026-08-11：Bug 修复 — 存档恢复格式化错误

### 修复

- **`global.gd:197`**：`restore_checkpoint()` 的 `print()` 格式化字符串有 5 个占位符但只传了 4 个参数，导致 `String formatting error: not enough arguments for format string`。补上缺失的第 5 个参数 `team.size()`（队伍人数）。

---

## 2026-08-10：推击系统全面优化 — 连锁推挤 + 疲劳冷却 + 攻击转向

### 1. 敌人连锁推挤传播修复

**问题**：被玩家推击的敌人虽然会推开附近敌人，但被推开的敌人不会继续影响更远的敌人（连锁断裂）。

**根因**：`EnemyKnockbackState._chain_push_nearby_enemies()` 跳过了 Hitstun 状态中的敌人，且推挤力度阈值偏高。

**修复**：
- 移除对 Hitstun 状态的跳过 — 击退可打断硬直（重置硬直计时）
- 新增强度阈值常量 `CHAIN_PUSH_FORCE_THRESHOLD = 5.0`（原硬编码 10.0）
- 连锁推挤硬直时长从 0.15s 提升至 0.25s（`CHAIN_PUSH_STUN`）
- 添加调试日志：每次连锁推挤打印推开数量和自身速度

### 2. 敌人互相推开系统

**新增**：`EnemyChaseState._push_apart_from_other_enemies()` — 两个敌人距离 < 28px 时互相推开。

- 推开力度随距离衰减（线性，边缘=0，中心=80px/s）
- 跳过已死亡/击退中的敌人
- 使用 `move_and_collide` 保证与墙壁碰撞兼容

### 3. 敌人寻路避开其他敌人

**修复**：`EnemyChaseState._is_cell_blocked_by_entity()` 恢复对 "enemy" 组中其他敌人的检测（之前被注释/移除，导致敌人 A* 寻路不会绕开其他敌人实体）。

### 4. 玩家攻击中允许转向

**修改**：`PlayerPistolAttackState`、`PlayerKnifeAttackState`、`PlayerShoveState` 的 `physics_update()` 现在调用 `character.update_facing(move_dir)`，攻击中可按方向键改变朝向。

### 5. 放下武器时取消固定朝向

**修改**：`PlayerPistolState._begin_lower()` 和 `PlayerKnifeState._begin_lower()` 在开始放下动画前调用 `character.unlock_facing()`。

### 6. 推击疲劳系统（L4D2 风格）

**新增**：模仿 L4D2 推击机制 — 连续推击 N 次后进入 2 秒冷却，停止推击几秒后重置计数。

特性：
- **可配置**：`CharacterData` 中新增 3 个 `@export` 参数：
  - `shove_fatigue_limit`（默认 3）：连续推击多少次触发冷却，0=禁用
  - `shove_cooldown_duration`（默认 2.0s）：冷却时长
  - `shove_fatigue_reset_time`（默认 3.0s）：停止推击多久后重置计数
- **空闲计时**：在非推击状态下自动累加，达到 `reset_time` 后重置计数
- **冷却中拒绝**：`PlayerShoveState.enter()` 调用 `character.can_shove()` → false 时直接退回武器就绪状态
- **计数递增**：`character.on_shove_performed()` 在推击确认后调用

### 相关代码位置

| 文件 | 变更 |
|------|------|
| `script/enemy/EnemyKnockbackState.gd` | 移除 Hitstun 跳过 + 强度阈值常量 + 连锁推挤日志 |
| `script/enemy/EnemyChaseState.gd` | 新增 `_push_apart_from_other_enemies()` + 寻路敌人障碍恢复 |
| `script/player/PlayerPistolAttackState.gd` | `physics_update` 增加 `update_facing` 调用 |
| `script/player/PlayerKnifeAttackState.gd` | 同上 |
| `script/player/PlayerShoveState.gd` | 攻击转向 + 推击疲劳检查 + `on_shove_performed()` |
| `script/player/PlayerPistolState.gd` | `_begin_lower()` 增加 `unlock_facing` |
| `script/player/PlayerKnifeState.gd` | 同上 |
| `script/player.gd` | 新增推击疲劳变量 + `can_shove()`/`on_shove_performed()`/`_update_shove_fatigue()` |
| `script/character_data.gd` | 新增 `shove_fatigue_limit`/`shove_cooldown_duration`/`shove_fatigue_reset_time` |

---

## 2026-08-10：推击系统三连优化

### 1. 硬直状态推击不再闪白

**问题**：敌人在 Knockback/Hitstun 状态中被再次推击时，仍会播放白色闪光，观感重复/混乱。

**修复**：`enemy.gd:take_damage()` 中将 `current_state` 提前获取，推击（damage=0）时检查是否已在 `Knockback` 或 `Hitstun`，是则跳过 `_play_hit_feedback()`。

**影响**：连续推击视觉更干净，闪白只出现在首次命中。

### 2. 推击溅射击退

**新增**：推中敌人后，周围敌人也会被击退（L4D2 风格群体控场）。

- `WeaponData` 新增 `shove_splash_radius`（默认 64px，Inspector 可调，0=禁用）
- `PlayerShoveState._apply_splash_knockback()`：以推击判定区域为中心，遍历范围内所有敌人
- 跳过已直接命中的目标（`hit_ids` 去重）、已死亡敌人
- 击退方向：从溅射中心向外径向推开
- 力度衰减：中心 100% → 边缘 50%（线性）；时长等比衰减

### 3. 敌人寻路不再互绕

**变更**：`EnemyChaseState._is_cell_blocked_by_entity()` 移除 `"enemy"` 组遍历循环。

- 之前：A* 寻路将其他敌人占据的格子视为障碍 → 绕行（浪费迭代、路径不自然）
- 现在：敌人间仅通过 `move_and_collide` 物理滑墙自然推挤，不消耗 A* 迭代绕路
- 保留 `extra_obstacle_nodes` 检查（多人模式玩家等外部障碍接口不变）

### 4. 推击默认参数对齐小刀

`WeaponData` 默认值更新：
- `shove_char_sequence`: `[3,2]` → `[0,1,1]`
- `shove_frame_duration`: `0.1` → `0.05`
- `shove_knockback_force`: `400` → `300`
- `shove_knockback_duration`: `5.0` → `2.0`
- `shove_splash_radius`: `80` → `64`（新增字段）

### 相关代码位置

| 文件 | 变更 |
|------|------|
| `script/enemy.gd` | 255–267: 闪白判定提前 + 硬直状态跳过 |
| `script/player/PlayerShoveState.gd` | 153–217: 溅射击退方法 `_apply_splash_knockback()` |
| `script/enemy/EnemyChaseState.gd` | 532–543: 移除敌人组遍历，仅保留外部障碍 |
| `script/weapon_data.gd` | 142–158: 推击默认参数对齐小刀 + 新增 `shove_splash_radius` |

---

## 2026-08-04：敌人 A* 寻路实体障碍检测修复

### 问题现象

敌人寻路不会绕开其他敌人，表现为敌人互相推挤/穿模，而不是智能绕行。

### 根因

`EnemyChaseState._is_cell_blocked_by_entity()` 使用 `PhysicsDirectSpaceState2D.intersect_point()` 做**单点物理查询**——只在 32×32 格子的正中心一个像素点检测碰撞体。

```gdscript
# 旧实现（已废弃）
var query: PhysicsPointQueryParameters2D = PhysicsPointQueryParameters2D.new()
query.position = _grid_to_world(gp)  # 格子中心点
query.collision_mask = 8
query.exclude = [character]
var results = _space_state.intersect_point(query)
```

**失效条件**：当敌人的碰撞形状（20×28 矩形）没有覆盖到格子正中心时，检测失败。例如：
- 敌人在格子 (1,1) 边界附近，其碰撞体可能覆盖格子 (2,1) 的部分区域，但不覆盖格子 (1,1) 的中心点
- `intersect_point` 在格子 (1,1) 中心查不到该敌人 → 格子被标记为可通行 → A* 规划穿人路径

**恶化因素**：`move_speed` 100→160、移动逻辑改为全速滑墙（`_move_with_stuck_recovery`）后，敌人更频繁处于格子边界，漏检概率增大。

### 修复

用 `get_tree().get_nodes_in_group("enemy")` 遍历所有敌人，直接比较格子坐标。哪个敌人占哪个格子一目了然，不依赖物理查询精度。

```gdscript
# 新实现
func _is_cell_blocked_by_entity(gp: Vector2i) -> bool:
    var tree: SceneTree = character.get_tree()
    if tree:
        for other in tree.get_nodes_in_group("enemy"):
            if other == character or not is_instance_valid(other):
                continue
            if other.get("_is_dead") == true:
                continue
            if _world_to_grid(other.global_position) == gp:
                return true
    # extra_obstacle_nodes 检查（多人模式接口，保留）
    ...
```

### 影响范围

- **文件**：`script/enemy/EnemyChaseState.gd`
- **变更**：`_is_cell_blocked_by_entity()` 重写；移除不再使用的 `_space_state` 变量
- **副作用**：无。新逻辑更简单、更可靠
- **性能**：A* 搜索最多探索 2000 格，按 50 个敌人计算 = 10 万次迭代/寻路，0.5s 间隔，可忽略

### 相关代码位置

| 文件 | 行号 | 内容 |
|------|------|------|
| `script/enemy/EnemyChaseState.gd` | 532–556 | `_is_cell_blocked_by_entity()` — 实体障碍检测 |
| `script/enemy/EnemyChaseState.gd` | 471–521 | `_is_walkable()` — 调用上述函数 |
| `script/enemy/EnemyChaseState.gd` | 399–408 | `_get_neighbors()` — A* 邻居遍历入口 |
| `script/enemy.gd` | 127 | `add_to_group("enemy")` — 敌人注册到组 |
| `script/enemy.gd` | 88 | `_is_dead: bool` — 死亡标志 |

---

## 2026-08-12：攻击特效偏移 + 角色切换/死亡Bug修复

### 攻击特效位置偏移系统

新增 `WeaponEffectOffsets` Resource 类型，支持按角色 × 武器 × 朝向的四方向独立偏移。

**优先级**：`CharacterData.attack_effect_offsets[武器名].get_offset(朝向)` → `WeaponData.attack_effect_offset_override` → `Vector2.ZERO`

| 文件 | 改动 |
|------|------|
| `script/weapon_effect_offsets.gd` | 新建 Resource 类（down/left/right/up 四方向 Vector2） |
| `script/character_data.gd` | 新增 `attack_effect_offsets: Dictionary` + `get_attack_effect_offset(weapon, facing, fallback)` |
| `script/player/PlayerPistolAttackState.gd` | 特效发射时传入 `character.facing`，查询角色字典 |
| `script/player/PlayerKnifeAttackState.gd` | 同上 |

### Bug #1：切换角色行走图显示武器纹理

**根因**：`refresh_after_switch()` 无条件调用 `enter_weapon_mode()` → `_weapon_mode = true` → `_refresh_sprite()` 渲染武器行走图。此时状态机已在 Idle，不应显示武器纹理。

**修复**：`refresh_after_switch()` 不再进入武器模式，仅记录 `_weapon_data` 引用，`_weapon_mode = false`。按武器键后正常进入武器状态。

### Bug #2+#3：死亡读档血量不恢复 + 武器不同步

**根因**：`_reload_from_save()` 硬编码 `player_hp = 200` 并清空装备，完全没有调用 `restore_checkpoint()`。

**修复**：`_reload_from_save()` 先调 `Global.restore_checkpoint()` 恢复完整状态，再 `checkpoint.clear()`。

### 安全屋自动存档

**新增**：`GameInit._ready()` 检测场景路径含 `安全屋` 或 `safe` 时自动 `capture_checkpoint()`，确保死亡后回到安全屋的状态一致。

| 文件 | 改动 |
|------|------|
| `script/player.gd` | `refresh_after_switch()` — 不进武器模式；`_reload_from_save()` — 恢复 checkpoint |
| `script/game_init.gd` | 安全屋场景加载时自动存档 |

---

## 2026-08-16：移除 puddle_system + 启用 Script-IDE

### GDExtension 版本不匹配错误

启动报错：
```
ERROR: godot-cpp\src\godot.cpp:180 - Cannot load a GDExtension built for Godot 4.7.0 using an older version of Godot (4.6.3).
ERROR: core/extension/gdextension_library_loader.cpp:235 - GDExtension initialization function 'puddle_library_init' returned an error.
```

**根因**：`addons/puddle_system/` 是 C++ 编译的 GDExtension（水洼遮罩系统），DLL 用 Godot 4.7.0 的 godot-cpp 编译，与项目 4.6.3 不兼容。改 `.gdextension` 的 `compatibility_minimum` 无效——运行时校验的是 DLL 编译时的 API 版本，不是声明的 `compatibility_minimum`。

**处理**：删除 `addons/puddle_system/`（未被 git 跟踪的新增目录，删除无需 commit）。

**附带**：Script-IDE 插件（v2.2.3，IDE 风格脚本编辑器，纯 GDScript）此前被误禁用（以为是报错源头），已重新加入 `[editor_plugins] enabled`。

---
