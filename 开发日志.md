# 开发日志

本文件记录项目开发过程中的错误修复、架构变更和重要决策。

---

## 2026-08-05：多人联机系统 Phase 1–3（核心网络基础设施 + 玩家同步）

### 背景

在继续做游戏内容之前，优先建立联机架构，避免后续内容堆积后改造代价指数增长。目标：2 人 Host-Client PvE 合作模式。

### 架构决策

- **Host 权威模型**（类似 L4D2 P2P）：Host 运行敌人 AI / 子弹物理 / 伤害判定 / 导演系统；Client 渲染同步状态
- **Godot 4.6 ENet** 网络层 + **LANServerBroadcast** 插件（Godot 3 → 4.6 手动迁移，13 处 API 适配）
- **RPC 位置同步**（30Hz）代替 MultiplayerSynchronizer（程序化配置 API 不完整，频繁报 C++ 层错误 `!sync || !sync->get_replication_config_ptr()`）

### Phase 1：输入抽象层

**问题**：所有玩家状态类（8 个 State 文件 + weapon_pickup）共 60+ 处直调 `Input` 单例，无法区分本地/远程输入。

**方案**：
- 新建 `PlayerInput` Resource 基类（`get_move_vector()` / `is_action_pressed()` / `is_action_just_pressed()`）
- `LocalPlayerInput` 每帧 `read()` 从 `Input` 单例捕获，存为字典 → 状态类统一从 `character._player_input` 读取
- 联机时远程玩家替换为基类 `PlayerInput`（网络数据填充）

**踩坑**：
| 错误 | 原因 | 修复 |
|------|------|------|
| `Cannot find member in base "Callable"` | 变量名 `_input` 与 Godot 内置 `Node._input(event)` 冲突 | 改名 `_player_input` |
| `LocalPlayerInput not declared` | GDScript 内部类不可跨文件 `class_name` 引用 | 移到独立文件 `local_player_input.gd` |
| `Cannot infer type of "tree"` | `State.gd` 的 `character` 从 `CharacterBody2D` 改为无类型后，`:=` 推断失败 | `var tree: SceneTree = character.get_tree()` |

### Phase 2：网络基础设施

**新增文件**：
- `network_manager.gd` — Autoload（Host/Join/断开/start_game RPC）
- `lobby.gd` + `scene/lobby.tscn` — 联机大厅（创建房间/加入/服务器列表）
- `network_spawner.gd` — 玩家生成管理
- `network_player.gd` + `object/network_player.tscn` — 远程玩家视觉节点

**集成**：
- 标题画面新增 "联机游戏" 入口 → 跳转 Lobby
- `game_init.gd` 扩展 `_spawn_network_support()` → 联机时创建同步节点
- LAN 广播：Host 创建 `ServerAdvertiser`，Client 创建 `ServerListener`

**踩坑**：
| 错误 | 修复 |
|------|------|
| `Node not found: Panel/StatusLabel` | tscn 有 VBoxContainer 嵌套，脚本路径补上 `/VBox/` |
| `PacketPeerUDP.listen()` 不存在 | Godot 4 改名 `bind(port)` |
| `UDP 监听失败 port=3111` | 同机双实例端口冲突 → bind 失败设 `_socket=null` 静默降级 |
| `player_connected 信号重复连接` | `join/host` 前加 `is_connected()` 守卫 |
| Client 连接后 UI 卡 "连接中" | `_on_player_connected` 加状态更新 |
| 服务端 `port=9999.0` | `server_info["port"]` 未转 int → `int(port)` |

### Phase 3a：玩家位置同步

**方案**：RPC 广播代替 MultiplayerSynchronizer

```
Player._physics_process → 每 33ms
  → NetworkManager.broadcast_player_state.rpc(id, x, y, facing, hp, moving, weapon_id)
  → 所有远程 peer 收到 → NetworkSpawner._on_player_state() → 更新 NetworkPlayer 位置
```

**踩坑**：
| 错误 | 修复 |
|------|------|
| `MultiplayerSynchronizer` 每帧报 C++ 层错误 | 程序化配置不完整 → 改用 RPC 方案，删除所有 MultiplayerSynchronizer |
| Host 看到两个自己 | NetworkPlayer 也为自己创建了 → `pid == get_unique_id()` 时跳过创建 |
| 远程玩家无行走动画 | RPC 新增 `moving: bool` → NetworkPlayer 根据状态切换帧 |
| 同步卡顿 | 20Hz → 30Hz，数据量小延迟可接受 |

### Phase 3b：敌人 + 武器掉落物同步（部分完成）

**✅ 已完成**：
- 武器掉落物拾取后 RPC 广播移除 → 所有 peer 可见
- 敌人位置通过 `network_enemy_syncer.gd` 10Hz 广播（使用 `get_path().hash()` 跨 peer 匹配）
- 玩家 RPC 新增 `weapon_id` 字段

**⚠️ 已知问题**（下次修复）：
| 问题 | 根因 |
|------|------|
| 敌人位置回弹 | Client 端 StateMachine 未完全禁用，AI 与 Host 同步位置冲突 |
| Client 打不死敌人 | 伤害只在本地判定，未通过 RPC 传 Host |
| 敌人死亡不同步 | 死亡状态未广播 |
| Client 捡武器 Global 状态不同步 | 各自写本地 Global，无跨 peer 装备同步 |

**踩坑**：
| 错误 | 修复 |
|------|------|
| 敌人位置完全不更新 | `get_instance_id()` 在 Host/Client 上不同 → 改用 `get_path().hash()` |
| `remove_pickup RPC authority` 拒绝 Client 调用 | `@rpc("authority")` → `@rpc("any_peer")` |

### CLAUDE.md 精简

42.7k 字符超限 → 提取详细内容到 `VX动画特效与图块系统.md` + 压缩冗余段落 → 17.5k 字符。

### 本次会话新建文件清单

| 文件 | 用途 |
|------|------|
| `script/player_input.gd` | 输入抽象层基类 |
| `script/player/local_player_input.gd` | 本地输入实现 |
| `script/network_manager.gd` | 网络管理器 Autoload |
| `script/network_player.gd` | 远程玩家视觉节点 |
| `script/network_spawner.gd` | 玩家生成与位置同步 |
| `script/network_enemy_syncer.gd` | 敌人状态同步 |
| `script/network_pickup_syncer.gd` | 武器掉落物同步 |
| `script/lobby.gd` | 联机大厅 UI |
| `scene/lobby.tscn` | 联机大厅场景 |
| `object/network_player.tscn` | NetworkPlayer 场景资源 |
| `VX动画特效与图块系统.md` | VX/Tile 详细参数文档 |
| `LANServerBroadcast插件说明.md` | LAN 插件迁移记录 |

---

## 2026-08-04：敌人 A* 寻路实体障碍检测修复

### 问题现象

敌人寻路不会绕开其他敌人，表现为敌人互相推挤/穿模，而不是智能绕行。

### 根因

`EnemyChaseState._is_cell_blocked_by_entity()` 使用 `PhysicsDirectSpaceState2D.intersect_point()` 做**单点物理查询**——只在 32×32 格子的正中心一个像素点检测碰撞体。

```gdscript
# 旧实现（已废弃）
var query: PhysicsPointQueryParameters2D = PhysicsPointQueryParameters2D.new()
query.position = _grid_to_world(gp)  # 格子中心点
query.collision_mask = 8
query.exclude = [character]
var results = _space_state.intersect_point(query)
```

**失效条件**：当敌人的碰撞形状（20×28 矩形）没有覆盖到格子正中心时，检测失败。例如：
- 敌人在格子 (1,1) 边界附近，其碰撞体可能覆盖格子 (2,1) 的部分区域，但不覆盖格子 (1,1) 的中心点
- `intersect_point` 在格子 (1,1) 中心查不到该敌人 → 格子被标记为可通行 → A* 规划穿人路径

**恶化因素**：`move_speed` 100→160、移动逻辑改为全速滑墙（`_move_with_stuck_recovery`）后，敌人更频繁处于格子边界，漏检概率增大。

### 修复

用 `get_tree().get_nodes_in_group("enemy")` 遍历所有敌人，直接比较格子坐标。哪个敌人占哪个格子一目了然，不依赖物理查询精度。

```gdscript
# 新实现
func _is_cell_blocked_by_entity(gp: Vector2i) -> bool:
    var tree: SceneTree = character.get_tree()
    if tree:
        for other in tree.get_nodes_in_group("enemy"):
            if other == character or not is_instance_valid(other):
                continue
            if other.get("_is_dead") == true:
                continue
            if _world_to_grid(other.global_position) == gp:
                return true
    # extra_obstacle_nodes 检查（多人模式接口，保留）
    ...
```

### 影响范围

- **文件**：`script/enemy/EnemyChaseState.gd`
- **变更**：`_is_cell_blocked_by_entity()` 重写；移除不再使用的 `_space_state` 变量
- **副作用**：无。新逻辑更简单、更可靠
- **性能**：A* 搜索最多探索 2000 格，按 50 个敌人计算 = 10 万次迭代/寻路，0.5s 间隔，可忽略

### 相关代码位置

| 文件 | 行号 | 内容 |
|------|------|------|
| `script/enemy/EnemyChaseState.gd` | 532–556 | `_is_cell_blocked_by_entity()` — 实体障碍检测 |
| `script/enemy/EnemyChaseState.gd` | 471–521 | `_is_walkable()` — 调用上述函数 |
| `script/enemy/EnemyChaseState.gd` | 399–408 | `_get_neighbors()` — A* 邻居遍历入口 |
| `script/enemy.gd` | 127 | `add_to_group("enemy")` — 敌人注册到组 |
| `script/enemy.gd` | 88 | `_is_dead: bool` — 死亡标志 |

---
