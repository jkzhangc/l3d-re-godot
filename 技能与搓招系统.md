# 技能与搓招系统

> 技能系统**空壳子** — 已实现格斗游戏式「搓招」输入框架 + TP 消耗，实际技能效果待设计。

## 输入模型

**举起武器 → 按住技能键(5) → 按触发键** 释放对应技能：

| 搓招 | 结果 |
|------|------|
| 按住技能键 + 确定键（Z/空格） | 释放 `command_trigger="确定键"` 的技能 |
| 按住技能键 + 取消键（X/Esc） | 释放 `command_trigger="取消键"` 的技能（如切换形态） |
| 按住技能键 + 方向序列 + 触发键 | 释放 `command_motion` 匹配的技能（如 "下右"） |

方向指令为简化版（4 方向：上/下/左/右），`↓↘→` 简化为 `下右`（先下后右）；对角线 ↘ 检测待扩展。

## 数据

### SkillData（`script/skill_data.gd`）

| 字段 | 类型 | 说明 |
|------|------|------|
| `skill_id` | String | 唯一标识 |
| `skill_name` | String | 显示名称 |
| `tp_cost` | int | 释放消耗的 TP |
| `cooldown` | float | 冷却（秒，0=无冷却，**尚未接入计时**） |
| `command_trigger` | String | 触发键（输入动作名："确定键" 攻击 / "取消键"） |
| `command_motion` | String | 方向指令序列（上/下/左/右，如 "下右"、"右右"；空=无需方向） |
| `icon` / `effect_anim` | Texture2D / PackedScene | 预留（图标 / VXAnimSprite 特效场景） |

### CharacterData（`script/character_data.gd`）

- `skills: Array[SkillData]` — 角色技能列表。
- `max_tp: int` / `current_tp: int`（运行时）— TP 上限与当前值。
- `tp_regen_amount` / `tp_regen_interval` — 每角色 TP 自动回复量 / 间隔（秒，0=不自动回复）。

### ItemData（`script/item_data.gd`）

- `tp_restore` / `hp_restore` — 使用后回复的 TP / HP 量。
- 「地面显示」组（`pickup_texture`/`pickup_char_idx`/`pickup_direction`/`pickup_step_frames`/`pickup_step_duration`/`pickup_animated`）— 掉落物行走图（原在 WeaponData，已上移到 ItemData 共用）。

## 代码位置

| 位置 | 职责 |
|------|------|
| `player.gd::use_skill(trigger)` | 按触发键查技能 → 匹配方向指令 → 扣 TP（空壳子） |
| `player.gd::_find_skill_by_trigger` | 按 `command_trigger` 查技能 |
| `player.gd::_match_motion` | 方向指令序列匹配（最近方向缓冲结尾匹配） |
| `player.gd::_update_motion_input` / `_record_motion` | 每帧记录方向键输入到缓冲 |
| `player.gd::restore_tp` / `_update_tp_regen` | TP 回复 + 每帧自动回复 |
| `PlayerPistolState.gd` / `PlayerKnifeState.gd` | READY 阶段顶部检测搓招输入（优先于攻击/朝向锁定） |
| `global.gd::_apply_item_effects` | 物品使用应用 HP/TP 回复 |

## 占位技能

- `object/skill_test.tres` — 测试技能（确定键 + `下右`，耗 10 TP）。
- `object/skill_form_switch.tres` — 切换形态（取消键，耗 20 TP）。
- `object/item_tp_potion.tres` — TP恢复品（SUPPORT 槽，数字4键，`tp_restore=30`）。

## 待办

- [ ] 实际技能效果（伤害/治疗/增益/切换形态/召唤等）
- [ ] `cooldown` 冷却计时接入
- [ ] 对角线方向（↘）检测、更复杂的方向指令
- [ ] 技能 UI（当前不显示图标栏，HUD 只显示 TP 数值，位置在 `combat_hud.tscn` 的 `TPLabel` 节点）
