# 投掷物系统

> 投掷物（手雷 / 燃烧瓶）— 数据、掉落、举起/瞄准/投掷、爆炸/燃烧。

## 输入

| 按键 | 作用 |
|------|------|
| 数字5 | 举起/放下投掷物 |
| 确定键（Z/空格） | 按住瞄准（显示路径+终点），松开投掷 |
| W / E | 调终点格数（加/减，0~8 格，默认3） |
| 取消键（X/Esc） | 瞄准时取消 |
| D | 技能键（从数字5让位给投掷物） |

瞄准时朝向锁定，但可移动（方向键）。

## 数据

### ThrowableData（`script/throwable_data.gd`，extends ItemData）

| 字段 | 说明 |
|------|------|
| `throw_range_max` | 最大投掷格数（默认 8） |
| `projectile_texture` | 飞行中的投掷物精灵 |
| `held_walk_texture` | 举起投掷物时的角色行走图精灵表（包含各角色持物外观；留空不显示持物外观） |
| `explosion_radius` | 爆炸半径（格，0=不爆炸） |
| `damage` | 伤害（爆炸瞬间 / 火焰灼烧） |
| `fire_radius` | 燃烧范围（格，0=不燃烧） |
| `fire_duration` | 燃烧持续（秒） |
| `throw_sound` / `explode_sound` | 音效 |

> 持物外观按角色区分：各角色在 `CharacterData.throwable_walk_char_idx` 里设置自己在 `held_walk_texture` 中的索引。

### 单槽位

`Global.throwable` 单槽位（L4D2 式），拾取替换，随队伍切换/存档同步。掉落物复用 `healing_pickup`。

## 代码位置

| 位置 | 职责 |
|------|------|
| `player/PlayerThrowableState.gd` | 举起/瞄准/投掷/取消状态机 |
| `throw_aim_indicator.gd` | 瞄准路径 + 终点绘制 |
| `throwable_projectile.gd` | 飞行投射物 + 落地爆炸 / 生成火海 |
| `fire_patch.gd` | 火精灵填充 + 周期性灼烧（12 帧完整动画：4方向×3踏步，每 3 帧切方向） |
| `global.gd` | `throwable` 槽位 + 拾取 + 存档同步 |

## 待办

- [ ] 爆炸/火海 VXAnimSprite 特效（当前纯伤害判定）
- [ ] 火精灵 char_idx 校正（当前 `fire_patch.gd` 用 char_idx=0）
- [ ] 投掷抛物线（当前直线飞行）
- [ ] 给 `item_grenade.tres`/`item_molotov.tres` 配置 `held_walk_texture` + 各角色 `throwable_walk_char_idx`（当前未设，不显示持物外观）
