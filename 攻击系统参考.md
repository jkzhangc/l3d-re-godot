# 攻击系统参考

> 自动加载：CLAUDE.md 中已注册，Claude Code 会话中自动读取。
> 最后更新：2026-08-10（推击疲劳系统 + 攻击转向 + 连锁推挤 + 敌人互推）

---

## 1. 操作速查

| 按键 | 功能 |
|------|------|
| `Shift` | 举起/放下当前武器 |
| `Z` / `Space` | 武器举起后 → 攻击（拾取物范围内自动跳过攻击，优先处理替换）；武器拾取物旁长按（默认 1.2 秒，可配）→ 替换武器（按住时显示圆环进度指示器） |
| `A` | 武器举起后 → 装填弹药（仅远程） |
| `Q` 或自定义推击键 | 武器举起后 → **推击**（近战推开敌人，无伤害，纯击退） |
| `1` | 切换到主武器槽位（举起中则重进举起状态） |
| `2` | 切换到副武器槽位（举起中则重进举起状态） |
| `3` | 使用治疗品 |
| `4` | 使用辅助品 |
| 方向键 | 移动（举起武器后 + **攻击中均可移动且可转向**） |
| `X` / `Esc` | **武器举起状态下：固定朝向**（模式由设置决定）；普通状态下：打开菜单 |

---

## 1.5 攻击时移动与转向

武器攻击状态下（`PistolAttack` / `KnifeAttack` / `Shove`）玩家**可以移动并转向**：
- `physics_update()` 读取方向键输入设置 velocity（`run_speed`）
- 攻击动画播放期间**朝向会随移动方向改变**（2026-08-10 更新）—— `update_facing(move_dir)` 每帧调用
- 子弹/近战判定方向**基于攻击按下时的朝向**（hitbox 在固定帧索引创建，位置在 enter 时确定）
- 攻击结束后自动切回 READY 状态，朝向继续跟随移动（除非已锁定）

## 1.6 固定朝向

武器举起（READY 阶段）时，可通过取消键锁定朝向。两种模式可在设置界面切换：

| 模式 | 操作 | 效果 |
|------|------|------|
| 切换式 (`facing_lock_mode=0`) | 按取消键 | 锁定朝向（再按解锁） |
| 按住式 (`facing_lock_mode=1`) | 按住取消键 | 按住时锁定，松开解锁 |

锁定后 `player.update_facing()` 被跳过——移动改变位置但不改变朝向。

**放下武器时自动取消固定朝向**（2026-08-10 更新）：`_begin_lower()` 中调用 `character.unlock_facing()`，确保下次举起武器时朝向不受之前锁定影响。

---

## 2. 武器槽位系统（L4D2 风格）

```
Global.equipment = {"primary": WeaponData, "secondary": WeaponData}
Global.active_weapon_slot = "primary" | "secondary"
```

- **主武器（primary）**：数字 1 键，如手枪
- **副武器（secondary）**：数字 2 键，如小刀
- 每个槽位只能装备一把武器
- 按对应数字键切换激活槽位；如在武器举起状态切换，自动进入新武器的举起状态

### 武器拾取物（`script/weapon_pickup.gd`）

放在地图上的武器物体，使用 VX Ace 精灵渲染（与角色/子弹相同格式）：
- `weapon_data`：要给予的 WeaponData 资源
- `pickup_texture` + `pickup_char_idx`：地面显示外观
- 槽位为空 → 接触**自动拾取并装备**（不举起）
- 槽位已有武器 → 按住确定键替换（默认 1.2 秒，`hold_time` 可配），按住时显示圆环进度指示器
- 不再限制武器举起/攻击状态（`_can_hold_pickup()` 仅检查玩家是否在范围内）
- **拾取范围内禁止攻击**：玩家进入拾取物范围时设置 `_near_pickup` 标志，武器 READY 状态跳过攻击触发，确保按住替换不被攻击动画打断

---

## 3. 消耗品系统

| 按键 | 类型 | ItemType 值 | 物品示例 |
|------|------|------------|----------|
| `3` | 治疗品 | HEALING (6) | 急救喷雾 |
| `4` | 辅助品 | SUPPORT (7) | 药品 |

```
Global.healing_item → 治疗品槽位（只能持有一个）
Global.support_item → 辅助品槽位（只能持有一个）
```

---

## 4. 攻击动画系统

### 动画帧序列

攻击动画通过切换角色精灵表的 `char_idx`（角色索引）实现。不使用 AnimatedSprite2D，而是数据驱动的帧序列。

**远程武器（手枪）默认序列：** `[3, 4, 3, 2]`
```
char_idx 3 → char_idx 4 → char_idx 3 → char_idx 2
(举枪瞄准)  (枪火/发射)  (后坐力)    (恢复到就绪)
           ↑ fire_at_sequence_idx=1 在此帧发射子弹
```

**近战武器（小刀）默认序列：** `[3, 4, 1, 2]`
```
char_idx 3 → char_idx 4 → [暂停 15 帧] → char_idx 1 → char_idx 2
(挥刀预备)  (砍击命中帧)   ↑                (收刀1)     (收刀2/就绪)
           ★ 在此帧（char_idx=4）判定伤害
           melee_pause_after_idx=1  在此帧后暂停
```

**敌人攻击默认序列：** `[1, 2, 3, (暂停20帧), 1, 0]`
```
char_idx 1 → char_idx 2 → char_idx 3 → [暂停 20 帧] → char_idx 1 → char_idx 0
(抬手预备)  (挥击1)      (命中帧)      ↑                (收手1)     (收手2)
                          ★ 在此帧（char_idx=3）判定伤害
```

### 精灵表 char_idx 布局

VX Ace 精灵表 576×512（12列×8行），每个角色占 3帧×4方向（48×64像素）：

| char_idx | 列范围 | 行 | 用途（のび太セット.png） |
|----------|--------|-----|------------------------|
| 0 | 1~3 | 0 | 待机/行走 |
| 1 | 4~6 | 0 | 举起武器第1帧 |
| 2 | 7~9 | 0 | 举起武器第2帧（就绪） |
| 3 | 10~12 | 0 | 攻击动画帧3（瞄准/预备） |
| 4 | 1~3 | 1 | 攻击动画帧4（枪火/砍击） |

公式：`char_col = char_idx % 4`, `char_row = char_idx / 4`

---

### 攻击后动画（Post-Attack）

攻击动画帧序列播放完毕后，可选播放一段"攻击后动画"（恢复/后摇），再切回武器举起状态。**所有武器共用同一套字段**。

**字段（`@export_group("攻击后动画")`）：**

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `post_attack_char_sequence` | Array[int] | [] | 攻击后动画 char_idx 序列（空=无攻击后动画） |
| `post_attack_frame_durations` | Array[float] | [] | 每帧持续秒数（空=默认0.1） |
| `post_attack_sound` | AudioStream | null | 攻击后音效 |

**流程：**
```
攻击动画帧结束 → 有 post_attack_char_sequence？
  ├─ 是 → 播放攻击后帧序列 + post_attack_sound → 切回武器举起状态
  └─ 否 → 直接切回武器举起状态（原有行为）
```

**示例配置（手枪后坐力恢复）：**
```
post_attack_char_sequence = [2, 1]   # 从后坐力帧恢复到就绪帧
post_attack_frame_durations = [0.08, 0.08]
```

---

### 4.5 武器掉落地面显示

掉落的武器从 WeaponData 读取地面外观参数：

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `pickup_texture` | Texture2D | — | 地面精灵表（空=回退 `weapon_walk_texture`） |
| `pickup_char_idx` | int | 0 | 角色索引（仅在 pickup_texture 非空时使用） |
| `pickup_direction` | int | 0 | 朝向（仅在 pickup_texture 非空时使用） |
| `pickup_step_frames` | Array[int] | [] | 踏步帧序列（空=默认 `[1,0,1,2]`） |
| `pickup_step_duration` | float | 0.0 | 每帧时长秒（0=默认 0.25s） |
| `pickup_animated` | bool | true | 是否启用踏步动画 |

---

## 5. WeaponData 完整参数

### 远程武器 (`@export_group("远程攻击")`)

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `is_ranged` | bool | true | true=远程, false=近战 |
| `magazine_capacity` | int | 0 | 弹夹容量（0=无需弹药） |
| `ammo_item_id` | String | "" | 对应弹药 ItemData.item_id |
| `attack_char_sequence` | Array[int] | [3,4,3,2] | 攻击动画 char_idx 序列 |
| `attack_frame_durations` | Array[float] | [] | 每帧持续秒数（空=默认0.1） |
| `fire_at_sequence_idx` | int | 1 | 在此序列索引发射子弹 |
| `bullet_texture` | Texture2D | — | 子弹精灵图（水平帧条，单帧宽=图宽/帧数） |
| `bullet_anim_frames` | int | 1 | 子弹动画帧数（水平排列） |
| `bullet_frame_duration` | int | 1 | 每帧持续的物理帧数 |
| `bullet_speed` | float | 300.0 | 子弹飞行速度 (px/s) |
| `bullet_range` | float | 300.0 | 最大飞行距离 (px) |
| `bullet_damage` | float | 0.0 | 子弹伤害（0=用 attack_power） |
| `bullet_destroy_on_hit` | bool | true | 击中后是否消失 |
| `bullet_penetration` | int | 0 | 穿透数（0=单目标） |
| `bullet_spawn_offset` | float | 24.0 | 子弹生成位置前方偏移 (px) |
| `bullet_offset_down` | Vector2 | (0,0) | 朝下时的额外偏移 |
| `bullet_offset_up` | Vector2 | (0,0) | 朝上时的额外偏移 |
| `bullet_offset_left` | Vector2 | (0,-4) | 朝左时的额外偏移 |
| `bullet_offset_right` | Vector2 | (0,-4) | 朝右时的额外偏移 |

### 装填系统 (`@export_group("装填/装弹")`)

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `reload_mode` | int (enum) | NORMAL=0 | 装填模式：NORMAL=一次装满, SHOTGUN=逐发装填 |
| `reload_char_sequence` | Array[int] | [] | NORMAL 模式装填动画帧序列 |
| `reload_frame_durations` | Array[float] | [] | 每帧持续秒数（空=默认0.1） |
| `reload_sound` | AudioStream | null | 装填音效 |
| `shotgun_reload_loop_char_sequence` | Array[int] | [] | SHOTGUN 单发装填循环帧序列 |
| `shotgun_reload_loop_frame_durations` | Array[float] | [] | 循环每帧持续秒数 |
| `shotgun_reload_loop_sound` | AudioStream | null | 单发装填音效 |
| `shotgun_reload_end_char_sequence` | Array[int] | [] | SHOTGUN 装填结束帧序列（上膛） |
| `shotgun_reload_end_frame_durations` | Array[float] | [] | 结束帧每帧持续秒数 |
| `shotgun_reload_end_sound` | AudioStream | null | 装填结束音效（上膛声） |
| `reload_wait_duration` | float | 0.3 | 装填完成后等待时长（秒），两种模式共用 |

### 近战武器 (`@export_group("近战攻击")`)

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `is_ranged` | bool | false | 设为 false |
| `attack_char_sequence` | Array[int] | [3,4,1,2] | 攻击动画序列 |
| `melee_range_size` | Vector2 | (48,32) | 判定矩形宽×高 |
| `melee_range_forward_offset` | float | 24.0 | 以角色方向为准的前方偏移 |
| `melee_pause_after_idx` | int | 1 | 攻击动画在此索引后暂停 |
| `melee_pause_frames` | int | 15 | 暂停帧数（60fps ≈ 0.25s） |

### 攻击输入 (`@export_group("攻击输入")`)

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `fire_mode` | int (enum) | TAP=0 | **TAP**=点按（按一次打一发）；**HOLD**=按住连发（自动步枪/冲锋枪） |
| `post_press_wait_frames` | int | 0 | 攻击动画播完后等待帧数才检测下一次按键。0=立即响应。用于控制射速 |

---

## 5.5 攻击输入模式详解

### TAP 模式（点按）

按一次确定键 → 播放一轮攻击动画 → 等待 `post_press_wait_frames` 帧 → 切回 READY。再次按下才触发下一轮攻击。

适用于：手枪、霰弹枪、近战武器。

### HOLD 模式（按住连发）

按住确定键 → 播放攻击动画 → [攻击后动画] → 等待 N 帧 → **自动检测按键仍按住 → 重新开始攻击**。松手后当前攻击播完 → 切回 READY。

适用于：自动步枪、冲锋枪。

空弹时 HOLD 连发自动停止并播放空弹音效，无需手动松手。

### 流程图

```
攻击动画 → [攻击后动画] → _on_attack_complete()
                              ├─ wait_frames > 0? → 倒计时
                              │    └─ 倒计时结束
                              │         ├─ HOLD + 按住? → 重新攻击
                              │         └─ 否则 → 切回 READY
                              └─ wait_frames = 0?
                                   ├─ HOLD + 按住? → 重新攻击
                                   └─ 否则 → 切回 READY
```

---

## 6. 子弹系统

### 子弹实体 (`script/bullet.gd`)

**渲染方式**：单张水平帧条图片（`art/Barrages/`），均分为 `bullet_anim_frames` 列。每帧宽 = 图宽 / 帧数，帧 X = `当前帧索引 × 单帧宽`。精灵旋转跟随飞行方向（图片默认朝 RIGHT = 0°）。

```
Bullet (Node2D)
├── Sprite2D        ← 水平帧条精灵，region_rect 按动画帧选列
├── Area2D          ← collision_mask=24（层4敌人+层5受击碰撞体），rotation = direction.angle()
│   └── CollisionShape2D  ← RectangleShape2D，尺寸/偏移由 BulletData 覆盖
```

**动画帧更新**：`_physics_process()` 中每物理帧 `_anim_counter++`，达到 `bullet_frame_duration` 时 `_current_frame = (_current_frame + 1) % bullet_anim_frames`，循环播放。

**碰撞体旋转**：`_update_area_rotation()` 将 `Area2D.rotation` 设为 `direction.angle()`，使碰撞矩形始终对齐飞行方向。

**碰撞体自定义**：`BulletData` 提供 `collision_size`（0=使用默认 24×28）和 `collision_offset` 字段，经 `PlayerPistolAttackState` → `bullet.setup()` → `_apply_collision_shape()` 写入 CollisionShape2D 的 shape.size 和 position。

**调试可视化**：`_draw()` 以青色（Cyan）绘制旋转+偏移后的碰撞矩形轮廓（TAB 键切换）。

**⚠️ `@onready` 时序**：`setup()` 在 `add_child()` 之前调用，`_apply_collision_shape()` 和 `_update_area_rotation()` 内部使用 `$` 路径直查而非 `@onready` 缓存引用。

**命中流程（2026-07-28 更新）**：
1. 解析真正可伤害目标（若传入 HurtArea 则取其 parent CharacterBody2D）
2. **发射者排除**：`_shooter` 不为空且目标=发射者 → 跳过
3. **去重**：目标 `instance_id` 在 `_hit_targets` 中 → 跳过（防 body + hurtbox 双重触发）
4. **尸体跳过**：`_is_dead` 或 `_is_dying` → 跳过（不消耗穿透、不播特效）
5. `_hits += 1`，掷骰暴击，调用 `take_damage()`，播放特效/音效

### 子弹生命周期

1. `PlayerPistolAttackState._fire_bullet()` 实例化 `bullet.tscn`
2. 调用 `bullet.setup({direction, speed, range, damage, collision_size, collision_offset, knockback_force, knockback_stun, shooter, ...})`
3. `add_child(bullet)` 将子弹加入场景树
4. `_physics_process`: 沿方向移动，累计距离，超射程 → `queue_free()`
5. `_on_body_entered/area_entered`: 解析目标 → 排除发射者 → 去重 → 跳过尸体 → 对目标调 `take_damage(...)`, 按 destroy_on_hit/penetration 决定是否销毁

### 子弹击退系统（NEW）

**BulletData 击退字段**（在 `object/` 下的 `.tres` 资源 Inspector 中配置）：

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `knockback_enabled` | bool | false | 是否启用击退 |
| `knockback_force` | float | 200.0 | 击退力度（像素/秒初速） |
| `knockback_stun_duration` | float | 0.5 | 硬直时长（秒），结束后自动切回追击 |

**击退效果**：
- 敌人被击退子弹命中 → 沿子弹方向被推开（速度线性衰减 600px/s²）
- 硬直期间无法移动/攻击
- 硬直结束自动切回 `Chase`（追击）状态
- 击退期间若 HP ≤ 0 仍然正常触发死亡

**多个子弹同时命中**：击退状态中再次被击退弹命中 → 不重复进入 Knockback（`take_damage()` 会检查当前状态）

### 空弹机制

**`PlayerPistolAttackState.enter()`** 在进入攻击动画前检查弹夹：
- 远程武器 + 弹夹为空 → 播放 `WeaponData.empty_fire_sound` → 立即 `transition_requested.emit("Pistol")`
- 不播放攻击动画、不进入武器模式、不发射子弹

### 子弹图片（Barrages）

子弹图片放在 `art/Barrages/` 目录，为**水平帧条**图片（帧从左到右排列）。图片尺寸不限。

**命名约定**：文件名中 `[x,y]` 表示 `[动画帧数, 每帧持续物理帧数]`。

| 文件 | 尺寸 | 帧数 | 持续帧 | 单帧尺寸 | 用途 |
|------|------|------|--------|----------|------|
| `bullet-1.png` | 12×36 | 1 | 1 | 12×36 | 手枪/霰弹枪弹 |
| `大剑气[3,3].png` | 288×106 | 3 | 3 | 96×106 | 剑气弹 |
| `榴弹[3,5].png` | 99×52 | 3 | 5 | 33×52 | 榴弹 |
| `grenade.png` | — | 1 | 1 | — | 手榴弹 |
| `手榴弾PIC.png` | — | 1 | 1 | — | 手榴弹图标 |

**在 Inspector 中配置**：BulletData 资源 → 外观分组：
- `bullet_texture` = 拖入 `art/Barrages/` 下的图片
- `bullet_anim_frames` = 文件名 `[x,y]` 中的 x（帧数）
- `bullet_frame_duration` = 文件名 `[x,y]` 中的 y（持续物理帧数）

### 调整子弹发射位置 & 速度

所有子弹参数在 **武器资源文件**（`.tres`）中配置，在 Godot 编辑器中双击即可编辑：

| 想调整的效果 | 参数 | 在哪改 | 说明 |
|-------------|------|--------|------|
| 子弹飞多快 | `bullet_speed` | 武器的 .tres 检查器 | 单位 px/s，默认 300 |
| 子弹飞多远 | `bullet_range` | 武器的 .tres 检查器 | 单位 px，默认 300 |
| 子弹从哪生成 | `bullet_spawn_offset` | 武器的 .tres 检查器 | 角色前方偏移 px，默认 24 |

**示例：** 双击 `object/weapon_pistol.tres` → 在检查器中找到「远程攻击」分组 → 修改数值。

**额外偏移（按方向）：** 如果某个方向的子弹位置偏高/偏低，可单独调整：
- `bullet_offset_left` / `bullet_offset_right` — 默认 `(0, -4)`，把左右方向子弹抬高 4px
- `bullet_offset_down` / `bullet_offset_up` — 默认 `(0, 0)`
- 这些值**叠加**到 `bullet_spawn_offset` 上，不会覆盖

**代码位置：** `PlayerPistolAttackState.gd` 第 109-110 行：
```gdscript
var extra: Vector2 = _wd.get_bullet_extra_offset(character.facing)
bullet.position = character.global_position + dir_vec * _wd.bullet_spawn_offset + extra
```
子弹速度在 `bullet.setup()` 传入，由 `bullet.gd` 的 `_physics_process` 驱动：
```gdscript
position += direction * speed * delta
```

### 弹夹 & 弹药系统

```
Global
├── weapon_magazines: {weapon_item_id: current_ammo}
│   例: {"pistol_01": 5}  → 手枪弹夹当前 5 发
├── inventory (ItemData items)
│   弹药 ItemType=AMMO, item_id="ammo_pistol"
└── count_ammo_item("ammo_pistol") → 背包中弹药总数
```

**装填系统（`PlayerReloadState`）：**

装填不再是即时算术操作，而是通过专用状态 `PlayerReloadState` 控制的完整动画流程。

**NORMAL 模式流程（手枪等）：**
```
Pistol READY --装填键(A)--> Reload.enter()
  → 检查：有弹药？弹夹未满？→ 通过
  → Phase.ANIM: 播放 reload_char_sequence 帧 + reload_sound（一次）
  → 动画结束 → 执行弹药转移（_do_reload）
  → Phase.WAIT: 等待 reload_wait_duration 秒
  → 切回 Pistol (skip raise)
```

**SHOTGUN 模式流程（霰弹枪）：**
```
Shotgun READY --装填键(A)--> Reload.enter()
  → Phase.ANIM (循环):
      播放 shotgun_reload_loop 帧 + loop_sound → 装入 1 发
      → 弹夹满或无备弹？→ 进入 END_ANIM
      → 否则继续循环
  → Phase.END_ANIM: 播放 shotgun_reload_end 帧 + end_sound（上膛）
  → Phase.WAIT: 等待 reload_wait_duration 秒
  → 切回 Shotgun (skip raise)
```

**关键特性：**
- 装填期间朝向锁定、移动锁定
- 退出时自动 `weapon_skip_raise`，返回武器举起后跳过举起动画
- 近战武器（`magazine_capacity <= 0`）无法进入装填
- 弹夹已满或无备弹时 `enter()` 直接 return

**射击流程（按 Z 键）：**
```
current = get_magazine_ammo(id)       # 12
set_magazine_ammo(id, 11)            # 弹夹 -1
spawn bullet                          # 生成子弹
```

---

## 7. 近战判定

### 判定区域生成

在攻击动画暂停期间，创建一个临时 `Area2D`：
- 矩形尺寸：`melee_range_size`（如 48×32）
- 位置：`角色全局坐标 + 朝向向量 × melee_range_forward_offset`
- `collision_layer = 0`, `collision_mask = 24`（层4敌人物理体 + 层5受击碰撞体）
- `get_overlapping_bodies()` + `get_overlapping_areas()` 检测目标
- **去重**：按 `instance_id` 跳过已命中目标（防 body + hurtbox 双重触发）
- **尸体跳过**：`_is_target_dead()` 检查 `_is_dead` / `_is_dying`
- 对每个目标调 `take_damage(damage, 0.0, direction, _is_headshot)`
- 攻击后 `queue_free()` 清理

---

## 7.5 推击系统（L4D2 风格近战推开）

推击是一种无伤害的纯击退攻击，用于推开靠近的敌人创造空间。

### 判定区域

- 矩形尺寸：`shove_range_size`（默认 48×32，与小刀近战相同）
- 位置：角色位置 + 朝向向量 × `shove_range_forward_offset`（默认 24px）
- 碰撞 mask = 24（层4敌人物理体 + 层5受击碰撞体）
- 去重：按 `instance_id` 跳过已命中目标
- 尸体跳过：`_is_target_dead()` 检查

### 溅射击退

推中敌人后，周围敌人也会被击退：
- 溅射中心 = 推击判定区域位置
- 溅射半径：`shove_splash_radius`（默认 64px，Inspector 可调，0=禁用）
- 击退方向：从溅射中心向外径向推开
- 力度衰减：中心 100% → 边缘 50%（线性）
- 跳过已直接命中的目标和已死亡敌人

### 连锁推挤（2026-08-10 新增）

被推开的敌人碰到附近敌人时，也会推开它们（多米诺效应）：
- 检测半径：32px（`CHAIN_PUSH_RADIUS`）
- 力度系数：原击退速度 × 0.6 × 距离衰减（`CHAIN_PUSH_FACTOR`）
- 最小力度阈值：5.0 px/s（`CHAIN_PUSH_FORCE_THRESHOLD`）
- 连锁硬直时长：0.25s（`CHAIN_PUSH_STUN`）
- 可打断 Hitstun 状态（击退优先级 > 硬直）
- 不重复推开已在 Knockback 状态的敌人

### 推击疲劳系统（L4D2 风格，2026-08-10 新增）

连续推击多次后进入短冷却，防止推击滥用：

| 参数（`CharacterData` 中配置） | 默认值 | 说明 |
|------|--------|------|
| `shove_fatigue_limit` | 3 | 连续推击多少次触发冷却（0=禁用） |
| `shove_cooldown_duration` | 2.0s | 冷却时长 |
| `shove_fatigue_reset_time` | 3.0s | 停止推击多久后重置计数 |

流程：
```
推击键 → can_shove()? 
  ├─ 冷却中 → 拒绝，退回武器就绪
  └─ 允许 → 执行推击 → on_shove_performed()
              ├─ 计数 +1，空闲计时器清零
              ├─ 计数 < fatigue_limit → 正常，可继续推
              └─ 计数 >= fatigue_limit → 冷却开始（计数清零）
              
每帧 _update_shove_fatigue(delta):
  ├─ 冷却中 → 倒计时
  └─ 非推击状态 → 空闲计时器累加 → 达 reset_time → 计数清零
```

### WeaponData 推击参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `shove_char_sequence` | [0, 1, 1] | 推击动画帧序列 |
| `shove_frame_duration` | 0.05 | 每帧持续秒数 |
| `shove_hit_at_sequence_idx` | 1 | 在此帧索引创建判定 |
| `shove_range_size` | (48, 32) | 判定矩形尺寸 |
| `shove_range_forward_offset` | 24.0 | 判定区域前方偏移 |
| `shove_knockback_force` | 300.0 | 击退力度（初速 px/s） |
| `shove_knockback_duration` | 2.0 | 击退硬直时长（秒） |
| `shove_splash_radius` | 64.0 | 溅射击退半径（0=禁用） |
| `shove_sound` | null | 推击音效 |
| `shove_walk_texture` | null | 推击行走图（回退 CharacterData） |

---

## 8. 状态流转

```
                    举起放下武器键 (Shift)
  Idle/Walk/Run ──────────────────────→ Pistol/Knife
       │                                      │
  数字1/2键│                             数字2/1键│
  (切换槽位)│                            (切到另一武器)
       │                                      │
       ▼                                      ▼
  切换 active_slot                     进入新武器举起状态

  Pistol READY ──确定键(Z)──→ PistolAttack ──动画+等待帧──→ Pistol (READY)
         │                    │  HOLD + 按住? ──→ 重新攻击 (循环)
         ├──推击键──→ Shove ──动画──→ Pistol (READY)
         │                    ├──溅射击退（半径内敌人）
         │                    └──连锁推挤（被推敌人推开附近敌人）
  Knife READY  ──确定键(Z)──→ KnifeAttack  ──动画+等待帧──→ Knife (READY)
         │                    │  HOLD + 按住? ──→ 重新攻击 (循环)
         ├──推击键──→ Shove ──动画──→ Knife (READY)
         │
  装填键 (A) → Pistol/Shotgun READY → Reload 状态
     ├─ NORMAL 模式：播放装填动画 → 装弹 → 等待 → 切回武器举起
     └─ SHOTGUN 模式：循环逐发装填 → 结束动画 → 等待 → 切回武器举起
  治疗品键 (3) / 辅助品键 (4) → 任意非攻击状态可用
```

---

## 9. 关键代码路径

| 功能 | 文件 | 关键方法 |
|------|------|----------|
| 播放攻击帧 | `player.gd` | `set_attack_char_index(idx)` |
| 发射子弹 | `PlayerPistolAttackState.gd` | `_fire_bullet()` |
| 近战判定 | `PlayerKnifeAttackState.gd` | `_create_melee_hitbox()` |
| 推击判定 | `PlayerShoveState.gd` | `_create_shove_hitbox()`, `_check_shove_hits()`, `_apply_splash_knockback()` |
| 推击疲劳 | `player.gd` | `can_shove()`, `on_shove_performed()`, `_update_shove_fatigue()` |
| 连锁推挤 | `EnemyKnockbackState.gd` | `_chain_push_nearby_enemies()` |
| 敌人互推 | `EnemyChaseState.gd` | `_push_apart_from_other_enemies()` |
| 装填弹药 | `PlayerReloadState.gd` | `enter()`, `_process_anim()`, `_start_shotgun_loop()`, `_load_one_shell()` |
| 弹药管理 | `global.gd` | `get/count/consume_ammo_item()` |
| 切换武器槽 | `global.gd` | `switch_to_slot(slot)` |
| 获取当前武器 | `global.gd` | `get_active_weapon()` |
| 使用消耗品 | `global.gd` | `use_healing_item()` / `use_support_item()` |
| 武器拾取 | `weapon_pickup.gd` | `_do_pickup()` |

---

## 10. 调试

运行游戏后在 Godot 控制台可看到：
```
[手枪] 发射！弹夹剩余: 11 / 12
[子弹] 击中: Enemy | 伤害=25 | 穿透剩余=0
[手枪] 装填完成: 5 → 12 / 12 (背包剩余: 13)
[小刀] 近战判定区域已创建 | 偏移=(24, 0) | 大小=(48, 32)
[小刀] 击中 body: Enemy | 伤害=15
[推击] 判定区域已创建 | 偏移=(24, 0) | 大小=(48, 32)
[推击] 击中 body: Enemy | 击退力度=300 | 击退时长=2.0s
[推击] 溅射命中: Enemy2 | 距离=40 | 力度=225
[连锁推挤] Enemy 推开了 2 个附近敌人 | 自身速度=250
[推击疲劳] 推击次数: 3 / 3
[推击疲劳] 达到上限！冷却 2.0s
[推击疲劳] 冷却中！剩余 1.5s
[推击疲劳] 冷却结束，可以推击了
[推击疲劳] 空闲 3.0s，疲劳计数已重置
```

敌人需要实现 `take_damage(damage: float, knockback_force: float, direction: Vector2, is_headshot: bool = false, knockback_stun: float = 0.0)` 方法才能接收伤害。击退参数由子弹传入：knockback_force > 0 且 knockback_stun > 0 时触发击退状态。

---

## 11. 敌人系统

### 敌人 AI 状态流转
```
Idle（未发现，不动）
  │  视野检测到玩家 → Discover（"!" + 发现音效，0.5秒）
  │  受击退伤害 → Knockback（硬直结束 → Chase）
  │
  ▼
Discover（"!" + 发现音效，0.5秒）
  │
  ▼
Chase（追击玩家 — A* 寻路 + 敌人互推 + 敌人间避让）
  │  距玩家 ≤ attack_range → Attack
  │  被推击子弹命中 → Knockback（击退）或 Hitstun（硬直）
  │  敌人间距 < 28px → 自动互相推开（80px/s 最大力度，2026-08-10 新增）
  ▼
Attack（近战攻击动画 + 攻击音效）
  │  动画结束 → Chase
  │  被击退子弹命中 → Knockback
  ▼
Hitstun（原地硬直冻结，无位移，2026-08-10 新增）
  │  硬直计时结束 → Chase
  │  被击退命中 → Knockback（击退打断硬直，重置计时）
  ▼
Knockback（击退 + 硬直，velocity 线性衰减 600px/s²）
  │  硬直结束 → Chase
  │  连锁推挤：碰撞到附近敌人 → 推开它们（2026-08-10 新增）
  │  HP ≤ 0 → Death / HeadshotDeath
  ▼
Death（普通死亡，char_idx=4，尸体保留）
  └─ 碰撞/视野/**受击碰撞体**禁用，永久保留在地图上；**子弹/近战穿透尸体**

HeadshotDeath（爆头死亡，char_idx=5→暂停→6，尸体保留）
  └─ 动画播放后碰撞/视野/**受击碰撞体**禁用，永久保留在地图上；**子弹/近战穿透尸体**
```

### 敌人导出参数（`script/enemy.gd`）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `max_hp` | 100 | 最大 HP |
| `move_speed` | 120 | 追击移动速度 |
| `attack_damage` | 10 | 攻击伤害 |
| `attack_range` | 20 | 距玩家多少 px 进入攻击 |
| `initial_facing` | 0 (DOWN) | 初始朝向 |
| `vision_angle` | 90° | 扇形视野角度 |
| `vision_range` | 200 px | 视野距离 |
| `lose_player_time` | 5 秒 | 脱离视野多久后放弃追击 |
| `attack_char_sequence` | [1,2,3,1,0] | 攻击动画序列 |
| `attack_pause_after_idx` | 2 | 在索引 2（帧3）后暂停 |
| `attack_pause_frames` | 20 | 暂停帧数 |
| `attack_hit_range` | (48,32) | 攻击判定矩形宽×高 |
| `attack_hit_forward_offset` | 28 | 攻击判定前方偏移 px |
| `attack_cooldown_frames` | 30 | 攻击后摇帧数（~0.5s） |
| `hit_at_sequence_idx` | 2 | 伤害判定索引 |
| `death_char_index` | 4 | 普通死亡角色索引 |
| `headshot_char_index_1` | 5 | 爆头死亡帧1 |
| `headshot_char_index_2` | 6 | 爆头死亡帧2（最终保持） |
| `headshot_pause_frames` | 20 | 爆头动画暂停帧数 |

### 玩家参数（`script/player.gd`）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `max_hp` | 200 | 最大 HP |

### TAB 调试可视化
- 敌人：红色碰撞体框 + **黄色受击碰撞体** + 黄色扇形视野 + 青色攻击触发矩形 + 橙红攻击判定矩形 + 红色 HP 条
- 玩家：绿色碰撞体框 + **黄色受击碰撞体** + 绿色 HP 条
- 子弹：青色（Cyan）碰撞矩形 + 随方向旋转
- 全局开关：`Global.debug_visuals`（TAB 键切换）

### 文档引用
- 完整 L4D2 机制参考：`L4D2特色机制参考.md`
- 缺失功能清单：`L4D2缺失功能清单.md`
