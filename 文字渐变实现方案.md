# 文字纵向渐变实现方案

> 创建日期：2026-07-19
> 最后更新：2026-07-19（新增 GradientLabel 封装）
> 状态：✅ 已实现（方案 D 变体 — CPU 预渲染 + 人工亮度渐变 + GradientLabel 统一接口）

---

## 当前架构

渐变文字系统由三层组成：

```
GradientLabel（统一接口，script/gradient_label.gd）
  ├─ use_gradient = true  → TextGradientRenderer → TextureRect
  │                         自动处理 bold/shadow/outline
  └─ use_gradient = false → Label + shader 纯色（关掉渐变）
```

**GradientLabel** 封装了渐变/非渐变双路径，外部只需操作一个节点。资源路径留空时自动从 Global 读取默认值。Global 提供 `get_cached_color_image()` 缓存色表 Image。

---

## 实现方案

采用 **SubViewport 预渲染 + CPU 像素处理**：

1. `TextGradientRenderer`（`script/text_gradient_renderer.gd`）将文字渲染到隐藏的 SubViewport 中的白色 Label
2. `await` 等待渲染完成后，`get_texture().get_image()` 获取像素
3. 从 20 色表取基准色（单元格中心像素），然后**人工生成亮度渐变**：
   - 顶部像素：基准色 × 1.35（更亮）
   - 底部像素：基准色 × 0.55（更暗）
4. 可选 CPU 端描边（8 邻域边缘检测）
5. 返回 `ImageTexture`，由 `TextureRect` 显示

### 内部流程

```
文字字符串
  → TextGradientRenderer.render()
    → SubViewport (UPDATE_ALWAYS, 白色 Label)
    → await 2 帧等待渲染
    → get_texture().get_image() 获取像素
    → 逐像素: brightness = 1.35 - (y/height) × 0.8 → 基准色 × brightness
    → 可选: 边缘检测描边
    → ImageTexture
  → TextureRect 显示
```

### 回退机制

每个 `GradientLabel` 内部维护 Label 子节点作为占位。渐变渲染期间 Label 即时可见，渲染成功后切换为 TextureRect，失败则 Label 保留。

### 相关文件

| 文件 | 说明 |
|------|------|
| `script/gradient_label.gd` | **GradientLabel 类** — 统一渐变/非渐变接口 |
| `script/text_gradient_renderer.gd` | 渐变文字渲染器（SubViewport + 像素处理） |
| `script/title_screen.gd` | 标题画面（使用 GradientLabel） |
| `script/global.gd` | 全局默认值 + `get_cached_color_image()` 缓存 |
| `shader/text_color.gdshader` | 非渐变着色器（Label 回退 + `use_gradient=false`） |
| `art/System/Text color, 20 types (each 16 x 16).png` | 20 色表（基准色来源） |

---

## 原始问题诊断

### 现象

为 Label 应用 canvas_item shader，试图根据 `UV.y` 采样色表纵向渐变 → **永远是纯色**，无渐变。

### 根因

使用**调试 shader** (`text_color_debug.gdshader`) 输出 `UV.y` 值作为颜色：

```glsl
COLOR = vec4(vec3(1.0 - UV.y, 0.0, UV.y), glyph.a);
// UV.y=0 → 纯红，UV.y=1 → 纯蓝，中间 → 渐变
```

结果为**纯红** → 说明 `UV.y ≈ 0`，恒定不变。

**结论**：Godot 4.6 Mobile 渲染器中，canvas_item shader 在 Label 上看到的 `UV.y` 是**字体图集 (font atlas) 的全局纹理坐标**，不是单个字形内的局部坐标。

由于 DotGothic16 是 TTF 字体，Godot 将字形光栅化到图集顶部区域 → 所有字形像素的 `UV.y` 都接近 0 → shader 采样永远落在色表单元格的同一行 → 纯色。

### 为什么 RPG Maker 可以

RPG Maker VX Ace 使用**位图字体 (bitmap font)**——每个字形的渐变是**预先画在像素里**的。引擎只是把字形纹理 blit 到屏幕上，颜色选择通过叠加 tint 实现。

TTF 字体的字形是矢量轮廓，光栅化后是纯色区域。渐变必须由着色器添加，而着色器拿不到字形内部 UV，于是就做不了。

---

## 解决方案

### 方案 A：位图字体（推荐）

**原理**：将 DotGothic16 预渲染为位图字体，在渲染时对每个字形应用渐变色。

**步骤**：

1. **生成渐变字形纹理**
   - 使用脚本或外部工具，将每个字符以 16–32px 大小渲染到纹理
   - 渲染时叠加纵向渐变：顶部亮色（色表单元格上排），底部暗色（色表单元格下排）
   - 输出为 PNG 精灵表

2. **创建 Godot BitmapFont**
   ```gdscript
   var bf := BitmapFont.new()
   # 从纹理创建字形映射
   # 设置每个字形的 UV 区域
   # 设置字符间距、行高等
   ```

3. **切换 Label 字体**
   ```gdscript
   label.add_theme_font_override("font", bitmap_font)
   ```

4. **颜色选择**
   - 位图字体的每个字形已包含渐变 → 不再需要色表 shader
   - 颜色切换：由于 bitmap font 的渐变色已固定在纹理中，切换颜色需要**不同颜色的字体纹理**
   - 或者：使用 `modulate` 对白色位图字体做 tint（牺牲色彩准确度）

**优点**：完全还原 RPG Maker 效果，性能好。

**缺点**：
- 需要为每种语言/字号生成单独的位图字体
- 颜色变体多时需要多套纹理（或接受 modulate tint 的色彩偏差）
- 生成工具链需要搭建

### 方案 B：双层 Label 模拟渐变

**原理**：两个 Label 叠放，上层裁剪显示上半（亮色），下层显示下半（暗色）。

```
上层 Label (亮色, clip_top_half)
    → modulate = light_color
    → clip_children 或自定义 shader 裁剪上 50%

下层 Label (暗色)
    → modulate = dark_color
```

**实现**：
- 主 Label 正常位置，modulate = 暗色
- 复制 Label +1px 下移，modulate = 亮色，用 `clip_children` 或一个 ColorRect 遮盖下半
- 或使用一个简单的 shader 做 Y 轴裁剪

**优点**：不需要离开 TTF 字体，工作量小。

**缺点**：
- 只有两段色阶（非平滑渐变）
- 每个 Label 需要 2 个节点，管理复杂度增加
- 描边/阴影效果需要额外处理

### 方案 C：RichTextLabel + BBCode 逐字着色

**原理**：用 RichTextLabel 的 `[color=...]` 标签逐字设置不同颜色，模拟渐变。

```gdscript
var text := "开始游戏"
var top_color := Color(1.0, 0.9, 0.7)  # 亮
var bot_color := Color(0.5, 0.3, 0.1)  # 暗
# 将每个字符拆分为上下两部分，分别着色...
```

**优点**：不依赖 shader。

**缺点**：
- 实现极其复杂（需要逐像素级的颜色控制，RichTextLabel 做不到字符内部渐变）
- 实际上不可行 — BBCode 只能按字符着色，不能按像素

### 方案 D：Image 预渲染文字（简单直接）

**原理**：在 CPU 端将文字渲染为 Image，手动对每个像素应用渐变色，然后显示为 TextureRect。

```gdscript
func render_text_with_gradient(
    text: String, font: Font, font_size: int,
    color_index: int, color_row: int
) -> ImageTexture:
    # 1. 用 Font.get_string_size() 计算尺寸
    # 2. 创建 Image，用 font 绘制文字
    # 3. 遍历每个像素，如果 alpha > 0，用色表对应 Y 位置的颜色替换
    # 4. 返回 ImageTexture 供 TextureRect 显示
```

**优点**：
- 完全控制每个像素
- 可以使用任何色表颜色
- 渐变效果完美

**缺点**：
- 文字更新时需要重新渲染 Image（不适合频繁变化的文字）
- 对标题画面（静态文字）非常适用
- TextureRect 不如 Label 方便（没有自动换行等特性）

---

## 推荐路径

| 场景 | 推荐方案 |
|------|----------|
| **标题画面 / 菜单**（文字内容固定） | **方案 D** — Image 预渲染，最快速实现 |
| **游戏内 HUD / 对话**（文字动态变化） | **方案 B** — 双层 Label，简单可用 |
| **长期完美方案** | **方案 A** — 位图字体，一劳永逸但工作量大 |

**建议优先实现方案 D 用于标题画面**：
1. 文字内容固定（"开始游戏"、"退出游戏"），不需要频繁重建
2. 可以在 `_ready()` 时渲染一次，存入 ImageTexture
3. 色表颜色切换也只需重新渲染
4. 不需要改动现有的 shader/字体架构

---

## 相关文件

| 文件 | 说明 |
|------|------|
| `script/title_screen.gd` | 标题画面控制器（当前纯色方案） |
| `script/global.gd` | 全局文字渲染默认值 |
| `shader/text_color.gdshader` | 当前文字着色器（纯色 + 描边） |
| `shader/text_color_debug.gdshader` | 调试 shader（UV.y 可视化） |
| `art/System/Text color, 20 types (each 16 x 16).png` | 20 色表（320×64，16×16 单元格） |
| `art/System/text_color_0_gradient.png` | 原渐变纹理（32×32，旧方案遗留） |
| `art/System/DotGothic16-Regular.ttf` | 日文像素字体 |
