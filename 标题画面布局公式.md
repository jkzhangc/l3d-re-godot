# 标题画面布局公式

> **のび太的求生之路** — 标题画面（title_screen.gd）选项布局的完整计算公式
> 对应脚本：`script/title_screen.gd`

---

## 布局参数速查

### 选项布局分组

| 参数 | 默认值 | 含义 |
|------|--------|------|
| `item_height` | 28.0 | 单项行高 — **项目大小** |
| `item_spacing` | 16.0 | 项与项之间的额外间隙 — **内间距** |
| `item_start_y` | 20.0 | 无标题时首项 Y — **外间距（上）** |
| `item_margin_bottom` | 0.0 | 末项下方边距 — **外间距（下）** |
| `item_title_gap` | 12.0 | 有标题时分隔线→首项间距 |
| `item_text_x` | 3.0 | 文字水平起始位置（`item_centered = false` 时生效） |
| `item_width` | 0.0 | 选项统一宽度，0 = 自动测量文字宽 |
| `item_centered` | false | 选项是否在窗口内水平居中 |
| `title_font_size` | 18 | 标题字号 |
| `item_font_size` | 19 | 选项文字字号 |

### 光标框分组

| 参数 | 默认值 | 含义 |
|------|--------|------|
| `cursor_base_height` | 32.0 | 光标素材基础高度 |
| `cursor_scale_y` | 1.0 | 纵向缩放系数（吸附 0.5×） |
| `cursor_snap_to_item` | false | 选择框是否自动匹配 item 位置/宽度 |
| `cursor_padding_x` | 24.0 | 光标框水平内边距（snap 关闭时生效） |
| `cursor_offset_x` | 10.0 | 光标框水平偏移（snap 关闭时生效） |
| `cursor_offset_y` | -3.0 | 光标框垂直微调偏移 |
| `cursor_min_width` | 64.0 | 光标框最小宽度（snap 关闭时生效） |
| `cursor_override_width` | 94.0 | 手动指定光标框宽度，>0 覆盖自动计算（snap 关闭时生效） |

### 窗口布局分组

| 参数 | 默认值 | 含义 |
|------|--------|------|
| `window_size` | (120, 108) | 窗口尺寸 (w, h) |
| `window_y_offset` | 130.0 | 窗口垂直偏移（相对 640×480 画面中心） |
| `show_title` | false | 是否显示标题 |
| `separator_y` | 30.0 | 分隔线 Y 坐标 |
| `panel_margin` | 6.0 | 背景图案到窗口边缘距离 |

---

## 核心计算公式

### 1. 行步长（row_step）

```
row_step = item_height + item_spacing
```

### 2. 列表起始 Y（start_y）

```
                  ┌─ item_start_y                          (show_title = false)
start_y =         │
                  └─ separator_y + item_title_gap           (show_title = true)
```

### 3. 可用高度（avail_h）

```
avail_h = window_size.y - start_y - item_margin_bottom
```

### 4. 可见项目数（_visible_items）

```
_visible_items = clamp( floor(avail_h / row_step),  1,  MENU_ITEMS.size() )
```

### 5. 第 i 项的 Y 坐标

```
item_y(i) = start_y + (i - scroll_offset) × row_step
```

### 6. 光标框高度 & Y 坐标

```
cursor_h = cursor_base_height × cursor_scale_y
cursor_y = start_y + display_idx × row_step + (item_height - cursor_h) / 2.0 + cursor_offset_y
```

光标框在 `item_height` 范围内垂直居中，`cursor_offset_y` 提供微调。

### 7. Item 区域宽度

```
                    ┌─ item_width                           (item_width > 0)
item_area_width =   │
                    └─ _calc_max_text_width()               (item_width = 0，自动测量)
```

其中 `_calc_max_text_width()` 遍历所有菜单项，测量 `"  %s" % item` 在最宽项像素宽度。

### 8. Item X 坐标

```
                    ┌─ item_text_x                          (item_centered = false)
item_x =            │
                    └─ (window_size.x - item_area_width) / 2 (item_centered = true)
```

### 9. 光标框宽度 & X 坐标

**当 `cursor_snap_to_item = true`：**

```
cursor_w = item_area_width             ← 与 item 完全一致
cursor_x = item_x                      ← 与 item 完全一致
```

**当 `cursor_snap_to_item = false`：**

```
cursor_w = cursor_override_width       (如果 > 0)
         = max(cursor_min_width, item_width, _calc_max_text_width()) + cursor_padding_x  (自动)

cursor_x = item_text_x + cursor_offset_x
```

### 10. 选项文字居中（Label 路径）

当 `item_width > 0`：
```
label.size.x = item_width
label.horizontal_alignment = CENTER
```

### 11. 选项文字居中（渐变 TextureRect 路径）

当 `item_width > 0`：
```
tr.position.x += (item_width - 纹理实际宽度) / 2.0
```

---

## 布局示意图

```
┌─────────────── window_size (120×108) ──────────────┐
│ panel_margin                                         │
│  ┌────────────────────────────────────────────┐      │
│  │ 背景图案 (tiled)                           │      │
│  │                                            │      │
│  │  [标题]          ← title_position          │      │ show_title=true
│  │  ─────────────── ← separator_y             │      │
│  │                    ← item_title_gap →      │      │
│  │  ┌──────────────┐  ← cursor (snap=false)   │      │
│  │  │  开始游戏     │  ← start_y              │      │
│  │  └──────────────┘                          │      │
│  │                    ← item_spacing →        │      │
│  │  ┌──────────────┐                          │      │
│  │  │  退出游戏     │  ← start_y+row_step     │      │
│  │  └──────────────┘                          │      │
│  │                                            │      │
│  │         ← item_margin_bottom →             │      │
│  └────────────────────────────────────────────┘      │
└──────────────────────────────────────────────────────┘
```

---

## 两种定位模式

### 手动模式（默认：`item_centered = false`, `cursor_snap_to_item = false`）

Item 和选择框各自独立控制：
- Item X = `item_text_x`（3px）
- 选择框 X = `item_text_x + cursor_offset_x`（3 + 10 = 13px）
- 选择框宽 = `cursor_override_width`（94px）

### 自动居中模式（`item_centered = true`, `cursor_snap_to_item = true`）

Item 和选择框完全联动：
- Item X = 选择框 X = `(window_size.x - item_area_width) / 2`
- 选择框宽 = `item_area_width`
- 只需调 `item_width` 一个参数即可同时控制文字和选择框

---

## 渐变文字（GradientLabel）

标题画面使用 `GradientLabel` 节点渲染菜单文字，自动应用色表纵向渐变。

### 使用方法

```gdscript
var gl := GradientLabel.new()
gl.text = "菜单选项"
gl.text_font_size = 19
gl.color_index = 1           # 色表索引
gl.use_gradient = true       # 启用渐变（默认）
gl.bold = true
gl.shadow = true
add_child(gl)
```

### 不使用渐变

```gdscript
gl.use_gradient = false      # 关掉渐变 → 普通 Label
```

### 资源路径

所有路径留空则从 Global 自动读取：
- `font_path_override` → Global.`text_font_path`
- `color_sheet_path_override` → Global.`text_color_sheet_path`
- `color_shader_path_override` → Global.`text_color_shader_path`

色表 Image 优先使用 Global 缓存（`get_cached_color_image()`），避免重复 I/O。

---

## 调整示例

### 增大选项间距

```
item_spacing = 24.0   # 原来 16.0
```

行步长从 44 变为 52。

### 统一选项宽度并居中

```
item_width = 100.0
item_centered = true
cursor_snap_to_item = true
```

每个选项占 100px 宽、文字居中、选择框自动对齐。

### 微调选择框垂直位置

```
cursor_offset_y = -5.0   # 上移 5px
```

---

## 相关文件

| 文件 | 内容 |
|------|------|
| `script/title_screen.gd` | 标题画面脚本（实现所有布局逻辑） |
| `script/gradient_label.gd` | 渐变文字标签（GradientLabel 类） |
| `script/text_gradient_renderer.gd` | 渐变文字渲染引擎 |
| `script/global.gd` | 全局设置 + 文字默认参数 + 色表缓存 |
| `scene/title_screen.tscn` | 标题画面场景 |
| `游戏系统架构文档.md` §6 | 标题画面 & 文字渲染系统完整说明 |
| `文字渐变实现方案.md` | 渐变文字渲染的详细方案与原理 |
