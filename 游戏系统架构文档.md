# 游戏系统架构文档

> **のび太的求生之路** — 野比大雄的 Left 4 Dead 2 同人游戏重制版
> 自动加载此文件：Claude Code 会话中可通过 Read 工具直接读取。
> 最后更新：2026-07-29（子弹渲染改为水平帧条动画 + Barrages 图片）

---

## 0. 游戏定位

本项目的目标是**尽可能还原 Left 4 Dead 2 的核心机制**，包括：
- 主/副武器槽位 + 拾取（空槽自动装备，不举起；已占用则长按替换）
- 治疗品/辅助品热键系统
- 导演 AI（后续）
- 特殊感染者（后续）
- 尸潮机制（后续）

完整 L4D2 参考见 **`L4D2特色机制参考.md`**，当前缺失功能见 **`L4D2缺失功能清单.md`**。

---

## 目录

1. [项目结构](#1-项目结构)
2. [输入映射](#2-输入映射)
3. [状态机系统](#3-状态机系统)
4. [武器系统](#4-武器系统)
5. [菜单系统](#5-菜单系统)
6. [标题画面 & 文字渲染](#6-标题画面--文字渲染)
7. [物品 & 武器数据](#7-物品--武器数据)
8. [角色系统](#8-角色系统)
9. [存档系统](#9-存档系统)
10. [Debug 全局开关](#10-debug-全局开关)
11. [可视化编辑工作流](#11-可视化编辑工作流)
12. [场景说明](#12-场景说明)
13. [玩家死亡系统](#13-玩家死亡系统)

---

## 1. 项目结构

```
script/
├── global.gd                   ← Autoload 单例：debug、背包、装备、初始物品
├── State.gd                    ← 状态基类 (class_name State)
├── StateMachine.gd             ← 通用状态机 (class_name StateMachine)
├── player.gd                   ← 玩家角色（由 CharacterData 驱动外观/HP/音效）
├── game_init.gd                ← 游戏启动初始化
├── item_data.gd                ← 物品数据资源类
├── weapon_data.gd              ← 武器数据资源类（继承 ItemData + 武器动画）
├── character_data.gd           ← 角色参数资源类
├── menu_controller.gd          ← 主菜单（物品/装备双面板/存档/退出）
├── save_manager.gd             ← 存档管理器 (class_name, 静态方法)
├── map_controller.gd           ← 地图测试控制器
├── map_editor.gd               ← 运行时图块编辑器
├── map_test_viewer.gd          ← 图块可视化器 (_draw)
├── tileset_generator.gd        ← @tool TileSet 生成器
├── palette_drawer.gd           ← 调色板 _draw 辅助
├── enemy/
│   ├── EnemyIdleState.gd              ← 敌人待机/视野轮询
│   ├── EnemyDiscoverState.gd         ← 敌人发现玩家（"!"提示 + 发现音效）
│   ├── EnemyChaseState.gd            ← 敌人追击玩家
│   ├── EnemyAttackState.gd           ← 敌人近战攻击 + 友军伤害 + 攻击音效
│   ├── EnemyKnockbackState.gd        ← 击退/硬直状态
│   ├── EnemyDeathState.gd            ← 敌人普通死亡状态
│   └── EnemyHeadshotDeathState.gd    ← 敌人爆头死亡状态
└── player/
    ├── PlayerIdleState.gd      ← 站立状态
    ├── PlayerWalkState.gd      ← 行走状态
    ├── PlayerRunState.gd       ← 跑步状态
    ├── PlayerPistolState.gd    ← 手枪举起状态
    ├── PlayerKnifeState.gd     ← 小刀举起状态
    ├── PlayerPistolAttackState.gd ← 手枪攻击（占位）
    ├── PlayerKnifeAttackState.gd  ← 小刀攻击（占位）
    └── PlayerReloadState.gd     ← 装填状态（NORMAL/SHOTGUN 双模式）

object/
├── player.tscn                 ← 玩家打包场景（含完整状态机）
├── enemy.tscn                  ← 敌人打包场景
├── bullet.tscn                 ← 子弹实体
├── weapon_pistol.tres          ← 手枪
├── weapon_knife.tres           ← 小刀
├── weapon_shotgun.tres         ← 霰弹枪（3发散射 + SHOTGUN装填模式 + 击退）
├── weapon_rifle.tres           ← 突击步枪（HOLD连发模式，30发弹夹）
├── weapon_smg.tres             ← 冲锋枪（HOLD连发模式，30发弹夹，复用 Rifle 状态）
├── weapon_pickup_pistol.tscn   ← 手枪拾取物
├── character_nobita.tres       ← 角色参数：のび太（外观/HP/音效）

scene/
├── main.tscn                   ← 主游戏场景
├── menu.tscn                   ← 菜单 CanvasLayer
├── map_editor.tscn             ← 运行时图块编辑器
├── map_viewer.tscn             ← 图块可视化器
└── map_test.tscn               ← 地图测试场景
```

---

## 2. 输入映射

| Action 名称 | 默认按键 | 用途 |
|-------------|---------|------|
| `左` | ← | 移动 / 菜单光标 |
| `右` | → | 移动 / 菜单光标 |
| `上` | ↑ | 移动 / 菜单光标 |
| `下` | ↓ | 移动 / 菜单光标 |
| `行走键` | Ctrl | 切换行走模式 |
| `确定键` | Z / Space | 确认 / 武器攻击 |
| `取消键` | X / Esc | 取消 / 返回 / **武器状态下固定朝向** |
| `菜单键` | X / Esc | 打开/关闭菜单（**武器状态下屏蔽**） |
| `举起放下武器键` | Shift | 举起/放下武器 |
| `装填键` | A | 武器举起后装填弹药（进入 Reload 状态） |
| `主武器键` | 1 | 切换到主武器槽位（举起中按同槽键 → 重进举起状态） |
| `副武器键` | 2 | 切换到副武器槽位（举起中按同槽键 → 重进举起状态） |
| `治疗品键` | 3 | 使用治疗物品 |
| `辅助品键` | 4 | 使用辅助物品 |

---

## 3. 状态机系统

### 架构

```
Player (CharacterBody2D)
└── StateMachine (Node)
    ├── Idle          ← initial_state
    ├── Walk
    ├── Run
    ├── Pistol        ← 手枪举起（Shotgun/Rifle 复用此脚本）
    ├── Knife         ← 小刀举起
    ├── PistolAttack  ← 手枪攻击（ShotgunAttack/RifleAttack 复用此脚本）
    ├── KnifeAttack   ← 小刀攻击
    ├── Shotgun       ← 霰弹枪举起（复用 PlayerPistolState）
    ├── ShotgunAttack ← 霰弹枪攻击（复用 PlayerPistolAttackState）
    ├── Rifle         ← 步枪/冲锋枪举起（复用 PlayerPistolState）
    ├── RifleAttack   ← 步枪/冲锋枪攻击（复用 PlayerPistolAttackState）
    └── Reload        ← 装填状态（NORMAL/SHOTGUN 双模式）
```

### 状态流转

```
         松行走键              按行走键
    ┌──── Run ◄──── Walk ────┐
    │     │          │        │
    │  无输入     无输入       │
    ▼     ▼          ▼        ▼
    └──► Idle ◄─── Idle ◄────┘
           │ 举起放下武器键
           ├──► Pistol ←→ PistolAttack（攻击时可移动）
           │       │                ▲
           │       └──► Reload ─────┘（装填动画 + 等待帧 → skip raise）
           ├──► Shotgun ←→ ShotgunAttack（攻击时可移动，SHOTGUN装填）
           │       │                      ▲
           │       └──► Reload ───────────┘
           ├──► Rifle ←→ RifleAttack（攻击时可移动，HOLD连发）
           │       │                ▲
           │       └──► Reload ─────┘
           └──► Knife  ←→ KnifeAttack（攻击时可移动）
                  │ 举起放下武器键
                  └──► (放下动画) → Idle
```

### 基类 API

```gdscript
class_name State extends Node

var character: CharacterBody2D
var last_state: State

signal transition_requested(nxt_state: String)

func enter()
func exit()
func process_update(delta: float)   # 输入检测 & 状态转换
func physics_update(delta: float)   # 移动 & 碰撞
```

### 添加新状态

1. 新建 `script/player/PlayerXxxState.gd`，`extends State`
2. 在 `object/player.tscn` 的 StateMachine 下添加子节点，挂脚本
3. 从其他状态 `transition_requested.emit("Xxx")` 切入

---

## 4. 武器系统

### 操作

| 按键 | 功能 |
|------|------|
| `举起放下武器键` (Shift) | 举起武器（进入武器状态）/ 放下武器（播放倒放动画 → Idle） |
| `确定键` (Z/Space) | 武器举起后 → 进入攻击状态（攻击时可移动，朝向锁定） |
| 移动键 | 武器举起后 + 攻击中均可移动 |
| `取消键` (X/Esc) | 武器举起 READY 阶段：固定朝向（模式由设置决定）；**武器状态下屏蔽菜单** |
| `装填键` (A) | 武器举起 READY 阶段 → 进入 Reload 状态（NORMAL 一次装满 / SHOTGUN 逐发装填）；装填期间锁定移动和朝向 |
| `主武器键` (1) / `副武器键` (2) | 切换装备槽位 |

### 攻击输入模式（FireMode）

武器攻击按键支持两种判定模式：

| 模式 | `fire_mode` 值 | 行为 | 适用武器 |
|------|---------------|------|---------|
| **点按 (TAP)** | 0 | 按一次确定键 → 打一发；松手后需再次按下才能打下一发 | 手枪、霰弹枪、近战 |
| **按住连发 (HOLD)** | 1 | 按住确定键 → 连续攻击；攻击动画 + 等待帧播完后自动开始下一轮 | 自动步枪、冲锋枪 |

**等待帧（`post_press_wait_frames`）**：攻击动画（含攻击后动画）完全播完后，再等 N 帧才检测下一次按键输入，用于控制射速。0 = 立即响应。

**空弹行为不变**：弹夹打空后，HOLD 连发自动停止并播放空弹音效。

### 举起/放下动画

```
举起: frame0 (char_idx_1) → frame1 (char_idx_2) → READY
                                                    ↓ 可移动
放下: READY → frame1 (char_idx_2) → frame0 (char_idx_1) → Idle
```

每帧持续时长由 `weapon_raise_frame_durations` 数组控制，两帧共 2 段动画。

### 武器配置

| 武器 | 精灵表 | char_idx_1 (第4~6列) | char_idx_2 (第7~9列) | 状态名 | 攻击模式 | 弹夹 | 弹药类型 |
|------|--------|---------------------|---------------------|--------|---------|------|---------|
| 手枪 | のび太セット.png | 1 | 2 | Pistol | TAP | 12 | ammo_pistol |
| 小刀 | のび太セットナイフ.png | 1 | 2 | Knife | TAP | — | — |
| 霰弹枪 | のび太セットショットガン.png | 1 | 2 | Shotgun | TAP | 5 | ammo_shotgun |
| 突击步枪 | のび太セットアサルトライフル.png | 1 | 2 | Rifle | HOLD（连发） | 30 | ammo_rifle |
| 冲锋枪 | [2K-VX]-のび太セットマシンガン.png | 1 | 2 | Rifle | HOLD（连发） | 30 | ammo_smg |

### WeaponData 关键字段

```gdscript
@export var weapon_state_name: String = ""     # 状态名（"Pistol"/"Knife"/"Shotgun"）
@export var weapon_walk_texture: Texture2D     # 武器行走图
@export var weapon_raise_char_sequence: Array[int] = [1, 2]  # 举起帧序列
@export var weapon_raise_frame_durations: Array[float] = [0.12, 0.12]  # 每帧时长
# 攻击后动画（Post-Attack）
@export var post_attack_char_sequence: Array[int] = []       # 攻击后帧序列（空=无）
@export var post_attack_frame_durations: Array[float] = []   # 攻击后每帧时长
@export var post_attack_sound: AudioStream = null             # 攻击后音效
# 装填系统
@export var reload_mode: int = ReloadMode.NORMAL  # NORMAL=一次装满, SHOTGUN=逐发装填
@export var reload_char_sequence: Array[int] = []             # NORMAL 装填帧序列
@export var reload_sound: AudioStream = null                   # 装填音效
@export var shotgun_reload_loop_char_sequence: Array[int] = [] # SHOTGUN 循环帧序列
@export var shotgun_reload_loop_sound: AudioStream = null      # 逐发装填音效
@export var shotgun_reload_end_char_sequence: Array[int] = []  # SHOTGUN 结束帧序列
@export var shotgun_reload_end_sound: AudioStream = null       # 结束音效（上膛）
@export var reload_wait_duration: float = 0.3                  # 装填后等待（共用）
```

### player.gd 武器模式 API

```gdscript
character.enter_weapon_mode(weapon_data)   # 切换到武器纹理/角色索引
character.exit_weapon_mode()               # 恢复到普通模式
character.set_weapon_frame(idx)            # 0=帧1, 1=帧2
character.get_weapon_data() → WeaponData   # 获取当前武器数据
```

---

## 5. 菜单系统

### 操作

| 按键 | 功能 |
|------|------|
| `菜单键` | 打开/关闭菜单 |
| `取消键` | 返回上级 / 关闭菜单 |
| `上`/`下` | 移动光标 |
| `确定键` | 进入子界面 / 确认选择 |

### 菜单结构

```
主菜单（标题画面）
├── 开始游戏  → 角色选择
├── 设置      → 设置面板（音乐音量/音效音量/固定朝向/返回）
└── 退出游戏  → 退出程序

主菜单（游戏内 X/Esc）
├── 继续游戏  → 关闭菜单
├── 设置      → 设置面板（同上，游戏暂停）
└── 退出游戏  → 退出程序
```

### 物品子界面（左右双面板）

- **左侧**：物品列表（图标 + 名称），过滤掉武器
- **右侧**：名称、类型、描述、效果（`effect_text`）
- 光标与 `_display_list` 索引同步

### 装备子界面（左右双面板）

- **左侧**：
  - 已装备武器 → ● 标记 + 绿色，排在最前
  - 背包武器 → 排在后面
- **右侧**：选中武器的名称、描述、属性（ATK/速度/范围/~~击退~~）
- 选中已装备武器按确定 → **卸下**，放回背包
- 选中背包武器按确定 → **装备**，换下当前武器

### 场景暂停

打开菜单时 `get_tree().paused = true`，菜单节点设置 `process_mode = PROCESS_MODE_ALWAYS` 确保暂停后仍接收输入。

### 物品/装备列表特性

- **同类物品合并**：按 `item_id` 分组，显示 `物品名 ×数量`（如 `手枪子弹 ×20`）
- **滚动裁剪**：列表用 `ScrollContainer` 限制最多 10 行可见，超出自动滚动
- **右侧详情实时更新**：光标移动时同步刷新右侧面板

### 布局参数（@export 可在编辑器中调整）

选中 `Menu` 节点，在检查器中可见以下分组：

| 分组 | 参数 | 默认值 | 说明 |
|------|------|--------|------|
| 主菜单布局 | `main_menu_cursor_x` | 24 | 箭头光标 X |
| | `main_menu_item_start_y` | 38 | 第一项 Y |
| | `main_menu_item_height` | 24 | 行高 |
| 子界面布局 | `sub_list_x` | 24 | 列表容器 X |
| | `sub_list_y` | 30 | 列表容器 Y |
| | `sub_cursor_x` | 6 | 箭头光标 X |
| | `sub_item_height` | 22 | 行高 |
| 右侧详情 | `detail_panel_x` | 216 | 详情面板 X |
| | `detail_label_x/y/w/h` | — | 文字区域位置 |

---

## 6. 标题画面 & 文字渲染

### 标题画面（title_screen.gd）

RM2K3 风格窗口，控制菜单项的**选择光标框**和**文字渲染**。

**操作**：上/下移动光标，确定进入，取消无操作。

**布局参数**（@export 可在编辑器中调整）：

| 分组 | 参数 | 默认值 | 说明 |
|------|------|--------|------|
| 窗口布局 | `window_size` | (220, 158) | 窗口尺寸 |
| | `window_y_offset` | 130.0 | 垂直偏移量 |
| | `show_title` | false | 是否显示游戏标题 |
| | `title_position` | (14, 8) | 标题文字位置 |
| | `separator_y` | 30.0 | 分隔线 Y 坐标 |
| | `panel_margin` | 6.0 | 背景纹理到窗口边缘的距离 |
| 选项布局 | `title_font_size` | 18 | 标题字号 |
| | `item_font_size` | 19 | 选项文字字号 |
| | `item_start_y` | 20.0 | 无标题时首项 Y 坐标 |
| | `item_height` | 28.0 | 每项行高（光标在此高度内垂直居中） |
| | `item_spacing` | 16.0 | 项与项之间的额外间隙 |
| | `item_margin_bottom` | 0.0 | 末项下方的边距 |
| | `item_title_gap` | 12.0 | 有标题时分隔线到首项的间距 |
| | `item_text_x` | 3.0 | 选项文字水平位置（`item_centered=false` 时） |
| | `item_width` | 0.0 | 选项统一宽度（0=自动） |
| | `item_centered` | false | 选项在窗口内水平居中 |
| 文字颜色 | `text_color_index` | 1 | 选项文字色表索引 (0–19) |
| | `text_color_row` | 0 | 色表行 (0–3) |
| | `text_title_color_index` | 1 | 标题文字色表索引 |
| 文字效果 | `text_bold` | true | 粗体（1px 右偏移叠加） |
| | `text_outline` | false | 描边开关 |
| | `text_outline_color` | Color.BLACK | 描边颜色 |
| | `text_shadow` | true | 阴影开关 |
| | `text_shadow_color` | Color(0,0,0,0.6) | 阴影颜色 |
| | `text_shadow_offset` | (2, 2) | 阴影偏移 |
| 光标框 | `cursor_base_height` | 32.0 | 光标基础高度 (px) |
| | `cursor_scale_y` | 1.0 | 纵向缩放（吸附 0.5×） |
| | `cursor_snap_to_item` | false | 选择框自动匹配 item 位置/宽度 |
| | `cursor_padding_x` | 24.0 | 水平内边距（snap 关时生效） |
| | `cursor_offset_x` | 10.0 | 光标水平偏移（snap 关时生效） |
| | `cursor_offset_y` | -3.0 | 光标垂直微调 |
| | `cursor_min_width` | 64.0 | 最小宽度（snap 关时生效） |
| | `cursor_override_width` | 94.0 | 手动指定宽度（>0 覆盖自动） |

> 布局计算公式详见 **`标题画面布局公式.md`**

### 文字渲染管线

标题画面使用 **GradientLabel**（`script/gradient_label.gd`）渲染菜单文字，内部自动选择渲染路径：

```
GradientLabel
  ├─ use_gradient = true  → TextGradientRenderer → TextureRect（渐变纹理）
  │                         自动处理 shadow/bold/outline
  └─ use_gradient = false → Label + shader 纯色（无渐变）
```

**GradientLabel 核心属性**：

| 属性 | 说明 |
|------|------|
| `text` | 显示文字 |
| `use_gradient` | 渐变开关（**false = 关掉渐变**） |
| `text_font_size` | 字号 |
| `color_index` / `color_row` | 色表索引 |
| `bold` / `shadow` / `outline` | 效果开关 |
| `font_path_override` 等 | 留空自动从 Global 继承 |

### 渐变文字渲染器（TextGradientRenderer）

**`script/text_gradient_renderer.gd`** — RefCounted 工具类，被 GradientLabel 内部调用：
- `render(text, color_index, outline, outline_color)` → 渐变 `ImageTexture`
- `render_flat(text, color_index)` → 纯色 `ImageTexture`（阴影用）
- 内部使用隐藏 SubViewport + Label 光栅化文字，`await` 等待渲染完成后逐像素处理
- 纵向渐变通过**人工亮度系数**实现：基准色 × 亮度系数（上 1.35 → 下 0.55）

### 全局文字默认（Global.gd）

`@export_group("文字默认")` 下的参数供所有 UI 场景读取：

| 参数 | 类型 | 说明 |
|------|------|------|
| `text_font_path` | String | 字体路径 |
| `text_color_sheet_path` | String | 20 色表路径 |
| `text_color_shader_path` | String | 着色器路径（非渐变 Label 用） |
| `text_color_index` | int | 默认颜色索引（0–19） |
| `text_color_row` | int | 默认色表行（0–3） |
| `text_gradient_enabled` | bool | 渐变文字开关 |
| `text_bold` | bool | 粗体开关 |
| `text_outline` | bool | 描边开关 |
| `text_outline_color` | Color | 描边颜色 |
| `text_shadow` | bool | 阴影开关 |
| `text_shadow_color` | Color | 阴影颜色 |
| `text_shadow_offset` | Vector2 | 阴影偏移 |

`GradientLabel` 在 `_enter_tree()` 时自动从 Global 读取默认值（资源路径留空时）。
Global 提供 `get_cached_color_image()` 缓存色表 Image，避免 GradientLabel 实例重复 I/O。

### 文字效果实现

| 效果 | 渐变路径 | 非渐变路径 |
|------|----------|-----------|
| 颜色 | CPU 取基准色 × 亮度系数（1.35~0.55） | CPU 取色表单元格中心纯色 → shader uniform |
| 纵向渐变 | ✅ 像素 Y 映射亮度（上亮下暗） | ❌ |
| 粗体 | TextureRect 复用渐变纹理 + 1px X 偏移 | 额外 Label + 1px X 偏移 |
| 阴影 | 额外 TextureRect + offset + modulate | 额外 Label + offset + 主题色 |
| 描边 | CPU 端 8 邻域边缘检测 | shader 内 8 邻域 alpha 检测 |

---

## 7. 物品 & 武器数据

### ItemData

```gdscript
class_name ItemData extends Resource

enum ItemType { CONSUMABLE, MATERIAL, KEY_ITEM, WEAPON, ARMOR }

@export var item_id: String
@export var item_name: String
@export var item_type: int
@export_multiline var description: String
@export var icon: Texture2D
@export_multiline var effect_text: String    # 效果描述
@export var sell_price: int
@export var stackable: bool
@export var max_stack: int
```

### WeaponData（继承 ItemData）

```gdscript
class_name WeaponData extends ItemData

# 战斗属性
@export var attack_power: int = 10
@export var attack_speed: float = 1.0
@export var attack_range: float = 48.0
@export var weapon_sprite: Texture2D
~~@export var knockback: float = 100.0~~  *（击退已移除）*

# 武器动画
@export var weapon_state_name: String = ""
@export var weapon_walk_texture: Texture2D
@export var weapon_raise_char_sequence: Array[int] = [1, 2]
@export var weapon_raise_frame_durations: Array[float] = [0.12, 0.12]

# 攻击输入（NEW）
enum FireMode { TAP = 0, HOLD = 1 }
@export var fire_mode: int = FireMode.TAP     # TAP=点按, HOLD=按住连发
@export var post_press_wait_frames: int = 0   # 攻击后等待帧数（射速控制）
```

### 创建新物品/武器

1. FileSystem → 右键 → **新建资源…** → `ItemData` 或 `WeaponData`
2. 保存到 `object/` 目录
3. 在检查器中编辑所有属性（可视化编辑）

---

## 8. 角色系统

### CharacterData

```gdscript
class_name CharacterData extends Resource

# 基础属性
@export var character_name: String = "のび太"
@export var level: int = 1
@export var max_hp: int = 100
@export var max_mp: int = 50

# 战斗属性
@export var base_attack: int = 15
@export var base_defense: int = 5
@export var base_speed: float = 1.0
@export var critical_rate: float = 0.05
@export var critical_damage: float = 1.5

# 成长率
@export var hp_growth: int = 10
@export var atk_growth: int = 3
@export var def_growth: int = 2

# 外观（精灵表 + 角色索引）
@export var portrait: Texture2D
@export var walk_texture: Texture2D
@export var walk_char_index: int = 0
@export var walk_frame_duration: float = 0.18
@export var run_texture: Texture2D
@export var run_char_index: int = 1
@export var run_frame_duration: float = 0.10
@export var death_texture: Texture2D          # 留空则回退到 walk_texture
@export var death_char_index: int = 7

# 音效
@export var hurt_sound: AudioStream = null
@export var death_sound: AudioStream = null

func get_effective_attack() → int    # 等级加成后的攻击力
func get_effective_max_hp() → int    # 等级加成后的最大 HP
```

### player.gd 集成

Player 节点新增 `@export var current_character: CharacterData`。`_ready()` 调用 `_apply_character_data()` 从中读取外观/动画/HP/音效参数。

player.gd 中原有的 `@export` 行走图/HP/音效参数已移除，改为内部变量，统一由 CharacterData 驱动。保留 `@export` 的仅剩：
- `current_character` — 角色资源引用
- `walk_speed` / `run_speed` — 移动速度
- ~~`knockback_stun_duration`~~ — ~~击退硬直（已移除）~~
- `death_fade_duration` / `death_black_hold` — 死亡黑屏

---

## 9. 存档系统

### 文件位置

```
user://saves/save_data.json    ← 存档文件
user://config.json             ← 配置文件（debug 开关）
```

### SaveManager API

```gdscript
SaveManager.save_game()
SaveManager.load_game() → Dictionary
SaveManager.auto_load_on_start() → bool
SaveManager.has_save() → bool
```

### 启动流程

1. `Global._ready()` → 加载 `config.json`
2. `GameInit._ready()` → `Global.try_load_or_init()`
3. 有存档 → 加载；无存档 → `init_new_game()`（初始化背包、装备、角色）

### 初始物品设置

在 `script/global.gd` → `init_new_game()` 中配置：
```gdscript
add_item(load("res://object/weapon_pistol.tres") as ItemData)
add_item(load("res://object/item_potion.tres") as ItemData)
```
修改后**删掉 `user://saves/` 下的存档**才能在新游戏中生效。

---

## 10. Debug 全局开关

```gdscript
Global.debug_enabled = true    # 菜单显示「存档」选项
# 状态保存在 user://config.json
```

---

## 11. 可视化编辑工作流

### 编辑物品/武器

FileSystem → 右键 → 新建资源 → ItemData/WeaponData → 保存到 object/ → 检查器编辑

### 编辑角色参数

FileSystem → 右键 → 新建资源 → CharacterData → 保存到 object/ → 检查器编辑

### 编辑图块 TileSet

参考 `TileSet工作流完整指南.txt`

---

## 12. 场景说明

| 场景 | 用途 |
|------|------|
| `scene/title_screen.tscn` | 标题画面（RM2K3 风格窗口 + 光标动画 + 文字渲染） |
| `scene/character_select.tscn` | 角色/关卡选择（占位） |
| `scene/main.tscn` | 主游戏场景（Player + Map + Menu + GameInit） |
| `scene/menu.tscn` | 菜单 CanvasLayer，可实例化到任意场景 |
| `scene/map_editor.tscn` | 运行时图块编辑器（调色板 + 浏览器 + TileMapLayer） |
| `scene/map_viewer.tscn` | 图块可视化器（_draw 快速浏览） |
| `object/player.tscn` | 玩家打包场景（可直接实例化） |

---

## 13. 玩家死亡系统

### 死亡流程

```
_die()                           # 触发死亡
  ├─ 停止状态机 + 动画计时器      # 防止 _refresh_sprite 覆盖死亡帧
  ├─ 设置死亡精灵帧               # death_char_index（默认 7），使用 walk_texture
  ├─ 禁用碰撞体                   # CollisionShape2D.disabled = true
  ├─ 播放死亡音效/音乐
  └─ 创建渐黑遮罩 → _process_death()
       ├─ Phase 1: 渐黑           # death_fade_duration 秒内 alpha 0→1
       ├─ Phase 2: 全黑等待       # 持续 death_black_hold 秒
       └─ Phase 3: 重载存档       # SaveManager.load_game() → reload_current_scene()
```

### 关键参数

| 参数 | 位置 | 类型 | 默认值 | 说明 |
|------|------|------|--------|------|
| `death_texture` | CharacterData | Texture2D | null | 死亡行走图（留空回退 `walk_texture`） |
| `death_char_index` | CharacterData | int | 7 | 死亡精灵角色索引 |
| `death_sound` | CharacterData | AudioStream | null | 死亡音效 |
| `death_music_path` | Global | String | "" | 死亡 BGM 文件路径 |
| `death_fade_duration` | player.gd | float | 2.0 | 黑屏淡入时长（秒） |
| `death_black_hold` | player.gd | float | 1.0 | 全黑后等待时长（秒） |

### HP 系统

`max_hp` 由 `CharacterData.get_effective_max_hp()` 计算（含等级加成），`_apply_character_data()` 在 `_ready()` 时同步到 player。

```
~~take_damage(damage, knockback_force, direction, is_headshot)~~  *→ take_damage(damage, 0.0, direction, is_headshot)*
  ├─ _is_dying 时直接 return（防止重复死亡）
  ├─ current_hp -= damage
  ~~├─ 击退力 + 硬直（_knockback_stun_timer）（已移除）~~
  ├─ 同步 Global.player_hp
  └─ current_hp <= 0 → _die()

heal(amount)                       # 回复 HP，同步 Global
```

### 注意事项

- **动画计时器必须停止**：`_die()` 中调用 `animation_timer.stop()`，防止 timer 回调覆盖死亡帧。
- **`_refresh_sprite()` 有 `_is_dying` 守卫**：死亡后拒绝一切精灵刷新，作为兜底防御。
- **碰撞体用 `set_deferred` 禁用**：在物理回调中直接修改可能导致报错。
- **受击碰撞体也需禁用**：`hurt_area.monitoring = false; hurt_area.monitorable = false`，防止尸体被子弹/近战检测到（2026-07-28）。
- **外观/HP/音效由 CharacterData 统一管理**：`death_texture`、`death_char_index`、`death_sound` 等在 CharacterData 资源中设置，player.gd 通过 `current_character` 引用读取。修改角色参数只需编辑 `.tres` 文件。
- **渐黑遮罩必须挂在 CanvasLayer 下**：ColorRect 是 Control 节点，直接挂 Node2D 场景根不会渲染。用 CanvasLayer（layer=128）包裹，并手动设置 `size = get_viewport().get_visible_rect().size`。
- **死亡后重载存档**：`_reload_from_save()` 先调 `SaveManager.load_game()` 恢复 Global 数据，再 `reload_current_scene()`。

---

## 14. 敌人死亡系统

### 概述

敌人死亡有两种模式，尸体**永久保留在地图上**（不 `queue_free()`），碰撞和视野均已禁用：

| 死亡模式 | 触发条件 | 精灵帧 | 状态类 |
|----------|---------|--------|--------|
| 普通死亡 | HP≤0 且非爆头 | `death_char_index`（默认 4） | `EnemyDeathState` |
| 爆头死亡 | HP≤0 且 `is_headshot=true` | 帧5 → 暂停20帧 → 帧6 | `EnemyHeadshotDeathState` |

### 死亡流程

```
take_damage() → current_hp ≤ 0
  └─ _die(is_headshot)
       ├─ _is_dead = true                    ← 核心守卫：阻止 _refresh_sprite() 覆盖死亡帧
       ├─ 播放死亡音效
       └─ StateMachine 有对应死亡状态？
            ├─ YES → sm._on_transition_requested("Death"/"HeadshotDeath")
            │         ├─ EnemyDeathState.enter()
            │         │    ├─ 停止 AnimationTimer
            │         │    ├─ 禁用碰撞体 + 视野 Area2D
            │         │    ├─ _refresh_sprite_with_index(death_char_index)  ← 设置普通死亡帧
            │         │    └─ _register_corpse()
            │         │
            │         └─ EnemyHeadshotDeathState.enter()
            │              ├─ 停止 AnimationTimer
            │              ├─ 禁用碰撞体 + 视野 Area2D
            │              ├─ _refresh_sprite_with_index(headshot_char_index_1)  ← 爆头帧1
            │              └─ process_update() 驱动动画阶段:
            │                   ├─ ANIM → PAUSE（等待 headshot_pause_frames 帧）
            │                   ├─ PAUSE结束 → _refresh_sprite_with_index(headshot_char_index_2)  ← 最终帧
            │                   ├─ _register_corpse()
            │                   └─ 关闭 StateMachine process
            │
            └─ NO（回退路径）→ _become_corpse(is_headshot)
                 ├─ _disable_for_corpse()     ← 停止 timer、禁用碰撞/视野、清速度
                 ├─ _refresh_sprite_with_index(death_char_index/headshot_char_index_1)
                 └─ _register_corpse()
```

### 关键方法

| 方法 | 位置 | 作用 |
|------|------|------|
| `_die(is_headshot)` | `enemy.gd` | 死亡入口：设 `_is_dead` + 状态机转换/回退 |
| `_become_corpse(is_headshot)` | `enemy.gd` | 回退路径：无死亡状态节点时的手动尸体化 |
| `_disable_for_corpse()` | `enemy.gd` | 停止 timer + 禁用碰撞体/视野/发现标签 |
| `_register_corpse()` | `enemy.gd` | 注册到 `Global.register_corpse()` |
| `_refresh_sprite_with_index(idx)` | `enemy.gd` | 设置指定角色索引的精灵帧（**绕过 `_is_dead` 守卫**） |

### `_is_dead` 守卫机制（2026-07-21 修复）

这是防止**死亡后行走图覆盖死亡帧**的核心修复：

```gdscript
# enemy.gd — _refresh_sprite()
func _refresh_sprite() -> void:
    if not sprite or not walk_texture:
        return
    if _is_dead:            # ← 守卫：死亡后拒绝一切动画刷新
        return
    # ... 正常绘制行走/站立帧
```

**问题根因**：`AnimationTimer` 的 `timeout` 信号在任何时候都可能触发。死亡状态切换和 timer 回调之间存在竞态窗口 — 如果 timer 在死亡状态 `enter()` 停止 timer 之前触发，`_refresh_sprite()` 会把死亡帧覆盖为行走帧。

**修复策略（双层防御）**：
1. **`_die()` 第一时间设 `_is_dead = true`** — 在任何状态切换前激活守卫
2. **死亡状态 `enter()` 中停止 `AnimationTimer`** — 从源头消除回调
3. **`_refresh_sprite()` 检查 `_is_dead`** — 兜底防御，即使 timer 因故未停止也不会覆盖

注意：`_refresh_sprite_with_index()` **不检查** `_is_dead`，因此死亡状态可以正常调用它来设置死亡帧。

### 尸体系统

死亡后敌人变为**不可交互的尸体**：
- **碰撞体**：通过 `set_deferred("disabled", true)` 禁用
- **视野区域**：`monitoring` 和 `monitorable` 均设为 false
- **受击碰撞体**：`monitoring` 和 `monitorable` 均设为 false（2026-07-28 新增，防尸体被子弹/近战检测）
- **发现标签**（"!"）：隐藏
- **动画计时器**：停止
- **速度**：归零
- **全局注册**：`Global.register_corpse(self)` 将尸体加入列表（供后续功能使用，如复活、清理等）

### 导出参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `death_char_index` | 4 | 普通死亡角色索引 |
| `headshot_char_index_1` | 5 | 爆头死亡帧1（初始显示） |
| `headshot_char_index_2` | 6 | 爆头死亡帧2（暂停后切换，最终保持） |
| `headshot_pause_frames` | 20 | 爆头动画在帧1停留帧数（≈0.33s@60fps） |

---

## 15. 敌人 AI 系统

### 状态机架构

```
Enemy (CharacterBody2D)
└── StateMachine (Node)
    ├── Idle          ← initial_state（待机，每帧轮询视野）
    ├── Discover      ← 发现玩家（显示 "!" + 发现音效，持续 0.5 秒）
    ├── Chase         ← 追击玩家（直接朝玩家移动）
    ├── Attack        ← 近战攻击（动画序列 → 命中判定 → 后摇）
    ├── Knockback     ← 击退/硬直（子弹击退 + velocity 衰减 + 硬直倒计时 → Chase）
    ├── Death         ← 普通死亡
    └── HeadshotDeath ← 爆头死亡（两阶段动画）
```

### 状态流转

```
Idle ──(视野发现玩家)──→ Discover ──(0.5秒后)──→ Chase ──(距玩家≤attack_range)──→ Attack
  │        │                                      ↑                  │
  │        └──(受击退伤害)──→ Knockback ──────────┘                  │
  │                          (硬直结束)                                │
  └──────────────────────────(后摇结束)────────────────────────────────┘
                                              │
                           (被击退子弹命中)      │
                                              ▼
                              Knockback ──(硬直结束)──→ Chase

任意状态 ──(HP≤0)──→ Death / HeadshotDeath
```

> **击退修复（2026-07-28）**：Idle 状态的敌人被击退子弹命中 → 直接进入 Knockback（跳过 Discover），同时播放 discover_sound。击退结束后自动追击玩家。
>
> **音效修复（2026-07-28）**：
> - 敌人攻击命中时播放 `attack_sound`（之前在 `EnemyAttackState._do_attack_hit()` 中遗漏）
> - 自然视野发现时播放 `discover_sound`（之前在 `EnemyDiscoverState.enter()` 中遗漏，仅受击发现会播放）

### 视野检测（仅检测玩家）

敌人通过 `VisionArea`（Area2D 扇形区域）检测目标，**仅识别玩家角色，不会将其他敌人误认为目标**。

核心方法 `_is_player_body(body)` 通过检查 `has_method("get_weapon_data")` 精确识别玩家——此方法仅玩家节点拥有，敌人不具备。

视野检测触发路径：

| 路径 | 说明 |
|------|------|
| `_on_vision_area_body_entered()` | 信号驱动：目标进入 VisionArea |
| `EnemyIdleState.process_update()` | 主动轮询：每帧调用 `vision_area.get_overlapping_bodies()` |

两条路径均通过 `_is_player_body()` 过滤，确保只有玩家能触发发现。

### 导出参数

| 分组 | 参数 | 类型 | 默认值 | 说明 |
|------|------|------|--------|------|
| 属性 | `max_hp` | float | 100.0 | 最大 HP |
| | `move_speed` | float | 120.0 | 移动速度 |
| | `attack_damage` | float | 10.0 | 攻击伤害 |
| | `attack_range` | float | 40.0 | 距目标多少像素进入攻击 |
| | `attack_hit_range` | Vector2 | (48, 32) | 攻击判定矩形（宽×高） |
| | `attack_hit_forward_offset` | float | 28.0 | 攻击判定前方偏移 |
| | `attack_cooldown_frames` | int | 30 | 攻击后摇帧数 |
| 受击碰撞体 | `hurtbox_size` | Vector2 | (28, 44) | 受击碰撞体尺寸 |
| | `hurtbox_offset` | Vector2 | (0, -8) | 受击碰撞体偏移 |
| 视野 | `vision_angle` | float | 90.0 | 视野角度（度） |
| | `vision_range` | float | 200.0 | 视野距离（像素） |
| 友军伤害 | `can_damage_enemies` | bool | false | 是否可对其他敌人造成伤害 |
| 攻击动画 | `attack_char_sequence` | Array[int] | [1,2,3,1,0] | 攻击动画帧序列 |
| | `attack_frame_durations` | Array[float] | [] | 每帧持续时间 |
| | `hit_at_sequence_idx` | int | 2 | 判定帧位置 |
| | `attack_pause_frames` | int | 20 | 暂停帧数 |
| | `hit_at_sequence_idx` | int | 2 | 在此索引处判定伤害 |
| 死亡 | `death_char_index` | int | 4 | 普通死亡角色索引 |
| | `headshot_char_index_1` | int | 5 | 爆头死亡帧1 |
| | `headshot_char_index_2` | int | 6 | 爆头死亡帧2（最终保持） |
| | `headshot_pause_frames` | int | 20 | 爆头动画暂停帧数 |
| 音效 | `hurt_sound` | AudioStream | null | 受伤音效 |
| | `death_sound` | AudioStream | null | 死亡音效 |
| | `headshot_sound` | AudioStream | null | 爆头音效 |
| | `headshot_fall_sound` | AudioStream | null | 爆头倒地音效 |
| | `attack_sound` | AudioStream | null | 攻击音效 |
| | `discover_sound` | AudioStream | null | 发现玩家音效 |

### 友军伤害机制

- **默认关闭**：`can_damage_enemies = false`，敌人攻击仅对玩家造成伤害
- **开启后**：攻击命中矩形内的**其他敌人**也会受到同等伤害（`attack_damage`），攻击者自身和目标玩家不受友军伤害影响
- **实现**：`EnemyAttackState._do_attack_hit()` 中通过 `get_tree().get_nodes_in_group("enemy")` 遍历所有敌人，检测是否在攻击命中矩形内
- **敌人组注册**：`_ready()` 中 `add_to_group("enemy")`，用于友军伤害时高效查找

### 攻击命中判定

```
_enemy.global_position
  │
  └── +fv × attack_hit_forward_offset  →  hit_center（矩形中心）
                                            │
                        ┌───────────────────┼───────────────────┐
                        │                                        │
               attack_hit_range.x（宽/左右）        attack_hit_range.y（高/前后）
```

- **上下朝向**：矩形不旋转，X=宽，Y=高
- **左右朝向**：矩形旋转 90°，宽高互换（forward=x 轴，lateral=y 轴）
- **判定**：目标相对于 `hit_center` 的坐标在矩形范围内即命中

### 与玩家识别的区别

| 检查方式 | 用途 | 匹配范围 |
|----------|------|---------|
| `_is_player_body(body)` | 视野检测、目标锁定 | **仅玩家**（`has_method("get_weapon_data")`） |
| `has_method("take_damage")` | 攻击伤害接口 | 任何可受伤实体（玩家 + 敌人） |
| `is_in_group("enemy")` | 友军伤害遍历 | 仅敌人（用于 `can_damage_enemies`） |

---

## 16. 设置系统

### 概述

设置界面可从**标题画面**（"设置"菜单项）和**游戏内主菜单**（"设置"菜单项）进入。设置保存在 `config.json`，重启后自动恢复。

### 设置项

| 项目 | 操作 | 范围 | 说明 |
|------|------|------|------|
| 音乐音量 | 左/右 ±5 | 0–100% | 通过 `AudioServer.set_bus_volume_db("Music", ...)` 控制 |
| 音效音量 | 左/右 ±5 | 0–100% | 通过 `AudioServer.set_bus_volume_db("SFX", ...)` 控制 |
| 固定朝向 | 确认键切换 | 切换式 / 按住式 | 0=按取消键切换锁定状态，1=按住取消键锁定 |

### 视觉

- 每个音量项右侧有**蓝色音量条**（`ColorRect`，宽度按百分比变化）+ 百分比文字
- 固定朝向项显示当前模式文字
- 标题画面使用 `GradientLabel`（RM2K3 风格），游戏内菜单使用 `Label`（简洁风格）

### 数据流

```
用户操作 → Global.set_xxx_volume() / set_facing_lock_mode()
  → 更新 Global 变量 → _apply_volume()（立即生效）
  → save_config() → 写入 config.json
```

### config.json 格式

```json
{
    "debug": true,
    "music_volume": 80,
    "sfx_volume": 80,
    "facing_lock_mode": 0
}
```

---

## 17. 音频系统

### 音频总线

Global 在 `_ready()` 中通过 `AudioServer.add_bus()` 创建 3 个总线：

| 总线 | 索引 | 用途 |
|------|------|------|
| Master | 0 | 最终输出（默认） |
| SFX | 1 | 所有音效（武器、敌人、UI、受伤、命中） |
| Music | 2 | 背景音乐（标题 BGM + 死亡音乐） |

### 总线路由

所有创建 `AudioStreamPlayer` 的地方均设置了 `player.bus = "SFX"` 或 `player.bus = "Music"`：

| 文件 | 方法 | 总线 |
|------|------|------|
| `player.gd` | `_play_sound()` | SFX |
| `player.gd` | `_play_music()` | Music |
| `enemy.gd` | `_play_sound()` | SFX |
| `PlayerPistolState.gd` | `_play_sound()` | SFX |
| `PlayerKnifeState.gd` | `_play_sound()` | SFX |
| `PlayerPistolAttackState.gd` | `_play_attack_sound()` | SFX |
| `PlayerKnifeAttackState.gd` | `_play_attack_sound()` | SFX |
| `bullet.gd` | 命中音效 | SFX |
| `title_screen.gd` | BGM AudioStreamPlayer | Music |

### 音量控制

```gdscript
Global.set_music_volume(pct: int)  # 0–100 → linear_to_db(pct/100) → AudioServer
Global.set_sfx_volume(pct: int)    # 同上
```

---

## 18. 固定朝向系统

### 两种模式

| 模式 | `facing_lock_mode` | 行为 |
|------|-------------------|------|
| 切换式 | 0 | 按一次取消键 → 锁定朝向；再按一次 → 解锁 |
| 按住式 | 1 | 按住取消键 → 锁定朝向；松开 → 解锁 |

### 生效范围

固定朝向在以下状态中生效：
- **Pistol READY**（手枪举起后就绪）
- **Knife READY**（小刀举起后就绪）
- **PistolAttack**（攻击时不更新朝向，移动不影响子弹方向）
- **KnifeAttack**（同上，近战方向保持攻击按下时的朝向）

### 实现

- `player.gd`：`_facing_locked` 标志 + `lock_facing()`/`unlock_facing()`/`toggle_facing_lock()` 方法
- `update_facing()` 开头检查 `if _facing_locked: return`
- 各武器状态的 `process_update()` 中处理取消键输入
- `player_in_weapon_state` 标志通知 `menu_controller.gd` 屏蔽菜单键
- `player.gd` `_ready()` 中 `add_to_group("player")`，供 menu_controller 查找

---

## 快速参考
