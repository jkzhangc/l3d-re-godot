# 系统架构注释总览

> 本文档为 `l3d-re-godot`（のび太的求生之路 / のびハザ L3D）的**系统与架构注释总纲**。
> 每个 `script/**/*.gd` 文件的头部都写有统一的 `## ── 架构定位 ──` 注释块，字段为
> **系统 / 层 / 联机 / 职责 / 依赖**；本文档是这些注释的汇总视图与阅读地图。
>
> 核对时间：2026-09-10。**代码与本文冲突时以代码为准，并请顺手修正本文。**
>
> 快速定位：`grep -rn "架构定位" script/` 列出全部 95 个脚本的系统归属。

---

## 1. 分层与依赖方向

```
                    ┌──────────────────────────────────────────┐
   单例 / 全局       │ Global · Players · Net · Director ·      │
                    │ ChapterStats · PhantomCameraManager      │
                    └──────────────────────────────────────────┘
                                     ▲ 被读
  ┌────────────┐   ┌────────────┐   ┌────────────┐   ┌──────────────┐
  │  数据层     │◀──│  玩法层     │◀──│  表现层     │   │  网络层       │
  │ Resource / │   │ 实体与状态  │   │ UI/文字/动画│   │ Net/World    │
  │ RefCounted │   │ CharacterBody│   │ CanvasLayer│   │ 权威边界     │
  └────────────┘   └────────────┘   └────────────┘   └──────────────┘
                                     ▲
                          ┌──────────┴──────────┐
                          │  工具 / 编辑器层      │
                          │ @tool、命令行、预览   │
                          └─────────────────────┘
```

**依赖方向（自下而上，禁止反向引用）**

| 层 | 位置 | 特征 | 允许依赖 |
|----|------|------|----------|
| 数据层 | `script/*_data.gd`、`player_state.gd`、`item_codec.gd`、`save_manager.gd`、`campaign_data.gd` | 纯 Resource / RefCounted，无节点、无碰撞、无动画 | 仅数据层内部 |
| 框架层 | `State.gd`、`StateMachine.gd` | 不解释规则，只保证生命周期与路由 | 数据层（PlayerState） |
| 玩法层 | `player.gd`、`enemy.gd`、`player/*`、`enemy/*`、`director/*`、`bullet.gd`、`*_pickup.gd`、`corner_assist.gd` | 实体、状态机规则、权威结算、移动手感 | 数据层、框架层、单例 |
| 表现层 | `ui/*`、`gradient_label.gd`、`vx_anim_sprite.gd`、`damage_number.gd`、`*_indicator.gd`、`camera_follow.gd` | 只读玩法状态并渲染，不写权威数据 | 数据层、玩法层（只读）、单例 |
| 网络层 | `network_manager.gd`（Net）、`network_world.gd`、`network_snapshot_interp.gd` | 权威边界：写 HP/弹药/库存/生成的**唯一**入口 | 全部（受白名单约束） |
| 工具层 | `map_editor.gd`、`tileset_generator.gd`、`*_viewer.gd`、`palette_drawer.gd`、`map_restore/*` | `@tool` 或命令行，不参与运行时 | 引擎 API |

> `item_codec.gd` 单独存在的理由：`PlayerState` 需要序列化物品，`SaveManager` 需要创建 `PlayerState`，
> 两者直接互相引用会让 GDScript 编译失败（报的是误导性的 "Identifier not found"）。
> 拆出编解码类即打断该循环，它处在依赖链最底层。

---

## 2. 全局单例（autoload）

定义于 `project.godot` 的 `[autoload]` 段：

| 名称 | 脚本 | 系统 | 职责边界（**不要越界**） |
|------|------|------|--------------------------|
| `Global` | `script/global.gd` | 全局配置 | 本机设置、文字渲染默认值、色表缓存、音量总线、死亡演出参数、待生效到达点、检查点。**不放**联机 HP / 背包 / 伤害 / 敌人等权威事实 |
| `Players` | `script/player_registry.gd` | 玩家座位注册表 | 「座位（PlayerState 数据）」与「实体（Player 节点）」两层映射；`State.get_player_state()` 的唯一入口 |
| `Net` | `script/network_manager.gd` | 联机连接层 | ENet、握手、玩家名单、角色表白名单校验、场景 ready 与安全切图协议 |
| `Director` | `script/director/director.gd` | 导演系统 | 紧张度评估、节奏阶段、敌人生成、物品投放、剧本事件编排。**仅单机 / Host 运行** |
| `ChapterStats` | `script/chapter_stats.gd` | 章节统计 | 按座位号累计击杀 / 爆头 / 伤害 / 治疗品使用与计时 |
| `PhantomCameraManager` | 插件 | 相机 | Phantom Camera 插件的宿主管理器 |
| `_mcp_game_helper` | 插件 | 调试 | godot_ai 插件的运行时辅助，非玩法相关 |

---

## 3. 运行模式与权威边界

三种模式共用同一套实体脚本，靠 `Player.network_controlled` / `network_local_prediction` 与
`PlayerState` 的归属区分，**修改任何玩法代码前先判断自己落在哪一列**：

| 关注点 | 单机 | 联机 Host | 联机 Client |
|--------|------|-----------|-------------|
| 输入来源 | `Input.get_vector()` 直接读键 | NetworkWorld 注入的已验证输入 | 本地预测 + 快照纠偏 |
| 移动 | `move_with_corner_assist()` | 同一方法（Host 权威模拟） | 同一方法（本地预测），另按快照误差收敛 |
| 伤害 / HP | 本地结算 | **唯一权威** | 只应用 `apply_network_*` 表现接口 |
| 弹药 / 库存 | 本地 `PlayerState` | 权威 `PlayerState` | 请求式（`*_request` RPC），Host 校验后广播 |
| 敌人 AI | 本地 FSM | **唯一权威** | 不寻路，只表现 Host 快照 |
| 导演 / 生成 | Director 运行 | Director 运行 | 不运行，仅接收同步结果 |
| 掉落物拾取 | 本地提交 | `_try_host_pickup()` 唯一提交点 | 提交请求，等 Host 结果 |

**硬约束**

- 禁止使用 `MultiplayerSpawner` / `MultiplayerSynchronizer`；实体一律由可靠 RPC 显式 spawn/despawn，位置快照走 `unreliable_ordered`。
- Client 不得修改权威事实；所有跨端缓存的 `Object` 引用在**消费端**做 `is_instance_valid()` 过滤。
- 网络传输只允许白名单 ID / 路径 / 基础数值，**禁止**从 Client 接收任意 `Resource`。

---

## 4. 系统清单

### 4.1 框架层

| 文件 | 职责 |
|------|------|
| `State.gd` | 状态抽象基类：`enter/exit/process_update/physics_update` + `transition_requested` 信号；`get_player_state()` 经 `Players` 反查座位，避免联机误读本地状态 |
| `StateMachine.gd` | 按子节点名路由状态；只保证 `exit(旧) → 记录 last_state → enter(新)` 顺序；**允许同名重入**（换武器后重读资源） |

### 4.2 数据层

| 文件 | 关键内容 |
|------|----------|
| `item_data.gd` / `weapon_data.gd` / `throwable_data.gd` / `bullet_data.gd` | 物品→武器→投掷物→子弹的数据继承链；武器含开火模式、装填模式、举枪/攻击/装填/后坐力动画序列、枪声惊动范围 |
| `character_data.gd` / `character_catalog.gd` | 角色配置与角色资源目录（显式 preload，供角色选择与联机白名单共用） |
| `player_state.gd` | 单个座位的完整玩法数据（HP/TP、双槽武器与弹药、消耗品、投掷物、背包）+ 序列化/快照 |
| `save_manager.gd` / `item_codec.gd` | JSON 存档 v2（seats 数组），兼容读 v1；物品编解码独立成类以打断循环依赖 |
| `campaign_data.gd` / `vx_anim_cell_data.gd` / `weapon_effect_offsets.gd` / `skill_data.gd` | 战役定义、动画单格、武器特效偏移表、技能占位壳 |

### 4.3 玩法层

**玩家**（`player.gd` + `player/Player*State.gd`）

- `player.gd` 只管**实体表现与对外接口**（精灵表/动画/朝向/受击/死亡演出/联机接口/移动入口），
  移动输入与状态切换由状态机承担。
- 状态集合：`Idle / Walk / Run / Pistol / Knife / PistolAttack / KnifeAttack / Reload / Shove / Throwable`
  （`PlayerAttackState` 是旧版占位，无实际逻辑）。
- **移动统一入口 `move_with_corner_assist()`**：详见 §5.3。

**敌人**（`enemy.gd` + `enemy/Enemy*State.gd`）

- 八态：`Idle / Discover / Chase / Attack / Knockback / Hitstun / Death / HeadshotDeath`。
- **蹭墙**：与玩家共用 `CornerAssist` —— Idle/Discover/Attack 走 `move_with_corner_assist()`；
  Chase 走 `_move_with_stuck_recovery()`，卡住时调 `apply_corner_nudge()` 侧移（见 §5.3）。
- 寻路为单层 `AStarGrid2D`（32×32 网格、4 邻域），配合**同伴软代价避让**与分离推力；
  路径经「共线简化 → 视线简化（string pulling）→ 立足点重插 → 逐段硬校验」四步生成，
  **已删除「推墙」**（见 §7 陷阱 21）。详见 §5.4。

**战斗与道具**

| 文件 | 职责 |
|------|------|
| `bullet.gd` | 分帧渲染、飞行碰撞、命中结算、暴击判定、章节统计 |
| `throwable_projectile.gd` / `fire_patch.gd` | 抛物线投掷；落地爆炸或生成火海（周期灼烧） |
| `weapon_pickup.gd` / `healing_pickup.gd` / `hold_indicator.gd` | 地面掉落物与按住拾取进度环 |
| `character_switch_manager.gd` / `teammate_standin.gd` | 队伍成员切换与队友静态替身 |
| `damage_number.gd` / `throw_aim_indicator.gd` | 受击飘字、投掷路线指示器 |

### 4.4 导演系统（`script/director/`）

```
Director（autoload，总控）
  ├─ IntensityTracker   压力统计 → 0~1 紧张度（不生成实体）
  ├─ PacingController   紧张度 → build / peak / cooldown 阶段
  ├─ SpawnManager       build 撒散兵、peak 分批出尸潮
  ├─ ItemManager        按资源紧张度决定补给投放
  └─ EventManager       剧本事件与尸潮生命周期（含事件期敌人目标锁定/解锁）
```

配套实体与环境：`EventTrigger`（区域触发器）、`HoldoutMachine` + `HoldoutTeleportConfig`（防守战）、
`SafeDoor` / `TeleportPoint` / `ArrivalPoint` / `ArrivalResolver`（关卡流转）、
`SpawnPoint` / `SpawnZone` / `DirectorConfig`（生成与参数配置）。

**导航**：`enemy/EnemyChaseState.gd` —— `AStarGrid2D` 寻路（分帧构建全图障碍）+ 同伴软代价 + 分离推力。

### 4.5 表现层

| 子系统 | 文件 | 说明 |
|--------|------|------|
| 文字渲染 | `gradient_label.gd`、`text_gradient_renderer.gd`、`shader/text_color.gdshader` | 双路径：CPU 渐变纹理 / Label+shader；支持粗体、描边、阴影 |
| 战斗 HUD | `ui/combat_hud.gd` | 左上 HP 条与急救喷雾、右上主副武器图标、防守战倒计时 |
| 章节总结 | `ui/chapter_summary.gd` | 单人即时关闭；联机全员确认后推进 |
| 界面 | `title_screen.gd`、`character_select_menu.gd`、`campaign_select.gd`、`difficulty_select.gd`、`menu_controller.gd`、`network_lobby.gd` | RM2K3 风格窗口与光标，文字统一走 GradientLabel |
| 联机表现 | `player_nameplate.gd`、`network_revive_indicator.gd` | 头顶名牌血条、救援进度环 |
| 动画特效 | `vx_anim_sprite.gd` | VX Ace 动画播放器（多格合成 + 三层偏移 + 跟随） |
| 相机 | `camera_follow.gd` | Phantom Camera 跟随 + 地图边界限制 |

### 4.6 网络层

| 文件 | 职责 |
|------|------|
| `network_manager.gd`（Net） | 连接与大厅结构状态：`hello → hello_ack` 握手、角色表白名单校验、`start_game` / `scene_transition_*` 安全切图协议 |
| `network_world.gd` | 场景内权威世界：Host 模拟移动/战斗/拾取/复活/团灭，Client 提交输入并渲染快照 |
| `network_snapshot_interp.gd` | 远端位置插值缓冲（固定 `render_delay` 落后渲染，缺样本时停住不预测） |

### 4.7 工具与编辑器

`tileset_generator.gd`（VX 图块 → TileSet）、`map_editor.gd` + `palette_drawer.gd`（运行时图块编辑）、
`map_controller.gd` / `map_test_viewer.gd` / `vx_tilemap_test.gd`（图块验证）、
`map_restore/*`（RM2K3 还原预览与命令行生成器）、
`debug_capture.gd`（**现场抓取器**，见 §5.6）。

---

## 5. 关键数据流与实现要点

### 5.1 玩家输入 → 移动

```
Input（单机） / NetworkWorld 注入（联机 Host）
   → PlayerIdle/Walk/Run.physics_update 设置 character.velocity
   → character.move_with_corner_assist()          ← 统一移动入口
   → CharacterBody2D 物理（图块碰撞层）
```

### 5.2 伤害结算

```
子弹/近战判定创建伤害 → 目标 take_damage()
   → ChapterStats 记账 → 受击表现（变色/出血/击退/硬直）
   → 联机时 Host 通过 network_damage_applied 广播纯表现事件，Client 不重复结算
```

### 5.3 像素移动的蹭墙 / 拐角平滑（corner assist）

**问题**：角色碰撞矩形 24×27、图块 32×32，两者不是整数倍关系。贴着墙走向门框/墙角时，
只要横向有 1~2 像素错位，矩形就会顶在凸角上——表现为「先走几像素然后彻底停住」。

**基础手感来自引擎**：`move_and_slide()` 本身就提供「斜向顶墙 → 沿墙滑行」，而且滑行速度
接近全速（实测沿墙单帧位移 4.14px ≈ 速度 250/60），这就是「蹭着墙还能往前走」的基础，
不需要额外代码，蹭墙辅助也不应干涉它（实测贴墙滑行时辅助输出恒为 0）。

**实现**：`script/corner_assist.gd`（`CornerAssist`，纯 static，**玩家与敌人共用**）

1. **常规位移**：直接 `move_and_slide()`。畅通时不产生任何额外位移，直行不会被改成漂移。
2. **蹭墙修正**：若**某一轴**的本帧位移被挡，就沿**另一轴**做限速小位移。
   修正量按「能让该轴位移通行」的最小距离求解，但**每帧最多只挪 `corner_assist_speed × delta`**
   （默认 180px/s ≈ 3px/帧），于是观感是几帧内平滑蹭过去，而不是一帧瞬移。
3. **兜底**：蹭不开就原样交给 `move_and_slide()`，返回 `false`。

**三个关键实现细节（都踩过坑）**

- **按轴判断，而不是按「移动方向的垂线」**。阻挡永远来自轴对齐的墙体，所以要修正的是
  「另一条轴」。早期实现用移动方向的垂线作修正轴，斜向移动时垂线也是斜的，会把角色推向
  错误方向 —— 表现为「有时有效果、有时反而被弹开」。
- **探测必须分成两步，不能合成一次斜向扫掠**。实测（探针）表明：角色**贴平墙面**时，
  「侧移 + 位移」的斜向扫掠路径一定会切过墙角，`test_move` 恒为真 → 永远求不出解 → 蹭墙
  完全失效。正确做法是：
  - `test_move(global_transform, offset)` 为假 —— 纯侧移安全（也覆盖侧移过程中的中间位置）；
  - `test_move(global_transform.translated(offset), blocked_motion)` 为假 —— **侧移到位之后**能动。
- **探测只针对静态墙面**。探测期间把 `collision_mask` 临时收窄到 `corner_assist_world_mask`
  （默认仅「图块」层 = bit 1）。实测 `test_move` **确实遵守 `collision_mask`**；而玩家的掩码
  是 15（含敌人层），导演又持续在玩家身边刷怪 —— 不收窄的话，路过的丧尸会把探测判为阻挡，
  表现为「**时不时没效果**」。这是「按上键顶墙不侧移」的主因。

**可调参数**（`player.gd` / `enemy.gd`，Inspector 分组「蹭墙 / 拐角平滑」）：

| 参数 | 玩家默认 | 敌人默认 | 说明 |
|------|---------|---------|------|
| `corner_assist_enabled` | `true` | `true` | 总开关；关闭即退回原生 `move_and_slide()` |
| `corner_assist_speed` | `180.0` | `160.0` | 侧移速度上限（px/s）。越小越「轻微」，60 ≈ 每帧 1px |
| `corner_assist_max_shift` | `16.0` | `16.0` | 试探的最大侧移距离（px）。**不要调大**：上限 20 时会为满足 1px 横移把角色竖向挪 20px |
| `corner_assist_step` | `2.0` | `4.0` | 试探步长（px）。敌人数量多，取 4 降低 `test_move` 次数 |
| `corner_assist_follow_input` | `true` | `false` | 同距离两侧都可时优先跟随入力方向；敌人无入力 |
| `corner_assist_world_mask` | `1` | `1` | 只把「图块」层算作静态墙面（见上方第三点） |

**接入点**

- **玩家**：`script/player/` 全部 11 个状态 + `network_world.gd` 的 3 处玩家模拟
  （Host 权威模拟、Client 本地预测、倒地爬行）—— 单机 / Host / Client 三条路径手感一致。
- **敌人**：`enemy/Enemy*State.gd` 中原本调用 `move_and_slide()` 的三个状态（Idle / Discover / Attack）；
  追击状态自己用 `move_and_collide()` 走位，改在 `_move_with_stuck_recovery()` 里：
  卡住时不再朝同方向反复撞墙，而是调 `Enemy.apply_corner_nudge()` 沿垂直方向蹭 —— 两者
  共用同一份 `CornerAssist` 逻辑。

**验证**：`tools/corner_assist_test.tscn`（headless，23 checks）

```bash
GD="D:/Godot_v4.6.3-stable_win64.exe/Godot_v4.6.3-stable_win64_console.exe"
"$GD" --headless --path "D:/!bird's-eye-view-arpg-test-/l3d-re-godot" res://tools/corner_assist_test.tscn
```

fixture：横墙留 32px 门洞（玩家可通过区间 x∈[12,20]，敌人 20 宽 → x∈[10,22]）
+ 一道无开口长墙 + 一个「敌人层」碰撞体（验证探测隔离）。

| 用例 | 结果 |
|------|------|
| 满掩码 vs 仅图块层 | 敌人层碰撞体在满掩码下判为阻挡、收窄后被排除（**证明探测隔离生效**） |
| 玩家 / 关闭辅助 + 偏离 3px 直行进门 | 卡在门框（y=47.0），无横向位移（**反证**） |
| 玩家 / 开启辅助 + 偏离 3px 直行进门 | 穿门，穿门瞬间 x=14.0 在区间内，Δx=+5.0 |
| 单帧横向位移 | 3.00px ≤ 限速 3.0px（**无瞬移**） |
| 横向位移帧数 | 2 帧 ≥ 2（**分多帧平滑蹭过去**） |
| 玩家 / 斜向（右下 / 左下）偏离进门 | 均穿门，修正方向分别朝右 / 朝左 |
| 玩家 / 斜向顶住无开口长墙 | 未穿墙，沿墙滑行 Δx=360.7，未嵌进墙体 |
| **敌人** / 关闭辅助 + 偏离 8px 进门 | 卡在门框（y=50.0），无横向位移 |
| **敌人** / 开启辅助 + 偏离 8px 进门 | 穿门，穿门瞬间 x=10.7 在 [10,22] 内，Δx=+2.7 |
| 全用例单帧位移预算 | 最大 7.16 ≤ 7.97（= 移动 4.17 + 侧移 3.0 + 容差） |

> 判定「是否被修正到门洞区间」必须取**穿门那一瞬间**的 x，不能用终点 x ——
> 角色穿门后仍会按输入继续横向漂移。若输入本身带横向分量，区间精度无法精确测量，
> 此时断言「修正方向正确」，区间精度由纯纵向输入（无漂移）的用例保证。

结论：23/23 checks passed，退出码 0。

### 5.4 敌人寻路（`enemy/EnemyChaseState.gd`）

**实现位置**：`enemy/EnemyChaseState.gd`（Host 侧；Client 不寻路，只表现 Host 结果）。

**流程**

```
physics_update
  ├─ _build_step()                 分帧构建全图 AStarGrid2D（每帧 ≤1500 格）
  ├─ 到重算周期 → _find_path(from, to)
  │    ├─ _mark_enemy_soft_costs(player)   给挡路同伴加软代价（用完立即还原）
  │    ├─ _astar_grid.get_id_path()        A* 求解
  │    ├─ _clear_enemy_soft_costs()        还原
  │    └─ _smooth_path()                   共线简化 → 视线简化 → 立足点重插 → 逐段校验
  ├─ 沿航点推进（16px 到达半径）
  │    └─ 连续 15 帧几乎没位移 → 跳过该航点；末段则清空路径强制重算
  ├─ _update_stuck_state()          进度式卡住检测（比较「到航点的距离」而非单帧位移）
  ├─ _move_with_stuck_recovery()    临时屏蔽敌人层碰撞，只滑墙 + 卡住蹭墙
  └─ _push_apart_from_other_enemies()  同伴分离推力
```

**同伴避让：软代价，而非硬障碍（2026-09-10 修订）**

原实现对每只其他敌人 `set_point_solid(true)`。因为 `AStarGrid2D` 用 4 邻域
（`DIAGONAL_MODE_NEVER`），只要玩家四周的正交格被同伴占满，**目标格就彻底不可达** →
`get_id_path()` 返回空 → 连续失败 2 次后切「直接冲撞」降级模式 → 表现为一群丧尸挤在门口互相卡死。

现改为两级：

| 机制 | 参数 | 作用 |
|------|------|------|
| 软代价 | `ENEMY_SOFT_COST = 8.0` | 同伴格用 `set_point_weight_scale` 提高代价 → 倾向绕行但**不封死** |
| 玩家豁免 | `ENEMY_IGNORE_RADIUS = 96.0` | 玩家周围 3 格内的同伴完全不标记 → **保证终段永远可达** |
| 距离收敛 | `ENEMY_MARK_RADIUS = 288.0` | 只标记该半径内的同伴，减少无意义的写入与遍历 |

贴身围拢由 `_push_apart_from_other_enemies()` 的软推力解决 —— 那本就是它的职责。
软代价在同一次 `_find_path()` 内即时还原，不留残余状态。

**可行走性判定：以「碰撞体能否站得下」为判据（2026-09-10 二次修订）**

旧规则是「该格任一图块带碰撞多边形 → 整格不可行走」。它对两类地形必然误判：

- **一格宽的窄通道**：VX Ace 自动图块的边缘变体会把一条薄碰撞条带漏进通道格，
  于是「敌人身体明明过得去」的通道被整格判死 → 寻路找不到路 → 降级冲撞 → 挤墙；
- **墙体画在 Ground 层的地图**（如第二关·学校内部）：旧豁免只挂在 decor 碰撞上，
  且方向判定只扫描 upper/decor 层，于是豁免**永远不可能触发**。

现改为：`wall` 层整格硬阻挡 → 其余情况把碰撞体（收缩 `BODY_CLEARANCE`）放到
`_stand_center(gp)` 求出的立足点，与**所有图层**的碰撞多边形求交，不相交即可行走。

- `_stand_center()`：先试格心；格心站不下就按环向外试探（步长 4px、3 圈），
  找到能站的一侧并**按格缓存**。通道格里总有半边是空的，找到它、航点就落在那里 ——
  这是窄通道能走通的关键。
- 立足点本身必须留在格内，但**身体允许越格**，所以判定会连带检查身体覆盖到的相邻格。
- **未知图层名按「只阻挡、不提供地面」处理**：新加的图层（如 `铁门`、`MapLimit`）
  不再被寻路当空气，敌人不会直接穿门。
- ⚠ **图块碰撞多边形以「格心」为原点存储**（整格矩形存成 `-16..16`），代码内部统一用
  「格左上角为原点（0..32）」的格内局部坐标，所以 `_scan_cell()` 取 AABB 时要**补半格偏移**。
  漏掉这一步，整片碰撞几何会向左上错半格 —— 墙格自己看着还"像墙"，但会在格内留下一个
  假的"能站"角落（实测多认了 1469 格），敌人就会往那个角落里挤。
- 碰撞判定用**矩形 AABB**（不做 `Geometry2D.intersect_polygons`）：本工程图块碰撞体是
  轴对齐矩形，AABB 就是它本身；非矩形（如街道图块集里的少数圆角块）会偏保守。

**性能（必须守住，否则加载与每帧都会掉）**

整图 6.5 万格，判定的常数因子直接决定加载时长与每帧卡顿。第一版实现是 **0.54 ms/格
（整图 35.6 秒、按 BUILD_CHUNK=1500 折算每帧 813 ms）** —— 界面直接卡死。五处优化后
**约 0.005 ms/格（整图 0.3~0.9 秒）**，约 **100×**：

| 优化 | 说明 |
|------|------|
| 解析式立足点 | 由「阻挡贴哪条边」直接解出让开量，一次校验；替代「最多 49 个候选位置」的环搜索（环搜索仅作兜底） |
| 矩形 AABB 判定 | 替代 `Geometry2D.intersect_polygons`（每次都分配多边形数组） |
| 图层角色缓存 `_tilemap_roles` | 角色判定只做一次；热路径里 `to_lower()` + 子串搜索曾贡献 60 万次临时字符串分配 |
| 单次扫描 `_scan_cell()` | 一次遍历同时拿到「有地面 / 有 wall 层 / 自家碰撞盒」，替代三遍图层查询 |
| 分帧「数量 + 时间」双预算 | 只按格子数限流会在复杂地图上掉帧；`BUILD_TIME_BUDGET_USEC` 让每帧构建耗时封顶 |

另外：**热路径里禁止 `print`**。诊断打印挂在 `Global.debug_visuals` 下面 —— headless 下
控制台 I/O 很慢，基准里 10 行无条件 `print` 被记成了 140 ms 的开销（150 ms 的判定里只有
9.5 ms 是真正计算）。

**路径生成：三步 + 一道硬校验**

1. **共线简化** —— 方向不变的连续格子压成一个拐点；
2. **视线简化（string pulling）** —— 只要两点之间「碰撞体全程可通过」，中间拐点全部丢弃。
   这是让敌人走斜直线、而不是沿格子边缘走锯齿「楼梯」的关键；
3. **立足点重插** —— ② 会把中间格全丢掉，但窄通道的格立足点是偏离格心的，
   丢掉它线段就会从格心直穿、身体压到漏边上。`_reinject_offset_cells()` 把这类格插回来；
4. **逐段硬校验**（`_verify_or_raw()`）—— 复核**每一段**「碰撞体全程可通过」。
   任一段不通过就整条退回 **A\* 原始格心序列**：相邻格（4 邻域）中心的连线只经过这两格、
   而这两格都可行走，按构造必然可跟随。「路径一定走得通」是硬性质，简化只是启发式。

> ⚠ **已删除「校验式推墙」这一步**（2026-09-10 三次修订）。它把航点从格心推开 `half+6`
> 当作"离墙更远"，但本工程两套 tileset 的碰撞多边形**全是整格矩形** —— 格心本来就是
> 格内离墙最远的点，推开只会让航点贴到墙上。实测第二关学校内部：一格宽通道里航点被横推
> 14px 出通道，于是 ① 路径线穿过墙体、② 敌人朝通道外的航点走 → 沿墙前后 2.67px 振荡、
> 永远过不去。详见 §7 陷阱 21。

视线判定 `_has_line_of_sight()` 是**两层**，缺一不可：

- **格子级**：`_segment_cells()`（Amanatides–Woo 网格步进）遍历线段经过的每一格，
  与采样无关，因此不会漏格 —— 长线段上一个 32px 宽的墙不会掉在两次采样之间；
- **碰撞体级**：沿线段等距采样，每点要求 `_body_fits_at()` 按碰撞包围盒覆盖的格子区间判定，
  避免 20×28 的碰撞体贴角跨格时把「擦墙过」误判成通视。

**其他优化**

- **同伴列表分帧缓存**：`_cached_enemy_group()` 让同帧内所有敌人共享一次 `get_nodes_in_group()`，
  把「N 次数组分配 + N 次全量遍历」压到 1 次；
- **重算错峰**：`enter()` 时用实例 ID 生成确定性相位偏移，避免整批敌人同帧一起 A*；
- **日志收敛**：每敌人的首次寻路详述改由 `Global.debug_visuals` 控制，避免尸潮刷屏。

**验证**：`tools/pathfinding_test.tscn`（headless，33 checks）

前 21 项用合成可行走网格 + 手工构建的 `AStarGrid2D` 直接驱动真实算法（`_is_walkable` 命中缓存即返回，
故无需图块素材）。关键用例：

| 用例 | 结果 |
|------|------|
| 玩家四个正交邻居全被同伴占住 → `_find_path` | 返回有效路径（2 航点） |
| 反证：同样四格按旧实现写成 `set_point_solid` → `_find_path` | 返回空路径（**复现原缺陷**） |
| 玩家周围 4 个同伴是否被标记 | 0 个被标记；`is_point_solid` 全为 false |
| 软代价标记 / 还原 | 代价提升到 `ENEMY_SOFT_COST`，还原后残余 0 格 |
| 开阔区 19 格阶梯路径 → 简化后 | 2 航点（斜线直达） |
| 绕墙路径简化后 | 保留 4 航点，无任何片段穿墙，全部落在可行走格 |
| 视线：横穿墙 / 斜切墙角 | 均判为不可直达 |

后 12 项（`_test_body_fit_walkability`）**不走桩**，而是在运行时拼一个最小 TileSet
（地面 / 满格墙 / 左边缘 8px 薄条＝模拟自动图块漏边），铺出「两格厚横墙上只留一格宽门洞」的地形，
用真实 `TileData` 驱动 `_is_walkable`：

| 用例 | 结果 |
|------|------|
| 满格墙 / 无地面空区 / 普通地面 | 前两者不可行走，第三者可行走 |
| **反证**：门洞格的几何中心 | 确实站不下（旧实现据此整格判死） |
| **一格宽门洞（左侧 8px 漏边）** | **判为可行走**，立足点被推到 x=20.0（格心 16.0） |
| 未知名图层（`铁门`）带碰撞图块 | 阻挡（旧实现完全忽略它） |
| 名字含 `wall` 的图层、图块本身无碰撞 | 仍整格阻挡 |
| 端到端：横穿该门洞 `_find_path` | 4 航点，**确实取道门洞** |
| 路径的每一段 | 不穿过任何不可行走格 |
| 路径的每个航点 | 全部满足 `_body_fits_at`（碰撞体在航点上不与任何图块碰撞） |
| 平滑后每一段 | 都「碰撞体全程可通过」（`_segment_followable`，不可跟随 0 段） |
| 全格碰撞 + 一格宽通道（复刻学校内部地形） | 每段可跟随、航点未被推离格心 |
| 卡住判定 | 原地/往返振荡 → 计数达阈值；0.3px/帧 缓慢靠近 → 不触发 |

### 5.5 文字渲染

```
GradientLabel（统一节点）
  ├─ use_gradient = true  → TextGradientRenderer → SubViewport 光栅化 → 逐像素色表纵向渐变 → ImageTexture
  └─ use_gradient = false → Label + shader/text_color.gdshader（纯色 + 可选描边）
```

TTF 字体图集的 `UV.y` 是全局坐标，无法做逐字形渐变，因此必须走 CPU 预渲染路径绕过。

### 5.6 现场抓取（`script/debug_capture.gd`）

**要解决的问题**：像"敌人卡住 / 挤墙 / 拐不了弯"这种问题，单张截图看不出成因 ——
看不出它当前路径是什么、当前航点在不在墙里、它脚下那格被判定成可行走还是阻挡；
而录像这边没法直接看。所以这里把现场压成「连拍 PNG + 一份带数字的报告」。

**挂载方式**：由 `Global._ready()` 动态创建为 Global 的子节点，**不落进任何 `.tscn`** ——
所有场景（含联机、测试场）都能用，且不会污染关卡场景文件。

**操作**

| 按键 | 行为 |
|------|------|
| **F2** | 连拍 8 张 PNG（每 0.25s 一张）+ 写一份报告 |
| **F4** | 只写报告（瞬时，不产生图片） |

产物落在 `user://debug_capture/run_<时间戳>/`，绝对路径会打印到控制台。注意本工程是
自包含运行，`user://` 实际指向 `<项目目录>/Godot/app_userdata/<项目名>/`（该目录已在
`.gitignore` 里，抓取产物不会进版本库）。

**报告内容**：场景 / `debug_enabled` / `debug_visuals` / A* 网格状态（已构建、构建中、
两个缓存条数、图层清单）；逐实体（`player` / `enemy` 分组）打印所在格、HP、状态机状态、
Chase 的 `stuck` / `fallback` / `no_path` 计数、路径航点数与当前航点、后续航点序列；
最后给**每只实体周围 7×7 格的可行走 ASCII 地图**（`O` 可行走 / `X` 不可行走 /
`◎` 实体所在格 / `*` 下一个航点），以及 `◎` 与 `*` 两格的明细（可行走、立足点、
有无地面、有无 wall 层、碰撞盒个数）。

这张 ASCII 图是核心：它把"它为什么拐不了弯"变成可读的文字 —— 例如航点落在 `X` 里、
或实体所在格是 `O` 但立足点被推到格边。

**注意**：`DisplayServer.get_name() == "headless"` 时跳过截图（无画面），只写报告。

---

### 5.7 敌人投放：前方屏外定点刷怪（`script/director/front_spawner.gd`）

**问题**（用户反馈）：刷怪"不管有没有刷新点都会全图自动刷新"，且分布很散；玩家走得快时
前方反而空窗，看着很怪。

**旧实现的两处"乱撒"**：

1. 作者刷新点都不在附近时，`Director._find_walkable_near_player()` 会在玩家周围
   400~720px 的**任意方向**随机取点 —— 可能出现在玩家正前方可视范围内，也会散在背后、侧面；
2. 散兵只按 15~30s 的固定计时触发 → 玩家快速推进时前方空窗。

**现方案**（照 L4D 式，也是原作 L3D 的"配置／回収"思路）：`FrontSpawner` 每帧维护
「玩家前方、屏幕之外」的敌人数量：

| 规则 | 实现 |
|------|------|
| 只刷在**前方** | 以玩家速度方向为中心（静止时用朝向）的前方扇区，默认 ±60° |
| 只刷在**屏幕外** | 必须落在相机可视矩形**之外**并再外扩 `offscreen_margin`（默认 64px） |
| **固定数量** | 前方带内维持 `target_ahead`（默认 6）只；**带内够了就一只都不刷** |
| **移动时持续补** | 触发 = 玩家相对上一批的**净位移** ≥ `advance_step`（默认 160px）且距上批 ≥ `interval_min` 秒 |
| 静止不刷 | 默认 `spawn_when_idle=false`，站着不动不补怪（避免"脸前刷怪"） |
| 优先作者点 | 落在"前方+屏外"的 `SpawnPoint`/`SpawnZone` 优先；没有合适点才按距离带 × 角度自动找可行走格 |

**关键实现细节（都踩过）**

- **屏外判定必须是"矩形"，不能是"圆形"**：`|dx| > 半宽+余量 或 |dy| > 半高+余量`
  （至少一轴超出）。用"离相机中心 > R"会把屏幕四角方向的可视区域误判成屏外。
- **可视范围来自相机实况**：`视口尺寸 ÷ camera.zoom ÷ 2`（本工程视口 1280×960、缩放 2×
  → 世界可视 640×480，半宽半高 320×240）。**不要写死**，缩放或分辨率一改就会失效。
- **触发用"相对上批位置的净位移"，不是逐帧位移累加**：角色顶在墙上会有 1~2px 抖动，
  累加式会把它攒够 `advance_step` → 原地刷怪。
- **采样下限取 `max(min_dist, 沿该方向刚好出屏的距离)`**：否则一半采样点必然被屏外条件否掉
  （水平方向尤其明显：半宽 320 + 余量 64 = 384 > 默认 `min_dist` 360）。
- **作者 `SpawnZone` 的区域补齐也必须过屏外检查**：区域决定"刷在哪里"，
  但"何时刷"仍要避开玩家视野，否则会看到敌人凭空出现。

**参数**（`DirectorConfig` 分组「生成 — 前方屏外定点刷怪」，或直接改 `FrontSpawner` 节点）
：`front_spawn_enabled` / `front_target_ahead` / `front_batch` / `front_half_angle` /
`front_min_dist` / `front_max_dist` / `front_offscreen_margin` / `front_advance_step` /
`front_interval_min` / `front_spawn_when_idle`。

**接入点**：`Director._process()` 每帧驱动 `FrontSpawner.update()`；
`Director.spawn_ahead_batch(count)` 是"事件编排层"（`SpawnManager` 散兵/尸潮、防守战回退）
的统一出口 —— 它同样受"前方带数量闸门"约束，避免两套机制各自撒点。

**回收（离玩家太远就清除）——对应原作 #506「★☆敵の回収設定☆★」**

只有「配置」（#505）没有「回收」（#506）是不完整的，实测学校地图 60 秒（诊断 harness）：

| 指标 | 无回收 | 有回收 |
|------|--------|--------|
| 全场存活 | 6 → 49，**29s 顶满上限 50** | **稳定 20~28，永不顶满** |
| 距玩家 >1200px 的敌人 | 0 → 28 只（**只增不减**） | 0~5 只（持续被清） |
| 前方补位 | 29s 起被「全场存活已达上限」**永久掐断** | 全程持续补位 |
| 远处敌人还在跑 A* | 是（敌人一多就卡顿） | 否（已清除） |

机制：`Director._update_recycle()`（每 `recycle_interval` 秒一次，**先于存活计数**执行）——

- 存活敌人离玩家超过 `recycle_dist`（默认 1400px，屏外约 400px）→ 直接清除，
  **不管它有没有在追玩家**；
- 尸体也一并清远（`recycle_clear_corpses`），防长局尸体无限堆积；
- `recycle_exempt = true` 的敌人跳过（给必须留在原地的事件敌人留口子）；
- 联机会话走 `NetworkWorld.despawn_enemy_networkwide()` 可靠广播，
  否则 Client 会留下永久幽灵表现实体。⚠ NetworkWorld 挂在**当前场景**下，
  不是 `/root` 下的 autoload。

参数（`DirectorConfig` 分组「生成 — 回收」）：`recycle_enabled` / `recycle_interval` /
`recycle_dist` / `recycle_clear_corpses`。

**验证**：`tools/front_spawner_test.tscn`（headless，14 checks）
+ 真实学校地图集成跑（1200 帧、玩家持续前进）：**28 只新刷出的敌人 0 只落在屏幕内**。

#### 僵尸变体与狂暴（`script/enemy/zombie_variant.gd`，2026-09-10）

- **变体数据** = `ZombieVariant`（Resource，`tres/zombies/*.tres` 已建 10 种）：
  普通行走图（ゾンビ）+ 狂暴行走图（クリムゾンヘッド；null = 无狂暴形态）+
  普通/狂暴的 `move_speed` 与 `attack_damage` + 狂暴发现音效（默认 `クリム_叫び.wav`）。
- **刷哪种**：`DirectorConfig.zombie_pool`（按关卡固定池）内按 `weight` 随机；
  留空 = 内置默认池（男性/女性/学生）。`spawn_enemy` 在 `add_child` **之前**注入
  贴图/速度/伤害（`_ready` 的 `_refresh_sprite` 要用）。
- **狂暴**：尸潮 = pacing 的 **peak 阶段**（`horde_started/ended` 的同一处触发点）。
  peak 开始 → `_set_all_enemies_rage(true)` 全场切狂暴；结束 → 恢复普通；
  期间刷出的敌人直接以狂暴登场。无狂暴图的外观自动跳过。
- ⚠ 素材前提：普通图与狂暴图必须是**同布局**（本项目全部 576×512，走/攻/死/爆头
  帧索引一致），所以只换 `walk_texture`、帧索引原样复用。换素材源时先核对布局。
- 原作对照：原作的クリムゾン是「按概率刷出的独立类型」（#576 街レベルクリムゾン →
  発生ＢＯＷ番号=40；#634 クリムゾン死亡 联动 敵配置限界数），本设计按用户要求
  改为「尸潮触发的形态切换」。
- 验证：`tools/zombie_variant_test.tscn`（headless，**25 checks**）。

#### 角色挂载与 y 排序（层级问题的根因）

- 敌人生成父节点 = `Director._find_decor_layer()`：**全树深度优先找第一个名字含 "decor" 的节点**，
  即关卡里的 `DecorLayer`（两张图的 DecorLayer 都开了 `y_sort_enabled`）。
- 玩家却是场景里预摆的，且两张图挂法不一致：街道 = `DecorLayer/PlayerSpawn/Player`，
  学校内部 = `PlayerSpawn/Player`（挂在关卡根下）——**与敌人不同容器**。
  后果：学校图里整个玩家子树按根节点顺序画在 DecorLayer 之后 → 玩家永远盖住敌人。
- 修法（`GameInit._align_actor_layers()`，关卡就绪后执行）：把玩家 reparent 到 DecorLayer，
  与敌人成为兄弟节点，参与同一轮 y 排序。运行时进行，不改动场景文件。
- 仍存在的已知限制：`UpperLayer` 是根下独立画布项，**永远盖在角色之上**（不参与与角色的
  y 交错）。要彻底交错需要把 GroundLayer 之外的图层与角色放进同一个 y 排序容器 —— 属于
  场景结构改造，待确认后做。

#### 附：原作（E:/15.L3D，RM2K3）的刷怪逻辑逆向

工具：`tools/rm2k3_map_restore/dump_orig_events.py`（读 `RPG_RT.ldb` 公共事件 + `Map*.lmu`
地图事件，并把开关/变量 id 注解成名字）。2026-09-10 逐字节验证的格式要点：

- 公共事件区 = LDB `0x19`；每条 = `[条目号][0x01 名字][0x0B 触发][0x0C 开关标志][0x0D 开关号]
  [0x15 命令区长度][0x16 命令区]`；**命令区没有条数前缀**，解析到块尾即止（末条 code=0）；
- 条目边界用「chunk id 严格递增」判定；事件名是 **Shift-JIS**（不要先用 GBK 解，会出乱码但成功）；
- 命令编码：`code + indent + 字符串 + 参数表`（BER）；`12010` 条件分岐、`12120` 条件续接、
  `22010/22011` 否则/结束、`10220` 变量运算、`10210` 开关、`12330` 调用公共事件、
  `10860` 取坐标、`10` 空命令（编辑器占位）。

原作刷怪系统（852 条公共事件里的这些）：

| 事件 | 作用 |
|------|------|
| #489–494 増援判定１〜６ | 6 个槽位各自判断"是否需要补位"（该槽位所有敌人种类开关均为 OFF = 空缺） |
| #505 ★☆敵の配置設定☆★ | 按 `变量[662] 発生ＢＯＷ番号` 分派：开对应敌人开关 + 读玩家坐标（`变量[43/44] 主人公Ｘ/Ｙ保存`）后放置 |
| #506 ★☆敵の回収設定☆★ | 把离玩家太远的敌人回收（重定位/隐藏） |
| #495–500 敵N再配置時リセット | 再配置时重置该敌人的状态开关（避免"一出现就是攻击模式"） |
| #507 敵出現確率 | 按随机变量（886/914）决定刷哪一类 |
| #508 残留タイム / #509 敵撃破イベント | 击败后的残留计时与击破事件 |

**结论**：原作 = **固定数量槽位（敵１〜敵６，各带 X/Y/体力 变量）＋ 玩家周围回收/重放**，
敌人种类由随机变量选择。本作的 `FrontSpawner` 取同一思路：用"前方带目标数量 + 屏外定点"
代替"6 个硬槽位"，参数化后更容易按关卡调。

---

## 6. 扩展点

| 想加什么 | 改哪里 |
|----------|--------|
| 新武器 | 新建 `WeaponData` .tres（含动画序列与 `weapon_state_name`）；`bullet_list` 挂 `BulletData`；角色 `CharacterData.allowed_primary/secondary_weapons` 放行；若需新状态名，在 `object/player.tscn` 的 StateMachine 下加同名 State 节点 |
| 新敌人 | 复制 `object/enemy.tscn` 到关卡或生成点；调 `@export` 属性（HP/速度/攻击框/动画序列/视野）；导演侧把生成区/生成点指向它 |
| 新 UI 界面 | 用 `GradientLabel` 渲染文字，参数从 `Global` 取默认值；联机数据只读，不写权威事实 |
| 新图块 | 用 `tileset_generator.gd` 生成 TileSet，再在 `map_editor.gd` 中编辑；参考 `TileSet工作流完整指南.txt` |
| 新剧本事件 | 实现/复用 `EventTrigger` + `EventManager.start_scripted_event(config)` |
| 调整生成节奏 | 场景内放 `DirectorConfig` 节点做关卡级覆盖，参数见 `导演系统参数参考.md` |

---

## 7. 已知陷阱与项目约定

1. **`Object.get()` 只有 1 个参数且无默认值**。读 `Resource` 导出字段请用成员访问（`w.attack_effect_follow`），
   只有 `Dictionary.get(key, default)` 才接受默认值。把 `WeaponData` 当字典用会直接抛错。
2. **`StringName` 不能隐式转 `NodePath`**（`String` 可以）。传给 `get_node_or_null()` 的常量请声明为 `String`。
3. **同名 .tres 字段重复赋值会静默覆盖**：手工给 `.tres` 插字段前先 `grep` 是否已有同名项；
   `ext_resource` 必须写在 `[gd_resource]` 之后；新增属性要插在 `script = ExtResource(...)` 行**之后**，
   否则属性会被赋给未绑定脚本的裸 Resource 而静默失效。
4. **`_draw()` 里的手绘血条是调试可视化**（`Global.debug_visuals` 为真时随碰撞体绘制），
   正式 HUD 使用 `art/Ui/` 素材，二者互不相干。
5. **联机权威边界不可越**：Client 不得在本地 Player 上直接结算伤害 / 弹药 / 拾取 / 复活。
6. **跨帧缓存 `Object` 引用**必须在消费端用 `is_instance_valid()` 过滤——GDScript 中 freed 引用 `== null` 为 `false`。
7. **文档滞后**：仓库内文档 20+ 份且历史不同步，引用任何文档结论前先 `grep` 代码核实。
8. **动态单位不要写成寻路硬障碍**：`AStarGrid2D` 用 4 邻域，把同伴 `set_point_solid(true)` 会让
   被围住的玩家格彻底不可达。动态单位一律用 `set_point_weight_scale` 的**软代价**，
   并豁免目标附近 —— 见 §5.4。
9. **路径可以做视线简化，但视线判定必须按碰撞包围盒跨格算**：只查线段经过的格子中心，
   会把碰撞体「擦墙过」误判为通视，简化后立刻卡墙。
10. **`test_move` 的「侧移 + 位移」不能合成一次斜向扫掠**：角色贴平墙面时该扫掠路径必然切过
   墙角，`test_move` 恒为真 → 蹭墙辅助永远求不出解。必须拆成「纯侧移安全」+「侧移到位后
   再测位移」两步；且修正轴要按**被挡的是哪一轴**取另一轴，不能取移动方向的垂线。见 §5.3。
11. **判定「是否对齐」要在穿越的那一刻取，不能用终点位置**：角色穿过门洞后仍会按输入继续
   横向漂移，用终点 x 会误判修正效果。
12. **`test_move` 遵守 `collision_mask`**，可用来把探测限定在静态世界层。玩家的掩码含敌人层，
   而导演持续在身边刷怪 —— 任何"探测前方是否有墙"的逻辑都必须先收窄掩码，否则会被路过的
   丧尸判为阻挡（蹭墙"时不时失效"的真因）。
13. **蹭墙侧移的上限不要调大**：上限越大，辅助越容易为了满足一个很小的横向位移而把角色整体
   挪很远（实测上限 20 时出现"为让 1px/帧 的横移通过，竖向挪 20px"）。半格 16px 足够。
14. **把航点推开 / 推离墙面的位移必须校验可通行性**：推完不校验就会把航点推进墙里，
   表现是「路径调试线直接穿过不可通行区域」以及「敌人顶着墙不动、拐不了弯」。
   （三次修订后**推墙已整体删除**，见陷阱 21；此处保留教训：任何移动航点的步骤都必须校验。）
15. **图层角色是按「节点名子串」判定的，而 Godot 会自动改名**：同名冲突时 `add_child()`
   可能给出 `@TileMapLayer@N`，角色判定立刻失效（实测：GroundLayer 被改成
   `@TileMapLayer@7` 后整张地图都判成"没有地面"）。运行时动态造图层要用
   `add_child(node, true)` 强制可读名，或换一个不冲突的名字。
16. **分帧构建要连「行内列游标」一起存**：只存行号的话，本帧用完配额 return 后下一帧会从
   该行行首重扫 —— 已处理的格子被重复计数，日志出现「障碍物数 > 总格数」这种不可能的
   百分比（曾达 102.4%），还白白多花一倍构建时间。
17. **Godot 图块碰撞多边形以「格心」为原点**（整格矩形序列化成 `-16..16`，可在
   `tres/*_tileset.tres` 里看到）。按「左上角为原点」解释会让碰撞几何整体偏半格：墙格
   自身仍判为墙，但格内会剩下一个可以站的假角落，紧邻墙的格子也会被误判 ——
   实测多认了 1469 格可行走，敌人于是往墙角里挤。取 AABB 时记得 `+ cell_size/2`。
18. **多个碰撞盒不能只取并集 AABB 去推「必须让开多少」**：并集比实际阻挡大，可能把让开量
   高估到格内范围之外，于是把本可站下的格子判死（实测少认约 20% 可行走格）。
   正确做法是解析解失败就**回落环搜索兜底**，绝不早退判死。
19. **热路径里别做字符串操作，也别 print**：`tm.name.to_lower()` + 子串搜索在整图 6.5 万格
   下是 60 万次临时字符串分配；headless 下 10 行 `print` 实测占掉 140 ms。
   诊断输出一律挂 `Global.debug_visuals`。
20. **分帧构建要带时间预算**：只按「每帧多少格」限流，遇到格子更贵的地图（碰撞体多）就会
   掉帧。`BUILD_CHUNK`（数量上限）+ `BUILD_TIME_BUDGET_USEC`（时间预算）双约束更稳。
21. **本工程的图块碰撞多边形全是整格矩形**（`tres/*_tileset.tres` 里一律
   `(-16,-16, 16,-16, 16,16, -16,16)`），`physics_layer_0/collision_layer = 1`。推论两条：
   - 「某格有碰撞图块」等价于「整格不可通过」，「格心」就是格内离墙最远的位置 ——
     **任何"把航点从格心推开以免贴墙"的逻辑都是反的**（实测把一格宽通道里的航点横推 14px，
     路径线因此穿墙、敌人贴墙振荡）；
   - 「按格近似」（包围盒覆盖的格子是否都可走）与「真实碰撞盒求交」在本地图上等价，
     但一旦将来出现半格碰撞，只有后者是对的。现在两层的**与**都做（`_body_fits_at`）。
22. **卡住判定不能用单帧位移**：敌人顶墙时会被沿墙滑行带着来回蹭，每帧位移恒为
   `move_speed/60`（实测 2.67px），永远大于"没动"的阈值 —— 现场报告里于是出现
   「stuck=0 却有敌人 220 帧原地不动」。改用「到当前航点的距离有没有变小」即可识别振荡。
   实现时注意：那个"已到达过的最近距离"标尺**不能每帧跟着当前距离走**，
   否则慢速接近（被群挤着 0.3px/帧）会被误判成卡住（已写成单元用例兜住）。
23. **沿墙滑行不要重新归一化**：`motion.slide(n).normalized() * speed * delta` 会把
   极小的切向分量放大回满速 —— 正撞墙面时本应≈0 的横向位移被放大成每帧 2.67px，
   于是"贴墙来回磨"且"位移永远不为 0"（连卡住检测都被骗过）。保持 slide 后的真实长度，
   顶死时位移≈0，蹭墙侧移才会接手（`move_and_slide()` 也是这么做的）。
24. **只有"配置"没有"回收"的刷怪系统必然越玩越空**：被甩在身后的敌人永久存活，
   吃满 `max_active_common` 后所有生成路径一起断供（实测 29 秒顶满），而且远处敌人
   还在跑 A*。原作 #505（配置）与 #506（回收）是成对的 —— 移植时必须一起搬。
25. **Host 直接 `queue_free` 联机实体会让 Client 留幽灵**：Client 的表现实体只能由
   Host 的可靠 RPC 移除（`NetworkWorld.despawn_enemy_networkwide` → 广播
   `despawn_network_enemy`）。另外 NetworkWorld 挂在**当前场景**下，别按 autoload 路径找。

---

## 8. 验证命令

```bash
GD="D:/Godot_v4.6.3-stable_win64.exe/Godot_v4.6.3-stable_win64_console.exe"

# 1) 全项目脚本解析 + 主场景冒烟（应 EXIT 0，仅有 headless 退出时的资源泄漏噪声）
"$GD" --headless --path "D:/!bird's-eye-view-arpg-test-/l3d-re-godot" --quit

# 2) 前方屏外定点刷怪专项验证（14 checks）
"$GD" --headless --path "D:/!bird's-eye-view-arpg-test-/l3d-re-godot" res://tools/front_spawner_test.tscn

# 3) 拐角平滑 / 蹭墙专项验证（23 checks，含敌人）
"$GD" --headless --path "D:/!bird's-eye-view-arpg-test-/l3d-re-godot" res://tools/corner_assist_test.tscn

# 4) 敌人寻路专项验证（43 checks：含真实 TileSet 驱动的窄通道 / 全格碰撞通道 / 卡住判定）
"$GD" --headless --path "D:/!bird's-eye-view-arpg-test-/l3d-re-godot" res://tools/pathfinding_test.tscn
```

---

## 附：文件 → 系统索引

`grep -rn "## 系统：" script/` 可获得全部 95 条映射。以下为按系统的聚合清单：

| 系统 | 文件 |
|------|------|
| 状态机框架 | `State.gd`、`StateMachine.gd` |
| 全局配置 / 存档 | `global.gd`、`save_manager.gd`、`chapter_stats.gd`、`game_init.gd`、`item_codec.gd` |
| 玩家 | `player.gd`、`player/Player*.gd`（11）、`player_state.gd`、`player_registry.gd`、`character_switch_manager.gd`、`teammate_standin.gd` |
| 移动手感 | `corner_assist.gd`（蹭墙 / 拐角平滑，玩家与敌人共用） |
| 敌人投放 | `director/front_spawner.gd`（前方屏外定点刷怪，见 §5.7） |
| 敌人 | `enemy.gd`、`enemy/Enemy*State.gd`（8） |
| 战斗 / 武器 | `bullet.gd`、`bullet_data.gd`、`weapon_data.gd`、`weapon_pickup.gd`、`weapon_effect_offsets.gd`、`damage_number.gd` |
| 道具 / 投掷 | `item_data.gd`、`throwable_data.gd`、`throwable_projectile.gd`、`throw_aim_indicator.gd`、`healing_pickup.gd`、`fire_patch.gd`、`hold_indicator.gd` |
| 角色数据 | `character_data.gd`、`character_catalog.gd`、`skill_data.gd` |
| 导演 | `director/*.gd`（16） |
| 导航 | `enemy/EnemyChaseState.gd`（A* 寻路 + 同伴软代价 + 视线简化） |
| 关卡流程 | `director/safe_door.gd`、`director/teleport_point.gd`、`director/arrival_*.gd`、`campaign_data.gd`、`safehouse_arrival_music.gd` |
| 表现 / UI | `ui/*.gd`、`gradient_label.gd`、`text_gradient_renderer.gd`、`title_screen.gd`、`character_select*.gd`、`campaign_select.gd`、`difficulty_select.gd`、`menu_controller.gd`、`vx_anim_sprite.gd`、`vx_anim_cell_data.gd`、`camera_follow.gd`、`player_nameplate.gd` |
| 联机 | `network_manager.gd`、`network_world.gd`、`network_lobby.gd`、`network_snapshot_interp.gd`、`network_revive_indicator.gd` |
| 工具 / 编辑器 | `tileset_generator.gd`、`map_editor.gd`、`map_controller.gd`、`map_test_viewer.gd`、`palette_drawer.gd`、`vx_tilemap_test.gd`、`map_restore/*.gd` |
