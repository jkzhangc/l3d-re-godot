# 联机同步分析 —— 单人功能差距清单（v2，2026-09-18 二次核对重写）

> 2026-09-17 首版编制，2026-09-18 对照代码逐项二次核对后重写。
> 目的：盘点 M5「第一章贯通」期间在**单人侧**落地的功能，逐项对照联机架构
> （`联机系统架构设计.md` v2.2：Host 全量模拟 + Client 请求制 + 快照插值），
> 列出联机模式需要补的同步点。**本文档只做分析，不含实现。**
>
> v2 变化：①B1~B3/A1 已落地，移入「已完成」；②新增漏项 A5（丧尸变体行走图，
> 用户实测反馈）、A6（导演 BGM 不同步）；③「已有通道，不需工作」白名单补全
> （爆破墙/火海/闪光/ED）；④D1 收窄为实机验证清单。

---

## 0. 架构基线（开工前重读的铁律）

1. **Host 全量模拟**：玩家位置/HP/弹药、敌人 AI、子弹、伤害、掉落、事件，全部 Host 唯一提交；
   Client 只发 `submit_input / fire_request / pickup_request` 等**请求**，绝无本地判定入口。
2. **快照节拍**：玩家 60Hz / 敌人+玩家表现合并 40Hz（unreliable_ordered）；结构变化
   （spawn/despawn/掉落/流程）走 reliable RPC；每 2s 可靠世界重同步**软并流**进插值缓冲。
3. **敌人 Client 表现** = `apply_network_presentation(pos, facing, moving, hp,
   visual_char_index, is_dead, is_headshot, snap, element_state)`。
   **visual_char_index 是敌人动作帧的唯一同步通道** —— 任何新状态只要最终落到
   `set_attack_char_index`，Client 就能看到帧（但音效/事件不会自动跟过去）。
4. **资源解析走 Host 固定白名单**：`network_world.gd` 的 `NETWORK_WEAPONS`（15 把）、
   `NETWORK_THROWABLES`（grenade/molotov/flash）、`NETWORK_SPECIALS`（7 特感，
   2026-09-18 A1 新增）。Client RPC 永远只传 id。

---

## 1. 已完成（首版清单 → 落地记录）

| # | 缺口 | 落地方式 | commit |
|---|---|---|---|
| B1 | 难度选择未同步 | `start_game` RPC 第 4 参广播 `Global.selected_difficulty`，Client 写本地 Global（network_manager.gd `DIFFICULTY_SYNC`） | 6ea4ea59 前后 P0 包 |
| B2 | DevEnemySpawner 联机禁用 | `_physics_process` 首行 `_is_network_client_session()` 早退 | 同上 |
| B3 | Client 敌人元素染色 | 快照第 9 位 3bit（炎/氷/雷），`apply_network_element_tint` 氷>雷>炎，state=0 还原白 | 同上 |
| A1 | 特感 spawn/despawn 未复制 | **未新增 spawn RPC**：特感本就经 `_register_untracked_host_enemies` 收编；敌人公开快照加 `special_id`（读 `enemy.special_data`），Client 在 `_ensure_client_enemy` 建节点时按 `NETWORK_SPECIALS` 白名单取 tres 调 `SpecialEnemyData.apply_to_enemy()` 注入（Host 生成与 Client 重建共用同一份注入代码，从 spawn_special_enemy 抽出）。**Tank/BossEncounter 生成同走此路 → 一并覆盖**。despawn/死亡通道复用。专项 harness `special_net_check` 26/26 | 242fa4f5 |
| A5 | 丧尸变体行走图/狂暴换皮 Client 不同步 | `NETWORK_VARIANTS` 白名单（12 变体）；`_public_enemy_state` 加 `variant_id`（读 `enemy.variant_data`），Client `_ensure_client_enemy` 建节点时按 id 取 tres 调 `ZombieVariant.apply_to_enemy()` 注入（与 special_id 互斥，特感优先分支 else 变体）；尸潮狂暴换皮走快照 `element_state` bit3（与炎/氷/雷共用 1 字节，向后兼容），Client `apply_network_element_tint` 首行 `set_rage(state & 8)`（幂等，无狂暴图自动 no-op）。compact 快照不带 variant_id（`_normalize_enemy_snapshot` 剥掉）。专项 harness `variant_net_check` 40/40 | aff3cb15 |
| A2 | 敌弹（酸弹）Client 不还原 | 投掷物同款非权威镜像：`EnemyAcidSpit` 加 `_authoritative` 闸（默认 true 单机零改动）+ `spawn_mirror()` 静态构造（**⚠ 自场景必须运行时 `load()`，循环 preload 会令所有引用 EnemyAcidSpit 的脚本编译期挂死**）；`EnemySpitState._fire_acid` 出酸瞬间经 `NetworkWorld.announce_enemy_acid_spit` 广播（Host 闸+未收编丢弃防御）；RPC `enemy_acid_spit_presentation(entity_id, spawn_pos, dir)` reliable——速度/特效/音效由 Client 从本地 enemy 节点字段解析（A1 注入保证有值），零资源传输。镜像弹不结算伤害、不参与相消，撞墙/寿命/穿身播特效音效即焚。专项 harness `acid_spit_net_check` 20/20 | 本轮提交 |

---

## 2. 待做差距清单（按建议开工顺序）

### P1 —— 核心表现同步

| # | 缺口 | 方案建议 | 预估 |
|---|---|---|---|
| A3 | **敌人动作音效 Client 侧缺失** | Client 敌人不跑状态机 → 攻击/发现/受击/吐酸音全无声（死亡音已有专门广播 `enemy_death_presentation`；吐酸音已随 A2 镜像播放）。推荐**事件化**：新增轻量事件 RPC `enemy_sfx(entity_id, sfx_id, pitch)`，Host 在 `_play_sound` 处若为联机会话则转发；Client 按小音效表本地播。比逐状态复刻状态机便宜得多 | 4h |
| A4 | 特感 Client 预警表现（吐酸蓄力/丸呑み张嘴只有帧无事件） | 随 A3 的 sfx 事件自然解决大半 | 随 A3 |
| A6 | **导演 BGM Client 全部无声**（2026-09-18 二次核对新发现） | `Director._process` 在 Client 整体 return（联机 Host 权威）→ 尸潮 BGM（紧张度驱动 `_play_horde_music`）、Boss BGM（`play_boss_music`）在 Client 从不播放。防守战专属 BGM 同理：`holdout_state_sync` 只同步了 HUD 倒计时，`apply_remote_holdout_state` 不起 BGM。方案：Director 信号已齐全（`horde_started/horde_ended/tank_spawned/special_spawned/boss_music_changed`）→ 转发成 reliable 事件 RPC，Client 本地起停对应 BGM；防守战 BGM 随 holdout phase 变化起停 | ~2h |

### P2 —— 玩家侧新机制接线

| # | 缺口 | 方案建议 | 预估 |
|---|---|---|---|
| C1 | **覚醒（集中射撃）未接请求协议** | network_world 无 awaken 条目。照「换弹/推击」同款：Client 上报 `awaken_request`，Host 校验 TP→结算（覚醒期间射击的 instant_kill 已在 bullet Host 路径 ✓），覚醒染色 modulate 并入玩家表现快照 1 bit | 3h |
| C2 | **SA / 见切 / 反击** | 同上确认+接线（反击的 4.0 倍推击判定 Host 已有函数，接请求即可） | 3~4h |
| C3 | **丸呑み的玩家隐藏表现** | Host 侧吞入期间玩家节点隐藏/锁定，Client 的**远端玩家**只是位置快照——被吞的远端玩家照常显示移动。方案：敌人快照捎带 swallow_victim 或吞入事件广播玩家 id，Client 隐藏对应玩家 | 1~2h |

### P3 —— 验证与一致性（贯穿）

| # | 项 | 说明 |
|---|---|---|
| D1 | HoldoutMachine Client 实机验证 | HUD 倒计时同步链路已有（`holdout_state_sync` phase/remaining/token + 结束哨兵防幽灵 HUD）；**待实机过一遍**：①Client 倒计时与 Host 差 ≤0.5s；②结束传送点开闭；③BGM 缺口即 A6 |
| D2 | 联机回归用例扩充 | 补：①含特感图矿洞 spawn→Client 可见+外观正确（A1 已具备条件）；②酸弹 Client 视觉（A2 后）；③难度一致性断言（B1 后）；④丧尸变体外观（A5 后） |
| D3 | 压测 | 3~4 人 + 尸潮 40 + 特感 3 同屏 40Hz 快照带宽复测（特感 attack_texture 多表不影响带宽，快照只发帧号） |

---

## 3. 明确「不需要工作」的项（已核对代码，避免重复分析）

- **近战去重修复（next_attack_source_id）**：伤害 Host 单端结算，去重只作用于 Host 本地，双端一致。
- **即死耐性 / 正面抗性 / Normalize / 元素倍率**：纯 Host 数值分支。
- **新武器 7 把 / 闪光弹**：`NETWORK_WEAPONS`/`NETWORK_THROWABLES` 已收录；数值改动 Host 解析自动生效。
- **特感数据结构扩字段（spit/impact/即死耐性等）**：全部 Host 注入，Client 表现重建只要 tres 就够（A1 的 `apply_to_enemy` 已打通）。
- **投掷物 / 火海（FirePatch）/ 闪光弹**：Client 走非权威镜像（`throwable_presentation` → `ThrowableProjectile.spawn(authoritative=false)` → `FirePatch.spawn(authoritative=false)`），Host 结算伤害，双端表现齐全。**已核对，无需工作**。
- **爆破墙**：爆炸/放置 Host 结算，flag 广播 + Client 启动静默移除/引线纯表现（blast_wall.gd）。**已核对，无需工作**。
- **章节总结 / ED 名单**：`campaign_ending` Host 统一拉全部客户端进名单。**已有通道**。
- **安全门 / 掉落物拾取 / 换弹 / 推击 / 投掷物瞄准**：v2.2 既有请求-权威通道，无缺口。
- **试验场特感配置字段**：Inspector 配置只在 Host 读取。
- **UI 字体/伤害数字/提示文字/HP 条**：纯本地表现。
- **酸弹×玩家子弹相消**：物理层交互在 Host 世界，Client 视觉弹按 A2 直飞即可（Host 已相消而 Client 弹多飞 1~2 帧可接受）。
- **疾走体（runner）**：用户裁定不挂 zombie_pool、保持零引用 → 联机永远不刷，无需同步。

---

## 4. 开工顺序（v2，2026-09-18 用户裁定）

1. ~~B1+B2+B3~~ ✅ 已清（2026-09-17）
2. ~~A1 特感复制~~ ✅ 已落地（242fa4f5，2026-09-18）
3. ~~A5 变体行走图~~ ✅ 已落地（aff3cb15）→ ~~A2 酸弹视觉~~ ✅ 已落地（2026-09-18）→ **A3 音效事件化**（4h，含 A4）→ **A6 导演 BGM**（~2h，与 A3 同属事件转发模式）
4. **C1 覚醒 + C2 SA/见切/反击**（✅ 用户裁定本期必须）→ **C3 丸呑み表现**（随 C 组一起）
5. **D1~D3** 实机验证与回归扩充

> 用户裁定记录（2026-09-18）：①顺序 A5→A2→A3→A6；②C1/C2 本期必须；③A6 纳入本期；④C3 随 C 组。

## 5. 开工验收清单（每项过一遍才算同步完成）

- [x] Client 看得到全部 7 种特感（含 Tank/Boss）spawn，外观正确（A1；音效待 A3）
- [x] Client 看得到丧尸变体差异行走图与尸潮狂暴换皮（A5）
- [x] Client 看得到酸弹飞行与命中特效、听到吐酸音（A2 已落地 1c9b4987；其余敌人动作音效仍待 A3）
- [ ] Client 能听到尸潮/Boss/防守战 BGM（A6）
- [ ] Host 侧集中射撃对ブレインディモス ×1.5 不死，Client 血条表现一致（数值侧已在 A1 harness 覆盖）
- [ ] 女巫/丧尸连续攻击每击都结算（source_id 唯一化，Host 单端天然成立）
- [x] 两端难度一致（B1；`DIFFICULTY_SYNC` 日志可断言）
- [ ] 覚醒期间射击命中同一 tick 结算且 Client 染色表现正确（C1）
- [ ] 防守战 Client 倒计时与 Host 相差 ≤0.5s、传送点开闭、BGM 一致（D1+A6）
- [ ] 无头回归套件在含特感图（矿洞）全绿（D2）

## 6. 已知妥协（记录在案，非缺口）

- 敌人 Client 表现走帧快照：吐酸/突进/丸呑み的**起手预告**依赖 A3 事件化，纯帧同步有 1~2 帧滞后。
- 酸弹 Client 视觉弹与 Host 权威弹道存在插值误差，命中以 Host 为准（与子弹现状同策略）。
- A1 特感 / A5 变体 / A2 酸弹的 Client 重建只注入 tres 数据；Client 不跑 AI，注入的行为字段（starts_in_chase 等）是死数据（`should_start_in_chase` 对 presentation_only 硬 false）。变体/特感音效（攻击声等）注入不等于播放——Client 音效事件仍待 A3（吐酸音已随 A2 镜像单独广播）。
- 3~4 人压力测试未做（v2.2 文档「尚未承诺」清单继承）。
