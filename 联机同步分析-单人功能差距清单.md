# 联机同步分析 —— 单人功能差距清单（v2，2026-09-18 二次核对重写）

> 2026-09-17 首版编制，2026-09-18 对照代码逐项二次核对后重写。
> 目的：盘点 M5「第一章贯通」期间在**单人侧**落地的功能，逐项对照联机架构
> （`联机系统架构设计.md` v2.2：Host 全量模拟 + Client 请求制 + 快照插值），
> 列出联机模式需要补的同步点。**本文档只做分析，不含实现。**
>
> v2 变化：①B1~B3/A1 已落地，移入「已完成」；②新增漏项 A5（丧尸变体行走图，
> 用户实测反馈）、A6（导演 BGM 不同步）；③「已有通道，不需工作」白名单补全
> （爆破墙/火海/闪光/ED）；④D1 收窄为实机验证清单。

---

## 0. 架构基线（开工前重读的铁律）

1. **Host 全量模拟**：玩家位置/HP/弹药、敌人 AI、子弹、伤害、掉落、事件，全部 Host 唯一提交；
   Client 只发 `submit_input / fire_request / pickup_request` 等**请求**，绝无本地判定入口。
2. **快照节拍**：玩家 60Hz / 敌人+玩家表现合并 40Hz（unreliable_ordered）；结构变化
   （spawn/despawn/掉落/流程）走 reliable RPC；每 2s 可靠世界重同步**软并流**进插值缓冲。
3. **敌人 Client 表现** = `apply_network_presentation(pos, facing, moving, hp,
   visual_char_index, is_dead, is_headshot, snap, element_state)`。
   **visual_char_index 是敌人动作帧的唯一同步通道** —— 任何新状态只要最终落到
   `set_attack_char_index`，Client 就能看到帧（但音效/事件不会自动跟过去）。
4. **资源解析走 Host 固定白名单**：`network_world.gd` 的 `NETWORK_WEAPONS`（15 把）、
   `NETWORK_THROWABLES`（grenade/molotov/flash）、`NETWORK_SPECIALS`（7 特感，
   2026-09-18 A1 新增）。Client RPC 永远只传 id。

---

## 1. 已完成（首版清单 → 落地记录）

| # | 缺口 | 落地方式 | commit |
|---|---|---|---|
| B1 | 难度选择未同步 | `start_game` RPC 第 4 参广播 `Global.selected_difficulty`，Client 写本地 Global（network_manager.gd `DIFFICULTY_SYNC`） | 6ea4ea59 前后 P0 包 |
| B2 | DevEnemySpawner 联机禁用 | `_physics_process` 首行 `_is_network_client_session()` 早退 | 同上 |
| B3 | Client 敌人元素染色 | 快照第 9 位 3bit（炎/氷/雷），`apply_network_element_tint` 氷>雷>炎，state=0 还原白 | 同上 |
| A1 | 特感 spawn/despawn 未复制 | **未新增 spawn RPC**：特感本就经 `_register_untracked_host_enemies` 收编；敌人公开快照加 `special_id`（读 `enemy.special_data`），Client 在 `_ensure_client_enemy` 建节点时按 `NETWORK_SPECIALS` 白名单取 tres 调 `SpecialEnemyData.apply_to_enemy()` 注入（Host 生成与 Client 重建共用同一份注入代码，从 spawn_special_enemy 抽出）。**Tank/BossEncounter 生成同走此路 → 一并覆盖**。despawn/死亡通道复用。专项 harness `special_net_check` 26/26 | 242fa4f5 |
| A5 | 丧尸变体行走图/狂暴换皮 Client 不同步 | `NETWORK_VARIANTS` 白名单（12 变体）；`_public_enemy_state` 加 `variant_id`（读 `enemy.variant_data`），Client `_ensure_client_enemy` 建节点时按 id 取 tres 调 `ZombieVariant.apply_to_enemy()` 注入（与 special_id 互斥，特感优先分支 else 变体）；尸潮狂暴换皮走快照 `element_state` bit3（与炎/氷/雷共用 1 字节，向后兼容），Client `apply_network_element_tint` 首行 `set_rage(state & 8)`（幂等，无狂暴图自动 no-op）。compact 快照不带 variant_id（`_normalize_enemy_snapshot` 剥掉）。专项 harness `variant_net_check` 40/40 | aff3cb15 |
| A2 | 敌弹（酸弹）Client 不还原 | 投掷物同款非权威镜像：`EnemyAcidSpit` 加 `_authoritative` 闸（默认 true 单机零改动）+ `spawn_mirror()` 静态构造（**⚠ 自场景必须运行时 `load()`，循环 preload 会令所有引用 EnemyAcidSpit 的脚本编译期挂死**）；`EnemySpitState._fire_acid` 出酸瞬间经 `NetworkWorld.announce_enemy_acid_spit` 广播（Host 闸+未收编丢弃防御）；RPC `enemy_acid_spit_presentation(entity_id, spawn_pos, dir)` reliable——速度/特效/音效由 Client 从本地 enemy 节点字段解析（A1 注入保证有值），零资源传输。镜像弹不结算伤害、不参与相消，撞墙/寿命/穿身播特效音效即焚。专项 harness `acid_spit_net_check` 20/20 | 1c9b4987 |
| A3 | 敌人动作音效 Client 侧缺失 | **反查转发表**方案：`enemy.gd` 定义 `NETWORK_SFX_FIELDS`（sfx key→enemy 字段名，仅收 Client 无通道的 attack/discover/rage_discover/witch_scream/frontal_block；hurt=受击表现 RPC、death 系=死亡表现 RPC、spit=A2 镜像，均不进表避免双播），`_play_sound` 末尾挂 `_announce_network_sfx`（`Net.is_host`+`network_entity_id>0` 双闸，单机零开销；流/音调从本地 enemy 字段反查，零资源传输）；`network_world.gd` `announce_enemy_sfx` → `enemy_sfx_presentation(entity_id, sfx_key, pitch)` reliable RPC → Client `enemy.play_network_sfx` 直读字段播。rage 动态回退两端同字段语义天然兼容。实机回归日志中 `CLIENT_ENEMY_SFX discover/attack` 正常触发。专项 harness `sfx_net_check` 24/24 | 467c5aeb |
| A6 | 导演 BGM Client 全部无声（尸潮/Boss/防守战） | 双通道：①尸潮/Boss BGM——Director 在**真正起停处**（_play/_stop_horde_music、play/stop_boss_music）挂 `_announce_music(key, active)` → `announce_director_music`（Host 闸）→ `director_music_presentation` reliable RPC → Client `Director.apply_network_music` **复用既有起停函数**：音源/音量本地 current_config 解析（场景切换钩子对所有 peer 生效 → Client 也有值，零资源传输），`_boss_music_active` 与 `boss_music_changed` 信号一并维护（Client HoldoutMachine 的 stream_paused 挂起联动白嫖），reliable 保序保证 boss=false→horde=true 接回序列与 Host 同构；②防守战 BGM——`apply_remote_holdout_state` 挂 `_drive_remote_holdout_music(phase)`：ACTIVE 起播（playing 幂等闸防 500ms 周期包重头）、IDLE/SETTLE 停播，**复用 holdout_state_sync 通道零新 RPC**，中途加入补发自动起播。单机零行为变化（+87 全新增）。专项 harness `director_music_check` 34/34 | f94a067b |
| C1 | 覚醒（集中射撃）未接请求协议 | 请求-表现协议三件套：Client `_capture_awaken_input`（構え中 just_pressed；本地预校验 is_weapon_mode_active 减无效请求）→ `awaken_request`（any_peer reliable）→ Host `_try_host_awaken`（校验存活/is_weapon_mode_active/awaken_type/TP 权威值 → 调 `_activate_awaken_core` 结算）→ `awaken_presentation`（authority reliable）→ 双端 `apply_network_awaken_state` 染色。**TP 双域独立扣费**：`_process` network_controlled 分支挂 `_update_awaken`（与 _update_tp_regen 同款模式），各实体经 `get_state_for_entity` 解析各自域 PlayerState（Host 权威 / Client 本地显示），同速同规则 LAN 漂移 <1s；解除统一出口 `_deactivate_awaken` 挂 `announce_player_awaken` 广播 + `_awaken_active_peers` 白名单 + `_accept_ready_peer` 中途加入补发。**顺手补缺**：`_spawn_host_bullet` setup 原漏 `instant_kill` 键（原清单称「已在 bullet Host 路径 ✓」不准确）——补 `shooter.is_awaken_active() or bd.instant_kill`，覚醒权威弹即死・怯み与单机 PlayerPistolAttackState 同款。player 侧 try_activate_awaken 拆出 `_activate_awaken_core`（无 Input 入口，Host 读不到 Client 键盘）。单机零行为变化（+145 全新增）。专项 harness `awaken_net_check` 26/26 | 4756cb1e |
| C2 | **SA / 见切 / 反击** Client 不可用、无表现 | 请求-表现协议（C1 同款扩展，3 请求 RPC + 5 表现 RPC）：①技能——Client `_capture_sa_input`（SA键 just_pressed；搓招经 `validate_skill_motion` 本地预校验，缓冲由 `poll_network_motion_input` 每帧代驱）→ `sa_skill_request(trigger)` → Host `_try_host_sa_skill`（存活校验 → `node._use_skill_core(trigger, true)`：搓招信任 Client 预校验、TP 走权威 PlayerState）→ `_execute_skill_effect` 尾部 announce → `sa_presentation(peer, trigger)` → Client `apply_network_sa_skill` 按本地同名技能解析（sa_sound / 蹲下染色计时 / 感覚向上计时；RECITAL 踉跄由 Host 结算经快照体现）；②见切——`mukiri_request()` → Host `node._try_mukiri_input()` 零拆分直调（窗口/间隔登记在权威实体，`_should_negate_hit` 受击路径天然权威）→ 成功分支 announce → `mukiri_presentation` 播 `_play_mukiri_anim`；③反击——`_try_counter` 触发即 announce → `counter_presentation` 播 counter_sound（伤害/击退本就 Host 结算）；④蹲下维持——`sa_crouch_hold(active)` 边沿上报（含表现晚到 mismatch 补报）→ Host 权威实体 `_network_sa_crouch_hold` 登记延长+扣权威 TP；Client 本地预测实体直读键盘扣显示 TP（双域独立，C1 同精度）；⑤Heat——`_apply_heat` announce → `heat_presentation` 染色+本地计时置位。**新增 `_update_network_sa_state` 挂 network_controlled 分支**：Heat 计时推进（顺带修复联机实体 Heat 永不褪色）、蹲下超时/TP 尽各自结束、Host 结束经 `crouch_end_presentation` 幂等对齐。Host 本机玩家不进 `_capture_sa_input`（_update_sa_state 直读 + use_skill 扣 TP，防双扣）；投掷瞄准时 SA/见切输入随开火一并跳过。player 侧 use_skill 拆 `_use_skill_core(trigger, motion_ok)` 返回 bool（单机行为不变）。单机零行为变化（+~160 全新增）。专项 harness `sa_net_check` 66/66 | 940c8d5e |
| C3 | **丸呑み的玩家隐藏表现**：被吞的 Client 玩家照常显示移动（且 Host 侧 `_simulate_host_players` 直接按提交输入移动所有玩家，`_hide_victim` 的 `set_physics_process(false)` 挡不住——被吞玩家在 **Host 上也能走出 Hunterγ 肚子**） | 吞入/吐出事件广播：`EnemySwallowState._hide_victim/_restore_victim` 挂 `apply_network_swallow_state`（player 新增 `network_swallow_locked` 锁 + 隐藏/碰撞闸，幂等）+ `_announce_swallow` → `announce_enemy_swallow(enemy, victim, active)`（Host 闸；entity_id≤0 或 victim 无 peer 丢弃）→ `swallow_presentation(entity_id, peer_id, active)` reliable → Client 隐藏/恢复对应玩家节点。**三端冻结闸**：①`_simulate_host_players` 显式冻结被吞玩家（velocity/输入归零，位置钉在吞入点）；②Client `inputs_frozen` 纳入吞入态（`_is_local_player_swallow_locked`）——攻击/装填/SA/见切等捕获全停，`submit_input` 归零提交；③`_predict_client_local_movement` 经 inputs_frozen 传参归零。单机零行为变化（+105/−2 全新增）。专项 harness `swallow_net_check` 30/30 | 2c12acd1 |

---

## 2. 待做差距清单（按建议开工顺序）

### P1 —— 核心表现同步

| # | 缺口 | 方案建议 | 预估 |
|---|---|---|---|
| A4 | 特感 Client 预警表现（吐酸蓄力/丸呑み张嘴只有帧无事件） | **已随 A4 动作表事件化落地（e1923bee）**：真根因=独立动作表（攻击/突进/丸呑み）不跨端，Client 快照帧号落在行走表上显示错图——`enemy.gd` `NETWORK_TEXTURE_FIELDS` 反查（attack/swallow；death_texture 走 A1 死亡通道、狂暴换皮走 A5 bit3，不进表防双播），`push_action_texture` 尾部 / `restore_walk_texture` 弹出前挂 `_announce_network_action`（`Net.is_host`+entity_id 双闸，Client 再进 push 由 host 闸防回环）；`network_world.gd` `announce_enemy_action` → `enemy_action_presentation` reliable → Client `apply_network_action_texture` 本地字段解析 push/restore，后续帧号由快照继续驱动，零资源传输。吐酸蓄力只切帧号（EnemySpitState 无动作表）本就帧同步天然覆盖。专项并入 `swallow_net_check` 43/43 | e1923bee |

### P2 —— 玩家侧新机制接线（✅ 全部落地：C1 4756cb1e / C2 940c8d5e / C3 2c12acd1）

| # | 缺口 | 方案建议 | 预估 |
|---|---|---|---|
| （已清空）| — | — | — |

### P3 —— 验证与一致性（贯穿）

| # | 项 | 说明 |
|---|---|---|
| D1 | HoldoutMachine Client 实机验证 | HUD 倒计时同步链路已有（`holdout_state_sync` phase/remaining/token + 结束哨兵防幽灵 HUD）；**待实机过一遍**：①Client 倒计时与 Host 差 ≤0.5s；②结束传送点开闭；③BGM 通道已随 A6 落地（f94a067b），实机复核听感与切换时序 |
| D2 | 联机回归用例扩充 | 补：①含特感图矿洞 spawn→Client 可见+外观正确（A1 已具备条件）；②酸弹 Client 视觉（A2 后）；③难度一致性断言（B1 后）；④丧尸变体外观（A5 后） |
| D3 | 压测 | 3~4 人 + 尸潮 40 + 特感 3 同屏 40Hz 快照带宽复测（特感 attack_texture 多表不影响带宽，快照只发帧号） |

---

## 3. 明确「不需要工作」的项（已核对代码，避免重复分析）

- **近战去重修复（next_attack_source_id）**：伤害 Host 单端结算，去重只作用于 Host 本地，双端一致。
- **即死耐性 / 正面抗性 / Normalize / 元素倍率**：纯 Host 数值分支。
- **新武器 7 把 / 闪光弹**：`NETWORK_WEAPONS`/`NETWORK_THROWABLES` 已收录；数值改动 Host 解析自动生效。
- **特感数据结构扩字段（spit/impact/即死耐性等）**：全部 Host 注入，Client 表现重建只要 tres 就够（A1 的 `apply_to_enemy` 已打通）。
- **投掷物 / 火海（FirePatch）/ 闪光弹**：Client 走非权威镜像（`throwable_presentation` → `ThrowableProjectile.spawn(authoritative=false)` → `FirePatch.spawn(authoritative=false)`），Host 结算伤害，双端表现齐全。**已核对，无需工作**。
- **爆破墙**：爆炸/放置 Host 结算，flag 广播 + Client 启动静默移除/引线纯表现（blast_wall.gd）。**已核对，无需工作**。
- **章节总结 / ED 名单**：`campaign_ending` Host 统一拉全部客户端进名单。**已有通道**。
- **安全门 / 掉落物拾取 / 换弹 / 推击 / 投掷物瞄准**：v2.2 既有请求-权威通道，无缺口。
- **试验场特感配置字段**：Inspector 配置只在 Host 读取。
- **UI 字体/伤害数字/提示文字/HP 条**：纯本地表现。
- **酸弹×玩家子弹相消**：物理层交互在 Host 世界，Client 视觉弹按 A2 直飞即可（Host 已相消而 Client 弹多飞 1~2 帧可接受）。
- **疾走体（runner）**：用户裁定不挂 zombie_pool、保持零引用 → 联机永远不刷，无需同步。

---

## 4. 开工顺序（v2，2026-09-18 用户裁定）

1. ~~B1+B2+B3~~ ✅ 已清（2026-09-17）
2. ~~A1 特感复制~~ ✅ 已落地（242fa4f5，2026-09-18）
3. ~~A5 变体行走图~~ ✅ 已落地（aff3cb15）→ ~~A2 酸弹视觉~~ ✅ 已落地（1c9b4987）→ ~~A3 音效事件化~~ ✅ 已落地（467c5aeb）→ ~~A6 导演 BGM~~ ✅ 已落地（f94a067b，双通道：事件 RPC + holdout phase 驱动）
4. ~~C1 覚醒~~ ✅（4756cb1e）→ ~~C2 SA/见切/反击~~ ✅（940c8d5e）→ ~~C3 丸呑み表现~~ ✅（2c12acd1）——**C 组全部关闭**；~~A4 动作表事件化~~ ✅（e1923bee）——**A 组全部关闭**
5. **D1~D3** 实机验证与回归扩充

> 用户裁定记录（2026-09-18）：①顺序 A5→A2→A3→A6；②C1/C2 本期必须；③A6 纳入本期；④C3 随 C 组。

## 5. 开工验收清单（每项过一遍才算同步完成）

- [x] Client 看得到全部 7 种特感（含 Tank/Boss）spawn，外观正确（A1；音效已随 A3 落地 467c5aeb）
- [x] Client 看得到丧尸变体差异行走图与尸潮狂暴换皮（A5）
- [x] Client 看得到酸弹飞行与命中特效、听到吐酸音（A2 已落地 1c9b4987；其余敌人动作音效已随 A3 落地 467c5aeb）
- [x] Client 能听到尸潮/Boss/防守战 BGM（A6 已落地 f94a067b；实机听感待 D1 复核）
- [ ] Host 侧集中射撃对ブレインディモス ×1.5 不死，Client 血条表现一致（数值侧已在 A1 harness 覆盖）
- [ ] 女巫/丧尸连续攻击每击都结算（source_id 唯一化，Host 单端天然成立）
- [x] 两端难度一致（B1；`DIFFICULTY_SYNC` 日志可断言）
- [x] 覚醒期间射击命中同一 tick 结算且 Client 染色表现正确（C1 已落地 4756cb1e：Host 权威弹 instant_kill 标志 + awaken_presentation 染色；实机待 D1 复核）
- [x] Client 能发动 SA（含搓招/TP 权威校验）、蹲下无敌两端一致、见切成功动画与反击音效可感知、Heat 染色可见（C2 已落地；实机待 D1 复核）
- [x] ハンターγ 丸呑み：被吞玩家在两端同步隐藏/冻结在吞入点，吐出/致死表现一致（C3 已落地；实机待 D1 复核）
- [ ] 防守战 Client 倒计时与 Host 相差 ≤0.5s、传送点开闭、BGM 一致（D1+A6）
- [ ] 无头回归套件在含特感图（矿洞）全绿（D2）

## 6. 已知妥协（记录在案，非缺口）

- 敌人 Client 表现走帧快照：动作表（攻击/突进/丸呑み）切换已事件化（A4 e1923bee），起手预告与快照帧驱动间保留 1~2 帧滞后；动作表激活中途中加入的 Client 不补发（表现为行走图直至下一波动作）。
- 酸弹 Client 视觉弹与 Host 权威弹道存在插值误差，命中以 Host 为准（与子弹现状同策略）。
- A1 特感 / A5 变体 / A2 酸弹的 Client 重建只注入 tres 数据；Client 不跑 AI，注入的行为字段（starts_in_chase 等）是死数据（`should_start_in_chase` 对 presentation_only 硬 false）。变体/特感音效（攻击声等）注入不等于播放——Client 音效事件已随 A3 落地（吐酸音走 A2 镜像单独广播）。
- 3~4 人压力测试未做（v2.2 文档「尚未承诺」清单继承）。
- C2 表现按 LAN 精度妥协：见切窗口/蹲下染色经请求往返晚 ~RTT 生效（与武器切换/装填同类）；`_play_hit_feedback` 白闪只在 Host 本地实体，Client 远端玩家无闪白；RECITAL 的敌人踉跄不经事件广播，由敌人位置快照体现；蹲下/感覚向上短时状态不做中途加入补发（量小价值低）。
- C3 表现妥协：吞入表现经吞入帧广播（早于 `_hide_victim` 同帧），Hunterγ 未收编（entity_id≤0）时丢弃广播，被吞玩家在 Client 短暂数帧可见可接受；咀嚼中的敌方吞入动画帧同步归 A4（本项只做玩家隐藏/冻结）；中途加入不补发吞入态（整段约 1.3s，价值低）。
