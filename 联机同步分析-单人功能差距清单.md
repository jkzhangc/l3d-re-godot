# 联机同步分析 —— 单人功能差距清单（开工前必读）

> 2026-09-17 编制。目的：盘点 M5「第一章贯通」期间在**单人侧**落地的功能，逐项对照
> 联机架构（`联机系统架构设计.md` v2.2：Host 全量模拟 + Client 请求制 + 快照插值），
> 列出开工联机模式时需要补的同步点。**本文档只做分析，不含实现。**

---

## 0. 架构基线（开工前重读的铁律）

1. **Host 全量模拟**：玩家位置/HP/弹药、敌人 AI、子弹、伤害、掉落、事件，全部 Host 唯一提交；
   Client 只发 `submit_input / fire_request / pickup_request` 等**请求**，绝无本地判定入口。
2. **快照节拍**：玩家 60Hz / 敌人+玩家表现合并 40Hz（unreliable_ordered）；结构变化
   （spawn/despawn/掉落/流程）走 reliable RPC；每 2s 可靠世界重同步**软并流**进插值缓冲。
3. **敌人 Client 表现** = `apply_network_presentation(pos, facing, moving, hp, visual_char_index, is_dead, is_headshot, snap)`。
   **visual_char_index 是敌人动作帧的唯一同步通道** —— 任何新状态只要最终落到
   `set_attack_char_index`，Client 就能看到帧（但音效/事件不会自动跟过去）。
4. **资源解析走 Host 固定白名单**：`network_world.gd` 的 `NETWORK_WEAPONS`（15 把已全）与
   `NETWORK_THROWABLES`（grenade/molotov/**flash** 已全）。Client RPC 永远只传 id。

---

## 1. 自 2026-08-29 以来单人侧新增/变更盘点

| 功能 | 主要落点 | 联机相关代码标记 |
|---|---|---|
| 特感编排扩充（γ 丸呑み、吐酸ブレインディモス、女巫、Green Soldier、T-002 多段攻击） | director.gd `spawn_special_enemy`、EnemyPounceState/EnemySwallowState/EnemySpitState | spawn 有 Client 禁令；状态帧走 visual_char_index |
| 敌方远程酸弹（投射物+子弹相消+命中特效） | enemy_acid_spit.gd、bullet.gd layer7「玩家弹」 | 酸弹**仅 Host 生成**（EnemySpitState 头注释已声明） |
| 近战 source_id 每击唯一（女巫隔下掉血修复） | enemy.gd `next_attack_source_id()`、AttackState/PounceState/SwallowState/SpitState | Host 单端结算，**天然联机安全** |
| 即死耐性（集中射撃对ブレインディモス无效化） | bullet.gd ×1.5+怯み分支 | 纯 Host 数值，零同步需求 |
| 难度倍率接线（hp/damage/intensity） | global.gd `difficulty_multipliers` | **`selected_difficulty` 未在联机链路出现** |
| HoldoutMachine 防守战（学校 120s/列车台 KILL_COUNT） | holdout_machine.gd | 「仅单机/Host 启动，结果广播」已注释声明 |
| 敌人试验场 | dev_enemy_spawner.gd、scene/maps/测试-敌人试验场.tscn | 纯单机工具 |
| 敌人音效 pitch 体系（发现/攻击/受击/爆头/装填） | enemy.gd `_play_sound(stream, pitch)` 全链补齐 | 音效播放依赖**本地状态机**，Client 敌人不跑状态机 |
| 玩家覚醒（集中射撃） | player.gd `try_activate_awaken`、bullet instant_kill | **network_world.gd 无 awaken/SA/见切 任何条目** |
| 新武器 7 把 + 投掷物闪光弹 | NETWORK_WEAPONS/THROWABLES 白名单 | **已全覆盖，无缺口** |
| 6 图特感池接线 / 试验场接线 | 地图 DirectorConfig special_pool | Host 权威生成，Client 无需配置 |
| 提示文字统一 fusion / 伤害数字 36 / 倒计时 32 | UI 层 | 纯本地表现 |

---

## 2. 差距清单（按开工优先级排序）

### P0 —— 不做就联机报错/数值分裂（小工作量）

| # | 缺口 | 现状 → 目标 | 预估 |
|---|---|---|---|
| B1 | **难度选择未同步** | `Global.selected_difficulty` 只存在于各端本地 → Host 开局时把难度 int 随 game_init/场景 ready 广播，Client 写入本地 Global。Host 侧缩放已就绪（get_final_attack_damage 等均有 `network_presentation_only` 早退） | 0.5h |
| B2 | **DevEnemySpawner 联机禁用** | 试验场是单机工具；Client 侧运行会绕过 Director 的生成禁令私刷敌人 → `_physics_process` 首行加 `_is_network_client_session()` 早退（Director 同款） | 0.2h |
| B3 | ** consciously 梳理 Client 端敌人染色/异常表现**：灼烧/冻结/感电 tint 由 `_update_element_status` 驱动（只跑在 Host 实体上）→ Client 敌人看不出元素状态。方案：把当前元素状态并入敌人快照（1 byte）或随 hurt 事件下发 | 1h |

### P1 —— 核心玩法同步（中工作量）

| # | 缺口 | 方案建议 | 预估 |
|---|---|---|---|
| A1 | **特感 spawn/despawn 未复制** ✅ 已落地（2026-09-18） | 实现与原方案略有出入：特感本就会被 `_register_untracked_host_enemies` 收编并广播，因此**未新增 spawn RPC**——`_public_enemy_state` 加 `special_id`（读 `enemy.special_data`），复用现有 spawn/despawn/死亡通道；`NETWORK_SPECIALS` 白名单（7 特感 id→tres）+ `SpecialEnemyData.apply_to_enemy()`（从 spawn_special_enemy 抽出，Host 生成与 Client 重建共用同一份注入代码）；Client 在 `_ensure_client_enemy` 建节点时按 id 注入。专项 harness `special_net_check` 26/26 | 3h |
| A2 | **敌弹（酸弹）Client 不还原** | 直飞弹道可 Client 本地模拟：reliable 下发 `spawn_pos + dir + speed`，Client 起一个 `network_visual_only` 版酸弹（同层复刻 bullet 的 visual-only 模式）；命中/相消仍全部 Host 判定，Client 弹只做视觉与音效 | 3~4h |
| A3 | **敌人动作音效 Client 侧缺失** | Client 敌人不跑状态机 → 攻击/发现/受击/吐酸音全无声（死亡音已有专门广播）。推荐**事件化**：enemy 快照已带 visual_char_index，新增轻量 reliable/unreliable 事件 RPC `enemy_sfx(entity_id, sfx_id, pitch)`，Host 在 `_play_sound` 处若为联机会话则转发；Client 按 id 查小音效表本地播。比逐状态复刻状态机便宜得多 | 4h |
| A4 | **特感 Client 预警表现**：吐酸蓄力/丸呑み张嘴只有帧（visual_char_index），无音效无事件——玩家没反应窗口提示。随 A3 的 sfx 事件自然解决大半 | 随 A3 | — |

### P2 —— 玩家侧新机制接线（中工作量）

| # | 缺口 | 方案建议 | 预估 |
|---|---|---|---|
| C1 | **覚醒（集中射撃）未接请求协议** | network_world 无 awaken 条目。照「换弹/推击」同款：Client 上报 `awaken_request`，Host 校验 TP→结算（覚醒期间射击的 instant_kill 已在 bullet Host 路径 ✓），覚醒染色 modulate 属本地表现需随状态广播（并入玩家表现快照 1 bit） | 3h |
| C2 | **SA / 见切 / 反击**：同上确认+接线（反击的 4.0 倍推击判定 Host 已有函数，接请求即可） | 3~4h |
| C3 | **丸呑み的玩家隐藏表现**：Host 侧吞入期间玩家节点隐藏/锁定，Client 的**远端玩家**目前只是位置快照——被吞的远端玩家会照常显示移动。方案：敌人快照捎带"swallow_victim_entity"或在吞入事件里广播玩家 id，Client 隐藏对应玩家 | 1~2h |

### P3 —— 验证与一致性（贯穿）

| # | 项 | 说明 |
|---|---|---|
| D1 | HoldoutMachine Client HUD 验证 | 机器「结果广播」已注释声明，但 Client 端倒计时面板/BGM/结束传送点开闭需要实机过一遍（小学校/列车台各一次） |
| D2 | 联机回归用例扩充 | 无头 Host/Client 回归目前覆盖基础战斗——补：特感 spawn→Client 可见、酸弹 Client 视觉、覚醒请求、难度一致性断言 |
| D3 | 压测 | 3~4 人 + 尸潮 40 + 特感 3 同屏的 40Hz 快照带宽复测（特感 attack_texture 多表不影响带宽，快照只发帧号） |

---

## 3. 明确「不需要工作」的项（避免重复分析）

- **近战去重修复（next_attack_source_id）**：伤害 Host 单端结算，去重只作用于 Host 本地，双端一致。
- **即死耐性 / 正面抗性 / Normalize / 元素倍率**：纯 Host 数值分支。
- **新武器 7 把 / 闪光弹**：白名单已收录；数值改动 Host 解析自动生效。
- **特感数据结构扩字段（spit/impact/即死耐性等）**：全部 Host 注入，Client 表现重建只要 tres 就够。
- **酸弹×玩家子弹相消**：物理层交互发生在 Host 世界，Client 视觉弹按 A2 直飞即可（视觉上可能极少出现 Host 已相消而 Client 弹还在飞的 1~2 帧误差，可接受）。
- **试验场特感配置字段**：Inspector 配置只在 Host 读取。
- **UI 字体/伤害数字/提示文字**：纯本地表现。

---

## 4. 建议开工顺序

1. **B1+B2+B3**（半天内全清，扫清数值分裂与报错雷）
2. **A1 特感复制**（最大的结构缺口，完成后 Client 才能看见第二战役阵容）
3. **A2 敌弹视觉** + **A3 音效事件化**（表现完整度）
4. **C1/C2 玩家覚醒与 SA 接线**（第一章玩家侧玩法闭环）
5. **D1~D3** 实机验证与回归扩充

## 5. 开工验收清单（每项过一遍才算同步完成）

- [ ] Client 看得到全部 6 种特感 spawn 并听到发现音
- [ ] Client 看得到酸弹飞行与命中特效、听到吐酸音
- [ ] Host 侧集中射撃对ブレインディモス ×1.5 不死，Client 血条表现一致
- [ ] 女巫/丧尸连续攻击每击都结算（source_id 唯一化在联机同样成立）
- [ ] 两端难度一致（Easy 丧尸血量一致）
- [ ] 覚醒期间射击命中在同一 tick 结算且 Client 染色表现正确
- [ ] 防守战 Client 倒计时与 Host 相差 ≤0.5s
- [ ] 无头回归套件在含特感图（矿洞）全绿

---

## 6. 已知妥协（记录在案，非缺口）

- 敌人 Client 表现走帧快照：吐酸/突进/丸呑み的**起手预告**依赖 A3 事件化，纯帧同步会有 1~2 帧滞后。
- 酸弹 Client 视觉弹与 Host 权威弹道存在插值误差，命中以 Host 为准（与子弹现状同策略）。
- 3~4 人压力测试未做（v2.2 文档「尚未承诺」清单继承）。
