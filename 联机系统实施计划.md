# 联机系统实施计划（重做版）

> 基于 Phase 1-3b 失败教训 + Godot 4.6 最新 API 参考重新设计
> 最后更新：2026-08-06

---

## 目录

1. [文档审查结果](#1-文档审查结果)
2. [失败教训总结](#2-失败教训总结)
3. [核心原则](#3-核心原则)
4. [总体架构](#4-总体架构)
5. [同步数据设计](#5-同步数据设计)
6. [Phase 0：前置重构](#phase-0前置重构)
7. [Phase 1：基础网络框架](#phase-1基础网络框架)
8. [Phase 2：战斗同步](#phase-2战斗同步)
9. [Phase 3：完整体验](#phase-3完整体验)
10. [Phase 4：优化与扩展](#phase-4优化与扩展)
11. [风险与对策](#11-风险与对策)
12. [开发规范](#12-开发规范)

---

## 1. 文档审查结果

### Godot 4.6 网络 API 参考文档

`engine-reference/godot/networking/` 包含 6 个文件：

| 文件 | 内容 | 与本项目关系 |
|------|------|-------------|
| `high_level_multiplayer.rst` | **核心**：ENetMultiplayerPeer、@rpc、MultiplayerSynchronizer、Lobby 示例、认证 | ⭐ 主要参考 |
| `websocket.rst` | WebSocket 客户端/服务端 | 🔮 信令服务器备选 |
| `webrtc.rst` | WebRTC P2P（需 GDExtension 插件） | ❌ 不适用 |
| `http_request_class.rst` | HTTP 请求 | ❌ 不适用 |
| `http_client_class.rst` | HTTP 客户端 | ❌ 不适用 |
| `ssl_certificates.rst` | SSL 证书 | ❌ 不适用 |

### Godot 4.6 关键确认

来自 `engine-reference/godot/modules/networking.md`：
- **网络 API 自 4.3 以来无重大 break** — MultiplayerSynchronizer/MultiplayerSpawner 稳定可用
- `SceneMultiplayer` 支持认证回调（`auth_callback`）
- 常见错误：忘记对 Client→Server RPC 使用 `"any_peer"`、未验证客户端数据

来自 `engine-reference/godot/breaking-changes.md`：
- 4.5→4.6 无网络相关 breaking change

来自 `engine-reference/godot/current-best-practices.md`：
- Lambda 不能直接作为 `connect()` 参数（Godot 4.6 硬限制）
- `duplicate_deep()` 替代 `duplicate()` 用于嵌套资源（4.5+）

### Godot-Demos 状态

`engine-reference/Godot-Demos/` 目录为空（仅有 .git），无可参考的 Bomberman demo。

---

## 2. 失败教训总结

上次尝试（Phase 1-3b，17 文件 +1429 行）失败原因：

| # | 问题 | 后果 |
|---|------|------|
| 1 | **架构过于复杂** — 手动 RPC 广播 10 参数 @ 60Hz | 参数顺序/类型错误导致不可见 bug |
| 2 | **同步数据过多** — 纹理路径、动画帧、装备详情 | 互相依赖的字段乱码 |
| 3 | **RPC 太分散** — 16 条 RPC 分布在 6 个文件中 | 调试时无法追踪数据流 |
| 4 | **特效/音效用 RPC 逐个广播** | 容易遗漏、不可靠传输可能丢失 |
| 5 | **调试方法低效** — 每次双窗口 3-5 分钟周期 | 反馈慢，日志淹没 |
| 6 | **文件编辑技术问题** — Python 改 .gd 缩进错误 | 文件损坏 |

**核心教训**：当前"每个玩家广播自己状态 + 大量独立 RPC"的架构过于脆弱。

---

## 3. 核心原则

1. **最小化同步数据** — 只同步"游戏状态"（位置、HP、朝向、状态枚举），外观由客户端自行推断
2. **MultiplayerSynchronizer 替代手动 RPC 广播** — 框架自动处理序列化，减少手写错误
3. **权威服务器（Host）** — Host 运行所有逻辑，Client 只发输入 → 收状态
4. **增量验证** — 每个 Phase 只加一个功能，验证通过再继续
5. **集中管理** — 网络相关代码集中在少数文件中
6. **频繁提交** — 每个 Phase 结束立即 git commit
7. **特效本地化** — 特效/伤害数字由接收端根据权威数据（HP 变化、攻击事件）本地生成，不用 RPC 广播视觉效果

---

## 4. 总体架构

```
                    Host (玩家1)
┌────────────────────────────────────────────┐
│  输入处理 ← 本地玩家                         │
│  游戏逻辑 (敌人AI/伤害/物品/导演)              │
│  MultiplayerSynchronizer → 广播权威状态       │
│  @rpc → 接收客户端输入请求                    │
└────────────────────────────────────────────┘
         ↕ ENet UDP
┌────────────────────────────────────────────┐
│  Client (玩家2-4)                           │
│  本地输入 → @rpc 发给 Host                   │
│  接收 MultiplayerSynchronizer 状态 → 渲染     │
│  特效/音效由客户端根据状态变化本地生成          │
└────────────────────────────────────────────┘
```

### 权威分配

| 数据类型 | 权威方 | 同步方式 |
|----------|--------|---------|
| 玩家位置 | Host | MultiplayerSynchronizer（unreliable） |
| 玩家朝向 | Host | MultiplayerSynchronizer（unreliable） |
| 玩家 HP | Host | MultiplayerSynchronizer（reliable） |
| 玩家状态枚举 | Host | MultiplayerSynchronizer（reliable） |
| 移动输入 | Client → Host | @rpc（unreliable） |
| 攻击请求 | Client → Host | @rpc（reliable） |
| 装填请求 | Client → Host | @rpc（reliable） |
| 武器拾取请求 | Client → Host | @rpc（reliable） |
| 子弹生成/飞行 | Host only | — |
| 近战判定 | Host only | — |
| 敌人 AI | Host only | MultiplayerSynchronizer |
| 敌人 HP/死亡 | Host only | MultiplayerSynchronizer |
| 特效/音效 | 本地生成 | 根据状态变化触发（非 RPC） |
| 导演 AI / 物品投放 | Host only | @rpc 事件 |

---

## 5. 同步数据设计

### MultiplayerSynchronizer 属性（每个 Player 节点，Host 权威）

| 属性 | 类型 | 同步方式 | 说明 |
|------|------|---------|------|
| `position` | Vector2 | ON_CHANGE, unreliable | Godot 内置，CharacterBody2D 位置 |
| `hp` | float | ON_CHANGE, reliable | HP 变化时同步 |
| `facing` | int (0-3) | ON_CHANGE, unreliable | 朝向枚举（下/左/右/上） |
| `state` | int | ON_CHANGE, reliable | Idle/Walk/Run/Weapon/Attack/Downed |

> **注意**：position 由 Godot 的 `MultiplayerSynchronizer` 内置支持（CharacterBody2D），不需要手动同步。

### RPC（集中在 1 个文件中：`network_sync_manager.gd`）

| RPC | 方向 | 可靠性 | 用途 |
|-----|------|--------|------|
| `_request_move(dir, is_walking)` | Client→Host | unreliable | 移动输入（每帧） |
| `_request_attack()` | Client→Host | reliable | 攻击请求 |
| `_request_reload()` | Client→Host | reliable | 装填请求 |
| `_request_weapon_pickup(pickup_id)` | Client→Host | reliable | 拾取武器 |
| `_broadcast_event(event_type, data)` | Host→Clients | reliable | 游戏事件（尸潮/安全门等） |

> **不超过 6 条 RPC**。特效/音效/伤害数字由客户端本地生成，不占用 RPC。

---

## 6. Phase 0：前置重构

**目标**：不引入任何网络代码，把单机架构整理成适合联网的形态。
**预计时间**：1-2 天
**验证标准**：单机功能完全不受影响

### 6.1 Global 单例数据分离

**当前问题**：所有玩家数据（HP、装备、背包、队伍）存储在 `Global` autoload 中，是单机假设的核心。

**改动**：
- 创建 `PlayerData` Resource 类（`object/player_data.tres` 模板 + `script/player_data.gd`）
- Player 节点直接持有自己的 `player_data: PlayerData`
- Global 只保留：
  - 共享配置（字体路径、音量、debug 开关、文字渲染参数）
  - 队伍管理（team 数组 — 暂不动，角色切换系统依赖它）
  - 音频总线管理
  - checkpoint 数据（暂不动）
- Player 的 HP/装备/背包从 `self.player_data` 读取，不再从 Global 读取
- `game_init.gd` 初始化时创建 PlayerData 并赋给 Player 节点

**涉及文件**：`global.gd`、`player.gd`、`game_init.gd`、`hud.gd`、`menu_controller.gd`

### 6.2 添加 `owner_id` 字段

- Player 节点：`var owner_id: int = 1`（当前恒为 1 = 本地玩家）
- Enemy 节点：`var owner_id: int = 1`
- Bullet：`var owner_id: int = 0`（0 = 未设置）

**用途**：为将来网络代码的归属判断做准备，当前无行为影响。

**涉及文件**：`player.gd`、`enemy.gd`、`bullet.gd`

### 6.3 输入抽象层

**当前问题**：所有状态脚本直接调用 `Input.is_action_pressed()` / `Input.get_vector()`。

**改动**：
- 在 Player 节点上添加 `get_input_vector() -> Vector2` 和 `is_action_pressed(action) -> bool` 方法
- 当前实现直接代理到 `Input` 单例（零行为变化）
- 状态脚本改为通过 `player.get_input_vector()` 获取输入，而非直接调 `Input`

**涉及文件**：`player.gd`、所有 `player/Player*State.gd`（7 个文件）

---

## 7. Phase 1：基础网络框架

**目标**：两个 Godot 窗口能连接，看到对方移动。
**预计时间**：3-5 天
**验证标准**：Host 窗口 + Client 窗口 → 大厅连接 → 进入地图 → 双方可见移动

### 7.1 Lobby Autoload（连接管理）

参考 `high_level_multiplayer.rst` 第 359 行官方 Lobby 示例：

- 创建 `Lobby` autoload（`script/lobby.gd`）
- 功能：创建房间 / 加入房间 / 断开 / 玩家列表
- 连接：`ENetMultiplayerPeer`，默认端口 27015，最多 4 人
- 信号：`player_connected(id, info)` / `player_disconnected(id)` / `server_disconnected()`
- 局域网自动发现：UDP 广播（`PacketPeerUDP` + `set_broadcast_enabled`）

### 7.2 大厅 UI

- 简单的主机/加入界面（`scene/lobby_ui.tscn`）
- 显示已连接玩家列表
- "开始游戏"按钮 → Host 触发 `load_game.rpc(map_path)`

### 7.3 MultiplayerSpawner + Player puppet

- 修改 `main.tscn`：添加 `MultiplayerSpawner` 节点
- Player 场景添加 `MultiplayerSynchronizer` 子节点
- Host 端：Player 节点运行完整逻辑
- Client 端：Player 节点为 puppet（只读，不运行状态机）

### 7.4 玩家位置同步

- 使用 `MultiplayerSynchronizer` 同步 `position`
- Client 端用插值平滑远程玩家的移动（`_process` 中 lerp）
- 先只验证基本连通性

---

## 8. Phase 2：战斗同步

**目标**：能合作打怪。
**预计时间**：3-5 天
**验证标准**：Client 能看到敌人 + 攻击敌人 + 伤害数字正确 + HP 变化同步

### 8.1 HP 同步 + 伤害权威

- `MultiplayerSynchronizer` 添加 `hp` 属性
- 所有 `take_damage()` / `heal()` 在 Host 执行
- Client 的 HP 变化由同步器自动更新

### 8.2 子弹/近战服务端化

- Client 发送 `_request_attack()` RPC → Host
- Host 执行：生成子弹、判定碰撞、扣血
- 客户端只播放攻击动画（本地预测）、不生成子弹实体
- 伤害数字由 HP 变化事件触发（客户端本地 `DamageNumber.spawn()`）
- 近战同理

### 8.3 敌人 AI 服务端化

- 敌人 AI（状态机）只在 Host 运行
- 敌人位置/HP/状态通过 `MultiplayerSynchronizer` 同步给客户端
- Client 端敌人是 puppet：只驱动精灵动画、播放受击特效
- 客户端根据敌人 HP 变化本地生成伤害数字

---

## 9. Phase 3：完整体验

**目标**：可游玩的合作体验。
**预计时间**：3-5 天
**验证标准**：完整的合作游戏循环可用

### 9.1 武器拾取同步

- Client 发送捡起请求 RPC → Host 确认 → 同步装备变化
- 武器替换/掉落同步

### 9.2 角色切换同步

- 队伍数据由 Host 管理
- 切换请求 → Host 确认 → 广播当前角色变更

### 9.3 倒地/救援

- 死亡改为倒地（而非 reload 场景）
- 救援机制（队友交互）
- 全队倒地 → Host 触发关卡重载

### 9.4 HUD 多人适配

- 显示队友 HP/名字/状态
- 队友状态栏（小头像 + HP 条）

---

## 10. Phase 4：优化与扩展

**目标**：流畅的联网体验。
**预计时间**：2-4 天

### 10.1 延迟补偿

- 客户端预测（本地立即播放攻击动画）
- 远程玩家位置插值平滑

### 10.2 导演 AI 网络化

- 导演 AI 在 Host 运行
- 尸潮/事件通过 `_broadcast_event()` RPC 同步

### 10.3 断线重连

- 断线检测 + 自动重连
- 重连后状态恢复

---

## 11. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| `MultiplayerSynchronizer` 不适用某些 2D 属性 | 中 | 回退到手动 `@rpc`（≤5 属性） |
| 敌人 AI 服务端化性能不足 | 中 | 降频同步（10Hz）+ 客户端插值 |
| Godot 4.6 `MultiplayerSynchronizer` 有未知 bug | 高 | 第一时间查 GitHub issues，准备 `@rpc` fallback |
| 双窗口调试效率低 | 中 | 先用单窗口 log 验证 RPC 调用/接收，再用双窗口 |
| 大量改动破坏单机功能 | 高 | Phase 0 不涉及网络，每个 Phase 结束验证单机 |

---

## 12. 开发规范

### 提交策略

```
每个 Phase 结束后：
1. git add + git commit（描述 Phase 内容和验证结果）
2. 用 git diff 验证每次改动
3. 单机功能回归测试
```

### 文件组织

```
script/
├── lobby.gd                    ← Autoload：连接管理（新增）
├── network_sync_manager.gd     ← 所有 RPC 集中定义（新增）
├── player_data.gd              ← Resource：玩家数据容器（新增）
└── ...（现有文件）
```

### 测试方法

1. **Phase 0-1**：单窗口 log 验证（不需要双窗口）
2. **Phase 2+**：双窗口：
   - Host：编辑器运行
   - Client：`godot --path . --rendering-driver d3d12` 第二个实例
3. 每个 Phase 至少验证：连接 → 进入地图 → 核心功能 → 断开

---

## 参考文档

| 文档 | 用途 |
|------|------|
| `engine-reference/godot/networking/high_level_multiplayer.rst` | Godot 4.6 高层多人 API 官方文档 |
| `engine-reference/godot/modules/networking.md` | Godot 4.6 网络 API 速查 + 常见错误 |
| `engine-reference/godot/breaking-changes.md` | 4.4→4.6 breaking changes（网络无影响） |
| `engine-reference/godot/current-best-practices.md` | GDScript 4.6 最佳实践（lambda 限制等） |
| `联机系统架构设计.md` | 原架构设计文档（参考用） |
| `multiplayer-architecture-lessons-learned.md` | Phase 1-3b 失败经验教训 |
| `游戏系统架构文档.md` | 单机架构参考 |
