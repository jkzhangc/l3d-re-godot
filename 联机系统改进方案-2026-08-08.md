# 局域网联机系统改进方案（审查版）

> 状态：Phase 0 已开始实施；本文件保留原审查结论，执行状态见文末。  
> 审查日期：2026-08-08  
> 对照资料：`engine-reference/[new!]godot-docs-master/tutorials/networking/high_level_multiplayer.rst`、`classes/class_scenemultiplayer.rst`、`classes/class_multiplayerspawner.rst`、`classes/class_multiplayersynchronizer.rst`、`classes/class_enetmultiplayerpeer.rst`，以及项目现有联机文档和脚本。

## 结论

当前项目已经具备 ENet Lobby、场景加载、玩家生成、Host 敌人 AI 和手写 RPC 同步框架；但还不能把它视为“已验收的 Host-Authoritative 联机”。主要问题是：玩家位置由各自 authority 直接广播而非 Host 验证；关键 RPC 信任客户端传入的 `peer_id`、武器 ID 和弹药参数；敌人/玩家采用手写 RPC 与元数据混合，动态生成没有使用 Godot 官方 `MultiplayerSpawner`；大厅就绪计数和状态机缺少幂等、超时、拒绝新连接和认证保护。

## 发现的问题（按优先级）

### P0：必须先修的权威与安全问题

1. **客户端可冒充其他玩家**  
   `NetworkSyncManager.request_attack/request_melee/request_reload` 接收客户端传入的 `peer_id`，服务器直接 `_find_player(peer_id)`。Godot 文档建议在 `@rpc("any_peer")` 内使用 `multiplayer.get_remote_sender_id()`，并验证所有参数。改为从 RPC sender 推导玩家对象，拒绝不匹配的 ID。

2. **客户端可伪造武器和资源状态**  
   `weapon_item_id`、`shell_count`、`reserve_count` 直接进入 Host 执行路径。Host 应以服务器保存的 PlayerData/装备为准，只接受动作意图（attack/reload/pickup），并校验当前状态、冷却、距离、弹药和武器槽位；弹药数绝不能由客户端上报。

3. **移动同步不是真正的 Host 权威**  
   `player.gd` 当前由拥有该 Player 的 peer 每 0.05 秒发送 `_sync_state(global_position, ...)`。文档的安全建议是客户端发送输入/移动意图，由 authority 计算并广播状态。建议分两阶段：先保留客户端预测但 Host 做位置/速度/地图碰撞校验；随后把 Host 的物理位置作为唯一可接受状态，客户端使用输入序列号和插值/纠正。

4. **大厅就绪 RPC 非幂等且无超时**  
   `players_loaded` 是整数，重复 `player_loaded` 会重复计数；断线没有从就绪集合移除；没有加载超时。改为 `Dictionary[int,bool]`/`PackedInt32Array`，按 sender ID 去重，断线删除，并增加 Host 等待超时和失败提示。

5. **缺少连接认证/准入控制**  
   `SceneMultiplayer.auth_callback` 为空时自动接受连接。局域网也建议至少做版本号、协议号、房间 token、玩家名长度/字符过滤和最大人数校验；不匹配时通过 `disconnect_peer()` 拒绝。不要开启 `allow_object_decoding`（官方文档警告可能执行不可信对象中的代码）。

### P1：稳定性与同步正确性

6. **动态节点没有采用官方复制节点**  
   当前 `GameInit._create_player.rpc()` 手动在各端 `add_child()`，敌人由 Host 广播名字/位置后 Client 自己 instantiate。Godot 的 `MultiplayerSpawner` 可由 authority 自动复制生成/销毁，要求节点路径一致；应优先用于 Player 和 Enemy。动态 `add_child(..., true)` 要保留 readable name，避免 RPC NodePath 不一致。

7. **MultiplayerSynchronizer 尚未落地**  
   当前玩家和敌人使用手写 `_sync_state`/`sync_enemies_batch`，且通过 `set_meta("synced_state")` 传状态。建议在 Player/Enemy 场景加入 `MultiplayerSynchronizer` + `SceneReplicationConfig`：位置/朝向/移动状态用 `unreliable_ordered`，HP/死亡/装备/关键事件用 reliable；客户端只读同步属性。

8. **传输通道未分离**  
   文档说明 reliable 消息会产生性能代价，通道可避免聊天/事件阻塞战斗包。建议约定：channel 0 reliable（连接、装备、HP、死亡、关卡事件），channel 1 unreliable_ordered（位置/朝向），channel 2 unreliable/reliable（攻击表现和非关键特效）。

9. **敌人批量同步缺少删除/重生快照**  
   `sync_enemies_batch` 只更新现存节点，死亡依赖单独 HP RPC；Client 端没有统一的 spawn/despawn 序列号和初始快照。Spawner 化后由 Host 管理生命周期；否则至少加入 generation ID、场景切换清理和全量快照重发。

10. **按名称寻址不够稳健**  
    `_find_enemy()` 遍历 group 后按 `enemy.name` 匹配。建议使用 authority 分配的稳定网络 ID（或 MultiplayerSpawner 的节点路径），不要把客户端可控名称作为唯一身份。

11. **断线和服务器关闭流程不完整**  
    目前只移除 Player 节点并清空 Lobby；没有暂停/迁移 Host、清理敌人和投射物、恢复单机 OfflineMultiplayerPeer 后的场景状态。应集中实现 `leave_game()`/`shutdown_session()`，断线信号统一清理。

### P2：局域网体验与可维护性

12. **大厅只支持手动 IP，未实现 LAN 发现**  
    `未安装的插件/LANServerBroadcast` 可提供 UDP 广播，但属于未安装插件；可以先用 Godot `PacketPeerUDP` 做发现层（广播端口与游戏端口分离），发现信息只用于 UI，最终仍通过 ENet 连接。

13. **连接参数应配置化**  
    PORT、MAX_CONNECTIONS、协议版本、发现端口、连接超时和同步频率集中到 `network_config.gd`/Resource，避免散落常量。

14. **缺少可重复网络测试工具**  
    增加 `multiplayer_test.tscn` 的测试清单：双实例启动、2–4 人加入、移动、攻击、装填、拾取、敌人生成/死亡、断线、重复 RPC、错误版本、满员拒绝。记录每项的 Host/Client 预期日志。

15. **延迟与丢包可观测性不足**  
    增加 peer RTT、发送/接收频率、丢包/重传、校正次数和 RPC 拒绝原因的调试面板；不要依赖大量 `print()` 作为长期诊断手段。

## 推荐目标架构

```text
Lobby/Session
  ├─ ENetMultiplayerPeer（LAN，配置化端口）
  ├─ SceneMultiplayer.auth_callback（版本/token/人数校验）
  ├─ MultiplayerSpawner（Host 生成 Player/Enemy）
  └─ 会话状态机：LOBBY → LOADING → PLAYING → DISCONNECTING

Host（唯一游戏权威）
  ├─ 接收并验证输入/动作意图
  ├─ 运行玩家物理、敌人 AI、伤害、掉落、Director
  └─ Synchronizer 广播可复制状态 + reliable 关键事件

Client
  ├─ 本地玩家输入与可选预测
  ├─ 远程 Player/Enemy puppet 插值
  └─ 根据权威状态本地播放动画、特效、音效和伤害数字
```

## 分阶段实施顺序

### Phase 0：只做安全和会话正确性

- sender ID 校验、参数白名单、动作冷却/频率限制
- 就绪集合去重、断线移除、加载超时
- 连接版本/token/玩家名校验；拒绝新连接开关
- 集中 leave/shutdown 清理

验收：恶意/错误参数不能改变 Host 状态；重复 ready 不会提前开始；断线后 Host 可继续或安全退出。

### Phase 1：官方复制机制

- Player/Enemy 场景加入 `MultiplayerSynchronizer` 和 replication config
- GameInit/Director 改用 `MultiplayerSpawner`
- 为动态节点定义稳定 readable name/网络 ID

验收：新玩家/敌人的生成、死亡、重连快照在所有端一致；不再需要按名称猜测节点。

### Phase 2：移动与战斗权威化

- 客户端发送输入意图（含 sequence ID），Host 运行移动和碰撞
- 位置使用 unreliable_ordered；HP/装备/死亡使用 reliable
- Host 对攻击、装填、拾取、投掷品执行完整状态验证

验收：客户端修改本地坐标或弹药不会绕过 Host；高延迟下能平滑插值并纠正。

### Phase 3：局域网发现与测试

- UDP 广播发现 + 手动 IP 回退
- 双实例自动化启动脚本、连接/断线/满员/版本不匹配测试
- RTT/丢包/拒绝原因调试 HUD

验收：同网段可发现房间；异常情况有明确 UI 提示；测试结果记录进联机实施计划。

## 暂不建议现在做的事

- 不立即引入 WebRTC、WebSocket 或中继服务器；当前目标是稳定 LAN ENet。
- 不开启 `SceneMultiplayer.allow_object_decoding`。
- 不在没有 Spawner/Synchronizer 基线前继续增加更多手写 RPC。
- 不把视觉特效、伤害数字和每帧动画帧作为网络状态传输。

## 与现有文档的处理建议

- 本方案是 2026-08-08 审查结果，不覆盖 `CLAUDE.md`。
- `联机系统架构设计.md`、`联机系统实施计划.md` 可在实施后补充实际验收状态；README 的“联机同步已完成”建议改成“联机框架已实现，仍需安全性和多人回归验收”。
- `memory/project-audit-2026-08-08.md` 只记录项目事实；代码规则仍以 `CLAUDE.md` 为准。

## 2026-08-08 执行记录

本轮已开始 Phase 0，已完成：

- 战斗请求 RPC 改用 `multiplayer.get_remote_sender_id()` 推导玩家，不再信任客户端传入的 `peer_id`。
- 攻击/近战/装填请求增加 Host 侧武器 ID 白名单校验和最小请求间隔。
- ready 状态改为按 peer 去重的集合，断线会移除 ready 状态，重复 ready 不再重复计数。
- 场景加载路径增加 `res://scene/maps/` 白名单校验，并清理上一局 ready 状态。

暂缓项：

- 远程玩家完整库存尚未迁移到 Host；装填仍保留旧的 reserve 参数兼容路径，后续 PlayerData 同步阶段再彻底移除。
- MultiplayerSpawner、MultiplayerSynchronizer、Host 权威移动尚未开始，属于后续 Phase 1/2。
