# 联机系统架构设计 v2（Host 全量模拟模型）

> **状态**：v2 重写（2026-08-04）。v1 采用「混合权威」模型，导致 **主机开枪客户端看不见子弹**、位置漂移/瞬移、状态对不上等根本性不同步问题。本文按 **「Host 全量模拟 + 客户端纯渲染」** 模型重写，是联机开发的**唯一依据**。
>
> **硬性前置条件**：`net_proto/` 独立原型 **全部验证通过**（见第 8 节清单）之前，**不允许**向主项目移植任何联机代码。

---

## 目录

1. [为什么重写（v1 的教训）](#1-为什么重写v1-的教训)
2. [核心原则（5 条铁律）](#2-核心原则5-条铁律)
3. [网络模型](#3-网络模型)
4. [权威分配（v2 全表）](#4-权威分配v2-全表)
5. [实体生命周期](#5-实体生命周期)
6. [同步策略](#6-同步策略)
7. [连接管理](#7-连接管理)
8. [原型验证清单（net_proto/）](#8-原型验证清单net_proto)
9. [主项目改造清单（v2 更新）](#9-主项目改造清单v2-更新)
10. [分阶段实施计划（v2）](#10-分阶段实施计划v2)
11. [技术选型](#11-技术选型)
12. [参考](#12-参考)

---

## 1. 为什么重写（v1 的教训）

### 1.1 实际症状

v1 模型实跑后暴露的问题（按严重度排序）：

| # | 症状 | 后果 |
|---|------|------|
| 1 | **主机开枪，客户端看不见子弹** | 最直观的不可接受 bug |
| 2 | 远端玩家位置漂移 / 瞬移 / 互相"穿模" | 战斗无法进行 |
| 3 | 敌人状态（追击/攻击）各端不一致 | 观感分裂 |
| 4 | 伤害结果对不上（有的端掉了血有的没掉） | 逻辑不可信 |
| 5 | 修复一个同步问题，又冒出两个新问题 | 开发进入负反馈循环 |

### 1.2 根因

v1 的权威分配是**混合权威**：

```
玩家位置  → 各自客户端    （玩家 A 的机器模拟 A）
玩家 HP   → Host          （Host 模拟 A 的 HP）
子弹生成  → Host          （Host 生成 A 的子弹）
```

**同一个实体（玩家 A）被两台机器同时模拟**：客户端 A 模拟 A 的移动，Host 模拟 A 的 HP 和 A 发出的子弹。而子弹的生成位置、方向、时机全部依赖 A 的位置和朝向 —— 这些数据在两端各有一套。**当两端数据不一致（延迟/丢包/输入时序），子弹、位置、HP 必然对不上。**

这不是 bug，是**架构缺陷**：混合权威 = 同一实体多端模拟 = 必然不同步。多端模拟之间的状态冲突，靠「同步修复」永远补不平，只会互相覆盖、互相污染。

### 1.3 结论

> **一个实体，有且只有一个权威模拟端。**
> 全游戏世界只在 Host 上模拟一份，客户端不做任何模拟，只做渲染与输入上报。

这条原则是本文档的全部基础。

---

## 2. 核心原则（5 条铁律）

1. **世界只有一个：Host。** 所有游戏逻辑（移动、攻击、伤害、AI、生成、掉落、事件）只在 Host 上运行一份。客户端**不模拟任何东西**。

2. **客户端 = 渲染器 + 输入器。** 客户端只做两件事：把自己的输入发给 Host；把 Host 同步来的世界状态渲染出来。客户端没有「游戏逻辑副本」。

3. **一切状态变化由 Host 发起。** 实体被创建/销毁、位置变化、HP 变化、状态切换，全部由 Host 产生，再广播给客户端。客户端永远不主动改变共享状态。

4. **高频数据用 unreliable，状态变更用 reliable。** 移动/朝向 → `unreliable_ordered`（丢了就丢了，下一帧覆盖）；攻击/伤害/生成/死亡/拾取 → `reliable`（绝不能丢）。**reliable 通道是游戏事件的主干，unreliable 通道只是视觉润滑。**

5. **表现与数据分离。** 动画/音效/粒子/伤害数字 = 表现（客户端本地触发）；HP/位置/状态 = 数据（Host 权威，同步下发）。表现永远可以从数据推导出来，但数据绝不依赖表现。

**推论**：玩家自己的「手感」从哪来？—— 客户端把输入**立即发给 Host**，Host 处理完**立刻把结果同步回来**。在局域网（<5ms）+ 高同步频率（20-30Hz）下，主观延迟远小于 100ms，手感可接受。这是 L4D2 官方 P2P 模型的做法，对合作 PvE 足够好。延迟补偿（客户端预测）是**后期优化**，不是基本盘（见第 10 节）。

---

## 3. 网络模型

### 3.1 Host-Authoritative（房间主 = 服务器）

```
┌─────────────────────────────────────────────┐
│  Host（玩家1的机器 = 服务器 + 玩家1渲染器）        │
│                                             │
│  ┌───────────────────────────────────────┐  │
│  │  Server Authority（唯一的世界）           │  │
│  │  · 所有实体的模拟：玩家/敌人/子弹/掉落/导演   │  │
│  │  · 所有判定：伤害/暴击/命中/生成            │  │
│  │  · 游戏流程：关卡/波次/救援/存档            │  │
│  └───────────────────────────────────────┘  │
│                                             │
│  Host 自己的输入 → 直接进 Server 模拟（零延迟） │
└──────────────────┬──────────────────────────┘
                   │ ENet UDP 双向
                   ▼
┌─────────────────────────────────────────────┐
│  Client 2/3/4（纯渲染 + 输入上报）              │
│  · 本地 Input → submit_input RPC → Host      │
│  · 渲染 Host 同步来的世界状态                   │
│  · 触发本地表现（音效/动画/伤害数字）            │
└─────────────────────────────────────────────┘
```

### 3.2 为什么不用 v1 的「客户端预测自己位置」

- 客户端预测 = 客户端模拟自己 = **违反了铁律 1**（一个实体多个模拟端）。
- 一旦客户端模拟自己，就必须处理「客户端预测值 vs Host 权威值」的**回滚/校正**，这个机制本身就是巨大复杂度来源，且会重新引入 v1 的同步污染问题。
- 合作 PvE（非竞技射击）对本地回馈延迟的敏感度远低于对抗游戏。局域网场景下 20-30Hz 同步足以支撑体验。
- **后期如需优化**：加「客户端预测 + 插值」只对本地玩家生效，且以 Host 数据为最终权威（保留回滚能力）。见第 10 节。

---

## 4. 权威分配（v2 全表）

> 与 v1 的核心差异：**玩家位置/朝向也从客户端收归 Host**。客户端只上报输入。

| 数据类型 | 权威方 | 客户端行为 |
|----------|--------|-----------|
| 玩家移动/朝向 | **Host** | 只上报 `submit_input(dir, buttons)`，不移动自己 |
| 玩家 HP/弹药/存活 | **Host** | 只显示同步结果 |
| 子弹生成/弹道/命中 | **Host** | 上报 `fire_request`，子弹由 Host 生成并广播 |
| 近战判定 | **Host** | 上报攻击帧，Host 判定命中并广播 |
| 敌人 AI/位置/HP | **Host** | 只渲染同步结果 |
| 物品拾取/掉落 | **Host** | 上报拾取请求，Host 确认 |
| 敌人生成/尸潮 | **Host** | 只渲染 |
| 导演 AI/关卡事件 | **Host** | 只接收事件通知 |
| 存档 | **Host** | 从 Host 加载 |

**判定归 Host 的推论**：
- 客户端**没有** `take_damage()`、`spawn_bullet()`、`spawn_enemy()` 的调用路径 —— 这些函数只存在于 Host 执行分支中，客户端代码里根本没有入口。
- 客户端收到子弹生成，只是「看到了」Host 生成的结果；客户端永远不能「生成」子弹。

---

## 5. 实体生命周期

### 5.1 实体类别

| 类别 | 所有权 | 数量 | 例子 |
|------|--------|------|------|
| 玩家实体 | 每客户端 1 个，Host 创建 | ≤4 | `Entities/2`（节点名 = peer_id） |
| 敌人实体 | Host 创建/销毁 | 动态 | `Enemies/E_17` |
| 子弹实体 | Host 创建/销毁 | 动态 | `Bullets/B_92` |
| 掉落物 | Host 创建/销毁 | 动态 | — |

### 5.2 玩家实体生成流程

```
[Client 加入房间]
    │  Net.hello.rpc_id(1, version, name)     ← lobby 阶段握手
    ▼
[Host] 记录玩家名，等待游戏开始
    │
[Host] 点「开始游戏」→ Net.start_game.rpc(scene_path)  ← call_local，所有端执行
    ▼
[所有端] change_scene → game 场景
    │
[Host game._ready] 为每个已连接 peer 创建玩家实体：
    │   Entities.add_child(player_tscn.instantiate(name=str(peer_id)))
    │   ← MultiplayerSpawner 自动向所有客户端广播生成
    ▼
[Client game._ready] 找到自己的节点 Entities/<my_peer_id>，绑定相机/HUD
    │   request_game_ready.rpc_id(1)           ← 保险：确保 Host 已为我生成 + 发快照
    ▼
[Host] 收到 request_game_ready → 若已存在则跳过（幂等）→ receive_snapshot.rpc_id(sender, 快照)
```

### 5.3 实体销毁流程

```
[Host] 敌人死亡 → Enemies.remove_child(enemy) + queue_free()
        ↑ MultiplayerSpawner 检测 remove_child → 向客户端广播 despawn
[Host] 玩家掉线 → Net.peer_left 信号 → 同上删除玩家实体
```

### 5.4 节点命名与查找

- **玩家节点名 = `str(peer_id)`**。客户端找自己：`get_node("Entities/" + str(Net.my_peer_id))`。
- 实体不依赖 `get_tree().current_scene` 的遍历查找，一律通过挂载点（Entities/Enemies/Bullets）直接索引，性能与正确性都更好。

---

## 6. 同步策略

### 6.1 三层数据流

```
┌─────────────────────────────────────────────────────────┐
│  Layer 3 · 低频状态（reliable 事件）                       │
│  攻击请求/伤害结果/死亡/生成/拾取/快照/系统消息               │
│  → @rpc("any_peer"/"authority", "call_remote", "reliable")│
├─────────────────────────────────────────────────────────┤
│  Layer 2 · 中频属性（MultiplayerSynchronizer，变化时同步）    │
│  HP/存活/弹药/敌人状态机                                   │
│  → SceneReplicationConfig 配置的同步属性                    │
├─────────────────────────────────────────────────────────┤
│  Layer 1 · 高频位置（unreliable_ordered，20-30Hz）          │
│  玩家/敌人 position、玩家朝向                               │
│  → MultiplayerSynchronizer 同步 position + 自定义插值       │
└─────────────────────────────────────────────────────────┘
```

### 6.2 移动同步（Layer 1）

```gdscript
# player.gd —— 客户端分支（仅对本地玩家）
func _send_local_input() -> void:
    var dir := Input.get_vector("move_left", "move_right", "move_up", "move_down")
    submit_input.rpc_id(1, dir, Input.is_action_pressed("fire"))

# player.gd —— Host 分支（Net.is_host 才执行）
func _simulate(delta: float) -> void:
    position += input_dir * SPEED * delta   # Host 权威移动
    # position 由 MultiplayerSynchronizer 自动广播

@rpc("any_peer", "call_remote", "unreliable_ordered")
func submit_input(dir: Vector2, firing: bool) -> void:
    if not Net.is_host: return            # 防御：非 Host 一律忽略
    input_dir = dir
    input_dir = input_dir.normalized()
    self.firing = firing
```

- **Host 自己的玩家**：不经过 RPC，`_simulate` 里直接读 `Input`（零延迟）。
- **客户端玩家**：`submit_input.rpc_id(1, ...)` 只发给 Host（不用 `rpc()`，避免广播到其他客户端）。
- 客户端不做任何 `position += ...` 的自移动。收到的 position 全部来自 Host 同步。

### 6.3 开火与子弹（Layer 3 + Layer 1）

```gdscript
# player.gd —— 客户端检测「按下沿」，发 reliable 请求
if is_firing and not _prev_firing:
    fire_request.rpc_id(1)

# player.gd —— Host 处理
@rpc("any_peer", "call_remote", "reliable")
func fire_request() -> void:
    if not Net.is_host: return
    _try_fire()

# player.gd —— Host 内生成子弹（本地调用，非 RPC）
func _try_fire() -> void:
    var b := BULLET_SCENE.instantiate()
    b.setup(global_position, aim_dir(), SPEED, peer_id)
    game.bullets.add_child(b)   # ← MultiplayerSpawner 广播给所有客户端
```

**关键点**：
- 客户端永远不 `instantiate` 子弹 —— 它只是收到 Host 生成的子弹实体。
- 主机自己的子弹：`_simulate` 里直接调 `_try_fire()`（本地），同样走 `game.bullets.add_child` → **广播路径与客户端子弹完全一致**。这就是「主机开枪客户端看得见子弹」的保证。
- 子弹碰撞/伤害**只在 Host 的 `_physics_process` 里跑**。客户端的子弹节点是纯表现（甚至不需要碰撞体）。

### 6.4 伤害（Layer 3 事件 + Layer 2 属性）

```
[Host] 子弹命中敌人 → enemy.take_damage(dmg)        ← 纯本地调用（只有 Host 有子弹逻辑）
[Host] enemy.hp -= dmg                              ← hp 是同步属性，自动广播
[Host] 敌人死亡 → 删除实体（spawner 广播 despawn）
[Client] 看到 hp 变 0 + 实体消失 → 本地播死亡表现（动画/音效/伤害数字）
```

- 客户端**没有** `take_damage()` 调用入口。
- 伤害数字/音效等表现：客户端检测到 hp 下降（或收到 Host 的 `show_hit_effect` RPC）后**本地播放**，不进同步。

### 6.5 快照（新玩家补课）

新客户端进入 game 场景时，Host 通过 `receive_snapshot.rpc_id(sender, snapshot)` 下发当前游戏状态（波次、消息、已存在敌人列表），避免客户端从"空世界"开始。**完整的快照系统（含全部实体状态）是阶段 3 的内容**，原型先做基础版。

### 6.6 同步频率汇总

| 数据 | 通道 | 频率 | 可靠性 |
|------|------|------|--------|
| 玩家/敌人 position | MultiplayerSynchronizer | 20-30 Hz | unreliable_ordered |
| 玩家朝向 | 同上 | 20-30 Hz | unreliable_ordered |
| 输入上报 | RPC 客户端→Host | 每物理帧 | unreliable_ordered |
| 开火请求 | RPC 客户端→Host | 按需 | **reliable** |
| HP/存活/弹药 | MultiplayerSynchronizer | 变化时 | reliable（内建） |
| 实体生成/销毁 | MultiplayerSpawner | 事件 | reliable（内建） |
| 伤害结果/击杀 | RPC Host→Client | 按需 | **reliable** |
| 快照 | RPC Host→指定Client | 加入时 | **reliable** |
| 系统消息/聊天 | RPC Host→Client | 按需 | **reliable** |

---

## 7. 连接管理

### 7.1 连接流程

```
[Client] Net.join_game(ip, port) → ENetMultiplayerPeer.create_client()
[Client] multiplayer.connected_to_server 信号 → hello.rpc_id(1, version, name)
[Host]   hello 校验版本 → 不符则 disconnect_peer(sender)；符合则记录名字
[Host]   peer_connected/peer_disconnected 信号 → 更新房间列表 / 游戏内清理实体
[Client] server_disconnected 信号 → 回到大厅 UI，提示「与主机断开」
```

### 7.2 版本握手

- 所有客户端 `hello` 必须携带 `PROTOCOL_VERSION` 常量。
- 不匹配 → Host 直接 `multiplayer.disconnect_peer(sender)`（Godot 4.2+ `SceneMultiplayer.disconnect_peer`）。
- 主项目移植后，版本号 = 联机协议版本（与游戏版本分离）。

### 7.3 掉线处理

- **客户端掉线**（Host 侧）：`peer_disconnected` → 删除该玩家的实体（spawner 自动广播 despawn）→ 更新房间列表。
- **Host 掉线**（客户端侧）：`server_disconnected` → 停止输入上报 → 回大厅 UI。阶段 3 再做「主机迁移」（存活者接管）。

### 7.4 发现方式（分阶段）

| 方式 | 阶段 | 说明 |
|------|------|------|
| 手动输入 IP（默认 127.0.0.1） | 原型/阶段 1 | 多窗口、局域网直连 |
| UDP 广播自动发现 | 阶段 1 | `PacketPeerUDP` 广播 27016，Host 应答端口/房间名 |
| UPnP 端口映射 + 信令 | 阶段 4 | 内网穿透 |
| 中继服务器 | 阶段 4 | 对称 NAT 回退 |

---

## 8. 原型验证清单（net_proto/）

**`net_proto/` 是独立 Godot 项目**（复用主项目的 `project.godot` 输入映射与编码规范），目标是**以最小代码验证 v2 模型的每一项关键假设**。全部勾选通过后才允许动主项目。

### 8.1 验证项

| # | 验证项 | 通过标准 | 状态 |
|---|--------|---------|------|
| P1 | 双进程连接 | Host 创建房间，Client 输入 127.0.0.1 加入，双方互见 | ⬜ |
| P2 | 版本握手 | 版本不符的客户端被踢出 | ⬜ |
| P3 | MultiplayerSpawner 玩家生成 | Host 开游戏后两端各自生成实体；新客户端中途加入能补生成 | ⬜ |
| P4 | 输入上报 + Host 移动 | 客户端移动，**Host 上看到客户端玩家在动**（客户端自己不动，纯等同步） | ⬜ |
| P5 | 位置同步广播 | 客户端位置从 Host 同步回来，客户端视角平滑 | ⬜ |
| P6 | **主机开枪，客户端看得见子弹** | Host 开火 → 客户端屏幕出现子弹并飞行 | ⬜ |
| P7 | 客户端开枪 → Host 生成子弹 | 客户端按 F → Host 生成子弹 → 广播回所有客户端 | ⬜ |
| P8 | 子弹伤害判定（Host 侧） | 子弹命中敌人，敌人 HP 下降（两端一致） | ⬜ |
| P9 | 敌人 AI 纯 Host 模拟 | 客户端**不运行 AI**，敌人位置/HP 全部来自 Host 同步 | ⬜ |
| P10 | HP 属性同步 | 玩家 HP 变化（被敌人打/显示），两端一致 | ⬜ |
| P11 | 实体 despawn 广播 | 敌人死亡消失、玩家掉线实体删除，所有端一致 | ⬜ |
| P12 | 新玩家中途加入快照 | 已开局后加入，能收到波次/消息快照 | ⬜ |
| P13 | Host 掉线处理 | 客户端收到 server_disconnected，回大厅 | ⬜ |
| P14 | 三客户端同跑 | 3 个进程同机（或 2 局域网），无明显崩溃/错乱 | ⬜ |

### 8.2 原型结构

```
net_proto/
├── project.godot          # 独立项目（autoload: Net）
├── scene/
│   ├── lobby.tscn         # 大厅（创建/加入/开始/玩家列表/日志）
│   └── game.tscn          # 游戏场景（Entities/Enemies/Bullets + 3 个 spawner）
├── object/
│   ├── player.tscn        # 玩家实体
│   ├── enemy.tscn         # 敌人实体
│   └── bullet.tscn        # 子弹实体
├── script/
│   ├── net/net_connection.gd   # Net autoload：连接生命周期 + 握手 + start_game
│   ├── lobby.gd           # 大厅 UI 逻辑
│   ├── game.gd            # 游戏根：spawn 管理 + 敌人定时生成 + 快照 + 掉线清理
│   ├── player.gd          # Host 模拟 / 客户端输入上报 + synchronizer
│   ├── enemy.gd           # Host AI + synchronizer
│   ├── bullet.gd          # Host 弹道 + 命中
│   └── ui/hud.gd          # 本地玩家 HP/状态/消息
└── tests/
    └── test_sync_config.gd   # headless 验证 SceneReplicationConfig API
```

### 8.3 原型运行方式

```bash
# 终端 1（Host）：
& "D:\Godot_v4.6.3-stable_win64.exe\Godot_v4.6.3-stable_win64.exe" --path net_proto

# 终端 2/3（Client）：
& "D:\Godot_v4.6.3-stable_win64.exe\Godot_v4.6.3-stable_win64.exe" --path net_proto

# 无头 API 自检：
& "D:\Godot_v4.6.3-stable_win64.exe\Godot_v4.6.3-stable_win64.exe" --headless --path net_proto -s tests/test_sync_config.gd
```

操作：Host 窗口「创建房间」→ Client 窗口「加入房间」（IP 默认 127.0.0.1）→ Host「开始游戏」→ 各自窗口 WASD 移动、鼠标左键开火。绿色方块=玩家1，蓝/红/黄=玩家2/3/4，红色小方块=敌人。

---

## 9. 主项目改造清单（v2 更新）

> 在 v1 改造清单基础上，按 v2 模型修订。**所有改动都遵循铁律 1：逻辑只在 Host 跑。**

### 9.1 基础架构（P0 — 必须先做）

| # | 改造项 | v2 要求 | 涉及文件 |
|---|--------|---------|---------|
| 1 | **Global 单例重构** | 玩家数据（HP/弹药/装备/位置）迁移到玩家实体节点；Global 只留共享配置与 `team` 元数据 | `global.gd`、状态脚本、`hud.gd`、`save_manager.gd` |
| 2 | **Player 节点数据化** | Player 持有 inventory/equipment/HP/ammo；**输入只上报，移动由 Host 模拟** | `player.gd` |
| 3 | **玩家注册表** | 不再遍历找玩家；节点名 = peer_id，挂载点直接索引 | `camera_follow.gd`、`hud.gd`、`game_init.gd`、`enemy.gd` |
| 4 | **PlayerSpawner** | `MultiplayerSpawner` 按 peer 生成/删除玩家实体 | 新增 spawner |
| 5 | **同步层接入** | 玩家/敌人挂 MultiplayerSynchronizer（position + hp + alive） | 实体场景 |
| 6 | **逻辑/表现分层** | 状态脚本拆为：Host 逻辑脚本（权威）+ 客户端表现脚本（渲染/音效） | 全部状态脚本 |

### 9.2 输入与 UI（P1）

| # | 改造项 | v2 要求 | 涉及文件 |
|---|--------|---------|---------|
| 7 | **输入上报化** | 本地输入 → `submit_input.rpc_id(1, ...)`；不直接驱动节点移动 | 玩家输入处理 |
| 8 | 菜单暂停隔离 | 仅暂停本地玩家 process_mode | `menu_controller.gd` |
| 9 | HUD 多玩家 | 每客户端一个 HUD，显示本地玩家 + 队友状态（队友数据来自同步） | `hud.gd` |
| 10 | 相机 | 客户端相机跟随自己的玩家实体 | `camera_follow.gd` |

### 9.3 战斗系统（P1）

| # | 改造项 | v2 要求 | 涉及文件 |
|---|--------|---------|---------|
| 11 | 子弹 Host 化 | `fire_request` RPC → Host 生成子弹 → spawner 广播；客户端子弹无碰撞 | `bullet.gd`、攻击状态 |
| 12 | 近战 Host 化 | 攻击帧上报 → Host 判定 → 广播命中 | 近战状态 |
| 13 | 敌人多目标 + Host AI | 敌人只挂 Host；`_threat_list` 选择目标；AI 在 Host `_physics_process` | `enemy.gd`、敌人状态 |
| 14 | 枪声惊敌 | Host 判定（`gunshot_range` 由 Host 计算） | 武器数据使用处 |

### 9.4 游戏流程（P2）

| # | 改造项 | v2 要求 | 涉及文件 |
|---|--------|---------|---------|
| 15 | 死亡不重载场景 | 倒地/救援 + Host 触发全员死亡流程 | `player.gd`、`game_init.gd` |
| 16 | 存档 Host 化 | Host 存 `user://`，客户端从 Host 加载 | `save_manager.gd` |
| 17 | 导演 AI Host 化 | Director 只在 Host 跑；客户端只渲染事件表现 | `director.gd` 及其子模块 |
| 18 | 碰撞层按玩家区分 | 每个玩家独立碰撞层，子弹区分敌我（Host 判定） | 场景配置 |

### 9.5 主项目启动门槛

以下清单**全部满足**才允许开始 P0（避免改造一半被未定型的单机系统推翻）：

- [ ] 完整武器系统（手枪/霰弹/步枪/冲锋枪 + 投掷品）稳定
- [ ] 特殊感染者（Hunter/Smoker/Boomer/Tank/Witch）稳定
- [ ] 导演 AI Phase 4 完成
- [ ] 完整关卡流程（安全屋→推进→救援）可通关
- [ ] 倒地/救援机制落地
- [ ] 单人模式下逻辑/表现已分层（第 9.1 条 #6）

---

## 10. 分阶段实施计划（v2）

| 阶段 | 目标 | 内容 | 完成标志 |
|------|------|------|---------|
| **0** | 架构定稿 + 原型 | 本文档 + `net_proto/` 全部验证项 P1–P14 通过 | 8.1 清单全 ⬜→✅ |
| **1** | 主项目基础联网 | Global 重构、Player 数据化、注册表、Spawner、ENet 连接、大厅 UI（复用原型）、玩家生成/销毁/移动同步 | 双窗口实跑主项目地图，玩家可互相看见 |
| **2** | 战斗联网 | 子弹 Host 化、近战 Host 化、敌人 Host AI + 多目标、HUD 多玩家、相机分屏/跟随 | 双人同场打尸潮，伤害/死亡两端一致 |
| **3** | 流程联网 | 倒地/救援、关卡事件同步、存档 Host 化、菜单暂停隔离、快照完整化、掉线主机迁移 | 双人通关一整关 |
| **4** | 优化扩展 | 延迟补偿（客户端预测+插值）、导演 AI 网络化、UDP 发现、UPnP+信令、中继回退 | 内网 4 人稳定游戏 |

**阶段 1 是原型代码的直接移植**，不重新设计 —— 原型验证过的模式（spawner、synchronizer、input RPC、快照）原样搬入主项目，只替换表现层。

---

## 11. 技术选型

| 项 | 选型 | 说明 |
|----|------|------|
| 传输 | ENet UDP（`ENetMultiplayerPeer`） | Godot 内建，可靠/不可靠双通道，低延迟 |
| 同步框架 | `MultiplayerSynchronizer` + `SceneReplicationConfig` | 属性级同步，增量广播 |
| 实体生成 | `MultiplayerSpawner`（spawn_path 挂载点） | 自动广播 spawn/despawn，新 peer 补发 |
| 事件 | `@rpc`（any_peer / authority + reliable） | 攻击/伤害/快照等 |
| 大厅 | 复用原型 lobby | 简单 UI，手动 IP + 默认本机 |
| 自动发现 | `PacketPeerUDP` 广播 | 阶段 1 接入 |
| 内网穿透 | `UPNP` 类 + 轻量信令 | 阶段 4 |
| 权威 | Host（P2P） | L4D2 同款模型，无需独立服务器 |
| 人数 | 2–4 人 PvE | — |

---

## 12. 参考

- `net_proto/` — 最小垂直切片原型（本文档第 8 节）
- `游戏系统架构文档.md` — 主项目单机架构
- `L4D2特色机制参考.md` / `L4D2缺失功能清单.md` — 游戏机制依据
- `engine-reference/godot/modules/networking.md` — Godot 4.6 网络 API 参考
- `CLAUDE.md` — Godot 4.6 GDScript 注意事项（lambda 不能直接 connect 等）
