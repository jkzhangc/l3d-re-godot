# 联机系统架构设计 v2.1（Host 全量模拟 + 显式 RPC/快照）

> **状态**：v2.1 更新（2026-08-20）。v1 采用「混合权威」模型，导致 **主机开枪客户端看不见子弹**、位置漂移/瞬移、状态对不上等根本性不同步问题。本文按 **「Host 全量模拟 + 客户端纯渲染」** 模型重写，是联机开发的**唯一依据**。
>
> **当前决定**：`net_proto/` 已完成静态检查、本机双进程冒烟测试，并于 **2026-08-19** 完成 1 台 Host + 2 台 Client 的真实局域网验证。主项目可以开始分阶段移植，但必须使用原型已验证的 **显式 RPC + Host 状态表 + 快照** 方案；旧版 `MultiplayerSpawner` / `MultiplayerSynchronizer` / `SceneReplicationConfig` 动态复制方案不得回流。

---

## 目录

1. [为什么重写（v1 的教训）](#1-为什么重写v1-的教训)
2. [核心原则（5 条铁律）](#2-核心原则5-条铁律)
3. [网络模型](#3-网络模型)
4. [权威分配（v2 全表）](#4-权威分配v2-全表)
5. [实体生命周期](#5-实体生命周期)
6. [同步策略](#6-同步策略)
7. [连接管理](#7-连接管理)
8. [原型验证状态（net_proto/）](#8-原型验证状态net_proto)
9. [主项目改造清单（v2.1）](#9-主项目改造清单v21)
10. [分阶段实施计划（v2.1）](#10-分阶段实施计划v21)
11. [技术选型](#11-技术选型)
12. [参考](#12-参考)

---

## 1. 为什么重写（v1 的教训）

### 1.1 实际症状

v1 模型实跑后暴露的问题（按严重度排序）：

| # | 症状 | 后果 |
|---|------|------|
| 1 | **主机开枪，客户端看不见子弹** | 最直观的不可接受 bug |
| 2 | 远端玩家位置漂移 / 瞬移 / 互相"穿模" | 战斗无法进行 |
| 3 | 敌人状态（追击/攻击）各端不一致 | 观感分裂 |
| 4 | 伤害结果对不上（有的端掉了血有的没掉） | 逻辑不可信 |
| 5 | 修复一个同步问题，又冒出两个新问题 | 开发进入负反馈循环 |

### 1.2 根因

v1 的权威分配是**混合权威**：

```
玩家位置  → 各自客户端    （玩家 A 的机器模拟 A）
玩家 HP   → Host          （Host 模拟 A 的 HP）
子弹生成  → Host          （Host 生成 A 的子弹）
```

**同一个实体（玩家 A）被两台机器同时模拟**：客户端 A 模拟 A 的移动，Host 模拟 A 的 HP 和 A 发出的子弹。而子弹的生成位置、方向、时机全部依赖 A 的位置和朝向 —— 这些数据在两端各有一套。**当两端数据不一致（延迟/丢包/输入时序），子弹、位置、HP 必然对不上。**

这不是 bug，是**架构缺陷**：混合权威 = 同一实体多端模拟 = 必然不同步。多端模拟之间的状态冲突，靠「同步修复」永远补不平，只会互相覆盖、互相污染。

### 1.3 结论

> **一个实体，有且只有一个权威模拟端。**
> 全游戏世界只在 Host 上模拟一份，客户端不做任何模拟，只做渲染与输入上报。

这条原则是本文档的全部基础。

---

## 2. 核心原则（5 条铁律）

1. **世界只有一个：Host。** 所有游戏逻辑（移动、攻击、伤害、AI、生成、掉落、事件）只在 Host 上运行一份。客户端**不模拟任何东西**。

2. **客户端 = 渲染器 + 输入器。** 客户端只做两件事：把自己的输入发给 Host；把 Host 同步来的世界状态渲染出来。客户端没有「游戏逻辑副本」。

3. **一切状态变化由 Host 发起。** 实体被创建/销毁、位置变化、HP 变化、状态切换，全部由 Host 产生，再广播给客户端。客户端永远不主动改变共享状态。

4. **高频数据用 unreliable，状态变更用 reliable。** 移动/朝向 → `unreliable_ordered`（丢了就丢了，下一帧覆盖）；攻击/伤害/生成/死亡/拾取 → `reliable`（绝不能丢）。**reliable 通道是游戏事件的主干，unreliable 通道只是视觉润滑。**

5. **表现与数据分离。** 动画/音效/粒子/伤害数字 = 表现（客户端本地触发）；HP/位置/状态 = 数据（Host 权威，同步下发）。表现永远可以从数据推导出来，但数据绝不依赖表现。

**推论**：玩家自己的「手感」从哪来？—— 客户端把输入**立即发给 Host**，Host 处理完**立刻把结果同步回来**。在局域网（<5ms）+ 高同步频率（20-30Hz）下，主观延迟远小于 100ms，手感可接受。这是 L4D2 官方 P2P 模型的做法，对合作 PvE 足够好。延迟补偿（客户端预测）是**后期优化**，不是基本盘（见第 10 节）。

---

## 3. 网络模型

### 3.1 Host-Authoritative（房间主 = 服务器）

```
┌─────────────────────────────────────────────┐
│  Host（玩家1的机器 = 服务器 + 玩家1渲染器）        │
│                                             │
│  ┌───────────────────────────────────────┐  │
│  │  Server Authority（唯一的世界）           │  │
│  │  · 所有实体的模拟：玩家/敌人/子弹/掉落/导演   │  │
│  │  · 所有判定：伤害/暴击/命中/生成            │  │
│  │  · 游戏流程：关卡/波次/救援/存档            │  │
│  └───────────────────────────────────────┘  │
│                                             │
│  Host 自己的输入 → 直接进 Server 模拟（零延迟） │
└──────────────────┬──────────────────────────┘
                   │ ENet UDP 双向
                   ▼
┌─────────────────────────────────────────────┐
│  Client 2/3/4（纯渲染 + 输入上报）              │
│  · 本地 Input → submit_input RPC → Host      │
│  · 渲染 Host 同步来的世界状态                   │
│  · 触发本地表现（音效/动画/伤害数字）            │
└─────────────────────────────────────────────┘
```

### 3.2 为什么不用 v1 的「客户端预测自己位置」

- 客户端预测 = 客户端模拟自己 = **违反了铁律 1**（一个实体多个模拟端）。
- 一旦客户端模拟自己，就必须处理「客户端预测值 vs Host 权威值」的**回滚/校正**，这个机制本身就是巨大复杂度来源，且会重新引入 v1 的同步污染问题。
- 合作 PvE（非竞技射击）对本地回馈延迟的敏感度远低于对抗游戏。局域网场景下 20-30Hz 同步足以支撑体验。
- **后期如需优化**：加「客户端预测 + 插值」只对本地玩家生效，且以 Host 数据为最终权威（保留回滚能力）。见第 10 节。

---

## 4. 权威分配（v2 全表）

> 与 v1 的核心差异：**玩家位置/朝向也从客户端收归 Host**。客户端只上报输入。

| 数据类型 | 权威方 | 客户端行为 |
|----------|--------|-----------|
| 玩家移动/朝向 | **Host** | 只上报 `submit_input(dir, buttons)`，不移动自己 |
| 玩家 HP/弹药/存活 | **Host** | 只显示同步结果 |
| 子弹生成/弹道/命中 | **Host** | 上报 `fire_request`，子弹由 Host 生成并广播 |
| 近战判定 | **Host** | 上报攻击帧，Host 判定命中并广播 |
| 敌人 AI/位置/HP | **Host** | 只渲染同步结果 |
| 物品拾取/掉落 | **Host** | 上报拾取请求，Host 确认 |
| 敌人生成/尸潮 | **Host** | 只渲染 |
| 导演 AI/关卡事件 | **Host** | 只接收事件通知 |
| 存档 | **Host** | 从 Host 加载 |

**判定归 Host 的推论**：
- 客户端**没有** `take_damage()`、`spawn_bullet()`、`spawn_enemy()` 的调用路径 —— 这些函数只存在于 Host 执行分支中，客户端代码里根本没有入口。
- 客户端收到子弹生成，只是「看到了」Host 生成的结果；客户端永远不能「生成」子弹。

---

## 5. 实体生命周期

### 5.1 实体类别

| 类别 | 所有权 | 数量 | 例子 |
|------|--------|------|------|
| 玩家实体 | Host 创建/销毁 | ≤4 | `Players/P_2`（实体 ID 通常等于 peer_id） |
| 敌人实体 | Host 创建/销毁 | 动态 | `Enemies/E_17` |
| 子弹实体 | Host 创建/销毁 | 动态 | `Bullets/B_92` |
| 掉落物/投掷物 | Host 创建/销毁 | 动态 | `Pickups/I_31` / `Projectiles/T_44` |

所有共享实体都拥有稳定的网络 ID。Host 保存权威状态表；客户端节点只是该状态的表现副本。

### 5.2 玩家实体生成流程

`net_proto_v3_persistent_scene_ready` 已验证的流程：

```
[Client 加入房间]
    │  Net.hello.rpc_id(1, protocol_version, player_name)
    ▼
[Host] 校验版本、记录 peer/座位，等待开始游戏
    │
[Host] 广播 start_game(scene_path)（reliable）
    ▼
[所有端] change_scene → 游戏场景
    │
[Client] 向常驻 /root/Net 发送 report_game_scene_ready
    │        在收到包含自己的完整快照前每 0.5 秒重试
    ▼
[Host Net] 若游戏场景尚未就绪，暂存 ready 请求
    │
[Host Game] 消费 pending ready，为 peer 建立权威玩家状态
    │  spawn_player.rpc_id(peer, public_state) / 向所有已 ready peer 广播
    ▼
[Client] 显式创建 Players/P_<entity_id> 表现节点
    │
[Host] receive_world_snapshot.rpc_id(peer, snapshot)
    ▼
[Client] 应用完整世界快照，绑定本地相机/HUD
```

关键要求：

- ready RPC 必须挂在场景切换期间仍存在的 Autoload 上，不能只发给可能尚未创建的 `/root/Game`。
- ready、spawn 和快照处理必须幂等；重复 ready 不能重复创建玩家。
- 主项目沿用 `PlayerState + Players` 座位模型：`owner_peer_id` 负责 peer→座位映射，网络实体 ID 负责座位→场景实体映射。
- 地图只提供 `PlayerSpawn` / 出生点，不再把唯一玩家实体写死为联网模式的事实来源。

### 5.3 实体销毁流程

```
[Host] 权威状态确认实体死亡/过期/玩家掉线
    │
    ├─ 从 Host 状态表删除
    ├─ 删除 Host 的实体节点
    └─ despawn_<type>.rpc(entity_id)（reliable）
             ↓
       所有 Client 按 entity_id 删除表现节点
```

- Client 不得自行决定共享实体销毁。视觉子弹可以做直线外推，但最终必须等待 Host 的可靠 despawn。
- 玩家掉线时，Host 同时清理输入缓存、peer→座位映射、玩家状态和实体节点，再广播删除。
- 新玩家中途加入不依赖历史 spawn 消息；完整世界快照必须能够重建当前仍存在的所有实体。

### 5.4 节点命名与查找

- 固定挂载点：`Players`、`Enemies`、`Bullets`，后续可增加 `Pickups`、`Projectiles`。
- 节点名建议为 `P_<id>` / `E_<id>` / `B_<id>`，查找以稳定网络 ID 为准。
- 主项目业务代码优先使用 `Players.get_entity_for_seat()`、`Players.all_entities()` 等注册表接口；网络层负责 ID 映射，禁止重新退回全树递归查找。

---

## 6. 同步策略

### 6.1 三层数据流

```
┌────────────────────────────────────────────────────────────┐
│ Layer 3 · 可靠事件（reliable RPC）                           │
│ 握手/切场景/实体生成销毁/开火请求/拾取/死亡/章节事件/完整快照 │
├────────────────────────────────────────────────────────────┤
│ Layer 2 · 高频状态快照（unreliable_ordered，目标 20 Hz）      │
│ 玩家/敌人的位置、朝向、HP、生命状态、必要的状态机枚举          │
├────────────────────────────────────────────────────────────┤
│ Layer 1 · 本地表现                                           │
│ 插值、动画、音效、粒子、伤害数字、子弹视觉外推                 │
│ 表现可由状态/事件推导，不反向修改权威数据                       │
└────────────────────────────────────────────────────────────┘
```

> Godot 4.6.3 原型曾尝试运行时动态配置 `SceneReplicationConfig`，但 `^position`、`^hp`、`^alive` 等属性路径无法稳定解析，复制数据没有可靠抵达 Client。因此当前正式方案不使用 `MultiplayerSpawner` / `MultiplayerSynchronizer` 自动复制，而使用显式 RPC 和快照协议。

### 6.2 输入与移动同步

Client 只采集并上报本地输入；Host 保存每个 peer 的最近输入，并在物理帧中模拟所有玩家：

```gdscript
# Client → Host：高频输入，允许丢包，后包覆盖前包
@rpc("any_peer", "call_remote", "unreliable_ordered")
func submit_input(sequence: int, move_dir: Vector2, buttons: int) -> void:
    if not multiplayer.is_server():
        return
    var sender_id: int = multiplayer.get_remote_sender_id()
    _latest_inputs[sender_id] = {
        "sequence": sequence,
        "move_dir": move_dir.limit_length(1.0),
        "buttons": buttons,
    }

# Host：唯一移动模拟端
func _physics_process(delta: float) -> void:
    if not multiplayer.is_server():
        return
    _simulate_all_players(delta)
    _simulate_all_enemies(delta)
    _simulate_all_projectiles(delta)
```

- Host 自己的输入直接写入同一份输入缓存，不走网络，但必须走同一套模拟函数。
- Client 不执行共享玩家的 `move_and_slide()`、伤害、弹药消耗或状态切换。
- Host 以约 20 Hz 广播玩家/敌人状态快照。客户端渲染位置可以插值，权威值仍是 Host 快照。
- 第一版不做客户端预测和回滚；确认局域网手感不足后再增加，并始终允许 Host 校正。

### 6.3 开火与子弹

```
[Client] fire_request.rpc_id(1, input_sequence, aim_data)（reliable）
    ↓
[Host] 校验玩家状态、武器、射速和弹药
    ↓
[Host] 消耗弹药，创建权威子弹状态并执行弹道/命中
    ↓
[Host] spawn_bullet.rpc(entity_id, public_state)（reliable）
    ↓
[Client] 创建无伤害判定的视觉子弹，可按速度直线外推
    ↓
[Host] 命中或过期 → despawn_bullet.rpc(entity_id)（reliable）
```

关键点：

- Host 自己开枪和 Client 请求开枪最终必须进入同一个 `_try_fire_for_peer()` 权威入口。
- Client 的视觉子弹没有碰撞和 `take_damage()` 权限；可靠 despawn 是最终生命周期依据。
- 高频状态快照不携带全部子弹列表，避免大量子弹使 unreliable 数据包超过 ENet MTU。当前原型曾出现 1520 bytes > 1392 MTU 警告，拆分后已消失。
- 新 Client 的首次完整世界快照可以包含当前仍存活的子弹，用于补齐历史可靠 spawn 消息之前的世界状态。

### 6.4 伤害与状态变化

```
[Host] 权威弹道/近战/敌人攻击命中
    → 修改 Host 状态表和实体数据
    → 可靠广播关键事件（受击、击杀、倒地、救起等）
    → 下一份高频快照继续携带 HP/生命状态作为最终校正
[Client]
    → 应用状态
    → 本地播放动画、音效、伤害数字和粒子
```

- Client 没有对共享目标调用 `take_damage()` 的合法路径。
- HP、弹药、生命状态、受控状态必须由 Host 修改；Client 只能提交“尝试攻击/交互”的请求。
- 可靠事件负责不能漏掉的语义和表现触发；状态快照负责最终一致性，避免偶发表现 RPC 顺序问题永久污染数据。

### 6.5 完整世界快照与场景就绪

完整快照至少包含：

- 房间/协议/当前场景标识。
- peer→座位、座位→网络实体 ID 映射。
- 所有玩家的公开状态。
- 所有仍存活敌人、子弹、投掷物和掉落物。
- Director 当前阶段、章节事件、安全门/目标状态。
- 必要的全局消息和计时器。

Client 在确认快照包含自己的玩家前持续重试 scene-ready。快照应用必须可以重复执行，并删除 Client 上不再存在于 Host 快照中的陈旧节点。

### 6.6 同步频率汇总

| 数据 | 通道 | 频率 | 可靠性 |
|------|------|------|--------|
| 输入上报 | RPC Client→Host | 每物理帧或输入变化时 | unreliable_ordered |
| 玩家/敌人状态快照 | RPC Host→ready Clients | 目标 20 Hz | unreliable_ordered |
| 开火/近战/交互请求 | RPC Client→Host | 按需 | reliable |
| 实体生成/销毁 | 显式 RPC Host→Clients | 事件 | reliable |
| 伤害/击杀/倒地/救起事件 | 显式 RPC Host→Clients | 按需 | reliable |
| 子弹位置 | Client 视觉外推；必要时 Host 校正 | 本地帧/低频 | 不进常规快照 |
| 完整世界快照 | RPC Host→指定 Client | scene-ready/中途加入 | reliable |
| 系统消息/聊天/章节事件 | RPC Host→Clients | 按需 | reliable |

### 6.7 数据包边界

- 高频快照只包含渲染当前帧所需的紧凑数据；不要把历史事件或大量短命实体数组塞入 unreliable 包。
- 可靠 RPC 也应按实体类别和事件拆分，避免单个巨型字典难以调试。
- 所有 RPC 先验证发送者身份、peer→座位所有权、请求频率和参数范围，再进入业务逻辑。

---

## 7. 连接管理

### 7.1 连接流程

1. Host 创建 `ENetMultiplayerPeer`，监听 UDP 27015。
2. Client 输入 IP 后创建客户端 peer 并连接。
3. Client 连接成功后向 Host 发送 `hello(protocol_version, player_name)`。
4. Host 校验协议版本、人数和名称，返回 `hello_ack` 或拒绝连接。
5. Host 维护 peer 列表与座位分配并广播大厅状态。
6. Host 选择关卡后可靠广播 `start_game(scene_path)`。
7. 每个 Client 完成场景切换后，向常驻 `/root/Net` 重复上报 scene-ready，直到收到包含自己的完整世界快照。

### 7.2 版本握手

- 协议版本与游戏显示版本分离，任何 RPC 或快照结构变化都必须提升协议版本。
- 当前原型协议：`net_proto_v3_persistent_scene_ready`。
- Host 只接受完全匹配的协议版本，避免“能连接但字段错位”的隐蔽故障。

### 7.3 场景切换竞态

真实局域网测试已经证明，Client 可能比 Host 更早进入游戏场景。可靠 RPC 只能保证传输，不保证目标 NodePath 当时存在。因此：

- scene-ready RPC 必须发送到常驻 Autoload，而不是临时游戏场景节点。
- Host Autoload 可以暂存提前到达的 ready 请求。
- Host Game 就绪后消费暂存请求。
- Client 在确认自己的玩家已出现在完整快照前，每 0.5 秒重试。
- 上述流程在自动测试中通过“Host 故意延迟 1.5 秒切场景”完成回归验证。

### 7.4 掉线处理

- **Client 掉线**：Host 清理其输入缓存、座位所有权和玩家实体，可靠广播 despawn，并刷新大厅/队伍状态。
- **Host 掉线**：Client 停止输入和世界更新，显示断线提示并返回大厅。
- **断线重连**：当前未实现；第一版主项目不承诺保留原 peer 身份。
- **主机迁移**：不属于首个可玩版本。必须等完整快照、Host 存档和断线恢复稳定后再评估。

### 7.5 发现方式（分阶段）

| 方式 | 阶段 | 说明 |
|------|------|------|
| 手动输入 IP（默认 127.0.0.1） | 原型/阶段 1 | 已验证本机与真实局域网直连 |
| UDP 广播自动发现 | 阶段 4 | `PacketPeerUDP` 广播房间信息 |
| UPnP 端口映射 + 信令 | 阶段 4 | 公网直连尝试 |
| 中继服务器 | 后续 | 对称 NAT 或直连失败时回退 |

---

## 8. 原型验证状态（net_proto/）

`net_proto/` 是独立 Godot 4.6.3 项目，用于验证 Host 权威 + 显式 RPC/快照协议。旧的自动复制节点已经从原型移除。

### 8.1 已验证项目

| 验证项 | 状态 | 说明 |
|--------|------|------|
| ENet Host/Client 连接 | ✅ | 本机双进程与真实局域网通过 |
| 协议版本握手 | ✅ | `hello/hello_ack`，版本不匹配可拒绝 |
| Host 广播开始游戏 | ✅ | 所有端切换到同一游戏场景 |
| 常驻 scene-ready + 重试 | ✅ | Host 延迟切场景 1.5 秒的竞态回归通过 |
| 多玩家生成 | ✅ | Host + Client 均能看到双方玩家 |
| Client 输入 → Host 移动 | ✅ | Client 不做权威移动 |
| 玩家位置/HP 快照 | ✅ | 20 Hz `unreliable_ordered` 快照抵达 Client |
| 敌人 Host AI/生成/销毁 | ✅ | Client 只应用状态和显示 |
| 子弹 Host 生成/命中/销毁 | ✅ | reliable spawn/despawn；Client 视觉外推 |
| Client 掉线清理 | ✅ | Host 删除对应玩家状态和实体 |
| 静态/资源测试 | ✅ | 退出码 0，0 个失败 |
| 本机双进程冒烟测试 | ✅ | Host 与 Client 均输出 PASS |
| 真实三机局域网 | ✅ | 2026-08-19：1 Host + 2 Client 成功进入并生成玩家 |

自动冒烟测试最终结果：

```text
[AUTO] host PASS players=2 moved=true hp_sync_source=true enemy_spawn=true bullet_spawn=true enemy_despawn=true
[AUTO] client PASS host_ok=true local_players=2 moved=true hp_synced=true enemy=true bullet=true
net_proto two-process smoke test PASS
```

### 8.2 尚未验证或未实现

- GUI 双窗口的完整视觉效果和操作手感。
- 4 名玩家同时联机。
- 高延迟、丢包和抖动环境。
- 长时间运行及大量实体压力。
- 断线重连、房间自动发现、NAT 穿透、公网安全与中继。
- 主项目真实地图中的导航、碰撞、完整武器、Director 和章节流程同步。

这些项目不再阻止“基础联网移植”，但分别是阶段验收项，不能被视为已经完成。

### 8.3 原型结构

```
net_proto/
├── project.godot
├── scene/
│   ├── lobby.tscn
│   ├── game.tscn          # 固定 Players / Enemies / Bullets 容器
│   └── hud.tscn
├── object/
│   ├── player.tscn
│   ├── enemy.tscn
│   └── bullet.tscn
├── script/
│   ├── net/net_connection.gd  # 常驻连接、握手、start_game、scene-ready
│   ├── lobby.gd
│   ├── game.gd                # Host 状态表、模拟、显式 spawn/despawn、快照
│   └── player.gd / enemy.gd / bullet.gd / hud.gd
└── tests/
    ├── test_sync_config.gd    # 关键资源加载与旧复制节点回归检查
    └── run_local_smoke.ps1    # 限时双进程 ENet 冒烟测试
```

### 8.4 运行与验证

```powershell
# 手动 GUI：Host 与 Client 各启动一个进程
& "D:\Godot_v4.6.3-stable_win64.exe\Godot_v4.6.3-stable_win64.exe" --path net_proto

# 静态/资源检查（单行执行）
& "D:\Godot_v4.6.3-stable_win64.exe\Godot_v4.6.3-stable_win64_console.exe" --headless --path net_proto --script res://tests/test_sync_config.gd

# 本机双进程冒烟测试
powershell -NoProfile -ExecutionPolicy Bypass -File net_proto/tests/run_local_smoke.ps1
```

详细故障与验证记录见 `net_proto/联机原型调试记录.md`。

---

## 9. 主项目改造清单（v2.1）

当前主项目已经完成 `PlayerState + Players` 座位模型、第一关安全门与章节结算流程；基础联网可以开始。旧 Phase 1–3 联机提交不直接恢复，只从最新 `net_proto` 移植协议和连接生命周期。

### 9.1 基础架构（阶段 1）

| # | 改造项 | v2.1 要求 | 涉及文件 |
|---|--------|----------|---------|
| 1 | **NetworkManager Autoload** | 移植 ENet、握手、peer 列表、start_game、常驻 scene-ready 和 pending ready | 新增网络脚本、`project.godot` |
| 2 | **座位所有权** | `PlayerState.owner_peer_id` 作为 peer→座位唯一映射；单人座位与真实联机会话明确区分 | `player_state.gd`、`player_registry.gd` |
| 3 | **玩家动态生成** | 地图只提供出生点；Host 创建权威玩家状态，所有端按显式 spawn 创建表现实体 | `game_init.gd`、玩家场景、地图场景 |
| 4 | **世界状态容器** | 固定 Players/Enemies/Bullets 等挂载点和网络 ID；Host 保存状态表 | 新增网络世界控制器 |
| 5 | **玩家输入 Host 化** | Client 只上报输入；Host 模拟全部玩家；Host 自己也走同一模拟入口 | `player.gd`、玩家状态机 |
| 6 | **玩家快照** | Host 约 20 Hz 广播位置、朝向、HP、状态；Client 插值和表现 | 网络世界控制器、`player.gd` |
| 7 | **大厅 UI** | 创建/加入/玩家列表/开始游戏；第一版手动 IP | 新增大厅场景/脚本 |

### 9.2 输入、相机与 UI（阶段 1–2）

| # | 改造项 | v2.1 要求 | 涉及文件 |
|---|--------|----------|---------|
| 8 | 输入抽象 | 状态机不能直接把 Client Input 变成共享状态；统一消费 Host 输入缓存 | 玩家状态脚本 |
| 9 | 菜单暂停隔离 | 联机菜单只禁用本地输入，不暂停 SceneTree 和 Host 世界 | `menu_controller.gd` |
| 10 | HUD 多玩家 | 本地 HUD + 队友生命/倒地状态；数据来自 Host 状态 | HUD 脚本/场景 |
| 11 | 相机 | 每个 Client 只跟随 owner_peer_id 对应的本地玩家实体 | `camera_follow.gd` |
| 12 | 单人兼容 | 没有真实 peer 时保持当前角色切换、存档和死亡流程 | `game_init.gd`、`character_switch_manager.gd` |

### 9.3 战斗系统（阶段 2）

| # | 改造项 | v2.1 要求 | 涉及文件 |
|---|--------|----------|---------|
| 13 | 子弹 Host 化 | fire_request → Host 校验/生成/命中；reliable spawn/despawn；Client 视觉外推 | `bullet.gd`、枪械状态 |
| 14 | 近战/推击 Host 化 | Client 报告尝试和方向；Host 判定范围、目标、伤害及击退 | 近战/推击状态 |
| 15 | 弹药/装填 Host 化 | Host 消耗与补充弹药，快照/事件同步 HUD 和动画 | 武器状态、`PlayerState` |
| 16 | 投掷物 Host 化 | Host 创建、模拟、爆炸/火焰伤害；Client 只显示 | 投掷物脚本 |
| 17 | 敌人 Host AI | Client 停止 AI/导航/伤害；Host 同步位置、HP、状态和目标表现 | `enemy.gd`、敌人状态、Director |
| 18 | 章节统计 Host 化 | 击杀、爆头、伤害、治疗均由 Host 记录并同步结算 | `chapter_stats.gd` |

### 9.4 多人倒地与流程（阶段 3）

| # | 改造项 | v2.1 要求 | 涉及文件 |
|---|--------|----------|---------|
| 19 | 生命状态 | 增加 ALIVE/DOWNED/DEAD；只有真实多人会话启用 DOWNED | `player_state.gd`、`player.gd` |
| 20 | 倒地/救援 | Host 管理倒地 HP/流血/救援者/进度；Client 只提交持续交互 | 玩家状态、HUD、交互系统 |
| 21 | 单人死亡规则 | 单人保持“切换存活队员；全灭后黑屏读档”，不进入倒地 | `player.gd`、`character_switch_manager.gd` |
| 22 | 团灭 | 联机仅 Host 判定全员 DOWNED/DEAD 并统一重载检查点 | `game_init.gd`、存档/流程控制器 |
| 23 | 安全门 | Host 检查队员是否满足进入条件，统一完成章节和切场景 | `safe_door.gd` |
| 24 | 章节结算 | 复用现有多人确认 RPC，接入正式座位所有权和 Host 继续流程 | `chapter_summary.gd` |
| 25 | 存档 Host 化 | Host 写检查点；Client 从 Host 快照恢复，不写共享战役进度 | `save_manager.gd`、`global.gd` |

### 9.5 Director 与特感预留（阶段 3–4）

| # | 改造项 | v2.1 要求 | 涉及文件 |
|---|--------|----------|---------|
| 26 | Director Host 化 | 紧张度、节奏、尸潮、事件、生成配额只在 Host 跑 | `script/director/` |
| 27 | 玩家受控状态 | 预留 NORMAL/DOWNED/PINNED/PULLED/STAGGERED/DEAD 和 controller_entity_id | `PlayerState`、玩家控制层 |
| 28 | 敌人类型 | 稳定 network_entity_id + enemy_archetype，用于普通感染者和特感场景创建 | 敌人数据/网络状态 |
| 29 | 特感技能协议 | Host 创建/解除“特感实体控制玩家实体”的关系；快照可恢复 | 后续特感脚本 |

### 9.6 当前启动条件

截至 **2026-08-20**：

- [x] Host 权威独立原型完成静态与双进程验证。
- [x] 真实局域网完成 1 Host + 2 Client 的玩家生成验证。
- [x] 主项目已有 `PlayerState + Players` 座位/实体注册模型。
- [x] 当前第一关已打通安全门与章节结算流程。
- [ ] 主项目基础联网尚未移植。
- [ ] 多人倒地/救援尚未实现。
- [ ] 特殊感染者与 Director Phase 4 尚未实现。

**决定**：允许开始阶段 1。特感和倒地不再作为基础联网的前置门槛；但阶段 3 通关验收必须包含多人倒地/救援，特感接入前必须完成玩家受控状态预留。

---

## 10. 分阶段实施计划（v2.1）

| 阶段 | 目标 | 内容 | 完成标志 |
|------|------|------|---------|
| **0：原型** | 架构与协议验证 | `net_proto` 显式 RPC/快照、scene-ready 竞态、双进程与三机 LAN | ✅ 已完成基础验证 |
| **1：基础联网** | 在当前第一关互相看见并移动 | NetworkManager、大厅、座位所有权、玩家动态生成、输入上报、Host 移动、玩家快照、相机/HUD 绑定 | 2–3 台电脑进入第一关并稳定移动 |
| **2：战斗联网** | 双人同场打普通尸潮 | 枪械、弹药、近战、推击、投掷物、敌人 Host AI、伤害、章节统计、队友 HUD | 两端伤害/弹药/敌人死亡一致 |
| **3：流程联网** | 双人完整通关第一关 | 多人倒地/救援、团灭、Host 存档、安全门、章节结算、Director Host 化、完整快照、Client 掉线处理 | 双人从关卡开始打到结算并继续 |
| **4：扩展优化** | 4 人与特感基础 | 玩家受控状态、Boomer/Tank/Witch/Hunter/Smoker、4 人压力测试、插值优化、房间发现 | 内网 4 人稳定游玩并支持首批特感 |
| **5：公网能力** | 非局域网连接 | UPnP、信令、中继、安全限制、断线重连；主机迁移另行评估 | 公网环境达到可重复连接标准 |

执行原则：

1. 阶段 1 直接移植 `net_proto` 已验证的连接生命周期、显式 spawn/despawn、输入 RPC 和快照思路，不恢复旧混合权威提交。
2. 每个阶段都必须同时保留单人模式回归；没有真实 multiplayer peer 时不启动网络等待或多人倒地规则。
3. 先完成最小垂直切片，再扩大武器、敌人和关卡覆盖范围。
4. 主机迁移不再写入阶段 3 完成标准，避免拖延首个可玩的局域网合作版本。

---

## 11. 技术选型

| 项 | 选型 | 说明 |
|----|------|------|
| 传输 | ENet UDP（`ENetMultiplayerPeer`） | Godot 内建；reliable 与 unreliable_ordered 双通道 |
| 权威模型 | Host 全量模拟 | 玩家、敌人、子弹、伤害、Director、流程只在 Host 判定 |
| 连接常驻层 | NetworkManager/Net Autoload | 握手、peer 生命周期、start_game、pending scene-ready |
| 高频同步 | 显式状态快照 RPC | 约 20 Hz；只含玩家/敌人紧凑状态 |
| 实体生命周期 | 显式 reliable spawn/despawn RPC | 稳定实体 ID；不依赖自动复制节点 |
| 首次/中途加入 | reliable 完整世界快照 | 可重复应用，补齐仍存在的实体和流程状态 |
| 子弹显示 | reliable spawn/despawn + Client 视觉外推 | 子弹不进入常规 20 Hz 快照，避免 MTU 超限 |
| 表现同步 | 可靠事件 + 状态差异推导 | 动画/音效/粒子本地播放，不反向决定数据 |
| 大厅 | 复用 `net_proto` 思路 | 第一版手动 IP + 默认本机 |
| 自动发现 | `PacketPeerUDP` 广播 | 阶段 4 |
| 公网能力 | UPnP + 信令 + 可选中继 | 阶段 5 |
| 人数 | 2–4 人合作 PvE | 第一阶段先验证 2–3 人，随后扩到 4 人 |

### 11.1 明确不采用

- 不恢复 v1 的混合权威实现。
- 不在当前方案中使用运行时动态 `SceneReplicationConfig`。
- 不依赖 `MultiplayerSpawner` / `MultiplayerSynchronizer` 自动生成或属性复制。
- 不让 Client 自行判定共享伤害、拾取、死亡、倒地、救援或关卡完成。
- 不把客户端预测、回滚、主机迁移作为首个局域网版本的前置条件。

---

## 12. 参考

- `net_proto/README.md` — 当前显式 RPC/快照协议与运行方式
- `net_proto/联机原型调试记录.md` — 2026-08-19 实机竞态修复与验证记录
- `游戏系统架构文档.md` — 主项目单机架构
- `L4D2特色机制参考.md` / `L4D2缺失功能清单.md` — 游戏机制依据
- `engine-reference/godot/modules/networking.md` — Godot 4.6 网络 API 参考
- `CLAUDE.md` — Godot 4.6 GDScript 注意事项（lambda 不能直接 connect 等）
