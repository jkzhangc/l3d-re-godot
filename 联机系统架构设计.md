# 联机系统架构设计（待实现）

> 优先级：🟢 **低** — 在核心战斗循环（武器/敌人/导演AI/关卡）全部稳定后再启动
> 最后更新：2026-07-24

---

## 目录

1. [为什么现在不做](#1-为什么现在不做)
2. [目标与范围](#2-目标与范围)
3. [网络模型](#3-网络模型)
4. [连接方案](#4-连接方案)
5. [同步策略](#5-同步策略)
6. [架构改造清单](#6-架构改造清单)
7. [分阶段实施计划](#7-分阶段实施计划)

---

## 1. 为什么现在不做

当前项目处于核心战斗循环搭建阶段，尚有 60%+ 的核心功能未实现（见 `L4D2缺失功能清单.md`）。

联网不是「加一个功能模块」——它是**横切所有系统的架构层**。当前的 15 项单机假设（详见本文第 6 节）遍布 Global 单例、输入处理、相机、HUD、状态机、子弹系统等所有模块。在核心循环定型之前加入联网，会导致每个新功能都需要同时考虑本地和网络两层逻辑，开发和调试成本翻 3-5 倍。

**推荐启动时机**：以下功能全部稳定后：
- 完整武器系统（手枪 + 冲锋枪 + 霰弹枪 + 投掷品）
- 完整敌人系统（至少 2 种特殊感染者 + 尸潮）
- 导演 AI 基础框架
- 至少 1 个完整关卡流程（安全门→推进→救援）
- 倒地/救援机制

---

## 2. 目标与范围

### 连接方式
| 方式 | 支持 | 说明 |
|------|------|------|
| 本地主机 | ✅ | 同一台机器多窗口（开发测试用） |
| 局域网 | ✅ | 同一网络内直连（自动发现 + 手动输 IP） |
| 内网穿透 | ✅ | UPnP 端口映射 + WebSocket 信令交换 IP |
| 对称 NAT 穿透 | ⚠️ | 需要中继服务器（成本高，可后续加） |
| Steam 联机 | 🟡 | 如果将来上架 Steam，可用 `SteamMultiplayerPeer` |

### 玩家数量
- 2-4 人合作 PvE（对标 L4D2）

### 不在范围内
- 独立 Dedicated Server（先用 Host = 玩家1 方案）
- PvP 对抗模式
- 大逃杀/MMO 架构

---

## 3. 网络模型

### 选择：Host-Authoritative PvE Co-op

```
┌──────────────────────────────────────┐
│  Host（玩家1的机器）                    │
│  ┌──────────────────────────────────┐ │
│  │ Server Authority                 │ │
│  │  - 敌人 AI / 波次生成             │ │
│  │  - 伤害判定 / 暴击掷骰            │ │
│  │  - 物品投放 / 导演 AI             │ │
│  │  - 关卡流程 / 救援事件             │ │
│  │  - 存档管理                       │ │
│  └──────────────────────────────────┘ │
│  ┌──────────────────────────────────┐ │
│  │ Client（玩家1的渲染和输入）         │ │
│  └──────────────────────────────────┘ │
└──────────────────────────────────────┘
          │ ENet UDP
          ▼
┌──────────────────────────────────────┐
│  Client 2（玩家2）                     │
│  - 本地渲染 + 输入                     │
│  - 通过 RPC 向 Host 请求移动/攻击       │
│  - 接收 Host 广播的状态同步             │
└──────────────────────────────────────┘
```

### 选择理由
- PvE 合作不需要反作弊精度，Host Authority 足够
- 无需独立服务端进程，降低部署门槛（玩家直接开房间）
- L4D2 原版也是 P2P Host 模型
- Godot 4.6 的 `ENetMultiplayerPeer` + `@rpc` + `MultiplayerSynchronizer` 完美支持此模型

### 权威分配

| 数据类型 | 权威方 | 说明 |
|----------|--------|------|
| 玩家位置/朝向 | 各自客户端 | 通过 `MultiplayerSynchronizer` 广播，Host 做合理性校验 |
| 玩家 HP | Host | 客户端请求治疗，Host 确认 |
| 玩家背包/装备 | 各自客户端 | 拾取需 Host 确认防作弊 |
| 子弹生成/飞行 | Host | 客户端发射请求 → Host 生成子弹 → 同步给所有客户端 |
| 近战伤害判定 | Host | 客户端发送攻击帧 → Host 判定 → 广播命中结果 |
| 敌人 AI / 位置 | Host | AI 在 Host 运行，位置同步给客户端 |
| 敌人 HP / 死亡 | Host | 客户端子弹命中请求 → Host 扣血 → 广播死亡 |
| 物品投放 | Host | 导演 AI 在 Host 运行 |
| 关卡事件 | Host | 安全门/救援/尸潮提示由 Host 触发 |

---

## 4. 连接方案

### 4.1 局域网（LAN）

```
Host:
  var peer = ENetMultiplayerPeer.new()
  peer.create_server(27015, 4)  # 最多 4 人
  multiplayer.multiplayer_peer = peer

Client:
  # 方案 A：UDP 广播自动发现
  var broadcaster = PacketPeerUDP.new()
  broadcaster.set_broadcast_enabled(true)
  broadcaster.set_dest_address("255.255.255.255", 27016)
  broadcaster.put_packet("DISCOVER".to_utf8_buffer())

  # 方案 B：手动输入 Host IP
  peer.create_client("192.168.1.100", 27015)
```

推荐：先做 UDP 广播自动发现 + 手动输 IP 备用。

### 4.2 内网穿透（WAN）

```
  Client A (NAT后)         信令服务器            Host (NAT后)
      │                       │                      │
      │── POST /register ────→│◄── POST /register ───│
      │     "I'm Client A"    │    "I'm Host"        │
      │                       │                      │
      │                       │── 交换公网IP+端口 ──→│
      │                       │                      │
      │                       │                      │
      │   Host 通过 UPnP 映射端口（UPNP.add_port_mapping）│
      │                       │                      │
      │────── ENet 直连 ──────│──────────────────────│
      │   (通过信令交换的公网IP)                       │
```

- **信令服务器**：轻量 WebSocket 服务（可用 Godot `WebSocketPeer` 实现，或 Python/Node.js 几十行代码）
- **UPnP**：Godot 内置 `UPNP` 类，可自动请求路由器端口映射
- **成功率**：锥形 NAT ~80%，对称 NAT ~40%（需中继回退）

### 4.3 低侵入性准备（可在当前阶段做）

以下改动不影响单机逻辑，但为将来联网铺路：

1. **输入抽象层** —— 把所有 `Input.is_action_pressed()` 封装到一个 `InputSource` 对象，当前用 `LocalInputSource`，将来加 `NetworkInputSource`
2. **`owner_id` 字段** —— 在 Player/Enemy 节点上添加 `var owner_id: int = 1`，当前恒为 1
3. **分离表现和数据** —— 确保 `_draw()` / 精灵更新等纯表现逻辑不与伤害计算等数据逻辑混在同一个方法里

---

## 5. 同步策略

### 5.1 高频状态（位置/朝向）

使用 `MultiplayerSynchronizer` 节点：

```gdscript
# 挂载到 Player 节点
$MultiplayerSynchronizer.add_to_group("player_sync")

# 同步属性
@export var sync_position: Vector2:
    set(v): position = v
    get: return position

@export var sync_facing: int:
    set(v): _facing = v
    get: return _facing
```

- **频率**：由 Godot 的 `MultiplayerSynchronizer` 默认频率（每网络帧）
- **插值**：客户端使用 `_process` 中的位置插值平滑移动

### 5.2 事件驱动（攻击/伤害/拾取/死亡）

使用 `@rpc` 注解：

```gdscript
# 客户端 → Host：请求攻击
@rpc("any_peer", "call_remote", "reliable")
func _request_attack() -> void:
    if not _can_attack():
        return
    _spawn_bullet()
    _broadcast_attack.rpc()

# Host → 所有客户端：广播攻击效果
@rpc("authority", "call_remote", "reliable")
func _broadcast_attack() -> void:
    _play_attack_animation()
    _play_attack_sound()

# Host → 所有客户端：广播伤害
@rpc("authority", "call_remote", "reliable")
func _apply_damage(target_id: int, damage: float, is_headshot: bool) -> void:
    var target = _find_node_by_id(target_id)
    if target:
        target.take_damage(damage, 0.0, Vector2.ZERO, is_headshot)
```

### 5.3 同步频率汇总

| 数据类型 | 同步方式 | 频率 | 可靠性 |
|----------|---------|------|--------|
| 玩家位置/朝向 | `MultiplayerSynchronizer` | ~30 Hz | unreliable_ordered |
| 敌人位置/AI状态 | `MultiplayerSynchronizer` | 10-15 Hz | unreliable_ordered |
| 攻击事件 | `@rpc` 事件驱动 | 按需 | reliable |
| 伤害/死亡 | `@rpc` 事件驱动 | 按需 | reliable |
| 物品拾取/投掷 | `@rpc` 事件驱动 | 按需 | reliable |
| 导演 AI 决策 | `@rpc` 事件驱动 | 按需 | reliable |
| 关卡事件 | `@rpc` 事件驱动 | 按需 | reliable |
| HP/弹药数字 | `MultiplayerSynchronizer` | 变化时 | reliable |

### 5.4 延迟补偿（可选优化）

PvE 合作中 100-200ms 延迟通常可接受。如果需要更好的手感：

- **客户端预测**：客户端立即播放攻击动画，Host 确认后保持 / 拒绝后回滚
- **延迟插值**：其他玩家的位置插值一个网络帧的延迟（~33ms），减少抖动

---

## 6. 架构改造清单

以下 15 项需要在启动联机开发后逐一改造：

### 6.1 基础架构（P0 — 必须先做）

| # | 改造项 | 当前状态 | 目标状态 | 涉及文件 |
|---|--------|---------|---------|---------|
| 1 | **Global 单例重构** | 所有玩家数据存为单例标量 | 玩家数据迁移到 Player 节点，Global 仅留共享配置 | `global.gd`、所有状态脚本、`hud.gd`、`weapon_pickup.gd`、`save_manager.gd` |
| 2 | **Player 节点数据化** | Player 通过 Global 间接访问数据 | Player 节点直接持有 inventory/equipment/HP/ammo | `player.gd` |
| 3 | **玩家注册表** | `_find_player()` 遍历场景树找首个匹配 | 服务端维护 `players: Dictionary`，客户端通过 ID 查找 | `camera_follow.gd`、`hud.gd`、`game_init.gd`、`enemy.gd` |
| 4 | **PlayerSpawner** | Player 硬编码在 .tscn 中 | 运行时 `MultiplayerSpawner` 自动为每个连接客户端生成 Player | 新增 `player_spawner.gd` |

### 6.2 输入与UI（P1 — 核心体验）

| # | 改造项 | 当前状态 | 目标状态 | 涉及文件 |
|---|--------|---------|---------|---------|
| 5 | **输入按玩家路由** | `Input.get_vector()` 无设备区分 | 每个 Player 读取自己的设备或 `pN_` 前缀的 action | 7 个玩家状态脚本 |
| 6 | **菜单暂停隔离** | `get_tree().paused = true` 暂停全部 | 仅暂停本地玩家的 `process_mode`，不影响其他玩家 | `menu_controller.gd` |
| 7 | **HUD 多玩家** | 单个 HUD 读取 Global.player_hp | 每个玩家一个 HUD 实例，显示队友状态栏 | `hud.gd`、`hud_slot.gd` |
| 8 | **相机分屏/跟随** | 单相机跟随首个玩家 | 4人=分屏 2×2，2人=左右分屏；或单屏跟随本地玩家 | `camera_follow.gd` |

### 6.3 战斗系统（P1 — 核心体验）

| # | 改造项 | 当前状态 | 目标状态 | 涉及文件 |
|---|--------|---------|---------|---------|
| 9 | **子弹所有权** | 子弹无 owner 字段 | 子弹带 `owner_id`，Host 判定伤害，广播命中 | `bullet.gd`、`PlayerPistolAttackState.gd` |
| 10 | **近战判定服务端化** | 客户端本地 Area2D 判定 | 客户端发送攻击帧，Host 判定命中，广播结果 | `PlayerKnifeAttackState.gd` |
| 11 | **敌人多目标** | `_player_ref` 单个引用 | 改为 `_threat_list: Array`，按威胁值选择目标 | `enemy.gd`、所有敌人状态 |
| 12 | **敌人 AI 服务端运行** | 各客户端独立运行 AI | Host 运行 AI，通过 `MultiplayerSynchronizer` 同步状态 | `enemy.gd`、所有敌人状态 |

### 6.4 游戏流程（P2 — 完整体验）

| # | 改造项 | 当前状态 | 目标状态 | 涉及文件 |
|---|--------|---------|---------|---------|
| 13 | **死亡不重载场景** | `reload_current_scene()` | 倒地/救援机制 + 全员死亡后 Host 触发关卡重载 | `player.gd` |
| 14 | **存档服务端化** | 本地 `user://` JSON | Host 存档，客户端从 Host 加载 | `save_manager.gd`、`game_init.gd` |
| 15 | **碰撞层按玩家区分** | 固定 collision_layer 3/4 | 每个玩家独立碰撞层（layer 2+N），子弹区分敌我 | `player.tscn`、`enemy.tscn`、`bullet.tscn` |

---

## 7. 分阶段实施计划

### 阶段 0：前置条件（当前 → 完成核心循环）
**目标**：完成单机核心游戏循环（详见 `L4D2缺失功能清单.md`）
**时长**：视开发速度而定

### 阶段 1：基础网络框架（估计 2-3 周）
- Global 单例重构（玩家数据迁移到 Player 节点）
- Player 节点数据化
- 玩家注册表 + PlayerSpawner
- ENet 连接/断开流程
- 局域网自动发现 + 手动 IP 连接
- 基础 lobby UI（创建/加入房间）

### 阶段 2：核心同步（估计 3-4 周）
- 玩家位置/朝向同步
- 输入按玩家路由
- 子弹服务端化
- 近战判定服务端化
- 敌人 AI 服务端运行 + 多目标
- HUD 多玩家 + 队友状态栏
- 相机分屏

### 阶段 3：游戏流程（估计 2-3 周）
- 倒地/救援机制（替换 reload_current_scene）
- 关卡流程同步（安全门/救援事件）
- 存档服务端化
- 菜单暂停隔离

### 阶段 4：优化与扩展（估计 2-4 周）
- 内网穿透（UPnP + 信令服务器）
- 延迟补偿（客户端预测 + 插值）
- 导演 AI 网络化
- 中继服务器（对称 NAT 回退）

---

## 参考

- `游戏系统架构文档.md` — 完整单机架构
- `L4D2缺失功能清单.md` — 核心功能待办列表
- `L4D2特色机制参考.md` — L4D2 原版机制参考
- `engine-reference/godot/modules/networking.md` — Godot 4.6 网络 API 参考
