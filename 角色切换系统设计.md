# 角色切换系统设计

> 状态：**✅ 已实现 (2026-08-04)** | **🔧 重构 (2026-08-12)** — 改为原地替换模式，默认不显示 standin

---

## 概述

单人游玩时可以设置一个队伍（最多 3 人），玩家只操控其中一个角色，其余角色不参与战斗、不跟随、不 AI——纯粹是**可切换的备选角色**。

类似 L4D2 单人模式下 AI 队友的简化版，但这里不做 AI，只做**切换**。

## 核心机制

### 队伍设置

- 在角色选择界面，玩家选择队伍成员（1~3 人）
- 队伍数据存在 `Global.team: Array[Dictionary]` 中
- 每个成员记录：角色数据（CharacterData）、HP、装备、弹药

### 切换操作

- 按 **Q 键**循环切换队员（`切换角色键`）
- 按 **Ctrl+1/2/3** 直接选择队员（`选择队员1/2/3键`）
- **原地替换**：切换后新角色出现在当前玩家位置，不传送
- 镜头瞬切到新角色位置（切换不传送，镜头实际不变）
- 切换有 **0.5 秒冷却时间**，防止误触

### 不在场的角色

- **默认不在场景中显示**（`show_teammate_standins = false`）
- 队员数据（HP、装备、弹药、位置）保存在 `Global.team` 中
- 如需 L4D2 风格可见队友，可在 Inspector 中开启 `show_teammate_standins`
- 开启后：队友留在原地为静态精灵（Node2D + Sprite2D），不移动、不攻击、不受伤害

### 切换时保留的数据

- 当前角色的 HP、装备、弹药 → 保存在队员数据中
- 切换到的角色 → 恢复其上次的 HP、装备、弹药

## 实现架构

### 数据层

```gdscript
# Global 单例 (script/global.gd)
var team: Array[Dictionary] = []   # 队伍成员列表
var current_team_index: int = 0    # 当前操控的角色索引
var selected_campaign: CampaignData = null
var selected_difficulty: int = 0   # 0=Easy, 1=Normal, 2=Hard, 3=Expert

# TeamMember 数据结构（Dictionary）
{
    "character": CharacterData,       # 角色资源
    "current_hp": float,              # 当前 HP
    "equipment": {primary: WeaponData, secondary: WeaponData},
    "weapon_magazines": {item_id: int},
    "active_weapon_slot": "primary" | "secondary",
    "facing": int,                    # FaceDir 枚举
    "position": Vector2,              # 场景位置
    "healing_item": ItemData,
    "support_item": ItemData,
    "inventory": Array,
}
```

### 关键文件

| 文件 | 作用 |
|------|------|
| `script/character_data.gd` | 角色数据 + `weapon_walk_textures` + `allowed_primary/secondary_weapons` |
| `script/global.gd` | 队伍数组 + 队员切换方法 + 战役/难度 |
| `script/character_switch_manager.gd` | 核心切换逻辑（输入/精灵管理/死亡切换） |
| `script/player.gd` | `refresh_after_switch()` / `_try_switch_on_death()` / 武器纹理查找 |
| `script/teammate_standin.gd` | 队友站立精灵（纯 Node2D + Sprite2D） |
| `script/campaign_select.gd` | 战役选择界面 |
| `script/character_select_menu.gd` | 角色选择界面 |
| `script/difficulty_select.gd` | 难度选择界面 |

## 场景结构

```
scene
├── Player (当前操控)     ← CharacterBody2D，参与战斗
├── TeammateStandin_0     ← Node2D + Sprite2D，无敌（如果队员0是当前操控则此节点不存在）
├── TeammateStandin_1     ← 同上
└── CharacterSwitchManager ← Node，处理切换输入和精灵管理
```

## 菜单流程

```
标题画面 → 战役选择 → 角色选择(选1-3人) → 难度选择 → 加载安全屋场景
```

## 武器行走图区分

- CharacterData 新增 `weapon_walk_textures: Dictionary`
- 键 = 武器的 `weapon_state_name`（如 "Pistol", "Knife", "Rifle"）
- 值 = 该角色的武器行走图 Texture2D
- 渲染时先查角色字典 → 回退到 WeaponData.weapon_walk_texture

## 武器限制

- CharacterData 新增 `allowed_primary_weapons: Array[String]`（item_id 白名单）
- CharacterData 新增 `allowed_secondary_weapons: Array[String]`
- 空数组 = 所有武器可用
- 武器拾取时检查 `can_use_weapon()`

## 死亡与切换

- 当前角色 HP=0 时优先自动切换到下一存活队员
- 全部队员死亡才走正常死亡流程（黑屏→回安全屋）

## 与导演系统的关系

- 导演只追踪当前 Player
- 紧张度只基于当前角色计算
- 清除其他角色的尸体引用

## 存档

- 队伍所有成员的完整状态都存档
- 当前操控角色索引存下来
- `checkpoint` 包含完整的 team 深拷贝
- `SaveManager` 支持队伍序列化/反序列化

---

## 未来扩展（不做当前）

- NPC AI 队友（跟随+战斗+救起倒地玩家）
- L4D2 风格的角色特殊能力
