# 问题追踪

> 合并归档 `2026.8.21测出来的问题.txt` 与 `2026.8.22 23时00分 测出来的问题.txt`。
> 归档日期：2026-09-02 · 状态依据：代码现状 + git 提交史交叉核对。
>
> 图例：✅ 已闭环（有代码或提交证据） · ⚠️ 部分闭环 / 需复测确认 · ❌ 未处理

---

## 一、双端问题（2026-08-21 提出）

| # | 问题 | 状态 | 依据 |
|---|------|------|------|
| 1 | 无法换弹 | ✅ | 08-22 `feat(network): add authoritative reload and shove`；`script/player/PlayerReloadState.gd` 已支持 NORMAL / SHOTGUN 双模式，网络侧由 `network_world.gd:807` 权威转发 |
| 2 | 无法推击 | ✅ | 同上提交；`script/player/PlayerShoveState.gd` 已实现溅射击退（`shove_splash_radius`） |
| 3 | 投掷物没法扔出跟举起 | ✅ | 08-22 `feat(network): add throwables revives and weapon transitions`；08-27 `修复(联机): 客户端可拾取投掷物`；`PlayerThrowableState.gd` 已通 |
| 4 | 玩家死亡后没完善，救援系统没做完 | ✅ | 08-28 倒地救援与团灭恢复落地（HP=0 倒地 → 爬行 + 流血 20s → 队友长按 3s 救起 30% HP → 全员非站立黑屏重载本章） |
| 5 | 举起武器没过渡（不播放举起/放下动画） | ✅ | `weapon_data.gd` 已有 `weapon_raise_char_sequence` / `weapon_raise_frame_durations` / `raise_sound` / `lower_sound` 四个字段，`PlayerIdleState._try_raise_weapon()` 驱动 |
| 6 | 进安全屋时玩家列表显示不正确（2 人却显示 3 个） | ⚠️ | 08-21 `fix(network): persist loadouts and coordinate safehouse entry` 改动过 `player_registry.gd` / `chapter_summary.gd`，**但无复测记录，需重跑确认** |
| 7a | 固定朝向失效 | ✅ | `player.gd` 有 `_facing_locked`（96 行）及 `lock_facing()` / `unlock_facing()`；08-22 `fix(network): sync facing lock input` 修过联机同步；设置项见 `config.json` |
| 7b | 举起武器后按 X 会弹出菜单 | ⚠️ | 08-22 `fix(network): sync facing lock input` 与 `sync combat feedback and local menu behavior` 均触及 `menu_controller.gd`，**无复测记录** |
| 7c | 主机开菜单 → 客户端跟着暂停；客户端开菜单 → 主机仍能动 | ⚠️ | 同上。`menu_controller.gd` 最后一次改动停在 08-22，**联机菜单是否应暂停全局属设计决策，尚未定案**，建议明确后再回归 |

## 二、客户端问题（2026-08-21 提出）

| # | 问题 | 状态 | 依据 |
|---|------|------|------|
| 1 | 玩家及敌人受伤动画未显示 | ✅ | 08-22 `fix(network): restore client combat feedback`；`hit_feedback_mode` / `hit_feedback_duration` @export 已生效 |
| 2 | 总结界面待准备人数显示不正确 | ⚠️ | 08-21 `fix(network): coordinate transitions and chapter readiness` 改动过 `chapter_summary.gd`；现有 `ready_count` 统计逻辑（541–551 行）已重写，**无复测记录** |
| 3 | 移动有延迟感 | ✅ | 08-29 快照插值缓冲（玩家 50ms / 敌人 60ms）取代指数平滑 + 客户端本地预测（`network_local_prediction` 启用）+ 2s 可靠重同步软并流 |
| 4 | 玩家死亡后还能走、能穿墙、死亡行走图未切换 | ✅ | 08-22 `fix(network): stop client input after death`；`network_world._get_local_player_life_state()` 三态闸门 |

## 三、客户端问题（2026-08-22 提出）

| # | 问题 | 状态 | 依据 |
|---|------|------|------|
| 1 | 客户端玩家掉落物看不见 | ✅ | 08-23 `修复(联机): 稳定掉落物自动回归` |

## 四、单机问题

| # | 问题 | 状态 | 依据 |
|---|------|------|------|
| 1 | 可选人物胖虎消失 | ✅ | 08-22 `修复(角色): 统一单人与联机角色目录`；现有 `character_bigg.tres` / `character_nobita.tres` / `character_suneo.tres` 三个角色 |

## 五、设定建议

| # | 建议 | 状态 | 依据 |
|---|------|------|------|
| 1 | 取消友伤 | ✅ 已满足 | `bullet.gd:159` 子弹跳过 `player` 组目标；`PlayerKnifeAttackState.gd:185,210` 近战同样跳过 `player` 组。**当前无任何玩家间伤害路径**，也就没有"友伤开关"可做 |

---

## 待复测清单

以下 3 项有修复提交但**无复测记录**，建议在下一次联机回归时一并验证（可用 `tools/net_regression` 或 `--net-test` 系列 flag）：

1. 进入安全屋时玩家列表条目数是否正确（2 人局应显示 2 条）
2. 武器举起状态下按 X 是否仍会弹出菜单
3. 章节总结界面"等待所有玩家 N / M"计数是否与实际人数一致

## 新增建议（2026-09-02 评估时发现，非原清单内容）

| 问题 | 说明 |
|------|------|
| Esc 同时命中两个动作 | `project.godot` 中 `取消键` 与 `菜单键` 都绑定了 `Esc`(4194305)。按一次 Esc 会同时触发两个 `is_action_just_pressed`，依赖两个消费点的处理顺序，属于易碎设计 |
| A / S 一键多用 | `A`(65) 同时是 `装填键` 与 `投掷加格键`；`S`(83) 同时是 `推击键` 与 `投掷减格键`。当前靠**状态隔离**（枪械态 vs 投掷态）不冲突，但新增任何同时读取二者的状态都会产生 bug |
| 移动未绑定 WASD | 输入映射里 `左/右/上/下` 只绑了方向键；W 未绑定，A/S/D 已分配给装填/推击/技能。README 曾写"方向键 / WASD"，已修正 |
