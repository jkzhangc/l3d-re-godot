# 防守战功能（Holdout Battle）— 使用说明

> 实现日期：2026-09-02（含 7 项 bug 修正）
> 适用版本：Godot 4.6 / Jolt Physics / Mobile 渲染
> 关联脚本：
> - `script/director/holdout_machine.gd`（机器交互节点 + 阶段机 + 联机令牌校验 + 驱动 CombatHUD + 碰撞体）
> - `script/director/holdout_teleport_config.gd`（完成事件「添加传送点」的单条配置 Resource）
> - `script/director/teleport_point.gd`（传送点；已为「隐藏时不传送」增加可见性校验）
> - `script/ui/combat_hud.gd`（倒计时 UI 承载节点，复用关卡内已存在的 CombatHUD）
> - `object/holdout_machine.tscn`（机器预制体，已含 32×32 碰撞体）
> - `script/director/event_manager.gd`（剧本事件管理；防守战刷怪自动沿用尸潮节奏，且会强制锁定敌人追击最近玩家）
> - `script/director/director.gd`（敌人生成 / 倒计时查询 API）
> - `script/network_world.gd`（**服务端权威倒计时同步**：广播 / 中途加入补发 / 切图清理）
> - `script/enemy.gd`（`lock_forced_target` 目标锁定 API；防守战用它把丧尸锁定到最近玩家）

---

## 一、功能概述

场景里放一台"机器"（视觉用 VX Ace 行走图渲染，外观与已有武器掉落物一致）。
玩家靠近按 **确定键** 触发一场限时防守战：

1. **UI**：屏幕左上角（关卡内 `CombatHUD` 的 `HoldoutCountdown` 节点）显示倒计时。
   **倒计时的位置、字号、颜色、背板等布局全部在 `scene/ui/combat_hud.tscn` 里直接编辑**，
   所见即所得；标题/告警阈值/闪烁/是否显示"分:秒"等少量非排版参数挂在 `CombatHud` 节点的导出项上。
2. **敌人**：`Director → EventManager` 在倒计时（ACTIVE 阶段）内按 **SpawnManager 的尸潮节奏**
   刷怪（同屏上限 `max_active_common`、每批 `horde_batch_size`、间隔 `horde_batch_interval`）。
   刷出的丧尸**会被强制锁定「最近玩家」并直接追击**——无视视野/距离，且不会因丢失视野而放弃，
   直到防守战结束才统一解锁恢复普通 AI。
   - 单人模式：场上只有一个玩家 → 锁定唯一玩家。
   - 联机模式：每个丧尸锁定离自己**最近**的玩家（即"追击附近的玩家"）。
3. **三阶段**：准备（PREPARE）→ 进行（ACTIVE）→ 结算（SETTLE）→ 闲置（IDLE），
   阶段切换时标题与数字同步刷新；结算阶段只显示标题、不显示数字。
4. **倒计时归零 → 完成事件**（仅权威端执行一次，Client 通过可靠广播同步）：
   - 隐藏 `hide_node_paths` 里的节点（同步关掉它们身上的 CollisionShape2D / CollisionPolygon2D / TileMapLayer 碰撞）
   - 显示 `show_node_paths` 里的节点
   - 按 `teleport_points` 数组 **生成多个** 传送点（沿用项目原版 `TeleportPoint` 逻辑，可配置是否初始隐藏）
   - 可选地对这些 `notify_node_paths` 调用无参回调方法 `notify_method`
   - **解除全部被锁定的丧尸**（新刷出的 + 开局附近已有的）的目标锁定，敌人恢复普通 AI、回归常规导演节奏

---

## 二、配置方式（编辑器 / `.tscn`）

### 1) 把机器放进地图

1. 打开目标地图场景（例如 `scene/maps/突袭-第二关-学校内部.tscn`）。
2. **实例化** `object/holdout_machine.tscn` 到合适图层（建议放在地图装饰层）。
3. 在 Inspector 里配置（所有参数都有中文 tooltip，可在编辑器里直接看）：

| 分组 | 关键参数 | 说明 |
|------|----------|------|
| 外观（行走图） | `walk_texture` / `walk_char_index` / `walk_direction` | 与 TeleportPoint、SafeDoor、weapon_pickup 完全一致的精灵表渲染规则。`holdout_machine.gd` 已是 `@tool`，**行走图在编辑器里实时可见**（改贴图/角色索引/朝向/偏移即可见）；预制体已内置一张默认武器掉落贴图，拖入地图即可看到。 |
| 外观（行走图） | `step_frames` / `step_duration` / `animated` | 踏步动画；想完全静止就保留 `step_frames=[1]` + `animated=false` |
| 交互 | `interact_label` | 待机时浮在头顶的提示文字（如「启动机器 / 触发防守战」） |
| 交互 | `interact_label_running` | 防守进行中的提示文字（如「防守中…」），一般建议留空让 HUD 独占视觉 |
| 交互 | `interact_label_done` | 完成后且 `one_shot=true` 时的提示文字 |
| 交互 | `interact_range` | 玩家与机器距离 ≤ 此值（像素）时才能按键触发，默认 56 |
| 交互 | `one_shot` | true=只能触发一次（搭配 `hide_self_on_complete` 可以彻底消失） |
| 交互 | `auto_start_on_approach` | true=玩家进入范围即自动触发；false=按「确定键」 |
| 防守战 | `event_name` | 事件标识，用于日志 / 信号 / 完成节点命名，留空则取节点名 |
| 防守战 | `holdout_duration` | **进行阶段（ACTIVE）倒计时时长（秒）**，默认 60 |
| 防守战 | `prepare_duration` | **准备阶段（PREPARE）时长（秒）**，默认 3；设为 0 则直接进入进行 |
| 防守战 | `settle_duration` | **结算阶段（SETTLE）时长（秒）**，默认 3；仅显示「防守成功」，无数字 |
| 音乐 | `holdout_music` | 防守战期间播放的专属 BGM（留空=不播放），走 "Music" 总线 |
| 音乐 | `holdout_music_volume_db` | 该 BGM 音量（dB），默认 0 |
| 音乐 | `holdout_music_loop` | 曲长小于防守时长时自动重头播，默认 true |
| 音乐 | `stop_music_on_finish` | 防守战结束 / 中止时停止该 BGM，默认 true |
| 完成事件 · 节点显隐 | `hide_node_paths` / `show_node_paths` | 倒计时结束后隐藏/显示这些节点（NodePath） |
| 完成事件 · 节点显隐 | `disable_collision_on_hidden` | 隐藏时同步关掉它们身上的 CollisionShape2D / CollisionPolygon2D / TileMapLayer 的碰撞 |
| 完成事件 · 节点显隐 | `hide_self_on_complete` | 完成后是否隐藏机器自身 |
| 完成事件 · 添加传送点 | `teleport_points: Array[HoldoutTeleportConfig]` | **数组，可配置多个传送点**，一个条目 = 防守战结束后生成一个传送点。每条配置见下文 |
| 完成事件 · 自定义回调 | `notify_node_paths` / `notify_method` | 对这些节点调用无参方法 |

> 已移除的旧参数（不要再填）：`spawn_interval` / `spawn_per_wave` / `max_active` / `lock_enemies_on_players` /
> `hint_message` 以及所有 `hud_*` 倒计时外观参数。刷怪节奏现自动跟随 SpawnManager 尸潮参数；
> 倒计时外观改在 `combat_hud.tscn` 里直接编辑，机器不再持有任何 UI 参数。

**`HoldoutTeleportConfig`（teleport_points 数组的每一条）字段：**

| 字段 | 说明 |
|------|------|
| `enabled` | false=跳过本条目 |
| `target_scene` | 传送目标场景（必填，`*.tscn`） |
| `target_arrival_id` / `use_target_arrival_position` / `target_arrival_position` | 目标场景入口 |
| `capture_checkpoint` | 传送前是否记录 checkpoint，默认 true |
| `marker_path` | 优先用该节点位置作为传送点坐标 |
| `offset` | 未指定 marker 时，相对机器的偏移 |
| `walk_texture` / `walk_char_index` / `walk_direction` / `animated` | 传送点行走图外观（留空沿用机器外观） |
| `interact_label` / `interact_range` | 交互提示文字与范围 |
| `start_hidden` | 生成后先隐藏；**隐藏期间玩家靠近按确定键不会传送**（见 teleport_point.gd 可见性校验） |

### 2) 倒计时 UI 怎么调（重要）

倒计时节点是 `scene/ui/combat_hud.tscn` 内的持久子节点 `HoldoutCountdown`
（`PanelContainer → Margin → VBox → TitleLabel / TimeLabel(GradientLabel) / MessageLabel`）。
**调位置、字号、背板、内边距、字体，直接在编辑器里改这套节点树即可**，机器侧没有任何相关参数。
只有这几项是挂在 `CombatHud` 节点上的导出项（非排版，仍可在 Inspector 调）：

| CombatHud 导出项 | 说明 |
|------------------|------|
| `holdout_title_prepare` / `holdout_title_active` / `holdout_title_settle` | 三阶段标题文字（默认「准备防守」「防守剩余」「防守成功」） |
| `holdout_warning_threshold` | 剩余秒数 ≤ 此值时数字切到警戒色，默认 10；设为 0 关闭 |
| `holdout_warning_blink` | 警戒时 HUD 闪烁（0.55~1.0 alpha 脉动），默认 true |
| `holdout_show_minutes` | true="1:05"，false="65"，默认 true |

倒计时**数字**复用 TP 的 `GradientLabel + text_color.gdshader` 渲染（色表索引：正常=1 近白 / 警戒=10 红系，
硬编码于 `combat_hud.gd` 的 `HOLDOUT_COLOR_NORMAL` / `HOLDOUT_COLOR_WARNING`），与 TP 数字视觉完全一致。

### 3) NodePath 写法

`hide_node_paths`、`show_node_paths`、`notify_node_paths`、`teleport_points[].marker_path`
按以下顺序三级解析（兼容在预制体上填路径、实例化后失效的情况）：

1. 相对本机器（`"../大门"`）
2. 相对当前场景根（`"GroundLayer/大门"`）
3. 取路径最后一段当节点名，整棵场景树递归查找

### 4) 配置示例

在 `scene/maps/突袭-第二关-学校内部.tscn` 里加一台「发动机」：

- 节点 `HoldoutMachine`，位于 `(640, 320)`
- `walk_texture = [2K-VX]-cps_obj01.png`，`walk_char_index = 9`（"止"标志，第 3 列第 1 行）
- `holdout_duration = 90`
- 隐藏节点：`hide_node_paths = ["学校门口-铁栅栏"]`（完成后放行）
- 添加两个传送点：`teleport_points` 加两条
  - 条目 0：`target_scene = res://scene/maps/突袭-第二关-结尾安全屋-室内.tscn`，`marker_path = TeleportSpawnA`，`start_hidden = false`
  - 条目 1：`target_scene = res://scene/maps/突袭-第二关-天台.tscn`，`marker_path = TeleportSpawnB`，`start_hidden = true`（结束后才出现在天台）
- 倒计时样式：在 `combat_hud.tscn` 里把 `HoldoutCountdown` 拖到合适位置、按需改字号/背板

---

## 三、运行时行为细节

### 联机（服务端权威倒计时 + CombatHUD 承载）

> 本功能专门针对联机 Client 看不到倒计时 HUD 的问题做了重构。
> 倒计时数据由 **Host 权威计算**，通过 `NetworkWorld` 的可靠 RPC 下发，
> 倒计时 UI **复用关卡内已存在的 CombatHUD** 节点，不再自建 CanvasLayer，也不再挂在 PlayerController 上。

**同步方式（为何选 RPC 而非 MultiplayerSynchronizer）**

本项目的联机架构明确禁止 `MultiplayerSpawner` / `MultiplayerSynchronizer`
（见 `network_world.gd` 文件头注释），所有实体均由可靠 RPC 显式同步。
防守战倒计时因此沿用既有的 **`_broadcast_* → `_apply_*` + `@rpc("authority","call_remote","reliable")`** 三段式
（与安全门 `safe_door_ready_status` 完全一致），理由如下：

1. **权威单一**：倒计时只在 Host 这台机器上被 `_phase_timer` 推进，
   Client 绝不做本地计时，从根本上杜绝「本地计时漂移」——Client 显示的剩余时间永远来自
   Host 最后一包，两次权威包之间仅做本地插值平滑，误差不会累积。
2. **数据极小且低频**：每帧只广播 `(phase, remaining, total, token)` 四个字段；
   用 `_holdout_last_broadcast_msec` 限速到 **≈2Hz**，阶段切换（`force=true`）时立即补发，
   既保证 Client 与 Host 阶段切换一致，又避免每帧 RPC 噪声。
3. **复用成熟管线**：与 `safe_door` 同款模式，便于维护，也自然继承了它的
   中途加入补发、断线/切图清理等机制。

**数据流**

```
[Host] HoldoutMachine._tick_countdown()    ← 唯一权威计时（PREPARE→ACTIVE→SETTLE→IDLE）
        │  _drive_local_hud() → CombatHud.update_holdout()     （Host 本机 HUD）
        │  _broadcast_state()  → NetworkWorld.broadcast_holdout_state()
        │                          ├─ 存储 _holdout_state（供中途加入补发）
        │                          └─ holdout_state_sync.rpc(phase, remaining, total, token)  [≈2Hz / 阶段切换立即]
        ▼
[Client] NetworkWorld.holdout_state_sync (RPC)
            → _apply_holdout_state() → HoldoutMachine.apply_remote_holdout_state()
                → CombatHud.update_holdout()                  （Client 本机 HUD）
```

> 倒计时外观**不存在网络传输**：位置/字号/颜色/背板都在 `combat_hud.tscn` 里编辑，
> Host 与 Client 同一 `.tscn` 实例天然一致；网络只传 4 个字段 `(phase, remaining, total, token)`。

**监听服务器 / 单机行为不变**

- 单机模式不创建 `NetworkWorld`（`game_init.gd` 仅在 `is_online_session()` 时建），
  机器直接 `CombatHud` 驱动本地 HUD，`_broadcast_state()` 为空操作。
- 监听服务器（Host）既是权威也自己显示，逻辑与单机一致，仅额外把状态广播给 Client。

**边界情况处理**

| 场景 | 处理方式 |
|------|----------|
| 中途加入（late-join） | Host 在 `_accept_ready_peer` 中若 `_holdout_state` 非空，立即对加入者 `rpc_id` 补发当前 `phase/remaining/total/token`，新玩家马上看到正确剩余时间 |
| 断线 / 重连 | 旧场景 NetworkWorld 随切图释放；重连进入新场景时，新 NetworkWorld 的 `_holdout_state` 为空，旧在途包被 `_scene_transitioning` 守卫丢弃，不会弹出幽灵 HUD |
| 倒计时结束 | Host `_finish_holdout()` 广播 `phase=0`，所有 Client `CombatHud.hide_holdout()` 隐藏并复位 |
| 提前中止 | 机器 `abort()`（外部调用或节点被移除 `_exit_tree` 触发）会广播 `phase=0` 并停止 BGM，Client 同步隐藏 |
| 旧场景残包 / 重复叠加 | 每个防守战实例在 Host 生成随机 `_net_token`；Client 用 token 校验收到的包，token 不符的过期包一律忽略；收到 `phase=0` 后置 `-1` 结束哨兵，之后任何迟到包都被忽略。HUD 用 `CombatHud` 的持久子节点，显示幂等，不会重复叠加 |

**与 CombatHUD 的布局兼容**

- 倒计时区块是 `CombatHud`（CanvasLayer，`layer=10`）的**持久子节点** `HoldoutCountdown`
  （`PanelContainer → Margin → VBox → 标题/数字/提示 三节点`），在 `scene/ui/combat_hud.tscn` 里就有，
  编辑器里直接可见、可拖动定位；默认 `visible=false`，运行时由 `show_holdout()` 显示。
  它与 HP 框、TP 数字、武器图标是**兄弟关系**，不遮挡血量 / 波次等元素，
  且随地图内 CombatHUD 在换图时一起释放，无悬空 UI 层。

- **倒计时数字复用 TP 的字体着色器**：`TimeLabel` 是一个 `GradientLabel`，与 HUD 里的 `TPLabel`
  共用同一套渲染管线——`text_color.gdshader` 材质 + 色表 `Text color, 20 types.png` 的纵向渐变着色，
  再加相同的阴影/描边参数。因此数字呈现的视觉样式（材质、渐变、描边、阴影）与 TP 显示**完全一致**。
  取色通过色表索引：正常 `1`（与 TP 同款近白）/ 警戒 `10`（红系），硬编码在 `combat_hud.gd`。
  标题与提示语仍是普通 `Label`（沿用主题默认色）。

### 刷怪与敌人行为（重要变更）

- 防守战刷怪**不再使用机器自己的刷怪参数**，而是 `EventManager` 在 CRESCENDO 剧本事件中
  **自动沿用 SpawnManager 的尸潮节奏**（`max_active_common` / `horde_batch_size` / `horde_batch_interval`）。
  要调节奏，直接改关卡里 Director/SpawnManager 的尸潮参数即可——「防守战 = 一场持续的尸潮」。
- 刷出的丧尸**会被强制锁定「最近玩家」并直接追击**（`target_lock=true`）：
  无视视野/距离，锁定期间不会因丢失视野而放弃；敌方 `lock_forced_target` 由 `EventManager` 在刷怪时调用。
  - 单人 = 锁定唯一玩家；联机 = 每个丧尸锁定离自己最近的玩家。
- 此外，防守战**开局**会把机器 `enemy_lock_radius`（默认 640 像素）半径内、开局前就已存在的丧尸
  也一并强制锁定追击最近玩家，让"附近已刷出来的丧尸"一开局就扑向玩家（与新刷的丧尸行为一致）。
- 「完成事件」会解除全部被锁定的丧尸（新刷出的 + 开局附近的）的目标锁定，敌人恢复普通 AI、回归常规导演节奏。

### 传送点的「隐藏时不传送」

- `teleport_points` 条目可设 `start_hidden=true`：生成后节点 `visible=false`。
- `teleport_point.gd` 的 `_input()` 已增加 `is_visible_in_tree()` 校验：
  **隐藏中的传送点即便玩家在范围内按确定键也不会传送**，直到它被显式设为可见（如完成事件 `show_node_paths` 显示，或开发者在别处显示）。

### 提前终止（可选 API）

```gdscript
Director.abort_scripted_event()
```

会立刻结束当前剧本事件，照常触发完成事件、停止 BGM。

---

## 四、与已有雏形（ScriptedEventTrigger）的关系

| 维度 | `ScriptedEventTrigger`（旧） | `HoldoutMachine`（本功能） |
|------|-------------------------------|----------------------------|
| 触发方式 | 玩家踩进 Area2D **自动触发** | 玩家靠近后**按键触发**（也可 `auto_start_on_approach` 自动） |
| 视觉表现 | 仅 Area2D 调试矩形 + HintLabel | **完整的行走图实体**（@tool 编辑器可见）+ 踏步动画 + HintLabel + 32×32 碰撞体 |
| 目标锁定 | 无（沿用普通视野发现） | 有（强制锁定最近玩家：刷出的 + 开局附近已有的都直接追击，结束统一解锁） |
| 倒计时 UI | 无 | 复用关卡内 CombatHUD 的 `HoldoutCountdown` 节点（位置/字号/颜色/背板在场景里直接改） |
| 完成事件 | 仅触发器自身 `monitoring=false` | **隐藏节点 / 显示节点 / 生成多个传送点（数组）/ 自定义回调** 全部可配置 |
| 复用管线 | 是（`Director → EventManager → ScriptedEventTrigger`） | 是（`Director → EventManager ← HoldoutMachine`） |

> 两者共存不冲突：旧触发器仍是「踩到就触发」的批量方案，新机器适合「主动开启一场战斗」的设计需求。

---

## 五、已知限制

1. **完成事件 → 添加传送点（联机）**：`TeleportPoint` 由 **Host 权威创建**，
   Client 不会重复生成（避免多实例）。Client 通过 `holdout_completed` 可靠广播执行
   节点显隐 / 自定义回调，使门/墙等场景元素在两端的可见性与碰撞保持一致；
   但传送点本身仍走项目既有的 NetworkWorld 切图协议，不依赖本功能复制节点。

2. **调试可视化**：开启 `Global.debug_visuals` 时，机器会画一个橙/绿圆环显示交互范围。
   这是项目统一的调试可视化约定，与 player.gd 里的碰撞体红框、敌人 A* 路径等一致。

3. **极端延迟下的迟到包**：理论上若中途加入补发包在防守战结束后才到达，会被 `-1` 结束哨兵
   忽略（不会误显 HUD）；若该补发包携带的是「下一场新防守战」的全新 token，则正常放行。
   本设计在 LAN 联机延迟下不会出现可感知的异常。

---

## 六、文件清单（本次新增/修改）

> 注：`script/ui/holdout_countdown_hud.gd`（独立 HUD）已废弃删除，倒计时 UI 改为复用 `combat_hud.gd`。

| 类型 | 路径 | 说明 |
|------|------|------|
| 新增 | `script/director/holdout_machine.gd` | 机器节点主脚本（阶段机 + 联机令牌校验 + 驱动 CombatHUD + 32×32 碰撞体 + 专属 BGM + 多传送点） |
| 新增 | `script/director/holdout_teleport_config.gd` | 完成事件「添加传送点」单条配置 Resource（`Array` 元素） |
| 修改 | `script/ui/combat_hud.gd` | 新增「防守战倒计时」区块：`show_holdout` / `update_holdout` / `hide_holdout` / `find_in_scene`；外观全部走场景节点，无 UI 配置参数 |
| 修改 | `script/director/teleport_point.gd` | `_input()` 增加 `is_visible_in_tree()` 校验：隐藏时不传送 |
| 修改 | `script/director/event_manager.gd` | CRESCENDO 刷怪默认沿用 SpawnManager 尸潮节奏（不再用硬编码 12/3/3）；`target_lock=true` 时每批新刷出的丧尸锁定「最近玩家」直接追击；新增 `_lock_nearby_enemies` 在开局把机器半径内已存在的丧尸也锁定，结束统一解锁 |
| 修改 | `object/holdout_machine.tscn` | 机器预制体，新增 32×32 `StaticBody2D` + `CollisionShape2D` |
| 修改 | `script/network_world.gd` | 新增 `broadcast_holdout_state` / `holdout_state_sync` / `broadcast_holdout_completed` 及中途加入补发、切图清理 |
| 修改 | `script/director/director.gd` | 新增 `spawn_horde_nodes()` / 倒计时查询 API |
| 修改 | `script/enemy.gd` | 新增 `lock_forced_target` / `release_forced_target` / `_maintain_forced_target`（防守战通过 `EventManager` 调用以锁定最近玩家） |
| 新增 | `防守战功能说明.md` | 本文档 |
