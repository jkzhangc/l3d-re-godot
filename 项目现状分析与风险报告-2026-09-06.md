# L3D 重制项目（l3d-re-godot）现状分析与风险报告

> 日期：2026-09-06
> 分析对象：`l3d-re-godot`（本仓，Godot 4.6.3）+ 同级目录工程核查（`Nobihaza L3D RE` / `L3D RE`）
> 方法：本仓代码/场景实测（Godot 4.6.3 headless + Godot AI MCP 编辑器诊断 + 实机运行）、git 历史与工作区状态核查、根目录 40+ 文档交叉核对、`memory/` 与 `.workbuddy/` 工作日志追溯
> 结论摘要：**技术骨架与素材积累扎实，但"制作治理"层面的三线工程并行、决策漂移、重做工程零版本控制是最优先风险；内容量与代码债务是第二梯队。本仓的角色定位（继续开发 or 证据源冻结）必须立即拍板。**

---

## 0. 一句话结论

旧版（本仓）承载了约 2.36 万行可运行代码、700+ 美术/音频资产、已审计的联机安全模型与还原管线工具，是**唯一可玩的版本**；但 9/3 用户确认"全面重做"后已先后启动**两个互不隶属的重做工程**（代码先行 `Nobihaza L3D RE` → 设计先行 `L3D RE`），且本仓仍在被编辑器打开、继续编辑并积累未提交成果——**三线并行、无统一决策记录、两个重做工程均无 git**。这比任何代码债务都更危险。

---

## 1. 现状盘点

### 1.1 三工程格局（最大背景事实，务必先看清）

| 工程 | 路径 | 引擎 | 规模（实测） | 版本控制 | 角色/状态 |
|---|---|---|---|---|---|
| **旧版（本仓）** | `l3d-re-godot` | Godot 4.6.3 | 95 `.gd` / 23,631 行；27+ 场景；762 PNG、700+ 音频；40+ 根文档 | ✅ git（3 分支 + GitHub remote `jkzhangc/l3d-re-godot`） | 唯一可玩构建；资产/数据/联机安全模型/还原管线的来源 |
| **重做 #1** | `Nobihaza L3D RE` | ~4.6（自带 project.godot） | 90 `.gd` / 8,849 行；8 场景 | ❌ **无 .git** | 代码先行重做，9/3–9/5 推进 R0→R18（harness 全绿记录于工作日志） |
| **重做 #2（strict）** | `L3D RE` | 声明 4.7.2 | 设计层：26 系统索引，12 Designed / 1 Draft / 其余未开始；**实现 0%**（仅 1 个 .gd） | ❌ **无 .git** | 设计先行 + 门禁审批；`adoption-plan-2026-09-04.md` 明确：**legacy 不作实现基线，只作设计/回归/资产证据源，不复制其实现** |

关键事实：
- 两次重做先后启动且与旧版在 9/3–9/6 时间重叠；重做 #2 的 adoption plan（9/4）与旧版文档声明的"当前焦点=第一章贯通 M0–M6"**直接冲突**。
- 三个工程的 `memory/`、`.workbuddy/`、`.zcode/` 日志**跨目录错放**（9/3–9/5 的重做 #1 日志写在旧版 `.workbuddy/` 下）。
- 本机只装了 Godot 4.1.3 / 4.6.3，**没有 4.7.2** → 重做 #2 目前在本机无法运行任何实现。

### 1.2 本仓代码现状（实测数据）

- 代码规模 top：`network_world.gd` 4191 / `player.gd` 1375 / `enemy.gd` 1022 / `EnemyChaseState.gd` 980 / `director.gd` 828 / `holdout_machine.gd` 647 行。
- Autoload 7 个：Global / Director / PhantomCameraManager / Players / Net / ChapterStats / **`_mcp_game_helper`**（godot_ai 插件的 MCP 运行时助手，随游戏常驻 autoload）。
- 完成度（复核《项目进度评估报告-2026-09-02》后认可）：加权 ≈65%（相对单人可玩 Demo）/ ≈40%（相对完整 L3D 体验）。
- git：HEAD 在 `Branch_34b342a2`（领先 main 90 / 落后 50；main 停在 8 月 merge，另有 `Branch_80fe8fa6`）。9/3 的"M0 止血"只完成**大二进制移出 + 部分提交**，未合并回 main、未清理旧分支。
- 工作区未提交：**8 修改 + 8 删除 + 9 未跟踪**——全部是 9/3 的防守战/流场/通道闸门/学校内部玩法化改动 + 3 份新分析文档；删除项是 `tools/TileConverter/src/*.py` 整目录。
- 未使用素材/ 仍被 git 跟踪 1348 个文件；`engine-reference/` 入库 3890 个文件。

### 1.3 内容现状（实测）

| 项 | 实测 |
|---|---|
| 战役登记 | `campaign_assault.tres` 已登记 5 图（3 第一关 + 学校门口 + 学校内部） |
| 第一关三图 | 完整玩法节点（GameInit/DirectorConfig/12 SpawnZone/安全门链路） |
| 学校门口 | 仅 TileMap + 2 TeleportPoint + DirectorConfig，**无刷怪/无安全门/空图** |
| 学校内部 | 9/3 起已加 PlayerSpawn/SpawnZone/HoldoutMachine/DirectorConfig（**未提交**），但**碰撞口径下几乎全图不可走**（见 §2） |
| `maps_auto/` 5 张还原图 | 纯 TileMap，**零玩法节点** |
| 敌人种类 | 仅 `enemy.tscn` 一种；Hunter/女巫/Tyrant/Crimson 全缺 |
| 战斗支柱 | 见切/反击/背刺/Guts：全部 `.gd` **零命中**，`PlayerMikiriState` 不存在 |
| 技能 | SkillData 仍为占位（cooldown 未计时、SA 空壳） |
| 导演 Phase 4 | 未开始 |

### 1.4 资产与工具（正面资产）

- `art/` 762 PNG、`sound/` 700+、`music/`、VX 动画素材体系完整；数据驱动 `.tres`（武器/角色/技能/战役）已就绪。
- 工具链可用：`tools/rm2k3_map_restore`（RM 地图→JSON/atlas/TileMap）、`tools/net_regression.py`（10 场景一键联机回归）、TileConverter、A4 转换器。
- 联机层纪律是亮点：17 处 `any_peer` RPC 全部做发送者校验 + 资源白名单，Host 权威架构已通过多轮双端回归。

---

## 2. 本轮独立验证结果（2026-09-06）

### 2.1 健康检查

- 全项目 headless `--quit` **exit 0**（需重定向 `user://`，即 `APPDATA` 指向可写目录，否则引擎启动崩溃——属运行环境问题，非项目问题）。
- `test.tscn`、`第一关-街道`、`学校内部` headless 实跑 **exit 0，运行时无脚本错误**（含未提交的 `flow_field.gd`/`passage_gates.gd`/`holdout_machine.gd` 全部改动）。
- 编辑器 MCP 诊断（会话 `l3d-re-godot@a1d3`，Godot 4.6.3 / godot_ai 3.2.2）：当前场景即学校内部；**未提交脚本在编辑器内干净解析**，无 Parse Error。编辑器日志噪声：`gradient_label.gd:290` 用绝对路径 `get_node()`（编辑器加载时报错 ×N）、色表 PNG 以 Image 方式 `load()`（**"won't work on export"** 警告）、`campaign_select.tscn` 两处 ext_resource UID 失效回退、`spawn_zone.gd` draw_rect 冗余参数。
- 实机运行学校内部（project_run current，autosave=false）：玩家/敌人/刷怪/防守战机器正常装配，运行 11s+ 无脚本错误。

### 2.2 关键实测发现：A* / 可行走性口径把地图判成"几乎全图墙"

| 场景 | A* cells | 障碍物比率 | 判定 |
|---|---|---|---|
| `test.tscn`（手工验证图） | 874 | **4.7%** | 正常（仅墙体阻挡） |
| `第一关-街道`（当前唯一可玩关） | 31,897 | **89.0%** | 建筑占地主体、道路约 11%——道路网络可行走性需连通性核查，非必然错误 |
| `学校内部`（9/3 编辑对象） | 65,704 | **102.4%** | **明确异常：全图判为不可走（障碍计数超 100% 系多源叠加计数）** |

判定口径（`EnemyChaseState._is_walkable`，行 804-871）：某格须"有 ground/floor 图块 **且** 无 ground 碰撞多边形 **且** 无 upper/decor 碰撞多边形"才可行走。实测机制（已核对引用资源）：
- `街道` GroundLayer=`tres/街道A5.tres`（地面几乎无物理），碰撞全在 `街道B.tres`（上层装饰，794 处物理）→ 建筑物=阻挡、道路=可走，A* 大致可用（与既有街道联机回归通过一致）。
- **`学校内部` GroundLayer=`tres/Map0142_lower_tileset.tres`：atlas 几乎每个图块都带物理多边形（314 行资源内 131 处物理/129 组点）** → 地面每格都判 `ground_block=true` → 全图不可走 → A*、流场、通道闸门全部拿不到路径，敌人只剩直冲/滑墙降级——"丧尸围尸/卡死/堵路"在**地图数据层**的真正根因；9/3 的流场修复**无法覆盖**此问题（流场与 A* 共享同一份可行走缓存）。
- 与还原图"无物理"（空 physics 警告）相反，本图是"地面全有物理"——两种失败的同一根源：**可行走性不应由图块物理多边形推断**，这正是重做路线改用"RM 地图 JSON → 图块 ID 规则 + design 墙覆盖"的原因。

另外：`[Global] debug=true` 默认开启（`[Global] debug = true` 日志），A* 构建期每格调试打印量大；`Director` 报 "found 12 spawn nodes / valid spawns: 1" 属距离过滤的正常现象，不是 bug。

### 2.3 工作区与库卫生

- `.gitignore` 本身以 **GBK 编码写入**（git 查看注释乱码）；`.gitattributes` 声明 `eol=lf` 但工作区大量文件为 CRLF（每次 diff 提示将被转换）→ 行尾规范化未真正生效，全仓换行未归一。
- 根目录残留 6 份联机控制台 `.txt`、`scene/` 下 4 个 `.tscn.*.tmp` 崩溃残留；`object/enemy.tscn647636915.tmp` 等未清理。
- 上级目录还存在 `NUL` 文件、`L3D RE.zip`、重复拷贝的重做规划文档；重做工程文档中多处路径/标点 mojibake（GBK/UTF-8 混用）。

---

## 3. 制作风险判断（按优先级）

### 🔴 S 级 — 治理 / 战略（决定生死，先于一切代码问题）

- **S1 三线工程并行、方向互相覆盖、无人能回答"现在到底在做哪个"**：旧版文档说"焦点=第一章 M0–M6"；重做 #2 的 adoption plan 说旧版仅证据源、实现另起炉灶（不复制实现）；重做 #1 已完成到 R18 却无 git、无明确去向。9/2–9/6 五次"定稿/再推翻/换工程"，任何一次反复都在烧掉数天成果且**无书面决策记录**。
- **S2 两个重做工程零版本控制**：`Nobihaza L3D RE` 8,849 行实现 + `L3D RE` 全部设计稿（26 系统 GDD/评审日志/AD/索引）都只有磁盘一份。误删/误改即永久丢失；设计稿是 9/4–9/6 的逐节审批成果，丢失成本不可估。
- **S3 旧版 9/3 成果悬空 + 今日仍在继续编辑**：防守战/流场/通道闸门/学校内部玩法化全未提交；按 adoption plan 它们大概率不进入新工程——但若旧版被当作"唯一可玩原型"继续用，这些改动又有价值。**角色未定 = 每继续编辑一天都在制造新的悬空资产。**
- **S4 引擎/工具分裂**：旧版 4.6.3、重做 #2 声明 4.7.2（本机未装，无法运行其实现）、重做 #1 ~4.6；`engine-reference` 只锁 4.6。三线各自为政后，API 知识与素材约定将快速分叉。

### 🟠 A 级 — 工程 / 代码（本仓，多数已被债务分析覆盖，复核一致）

- **A1 god-object**：`network_world.gd` 4191 行（约 1/4 为内嵌 `--net-test` 测试脚手架）、`player.gd` 1375 行双模式分叉、`EnemyChaseState.gd` 980 行内含整个导航子系统。→ 若进重做，此债不必在本仓偿还，但须记录"已验证的协议/安全模型"供移植。
- **A2 内容扩展零抽象**：敌种仅 1 个场景、无 Archetype；`CharacterData.base_speed/critical_rate` 声明后**零引用**（暴击量纲 0–1 vs 0–100 冲突）；战斗支柱（见切/背刺/Guts）完全缺失。
- **A3 可行走性口径（§2.2）**：连"可玩的街道关"障碍率都 89%——寻路数据源必须收敛为单一权威（如重做方案的 `WalkabilityProvider` + 地图 ID 规则），而不是按图层名/物理多边形做启发式。
- **A4 工程卫生**：编码（GBK/UTF-8）、行尾（CRLF/LF）、UID 失效、`.tmp` 残留、`未使用素材` 1348 文件仍入库、调试默认开（`debug=true`）、`_mcp_game_helper` 作为生产 autoload 常驻。
- **A5 自动化护栏缺失**：headless 冒烟不 gate；`net_regression.py` 无 CI/钩子；新版 harness 全部在无版本控制工程里。

### 🟡 C 级 — 内容 / 产品（决定"能不能兑现 L3D 体验"）

- **C1 内容量仍是最短板**：1 敌种、无特感编排、第二章两图空或半空；L3D 阵容（Crimson→Hunter 系→女巫→Tyrant）与 Rush 三型尚未在任何工程落地。
- **C2 学校内部**（正在编辑的图）：当前碰撞口径下几乎全图不可走，直接做战役等于让敌人在"假墙"里追玩家。
- **C3 联机验收后置**：旧版多人"主干通、长流程未验收"；重做 #2 明确 SYS-24（多人）排 R6、不阻塞首块单机战斗切片——若玩家/协作者期望"联机优先"，需要显式对齐预期。

---

## 4. 整改方向与建议

### 4.1 先回答一个决策（一切整改的前提）

**Q1：三线归一，以哪条线为"当前作品"？**

| 选项 | 含义 | 代价 |
|---|---|---|
| **A. 以 L3D RE（strict）为最终工程**（推荐方向，采纳其 adoption plan） | 旧版与重做 #1 都降级为证据/实验源；继续 GDD→ADR→实现的门禁流程 | 旧版与 #1 的新玩法改动停止；近期无新增可玩内容（设计先行天然慢） |
| B. 以 Nobihaza L3D RE（代码重做）为最终工程 | 它已跑通 R0–R18 单机闭环（Mikiri/敌人/导演/存档/联机域系统），把 strict 的设计结果作为规范喂给它 | 需补齐它缺失的设计文档与版本控制；strict 的 GDD 与其实现需合并核对 |
| C. 回到旧版继续（重做作废或无限期搁置） | 立即有可玩迭代 | 与 9/3 已拍板的"全面重做"矛盾，且代码债务会在每轮功能叠加中复利 |

> 无论选 A/B/C：**都不允许再保持当前"三线同时动"状态**；被放弃的线必须书面标记"已冻结 + 冻结原因 + 可复用资产清单"。

### 4.2 治理整改（无论哪条路线，1–2 天内完成，零代码风险）

1. **两个重做工程立刻 `git init` + 首次提交**：`Nobihaza L3D RE` 按 R 阶段拆 2–3 个 commit（8.8k 行实现 + harness 一次入库）；`L3D RE` 设计稿按 design/architecture/registry 目录提交。误删即不可逆的窗口越早关越好。
2. **旧版工作区立即收尾提交**（拆分 2 个 commit：① `tools/TileConverter` 删除 + 文档三件套；② 防守战/流场/通道闸门/学校内部玩法化），并**在提交信息与 memory 中标注"证据源归档 / 不并入重做实现"**。切勿 `git clean -fd` / `checkout .`。
3. **建立唯一"项目决策日志"**（建议放 `L3D RE/docs/decision-log.md` 或目标工程 README 决策段）：补记 9/2 L3D 混合定稿、9/3 全面重做、9/4 adoption、本次三线归一；此后一切"当前焦点/里程碑"只在此处维护，各工程 README 只留链接。
4. **收敛记忆目录**：9/3–9/5 分散在旧版 `.workbuddy/` 的重做日志搬迁/汇总到目标工程自己的 memory；约定每个 AI 工具只写自己所在工程的 memory。
5. **旧版 40+ 文档归档**：保留 ≤5 份活文档（设计总纲、当前决策日志、目录索引），其余文件头加 `ARCHIVED(2026-09-06)` 或移入 `docs/archive/`，从源头消灭文档漂移。

### 4.3 工程整改（目标工程落地时适用）

6. **内容/碰撞管线先行且数据驱动**：走已验证的"RM 地图 JSON → ID 规则（D/E 组）+ design 覆盖墙"路线（重做 #1 的 R7/R8/R12 已实证：真图 99–100% 可走、贪心碰撞合并 205× 压缩、装配 <500ms），不再用图层名字串/物理多边形做启发式判定。
7. **护栏前置**：headless 冒烟 + 每阶段 harness 进入 git hooks / 一键脚本（对标 `tools/net_regression.py` 与重做 #1 的 R1–R18 回归模式）；把"第一章验收清单"固化为可执行命令（单人户外→街道→校门→校内→出口，无空图/卡死）。
8. **敌人按 design 顺序**：SYS-12（敌原型与攻击合同）→ Crimson Head 参数变体 + 单发型 Rush → 再谈 Hunter 系/女巫/Tyrant；每类敌人自带可 headless 验证的合同测试。
9. **文档/编码卫生全局修复**：全部 `.md/.gitignore/.gd` 统一 UTF-8 + LF（`git add --renormalize` 一次完成）；安装 4.7.2 或把目标引擎明确写为 4.6.3；清根目录控制台 txt/`*.tmp`/`未使用素材`入库项。

### 4.4 若短期仍要在本仓产出（仅在 Q1 选 C 时）

- P0：提交止血 → 修学校内部/街道可行走性口径（统一 ground 无物理 + upper 墙物理约定，或切到 ID 规则）→ 用 `tools/net_regression.py` 回归。
- P1：`[Global] debug=true` 默认关闭并收敛 A* DEBUG 输出；存档 CRC/`.bak`；Esc/A/S 键位拆分；`gradient_label` 绝对路径 get_node 与"以 Image 加载色表"（export 必炸项）。
- P2：god-object 拆分等——仅在确认"本仓继续长期开发"后才投入。

---

## 5. 待拍板决策清单

- **Q1** 三线归一选 A（strict 设计线）/ B（代码重做线）/ C（旧版线）？（推荐：若认可 9/3 重做决定，选 A，并把 B 的已验证实现作为"可行原型"参考；若想最快有可玩版，选 B）
- **Q2** 旧版未来 1 周角色：冻结归档（推荐）还是继续当唯一可玩原型直到 strict 有垂直切片？
- **Q3** 本仓 9/3 学校内部/防守战成果：提交归档（推荐，不丢成果）还是直接废弃？
- **Q4** 是否由我执行"止血动作"：旧版收尾提交 + 两个重做工程 git init/首次提交 + 决策日志建立？
- **Q5** 是否由我把本仓 40+ 文档收敛归档，仅保留活文档？

---

## 附录：本报告验证命令与原始数据

```powershell
# headless 冒烟（需先重定向 APPDATA 到可写目录，避免 user:// 崩溃）
$env:APPDATA="<workspace>\tmp_appdata"
& "D:\Godot_v4.6.3-stable_win64.exe\Godot_v4.6.3-stable_win64_console.exe" --headless --path . --quit
# 逐图实跑（exit 0 = 无脚本/加载错误）
& "...exe" --headless --path . --quit-after 200 "res://scene/maps/突袭-第二关-学校内部.tscn"
```

| 场景 | cells | 障碍物 | 比率 |
|---|---|---|---|
| test | 874 | 41 | 4.7% |
| 第一关-街道 | 31,897 | 28,396 | 89.0% |
| 学校内部 | 65,704 | 67,249 | 102.4% |

数据佐证：95 `.gd`/23,631 行；7 autoload；git 分支 Branch_34b342a2（↑90/↓50 vs main）；工作区 8M/8D/9??；`未使用素材` 入库 1348、`engine-reference` 入库 3890；同级 `Nobihaza L3D RE` 90 `.gd`/8,849 行（无 git）、`L3D RE` 26 系统 12 Designed/0 实现（无 git）。

---

# 6. 决策分析（续 2026-09-06）：原地 strangler 重构 vs 另起炉灶重做

> 触发：用户提出"继续做旧版，但把代码按重做项目方向整理一遍会不会更好"。本分析基于三工程实测事实给出。

## 6.1 先厘清用户提案的真实结构

该提案其实是三个子决策的捆绑：
1. **产品线归一**：回旧版（legacy）为唯一代码库，不再等 strict 重做独立成型；
2. **施工图复用**：以 `L3D RE`(strict) 已定稿的 **15 份 GDD + 13 份评审日志 + ADR/TR 门禁** 为改造目标契约（SYS-01~13 已 Designed，边界规则已写明），不发明第四套架构；
3. **参照实现复用**：以 `Nobihaza L3D RE` 已验证的**分层实现为移植源**，而非从零重写——实测其已含全部关键件：`PlayerIntent/PlayerInput/Damageable/WalkabilityProvider(3 实现)/NavigationService/敌人 FSM(含 Mikiri 对接)/PlayerMikiriState/NetReplicator/SessionHealth/DomainContext/SaveManager/ChapterFlow/MapLevel(+design)`，合计 core 900 + gameplay 4,147 + network 604 + managers 201 + presentation 926 ≈ **6.8k 行 + 21 个回归 harness**。

本质：从"第三次从零重写"切换成"**单库 + 契约驱动的 strangler 重构 + 参照移植**"。

## 6.2 两方案对比

| 维度 | 维持 strict 独立重做（现状方向） | 回旧版原地 strangler 重构（用户提案，按 6.1 执行） |
|---|---|---|
| 首个可玩产物 | 实现 0%，最快还要数周（敌人链 SYS-12/14 后才谈得上关卡） | **已有**（第一关 3 图可玩）；第一步即可继续玩 |
| 每次改动可验证 | GDD 层可测；代码层无（尚未有实现） | 既有 headless + `net_regression` 10 场景当门禁，每步可跑 |
| 设计资产利用 | 本就在消费 | 同样消费（15 GDD 作契约）+ 还能利用 13 份评审日志避坑 |
| 已验证实现利用 | Nobihaza 的 6.8k 行被搁置（设计先行把它冻结在"原型"） | 直接作为移植源（Mikiri/意图层/导航/存档/联机域件都在） |
| 联机验收排期 | SYS-24 在 R6，多人遥远 | legacy 已有人能玩的 Host 权威联机，可边重构边保回归 |
| 架构纯净度上限 | 最高（从零、无旧债） | 中高（若不加门禁，会"整理成又一个旧版"） |
| 最大执行风险 | 第三次无产物窗口 → 单人动机枯竭；引擎 4.7.2 未装 | **god-object 拆到一半**：network_world(4,191)/player(1,375) 原地拆比新写更难；"整理一遍"若无契约=白整理（有前科） |
| 相对成本（依据 9/3 技术债分析） | 2–4× 债务清理成本 | ≈20–35% 债务清理成本（每步可独立验证）；network 单文件例外，见 6.4 |

## 6.3 结论与推荐

**推荐采纳用户方向，但有一个关键升级**：不是"把代码整理一遍"（无目标契约的整理历史上已证明会漂移——如旧的 Global 重构方案反复），而是执行 **"以 L3D RE GDD 为施工图、以 Nobihaza 实现为答案册、以 net_regression+headless 为门禁"的 strangler 重构**。三个前提必须同时成立：

- **P1 单产品库**：legacy 收尾提交后成为唯一演进库；`Nobihaza RE` 降级为**只读参照实现库**（冻结，不再演进，防第四套分叉）；`L3D RE` 降级为**设计权威**（只写 GDD/ADR，不写实现）。三者各司其职、只有一份代码在动。
- **P2 引擎锁定 4.6.3**（本机只有 4.1.3/4.6.3；4.7.2 未装，也非必需——strict 的 GDD 引擎兼容段可注明 4.6.3 基线）。
- **P3 硬约束写进 legacy 的 AGENTS.md 并强制**：单文件 ≤500 行、禁 `find_child("字符串")`/`get("_private")`、单例仅无状态服务、同步策略单一来源、测试不进生产文件——把重做 #2 的六条硬约束原样带过来，否则改造会原地复发旧模式。

## 6.4 子系统级施工对照（"按重做方向整理"的可执行解读）

| 阶段 | 目标契约（L3D RE SYS） | 参照实现（Nobihaza） | legacy 落点 | 门禁/完成定义 | 难度·时间盒 |
|---|---|---|---|---|---|
| S0 止血 | — | — | 收尾提交；编码 UTF-8/LF 归一；`debug=true` 默认关 | git 干净；headless+net_regression 基线记录 | 1·0.5–1 天 |
| S1 回归护栏 + 脚手架剥离 | SYS-22 | （harness 思路） | `network_world.gd` 内嵌 `--net-test` 抽到独立 harness | 协议零改动；`_ready()` 去 16 分支；10 场景回归全绿 | 2·1–2 天 |
| S2 核心小件替换 | SYS-02/06/21 | `player_input/intent`、`damageable`、`save_manager` | 输入意图层+键位拆分（Esc/A/S）；`Damageable` 统一命中；存档 CRC/`.bak`；`CharacterData` 死字段 | 加键不改 10 文件；存档损坏回退；回归全绿 | 2·2–3 天 |
| S3 敌人与导航 | SYS-12/13 | `walkability_provider/grid/tilemap`、`navigation_service` | **可行走性改 ID 规则**（顺带修学校内部 102% 全图阻挡）→ 导航服务替换 `EnemyChaseState` 内核（保留 9/3 流场成果） | test 图+真图寻路无卡死断言 | 3·3–5 天 |
| S4 战斗核心 | SYS-03~10 | `player_mikiri_state` 等 R2/R3 | 武器状态归并删死节点；**Mikiri→反击→背刺+Guts 移植**（接口已由 S2 铺好） | test 场全链路断言；手感实机 | 3·3–5 天 |
| S5 内容/章节 | SYS-15~18 | `chapter_flow/map_level(+design)/rush_controller` | 战役 5 图走 ID+design 管线；双 Rush；Crimson Head 参数变体 | 单人第一章贯通（验收清单命令化） | 3·1–2 周 |
| S6 联机层（**不原地拆**） | SYS-24 对应边界 | `net_replicator/session_health/domain_context` | **network_world 按"局部重写、wire 兼容"处理**（协议回归对照 net_regression），而非逐行拆 | 双人全章回归；重连/RTT 指标 | 5·2–3 周 |

要点：S3 是"内容+寻路"双赢的关键（legacy 学校内部的可玩性被它解锁）；S6 单独明确——god-object 拆解在 4.2k 行的联机文件上成本≈局部重写，沿用技术债分析结论：**以 net_regression 为安全网做 wire 兼容的局部重写**，不做"边拆边保"的中间态拖尾。

## 6.5 什么情况下维持 strict 独立重做（反方立场，供对照）

若以下条件成立，原方案仍值得考虑：你重视"边界绝对干净、旧债零传染"胜过"尽快可玩"；能接受 3–6 周无新可玩内容 + 多人更晚；且 strict 流程的门禁密度（逐节审批）你乐于维持。但请注意：Nobihaza 已实证的 6.8k 行实现若不入产品，是继两轮重做后的第三份"沉没实验"——单人项目里，被废弃的已验证代码越多，越接近"重做疲劳"。

**一句话**：用户提案在"单人开发 + 已存在可玩资产 + 已有两套验证成果（设计契约+参照实现）"的现实下是更优解；成败全在"是否按契约 + 有门禁地重构"而非"整理一遍"。

