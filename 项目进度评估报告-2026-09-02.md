# 项目进度评估报告

> 评估日期：2026-09-02  
> 项目：のび太的求生之路（l3d-re-godot）  
> 引擎：Godot 4.6.3 · 渲染 Mobile · 物理 Jolt  
> 评估依据：`script/`（91 个 GDScript / 约 23,400 行）、`scene/`（27 个 .tscn）、`object/`（26 个 .tres）、根目录 20+ 份设计文档、`memory/` 项目记忆、git 历史（40 次提交）、Godot 无头扫描

---

## 一、总体结论

| 维度        | 结论                                                                    |
| --------- | --------------------------------------------------------------------- |
| **项目体量**  | 91 个脚本 / 约 2.34 万行 GDScript、27 个场景、26 个数据资源、7 个 Autoload、761 张 PNG 素材 |
| **工程健康度** | 代码可编译可运行（无头扫描 exit 0，无脚本解析错误），但**工作区极脏**（1639 删除 + 26 修改 + 21 未跟踪未提交） |
| **功能完成度** | **相对"可玩单人 Demo"约 65%**；**相对"还原 L4D2 完整体验"约 40%**                      |
| **当前阶段**  | 核心战斗已稳定 → 正在做**关卡内容生产（RM2K3 地图还原）**，联机处于"主干通了、长流程未验收"状态               |
| **最大短板**  | **内容量**：只有 1 种敌人、战役仅 1 关有实际内容（第二关两张图已挂入 campaign_assault.tres 但仍是空图、不刷怪）、8 种特殊感染者全部为 0 —— L4D2 的核心卖点尚未兑现            |
| **最大风险**  | 版本管理混乱 + 文档漂移 + `network_world.gd` 已成 4435 行巨型文件                      |

一句话总结：**技术骨架扎实，内容血肉严重不足，工程卫生需要立刻收拾。**

---

## 二、模块实现进度

### 2.1 核心战斗 — ✅ 90%

| 子系统   | 状态 | 证据                                                                                                                               |
| ----- | -- | -------------------------------------------------------------------------------------------------------------------------------- |
| 玩家状态机 | ✅  | `script/player/` 11 个状态：Idle / Walk / Run / Pistol / Knife / PistolAttack / KnifeAttack / Reload / Shove / Throwable + Attack 基类 |
| 武器    | ✅  | 6 把：手枪、小刀、霰弹枪、步枪、冲锋枪、狙击枪（贯穿）                                                                                                     |
| 射击模式  | ✅  | TAP 点发 / HOLD 连发；弹夹+备弹、空弹音效                                                                                                      |
| 装填    | ✅  | `PlayerReloadState` 双模式（NORMAL 一次装满 / SHOTGUN 逐发循环+上膛）                                                                           |
| 近战推击  | ✅  | `PlayerShoveState` + 溅射击退（`shove_splash_radius`）                                                                                 |
| 投掷物   | ✅  | 燃烧瓶 / 手雷 + 火焰区域                                                                                                                  |
| 暴击/爆头 | ✅  | `critical_rate` 掷骰 → 爆头死亡状态                                                                                                      |
| 物品    | ✅  | 11 个 ItemData（治疗/弹药/投掷/TP 恢复）                                                                                                    |

**缺口**：弹药堆交互（按 E 补弹）、管状炸弹/胆汁罐、特殊弹药、双持手枪 —— 均为低优先级。

### 2.2 敌人 AI — ⚠️ 55%（种类是硬伤）

| 子系统      | 状态                                                                               |
| -------- | -------------------------------------------------------------------------------- |
| 状态机（8 态） | ✅ Idle / Discover / Chase / Attack / Hitstun / Knockback / Death / HeadshotDeath |
| A* 寻路    | ✅ 四方向 + 共线平滑 + 实体障碍 + 全图静态缓存 + 降级直冲                                              |
| 攻击判定     | ✅ 双矩形（触发框 + 命中框），随朝向旋转                                                           |
| 伤害去重     | ✅ 三层（子弹永久去重 / 目标 1s 窗口 / 近战实例）                                                   |
| **敌人种类** | ❌ **仅 1 种**（`object/enemy.tscn`）                                                 |

**L4D2 八种特殊感染者 —— 全部为 0**。`script/director/spawn_point.gd` 里 `enum SpawnType { COMMON=0, SPECIAL=1, BOTH=2 }` 的枚举**已预留但没有实体可生成**。全项目 grep `hunter|smoker|boomer|tank|witch|spitter|jockey|charger` 零命中（除 spawn_zone 注释）。

### 2.3 导演系统（Director）— ⚠️ 65%（Phase 1–3 完成，Phase 4 未开始）

16 个脚本，Autoload `Director`：

- ✅ 紧张度追踪 `intensity_tracker`、节奏/波次 `pacing_controller`、生成 `spawn_manager`、事件 `event_manager`、物品投放 `item_manager`、场景参数 `director_config`
- ✅ 生成点/生成区 `spawn_point` / `spawn_zone`、安全门 `safe_door`、传送点 `teleport_point`、到达点 `arrival_point` / `arrival_resolver`
- ❌ **Phase 4（特感与 Boss 编排）完全未开始** —— 无预算/时机/事件调度
- ❌ 动态音乐（Mood Music）未做

### 2.4 局域网联机 — 🧪 75%（主干通，验收缺）

| 能力              | 状态                                                                                |
| --------------- | --------------------------------------------------------------------------------- |
| Host 全权威架构      | ✅ Client 仅上报输入，Host 结算一切                                                          |
| 连接/大厅/选角        | ✅ ENet + `hello→hello_ack` 握手，支持 2–4 人、角色可重复                                      |
| 同步              | ✅ 结构事件 reliable RPC；位置快照 `unreliable_ordered`；不使用 MultiplayerSpawner/Synchronizer |
| 表现层优化           | ✅ 快照插值缓冲（玩家 50ms / 敌人 60ms）+ 客户端本地预测 + 2s 可靠重同步软并流（偏差 >96px 才硬校准）                 |
| 倒地/救援/团灭        | ✅ HP=0 倒地（爬行+流血 20s）→ 队友长按 3s 救起（30% HP）→ 全员非站立黑屏重载本章                             |
| 敌人死亡可靠广播        | ✅ 修"丧尸围尸"                                                                         |
| 头顶名牌 + HP 条     | ✅ 1P/2P 座位号 + `ＨＰバー`/`ＨＰメーター`                                                     |
| UPnP 端口映射       | ✅ 建房自动映射，失败提示 frp                                                                 |
| **自动化回归**       | ⚠️ 靠 `--net-test=host/client/...` 命令行 flag 手动驱动，**无一键 runner 脚本**，8/29 之后无回归记录    |
| **双人完整战役验收**    | ❌ 未做                                                                              |
| **3–4 人压力测试**   | ❌ 未做                                                                              |
| **断线重连 / 主机迁移** | ❌ 未做                                                                              |
| **房间发现 / 公网**   | ❌ 无房间列表（仅 UPnP + 手动端口）                                                            |

### 2.5 关卡与战役内容 — 🧪 35%（当前最大瓶颈）

**已登记战役**（`object/campaign_assault.tres`）只有第一关 3 张图：

```
开头安全屋-户外 → 第一关-街道 → 结尾安全屋-室内
```

**第二关场景已存在但未接入战役**：

- `突袭-第二关-学校门口.tscn`（Map0141 还原，已 tracked，含 2 个 TeleportPoint）
- `突袭-第二关-学校内部.tscn`（Map0142 还原，**未 tracked**，刚建）
- 链路已接通：第一关结尾安全屋 `target_scene` → 学校门口 → 学校内部
- ❌ 但**两张图均未登记进 `campaign_assault.tres` 的 `level_scenes`**
- ❌ 学校内部**没有 SpawnZone、没有 SafeDoor、没有敌人** → 进去是空图，无法游玩

**RM2K3 地图自动还原管线**（`tools/rm2k3_map_restore/`）已建成：

- ✅ `parse_lcf.py`（LMU/LMT/LDB 解析器）、`export_map.py`、`rm2k3_to_va.py`、`convert_map.bat` 一键入口
- ✅ `script/map_restore/map_tilemap_generator.gd` 生成真正可编辑的分层 TileMap
- ✅ 目标 7 张图中 6 张已产出 atlas + TileSet + 场景（Map0141/0143/0144/0145/0146/0147）；Map0142 转手工精修
- ❌ **所有还原图都不含碰撞/通行性/事件/寻路数据** —— Director 查询 TileSet physics 会产生空 physics 层警告，敌人无法正确寻路
- ❌ A1 水面动画、A2/A4/D 自动图块语义仍待校准

### 2.6 HUD / UI — ⚠️ 45%（且文档与代码不一致）

`scene/ui/combat_hud.tscn` 实际节点只有 6 个：  
`HPFrame` / `HPFill` / `SprayCount`（急救喷雾 0–15）/ `PrimaryWeaponIcon` / `SecondaryWeaponIcon` / `TPLabel`

> ⚠️ **文档漂移实锤**：`README.md` 声称存在"5 槽位竖排快捷栏（右侧垂直居中，含武器弹夹/备弹/物品数量）"，但全项目 grep `快捷栏|QuickSlot|quick_slot` **零命中**，场景里也没有对应节点。  
> 同理 `AGENTS.md` 称"`art/Ui/` 血条素材尚未接入代码"，实际 `combat_hud.tscn` 已引用 `ＨＰバーＴ.png` / `ＨＰメーターＴ.png`。

**缺口**：武器/物品快捷栏（清单标 🔴 高）、队友状态栏、低血受伤血框、尸潮提示音。

### 2.7 技能系统 — 🧪 20%（明确标注的"空壳子"）

搓招输入框架（技能键 + 触发键 + 方向序列）已通，TP 消耗/回复已通；但 `SkillData` 文件头自述"空壳子"，`cooldown` 冷却**尚未接入计时**，`effect_anim` 特效为预留字段。仅有 2 个占位技能（`skill_test` / `skill_form_switch`）。

### 2.8 系统层 — ✅ 80%

| 系统           | 状态                                                      |
| ------------ | ------------------------------------------------------- |
| 存档           | ✅ v2 格式（per-seat `PlayerState`），JSON + 自动存档 + L4D2 死亡重载 |
| 设置           | ✅ 音量 / 朝向模式 → `config.json`                             |
| 音频总线         | ✅ SFX / Music 独立总线                                      |
| 文字渲染         | ✅ `GradientLabel` 双路径（CPU 预渲染渐变 / shader 纯色）            |
| 角色切换/队伍      | ✅ Q 循环 + Ctrl+1~4 直选 + 队友站位精灵                           |
| 章节统计         | ✅ `ChapterStats`（伤害/击杀/爆头/治疗/用时）                        |
| 地图编辑工具       | ✅ 运行时图块编辑器 + VX 动画系统 + TileSet 生成器                      |
| 联机 Host 存档恢复 | ❌ 待设计                                                   |

### 2.9 进度总览

| 模块            | 权重   | 完成度       |
| ------------- | ---- | --------- |
| 核心战斗          | 20%  | 🟢 90%    |
| 敌人 AI         | 15%  | 🟡 55%    |
| 导演系统          | 10%  | 🟡 65%    |
| 联机            | 20%  | 🟡 75%    |
| 关卡内容          | 15%  | 🔴 35%    |
| HUD / UI      | 8%   | 🟡 45%    |
| 技能系统          | 4%   | 🔴 20%    |
| 系统层（存档/设置/工具） | 8%   | 🟢 80%    |
| **加权合计**      | 100% | **≈ 65%** |

---

## 三、已完成开发任务（近两周，来自 git 历史）

| 日期            | 任务                                                                                  |
| ------------- | ----------------------------------------------------------------------------------- |
| 09-01         | 传送点支持自定义到达位置；角色选择界面优化；RM2K3 地图还原场景更新；清除误入的 NUL 文件                                   |
| 08-31         | 缓存 RM2K3 地图树与还原方案；Map0141 纵向切片打通（atlas → TileSet → 可编辑 TileMap）；D 自动图块象限坐标修正        |
| 08-30         | 新增スネ夫角色 + 贯穿狙击枪；修复武器限制与初始手枪槽位；A4 转换器整理                                              |
| 08-29         | **联机表现层大修**：快照插值取代指数平滑、客户端本地预测、可靠重同步软并流、丧尸不围尸、敌人死亡可靠广播、头顶名牌+HP 条、初始武器统一发放、UPnP 端口映射 |
| 08-28         | **倒地救援与团灭恢复**落地；修复 ready 竞态；大厅支持自定义 UDP 端口                                          |
| 08-22 ~ 08-27 | 客户端可拾取投掷物、掉落物同步稳定、装填/推击权威化、战斗反馈同步、四人断线收敛测试、角色目录统一                                   |
| 08-20 ~ 08-21 | Host 权威开火、跨场景同步、动态敌人同步、安全门全员确认、章节准备协调                                               |

**8/21 与 8/22 的两份《测出来的问题》清单**（换弹/推击/投掷/客户端受伤动画/掉落物不可见/胖虎消失等）已基本被上述提交覆盖修复，但**清单文件本身未归档、未标注闭环状态**。

---

## 四、风险清单（按严重度）

### 🔴 R1 — 版本管理失控（最高优先级）

```
工作区：1639 删除 / 26 修改 / 21 未跟踪，自 2026-09-01 起未提交
  ├ 1341 个删除 = 未使用素材/ 的 .import（因新增 .gdignore 被 Godot 清理）
  ├  289 个删除 = chm_extract/ 的 .import（同上）
  └   26 个修改 = 含 project.godot、player.gd、network_world.gd、多个 .tscn/.tres
分支：当前 Branch_34b342a2 领先 main 86 个提交 / 落后 main 50 个提交
      另有 Branch_80fe8fa6（+6 / -50），main 停在 8 月的 merge commit
```

**风险**：一次误操作（`git checkout .` / `git clean -fd`）会丢掉 9/1 以来的全部地图还原成果（Map0142 / 学校内部场景 / 多个 TileSet，其中 3 个 .tres 显示为已修改、1 个 .tscn 已删除）。

### 🔴 R2 — 内容缺口导致项目定位无法兑现

- 敌人种类 1/9（L4D2 原版 8 种特感全缺）
- 战役登记 1 关，第二关 2 张图未接入、无生成区无安全门
- 所有还原地图无碰撞/通行性 → 敌人寻路在还原图上不可用

### 🟠 R3 — 文档漂移

三份文档对同一事实描述冲突（README 的 5 槽快捷栏不存在；AGENTS 说血条素材未接入但实际已接入；缺失清单标快捷栏为 🔴 待做）。文档越多、可信度越低，后续 AI 或新协作者极易被误导。

### 🟠 R4 — `network_world.gd` 4435 行巨型文件

联机的玩家同步、敌人同步、倒地救援、拾取、团灭、回归测试钩子全部堆在单文件里（占全项目 19% 代码量）。**继续往上加"断线重连/主机迁移/特感同步"会直接失控**。`player.gd` 1536 行同理。

### 🟡 R5 — 回归测试 runner（已补，但需纳入常驻流程）

`--net-test` 系列 flag 散落在 `network_lobby.gd` / `network_world.gd` / `game_init.gd` 里，现已封装为 `tools/net_regression.py`（10 场景一键跑、支持 `--list`/`--keep-logs`）——见「附：本轮已落地的收尾动作」。但仍**无 CI/提交钩子自动跑**，依赖人工记得执行；后续任何联机改动后建议先跑一遍。

### 🟡 R6 — 仓库体积与二进制污染

根目录 `bird's-eye_view-ARPG test.exe`(100MB) + `.pck`(93MB) + `rpg2003.chm`(6MB) + `chm_extract/`(500 文件) + `未使用素材/`(1347 文件) 全部被 git 跟踪，且 exe/pck 当前处于 modified 未提交状态。

### 🟡 R7 — 历史问题单未闭环归档

`2026.8.21/8.22 测出来的问题.txt` 中的"固定朝向失效""主机开菜单导致客户端暂停""总结界面待准备人数不正确"等条目，状态未在文档中标明。

### 🟢 R8 — 已知无害噪声

- 无头扫描 `exit 0`，仅有 Dummy 渲染器纹理泄漏 / 资源未释放告警
- 团灭测试协程换图瞬间的 `get_tree()` 空引用打印（既有竞态）
- `--net-test-weapon=smg_01` 近战子断言偶发不触发（僵尸先被子弹打死）

---

## 五、下一步建议

### P0 — 先止血（1 天内）

1. **立即提交并收敛工作区**
   - 先 `git add -A` 前确认：把 1341+289 个 `.import` 删除作为独立 commit（这是 Godot 加 `.gdignore` 后的正常清理），与代码改动分开提交
   - **不要**用 `git clean -fd` / `git checkout .`
2. **把大二进制移出版本库**：`.exe` / `.pck` / `rpg2003.chm` / `chm_extract/` 加入 `.gitignore` 并用 `git rm --cached` 移除跟踪（可省 ~200MB 历史体积）
3. **分支收敛**：将 `Branch_34b342a2` 合并回 `main` 并作为唯一主线，废弃 `Branch_80fe8fa6`（或确认其内容已合入后删除）

### P1 — 打通第一个可验收闭环（1~2 周）

1. **完成第二关并接入战役**
   - 给 `突袭-第二关-学校内部.tscn` 补 SpawnZone（参考第一关街道的 12 个生成区配置）+ SafeDoor + DirectorConfig
   - 把两张图写进 `campaign_assault.tres` 的 `level_scenes`
   - **补碰撞/通行性**：这是所有还原图的共同缺口，建议做成还原管线的标准步骤，而不是逐图手填
   - 目标：单人能从第一关连续打到第二关结束
2. **跑通双人完整战役回归**
   - 写一个 `tools/net_regression.bat`（或 .ps1），把散落的 `--net-test` flag 固化成 4~5 个场景的一键回归
   - 覆盖：建房→加入→跨两关→倒地救援→团灭重载→章节结算

### P2 — 补内容血肉（2~4 周）

1. **Hunter 作为第一个特感垂直切片**
   - 新 `object/enemy_hunter.tscn` + 状态机（潜行/扑倒/压制/被推开）
   - 同时接通 Director Phase 4 的最小闭环（生成预算 → 时机 → 生成）
   - 理由：Hunter 最能验证"特感 + 联机同步 + 倒地救援"三者交叉，做完再复制 Smoker/Boomer 成本最低
2. **HUD 补齐**：武器/物品快捷栏（清单标 🔴）+ 队友状态栏

### P3 — 偿还技术债（持续）

1. **拆分 `network_world.gd`**（4435 行）：按职责拆为 `net_player_sync` / `net_enemy_sync` / `net_downed_revive` / `net_pickup` 四个子模块，主文件只做调度
2. **文档对齐**：以代码事实为准，统一 README / AGENTS.md / L4D2缺失功能清单 三处对 HUD 与快捷栏的描述；建议在 AGENTS.md 顶部加一句"代码与文档冲突时以代码为准，并请修正文档"
3. **归档历史问题单**：把 8.21 / 8.22 两份清单合并为 `问题追踪.md`，标注每条的闭环状态

### 优先级排序理由

> **为什么先做"工程卫生 + 内容打通"，而不是先做特感？**  
> 特感（Hunter 等）是 L4D2 体验的灵魂，但它依赖三样东西：能跑的地图（现在只有 1 关）、能验证的回归流程（现在靠手敲命令）、能承载新逻辑的代码结构（network_world 已 4435 行）。  
> 在这三样就绪前直接加特感，等于在流沙上盖楼 —— 而且 9/1 以来的地图还原成果目前**全部未提交**，一次误操作就会全没。

---

## 附：快速验证命令

```bash
# 无头健康检查（当前 exit 0，仅 Dummy 渲染器泄漏噪声）
"D:/Godot_v4.6.3-stable_win64.exe/Godot_v4.6.3-stable_win64.exe" --headless \
  --path "D:/!bird's-eye-view-arpg-test-/l3d-re-godot" --quit

# 联机回归（现有 flag，建议封装成脚本）
#   --net-test=host / --net-test=client
#   --net-test-features / --net-test-enemies / --net-test=downed-wipe
#   --net-test-weapon=smg_01 / --net-test-character-select / --net-test-multi-disconnect
# 注意：清理无头进程勿用 taskkill /IM Godot_v4.6.3-stable_win64.exe（编辑器同名会被误杀）

---

## 附：本轮已落地的收尾动作（2026-09-02 续）

> 在评估报告给出的"先止血再盖楼"建议基础上，已非破坏性地推进了 P0/P1 中的可执行部分；涉及 git 破坏性操作（清理大二进制、分支收敛、提交拆分）仍待用户确认。

### A. 已交付

| 动作 | 产出 | 说明 |
| --- | --- | --- |
| 修复文档漂移 | 改 `README.md` / `AGENTS.md` / `CLAUDE.md` | 删除不存在的"5 槽位快捷栏"、修正操作按键表；血条素材状态改为"4 张已接入 / 6 张未接入"。**原则已写入 AGENTS.md：代码与文档冲突时以代码为准并修正文档** |
| 一键联机回归 runner | `tools/net_regression.py` | 固化 10 个 `--net-test` 双端场景，判定=期望 `*_COMPLETE` 标记齐 + 无 `*_FAILED`（退出码非 0 但标记已齐按通过计，以兼容 host 先退出的收尾竞态）；支持 `--list` / `--keep-logs` / `retries` / `known_fail`。调用 Godot **console** exe 抓 stdout，用户参数走 `--` 分隔符，进程只按 PID 终止（避免误杀编辑器）；命中决定性标记即早停，避免已知 bug/挂死场景空等满 timeout |
| 归档历史问题单 | `问题追踪.md` | 合并 8.21 / 8.22 两份《测出来的问题.txt》，逐条对照代码标注闭环状态（含"举放武器动画已做""友伤已关闭"等核实结论） |
| `.gitignore` | 更新根 `.gitignore` | 收纳 exe/pck/chm/chm_extract/未使用素材/`.tmp-user-*`。**仅改 ignore，未执行 `git rm --cached`，移出版本库需用户确认** |

### B. 回归结论（runner 实测）

**10 PASS / 0 KNOWN-BUG / 0 真回归失败**（共 10 场景，全部转绿）。

`features` 用例曾是唯一失败项，最终定位为**回归套件的环境依赖问题（非真实游戏 bug）**，已修复：

- **现象**：`AUTO_FEATURE_CLIENT_FACING_FAILED`，`lock_state=false`。锁命令生效瞬间（客户端日志出现一次 `locked=true`）立即被后续 `locked=false` 覆盖。
- **真因**：`config.json` 残留 `"facing_lock_mode": 1`（按住式），`global.gd:_load_config()` 每次启动都读进 `Global.facing_lock_mode`，无头回归随之进入按住式。测试用的是「显式 RPC 加锁」语义（切换式），于是客户端每帧的 `_capture_facing_lock_input()` 因未按住取消键而立刻 `facing_lock_request(..., false)` 解锁，把锁解掉。这是测试假失败——真实游玩中按住式完全由取消键驱动，不会触发此冲突。
- **修复**：在 `network_world.gd:_ready()` 的 `--net-test` 启动入口强制 `Global.facing_lock_mode = 0`（仅作用于无头进程，不回写 config，真实玩家设置不受影响）。另外此前顺手加的两道防护也保留：host 应用远端快照时忽略客户端自报的 `facing_locked`（`_ensure_client_player` 的 `is_host_applying_remote` 分支），以及投掷物每帧回放不通过 `unlock_facing` 解掉固定朝向锁（`apply_network_throwable_presentation(..., affect_facing_lock=false)`）。
- **验证**：强制切换式后 `features` 转绿，整套 10 场景复跑全 PASS，host/client 均 exit 0。

### C. 仍待用户决策（破坏性，未执行）

1. `.gitignore` 已就绪，需执行 `git rm --cached` 将大二进制移出版本库（当前根目录 100MB exe + 93MB pck + 6MB chm 仍被跟踪）。
2. 工作区极脏（1639 删除 + 26 修改 + 21 未跟踪，自 9/1 未提交）：建议分两个 commit 提交（`.import` 清理 vs 代码改动分开），且**切勿 `git clean -fd` / `git checkout .`**（会丢 9/1 的地图还原成果）。
3. 分支收敛：当前 `Branch_34b342a2` 领先 main 86 / 落后 50，另有 `Branch_80fe8fa6`，需合并回 main。

### D. 下一步（在原报告优先级之上微调）

1. ~~先修 facing-lock bug~~ —— 已澄清为非游戏 bug，`features` 失败源于 config 污染的回归假失败，套件已自洽（10 PASS）。无需再开游戏代码修复。
2. 按原建议推进：第二关接入战役 + 还原管线固化碰撞导入 + 用 runner 跑通双人双关回归。
3. 之后再做 Hunter 垂直切片打通 Director Phase 4。
```

